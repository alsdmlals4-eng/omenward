# C1 승인 룰렛 계약 복구 — 기계 수집 감사 입력

- 기준 커밋: `076b43d199fb3d76caa96c022ecd2aa80c3f63d7`
- 텍스트 파일: 242
- 룰렛 관련 파일: 105
- 구형 상태·명칭 후보 파일: 39
- 마크다운 내부 참조: 20
- 깨진 내부 링크: 0

이 파일은 기계 수집 입력이다. ACTIVE/HISTORICAL 판정과 삭제 여부는 후속 사람이 검증한다.

## 룰렛 관련 파일

### `.github/workflows/collect-c1-roulette-audit-once.yml` — ACTIVE / 27 lines

Hits: `roulette` [1, 8, 9, 16, 27]

```text
    1: name: Collect C1 Roulette Audit Once
    2: 
    3: on:
    4:   pull_request:
    5:     branches:
    6:       - main
    7:     paths:
    8:       - "tools/_collect_c1_roulette_audit.py"
    9:       - ".github/workflows/collect-c1-roulette-audit-once.yml"
   10: 
   11: permissions:
   12:   contents: write
   13: 
   14: jobs:
   15:   collect:
   16:     if: github.head_ref == 'agent/c1-approved-roulette-contract-recovery'
   17:     runs-on: ubuntu-latest
   18:     steps:
   19:       - uses: actions/checkout@v4
   24:         with:
   25:           python-version: "3.13"
   26:       - name: Collect repository audit input
   27:         run: python tools/_collect_c1_roulette_audit.py
```

<details><summary>Full content</summary>

```text
name: Collect C1 Roulette Audit Once

on:
  pull_request:
    branches:
      - main
    paths:
      - "tools/_collect_c1_roulette_audit.py"
      - ".github/workflows/collect-c1-roulette-audit-once.yml"

permissions:
  contents: write

jobs:
  collect:
    if: github.head_ref == 'agent/c1-approved-roulette-contract-recovery'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Collect repository audit input
        run: python tools/_collect_c1_roulette_audit.py

```

</details>

### `AGENTS.md` — ACTIVE / 222 lines

Hits: `룰렛` [98, 152, 154, 157]

```text
   95: 
   96: ## 프로젝트 핵심 경험
   97: 
   98: - 핵심은 `건물 구성 → 룰렛 확률 변화 → 병력 획득 → 3라인 배치 → 접전지·거점·성문 공방`의 연결이다.
   99: - 다음 공세를 미리 읽고 제한된 자원으로 대응을 설계한 뒤 전선을 간발의 차이로 뒤집는 경험을 우선한다.
  100: - 새로운 기능은 첫 10분과 핵심 판단을 강화하지 않으면 추가보다 삭제·통합을 먼저 검토한다.
  101: 
  149: 
  150: ## 기타 프로젝트 원칙
  151: 
  152: - 건물 Tier는 생산량·속도·품질과 전투 성능을 강화하지만 룰렛 토큰 수는 **완공된 건물 개수**로 계산한다.
  153: - 기본 포탑 한 기가 중간거점과 중앙 접전지 사이 전체를 단독으로 덮지 않는다.
  154: - 적은 플레이어와 같은 룰렛 AI를 사용하지 않고 StageManifest·웨이브·생산시설로 출격한다.
  155: - 핵심 수치와 상성은 데이터로 관리하고 코드에 흩어놓지 않는다.
  156: - 전방 시설 파괴, 식량 한도, 토큰 제거, 거점 권한 이전처럼 연쇄 효과를 명시적으로 테스트한다.
  157: - 화면은 세 라인의 위협, 성문·거점·접전지, 다음 웨이브와 룰렛 상태를 동시에 이해할 수 있어야 한다.
  158: 
  159: ## Base 공용 지식과 참고 저장소
  160: 
```

### `README.md` — ACTIVE / 103 lines

Hits: `룰렛` [3, 11, 20, 52, 62, 70], `roulette` [52, 86, 94]

```text
    1: # OMENWARD Prototype
    2: 
    3: **오멘워드**는 건물로 3×3 룰렛의 토큰과 확률을 설계하고, 베일의 징조로 예고된 공세에 맞서 상·중·하 세 전선을 지휘하는 판타지 전략 오토배틀 게임입니다.
    4: 
    5: > 현재 상태: **기술·데이터 그레이박스 수직 슬라이스 존재 / 코어 루프 미완결 / 사람 플레이 검증 대기**
    6: > 프로젝트 코어: **CORE_CONFIRMED / CORE_LOCKED**
    8: 
    9: ## 핵심 문장
   10: 
   11: > **건물을 지어 룰렛 확률을 바꾸고, 당첨된 병력으로 예고된 위기를 뒤집는다.**
   12: 
   13: ## 핵심 구조
   14: 
   17: - 중앙 접전지 점령 수입과 성문 공성.
   18: - 암살자를 안개 속 같은 라인 우회로로 보내 적 후열에 침투.
   19: - 전장 전체를 기본 전략 화면에서 조망하며 미니맵은 사용하지 않음.
   20: - 건물 개수가 룰렛 토큰과 확률을 바꾸고, Tier와 등급이 병종의 성장 방향을 결정.
   21: - 활성 전투 시간 기준 60초 공세와 W5·W10·W15·W20 이정표.
   22: - 벨루가 세계관, 튜토리얼, 공세 보고와 감정 반응을 담당.
   23: 
   49: 5. [`docs/DOCUMENTATION_MAP.md`](docs/DOCUMENTATION_MAP.md) — 작업별 책임 원본 라우터
   50: 6. [`docs/OMENWARD_GAME_DESIGN.md`](docs/OMENWARD_GAME_DESIGN.md) — 공식 전체 기획서
   51: 7. [`docs/design/APPROVED_PREPRODUCTION_POC_BASELINE_V1.md`](docs/design/APPROVED_PREPRODUCTION_POC_BASELINE_V1.md) — 승인 구조 통합 인덱스
   52: 8. [`docs/design/APPROVED_ROULETTE_CORE_RULES.md`](docs/design/APPROVED_ROULETTE_CORE_RULES.md) — 승인 룰렛 판정·등급·보상 계약
   53: 9. [`docs/OMENWARD_ROADMAP.md`](docs/OMENWARD_ROADMAP.md) — 현재 복구 순서와 단계별 완료 기준
   54: 10. [`docs/DECISIONS_PENDING.md`](docs/DECISIONS_PENDING.md) — 현재 결정 게이트와 PoC 조정 항목
   55: 11. [`docs/GODOT_PROJECT_STRUCTURE.md`](docs/GODOT_PROJECT_STRUCTURE.md) — 현재 Godot Scene·상태 소유·데이터 구조
   59: 
   60: ```text
   61: [현재] 정본·프로젝트 코어 복구
   62: → 승인 룰렛 계약 복구
   63: → 전투를 접전지·거점·성문·승패에 연결
   64: → 승인 코어 UX 6종 최소 구현
   65: → 10~15분 사람 플레이와 1080p·720p 가독성 검증
   67: → 콘텐츠·아트 확장
   68: ```
   69: 
   70: 현재 저장소에는 Godot 기술 기준선과 수직 슬라이스 구성요소가 존재하지만, 승인 룰렛 판정·전투 목적 루프·핵심 UX가 완결되지 않았다. 현재 판정은 `TECHNICAL_BASELINE_IMPLEMENTED`, `CORE_VERTICAL_SLICE_PARTIAL`, `CORE_LOOP_NOT_PROVEN`, `HUMAN_QA_NOT_RUN`이다.
   71: 
   72: 세부 근거와 다음 게이트는 [`docs/CURRENT_IMPLEMENTATION_STATUS.md`](docs/CURRENT_IMPLEMENTATION_STATUS.md)를 따른다. 자동 검증 명령과 수동 QA 항목은 [`docs/VERTICAL_SLICE_VALIDATION.md`](docs/VERTICAL_SLICE_VALIDATION.md)와 [`docs/PHASE_0_VALIDATION.md`](docs/PHASE_0_VALIDATION.md)에 남아 있으며, 실제 재실행 전에는 완료로 보고하지 않는다.
   73: 
   83: │  ├─ battle/
   84: │  ├─ buildings/
   85: │  ├─ units/
   86: │  ├─ roulette/
   87: │  ├─ waves/
   88: │  └─ ui/
   89: ├─ scripts/
   91: │  ├─ battle/
   92: │  ├─ buildings/
   93: │  ├─ units/
   94: │  ├─ roulette/
   95: │  ├─ waves/
   96: │  └─ ui/
   97: ├─ data/
```

### `docs/ACTIVE_CONTEXT.md` — ACTIVE / 245 lines

Hits: `룰렛` [47, 49, 53, 54, 57, 233, 242]

```text
   44: ```
   45: 
   46: - Phase 0 기술·데이터 기반과 다수 수직 슬라이스 구성요소는 실제 파일로 존재한다.
   47: - 승인 룰렛 판정, 전투→거점·성문·승패 연결, 코어 UX 6종은 완결되지 않았다.
   48: - 자동 테스트 파일의 존재를 최신 runtime·사람 플레이 증거로 간주하지 않는다.
   49: - 다음 게임 기능 변경은 승인 룰렛 계약 복구로 한정한다.
   50: 
   51: ## 핵심 정체성
   52: 
   53: - 장르: 실시간 3라인 전략 오토배틀 + 건물 기반 3×3 룰렛 빌드.
   54: - 핵심 루프: `베일의 징조 → 건물·확률 설계 → 룰렛 판정 → 라인 배치 → 거점·성문·우회 공방 → 원인 확인 → 다음 설계`.
   55: - 플랫폼: Windows PC / 마우스·키보드 / 싱글플레이 PvE.
   56: - 엔진: Godot + GDScript.
   57: - 첫 10분 안에 건설→룰렛→배치→역전 루프를 두 번 체험한다.
   58: 
   59: ## 전장 불변 구조
   60: 
  230: 
  231: ```text
  232: 정본·프로젝트 코어 복구
  233: → 승인 룰렛 계약 복구
  234: → 전투 목적 루프 연결
  235: → 승인 코어 UX 6종
  236: → 사람 플레이 검증
  239: 
  240: - 새 Codex 채팅은 `docs/PROJECT_CORE.md`와 `docs/CURRENT_IMPLEMENTATION_STATUS.md`를 먼저 읽는다.
  241: - 현재 저장소를 `구현 전` 또는 `수직 슬라이스 완료` 중 하나로 단순화하지 않는다.
  242: - 다음 게임 기능 PR은 승인 룰렛 계약 복구만 포함한다.
  243: - 시각·병종·UI 작업은 새 병종 비주얼 책임 문서와 시각자료 인덱스를 반드시 읽는다.
  244: - 실제 아트 제작 전 대표 병종 5종을 1080p·720p 전장에 삽입해 축소 가독성을 검증한다.
  245: - Base 공용 지식은 방법과 사례 참고용이며 오멘워드 책임 문서를 덮어쓰지 않는다.
```

### `docs/CORE_RECOVERY_AUDIT_2026-07-22.md` — REFERENCE_OR_OTHER / 133 lines

Hits: `룰렛` [21, 26, 40, 46, 48, 52, 108], `완성선` [26], `중앙 줄` [26]

```text
   18: 
   19: | 위치 | 기존 주장 | 실제 증거 | 조치 |
   20: |---|---|---|---|
   21: | `README.md` | 플레이 가능한 수직 슬라이스 구현 완료 | 기술·데이터 구성요소는 있으나 룰렛·전투 목적·코어 UX가 미완결 | `CORE_VERTICAL_SLICE_PARTIAL`로 교정 |
   22: | `OMENWARD_GAME_DESIGN.md` | Phase 0 대기·구현 전 | Godot 프로젝트·Scene·GDScript·Resource·테스트 존재 | 기술 기준선 구현과 코어 미완결을 분리 |
   23: | `OMENWARD_ROADMAP.md` | Phase 0 Plan Mode 대기 | P1 기반과 P2 일부가 이미 구현 | 현재 복구 단계 중심으로 재작성 |
   24: | `DECISIONS_PENDING.md` | 엔진·화면·상태 소유를 최초 승인 대기 | 실제 project·GameSession·Resource 구조 존재 | 구현 사실과 재검증 필요를 분리 |
   25: | 열린 Issue 다수 | 구현 금지·과거 브랜치 정본 | 현재 main과 상태가 다름 | 본 PR에서 문서 정본을 먼저 복구하고 후속 Issue 정리 대상으로 기록 |
   26: | 기존 테스트 | 9개 룰렛 카드 반환을 성공 계약으로 간주 | 승인 룰렛은 중앙 줄·완성선·등급·단일 보상 | 다음 단계의 P0 계약 복구 항목으로 지정 |
   27: 
   28: ## 4. 채택한 상태 모델
   29: 
   37: 이 네 상태를 동시에 사용한다. 어느 하나만 사용하면 다음 오판이 생긴다.
   38: 
   39: - `구현 전`만 사용: 이미 존재하는 기술 기반과 파일을 다시 설계한다.
   40: - `수직 슬라이스 완료`만 사용: 승인 룰렛·전투 목적·UX 미완성을 놓친다.
   41: - `테스트 통과`만 사용: placeholder 테스트를 제품 계약 검증으로 오인한다.
   42: - `플레이 가능`만 사용: 사람 플레이·가독성·재미 검증을 완료로 오인한다.
   43: 
   44: ## 5. 프로젝트 코어 스트레스 테스트
   45: 
   46: ### 공격 1 — 룰렛 없이도 같은 게임인가
   47: 
   48: 아니다. 룰렛을 제거하면 제한 노드 기반 3라인 디펜스가 된다.
   49: 
   50: ### 공격 2 — 건물이 확률을 바꾸지 않아도 되는가
   51: 
   52: 아니다. 건설과 룰렛이 분리돼 장르 혼합의 인과가 사라진다.
   53: 
   54: ### 공격 3 — 공세 정보를 숨기면 더 전략적인가
   55: 
  105: 
  106: 1. 사용자가 프로젝트 코어를 `코어확정`으로 확인.
  107: 2. 정본 PR에서 `CORE_CONFIRMED`·`CORE_LOCKED` 전환과 검증.
  108: 3. 승인 룰렛 계약 복구를 별도 Plan·Build·Review PR로 수행.
  109: 4. 전투 목적 루프, 코어 UX, 사람 플레이 순으로 분리 진행.
  110: 
  111: ## 9. 판정
```

### `docs/CURRENT_IMPLEMENTATION_STATUS.md` — ACTIVE / 139 lines

Hits: `룰렛` [37, 41, 86, 87, 123, 139], `중앙 가로줄` [47], `완성선` [48, 57], `결과 보관` [51], `unitspawndefinition` [56], `럭키` [57], `판정 줄` [57], `이동권` [57], `spin` [82]

```text
   34: | 건설 | 소유·안정화·점령 revision을 검사하는 건설 서비스 | `IMPLEMENTED_COMPONENT` |
   35: | 전투 | 독립 3라인, 공용 유닛, 기본 이동·타기팅·공격, 암살자 우회 상태 | `IMPLEMENTED_COMPONENT` |
   36: | 웨이브 | 튜토리얼 W1~4, 정규 W1~20 데이터와 60초 출격 시계 | `IMPLEMENTED_COMPONENT` |
   37: | 테스트 | bootstrap·데이터·경제·룰렛 placeholder·전투·웨이브·우회 관련 headless 테스트 파일 | `IMPLEMENTED` |
   38: 
   39: ## 3. 부분 구현 또는 승인 계약과 다른 영역
   40: 
   41: ### 3.1 룰렛 — `DIVERGENT`
   42: 
   43: 승인 계약:
   44: 
   45: ```text
   46: 3×3 보드
   47: → 중앙 가로줄 동일 비-X 심벌 판정
   48: → 동일 심벌 완성선 계산
   49: → 일반·엘리트·영웅·전설 등급
   50: → 실제 보상 1개 생성
   51: → 결과 보관·배치
   52: ```
   53: 
   54: 현재 구현:
   55: 
   56: - 20 Gold를 지불하고 9개의 `UnitSpawnDefinition`을 직접 반환한다.
   57: - 중앙 판정 줄, X·금화, 완성선, 등급, 전설 제한, 럭키 찬스, 이동권이 없다.
   58: - 현재 테스트도 9개 카드 반환을 기대하므로 승인 계약이 아니라 placeholder 계약을 고정한다.
   59: 
   60: 판정: `CORE_CONTRACT_DIVERGENT`.
   79: 
   80: ### 3.4 코어 UX — `NOT_IMPLEMENTED`
   81: 
   82: 현재 HUD는 금화·식량·웨이브·전조 초·Spin·Tower·Farm·문자열 카드·라인 버튼을 제공한다.
   83: 
   84: 다음 승인 UX는 아직 실제 데이터와 연결되지 않았다.
   85: 
   86: 1. 건설 전 룰렛 확률 미리보기.
   87: 2. 룰렛 토큰 장부.
   88: 3. T-30/T-15/T-5 공세 전조.
   89: 4. 상성·사거리·타기팅 오버레이.
   90: 5. 웨이브 종료 후 라인별 원인 보고.
  120: ## 5. 현재 우선순위
  121: 
  122: ```text
  123: 1. 승인 룰렛 계약 복구
  124: 2. 전투 → 거점·성문·승패 목적 루프 연결
  125: 3. 승인 코어 UX 6종 최소 구현
  126: 4. 10~15분 코어 플레이테스트
  136: - 현재 구현과 미구현을 파일 증거로 분리한다.
  137: - 과거 `구현 전`과 과도한 `수직 슬라이스 완료` 주장을 현재 상태로 사용하지 않는다.
  138: - 프로젝트 코어는 2026-07-22 사용자 확인으로 `CORE_CONFIRMED`·`CORE_LOCKED`다.
  139: - 다음 변경은 게임 코드 전체가 아니라 승인 룰렛 계약 복구로 한정한다.
```

### `docs/DECISIONS_PENDING.md` — ACTIVE / 414 lines

Hits: `룰렛` [5, 21, 33, 246, 258, 268, 274, 320, 378, 405, 406, 414], `완성선` [23, 26], `중앙 가로줄` [23], `럭키` [26, 272], `이동권` [26, 279, 284], `결과 보관` [27, 285]

```text
    2: 
    3: - 갱신일: 2026-07-22
    4: - 기준: `docs/PROJECT_CORE.md`, `docs/CURRENT_IMPLEMENTATION_STATUS.md`, `docs/HANDOFF_CONTEXT.md`
    5: - 현재 작업: 정본·프로젝트 코어 복구 / 다음 기능 게이트: 승인 룰렛 계약 복구
    6: - 원칙: `승인 구조`, `제안서 승인 대기`, `PoC 조정`, `장기 확장`을 구분한다.
    7: 
    8: 체크되지 않은 항목은 아직 구현 사양으로 확정하지 않는다. 표시명이나 이미지가 달라도 새 적군 UnitProfile을 만들지 않는다.
   18: - 확인 근거: 2026-07-22 사용자의 `코어확정` 지시.
   19: - 후속 변경은 사용자 명시적 승인, 제거 테스트, 대안과 영향 범위 기록을 요구한다.
   20: 
   21: ### B. 다음 기능 작업 — 승인 룰렛 계약 복구
   22: 
   23: 현재 구현은 9개의 카드를 직접 반환하지만 승인 계약은 중앙 가로줄 판정, 동일 심벌 완성선, 등급과 단일 보상 생성이다.
   24: 
   25: - [ ] placeholder 테스트를 승인 계약 테스트로 교체하는 Plan 승인.
   26: - [ ] 중앙 판정·완성선·등급·단일 보상을 먼저 복구하고 X·금화·럭키 찬스·이동권은 같은 계약 안에서 단계적으로 연결.
   27: - [ ] 결과 보관함 기본 용량과 UI는 기존 미확정 상태 유지.
   28: - [ ] 전투 목적 루프와 코어 UX를 같은 PR에 섞지 않음.
   29: 
   30: ### C. 검증 증거
   31: 
   32: - [ ] 문서 PR 뒤 Godot editor import와 기존 headless suite 재실행.
   33: - [ ] 룰렛 복구 PR에서 같은 시드·보드·결과 결정론 검증.
   34: - [ ] 코어 UX 뒤 1920×1080·1280×720 사람 플레이.
   35: - [ ] W1~W20 전체 플레이는 코어 루프 완결 뒤 실행.
   36: 
  243: - Tier 1 금화 40, 건설 25초 첫 기준.
  244: - 30초마다 Tier 2 비용 -10%, 150초 최대 -50%.
  245: - Tier 2 완료부터 선택 토큰과 일반 자동생산.
  246: - 건물 수가 룰렛 토큰 수를 결정하며 Tier는 토큰 수를 늘리지 않는다.
  247: 
  248: ### 건물 PoC 조정
  249: 
  255: 
  256: ---
  257: 
  258: ## 7. 경제·룰렛
  259: 
  260: ### 승인 초기값
  261: 
  265: 기본 수입 +5 / 20초
  266: 중앙 접전지 +4 / 60초 / 소유 지점
  267: 중간거점 +2 / 30초 / 소유 지점
  268: 기본 룰렛 20
  269: ```
  270: 
  271: - 적 처치·웨이브 클리어 고정 금화 없음.
  272: - 럭키 12%, 실패당 +8%p, 6회 실패 뒤 확정.
  273: - 전설 스테이지당 1회.
  274: - 룰렛 결과는 공용 `archetype_id`와 Rank를 생성한다.
  275: 
  276: ### 검증·조정
  277: 
  278: - [ ] 최소 100,000시드 분포.
  279: - [ ] X·금화·이동권 기본 가중치.
  280: - [ ] 병영·특수병단 토큰 가중치.
  281: - [ ] Rank 자연 확률.
  282: - [ ] 금화 EV가 회전비 30% 이하인가.
  283: - [ ] 기본·시장·접전지·거점 수입 시간축.
  284: - [ ] 이동권 가격·상한·상점 출현률.
  285: - [ ] 결과 보관함 3칸 UX.
  286: - [ ] 난이도별 시작 경제 배율.
  287: 
  288: ---
  317: - 1920×1080, 16:9.
  318: - 미니맵 없음.
  319: - 성문·거점·접전지 상태는 월드 위 표시.
  320: - 룰렛 좌하단, 건설·용병·전술 하단, 벨루 우하단.
  321: - 클린 전술 픽셀 + 미니어처 치비 + 고해상도 픽셀 재질.
  322: - 공용 10병종마다 allied/veil Visual Set.
  323: - 양 진영 시트는 공용 AnimationContract를 지킴.
  375: - [ ] 같은 seed·input log 결과 재현.
  376: - [ ] 3라인 연결·성문·거점·노드·우회 규칙.
  377: - [ ] StageManifest가 공용 archetype만 참조.
  378: - [ ] 룰렛·경제·웨이브·성능 상한 검증.
  379: 
  380: ---
  381: 
  402: 
  403: ```text
  404: 1. 프로젝트 코어 확정·잠금과 정본 복구 PR 병합
  405: 2. 승인 룰렛 계약 복구 Plan
  406: 3. 룰렛 계약 구현·자동 검증
  407: 4. 전투 목적 루프 연결
  408: 5. 승인 코어 UX 6종
  409: 6. 10~15분 사람 플레이와 1080p·720p QA
  411: 8. 콘텐츠·아트 확장
  412: ```
  413: 
  414: 현재는 새로운 병종·Tier·보스·캠페인 콘텐츠를 추가하는 단계가 아니다. 다음 기능 변경은 승인 룰렛 계약 복구로 제한한다.
```

### `docs/DOCUMENTATION_MAP.md` — ACTIVE / 156 lines

Hits: `roulette` [81, 108], `룰렛` [81]

```text
   78: | 새 이미지·도표·UI 시안 유입 | `images/README.md`, `images/VISUAL_REFERENCE_INDEX.md`, 관련 APPROVED 기획서 |
   79: | 벨루·튜토리얼·HUD·대사 | `design/APPROVED_BELLU_MASCOT_AND_GUIDE_CONTRACT.md`, `design/APPROVED_BELLU_SINGLE_GUIDE_AND_FIRST_10_MINUTE_FLOW.md` |
   80: | Dopamine Driven Design·첫 10분 | `design/APPROVED_DOPAMINE_DRIVEN_DESIGN_AND_FIRST_10_MINUTES.md` |
   81: | 룰렛·등급·토큰 | `design/APPROVED_ROULETTE_CORE_RULES.md`, `design/APPROVED_ROULETTE_PROBABILITY_TARGETS_POC_V1.md`, `design/APPROVED_UNIT_GRADE_AND_ABILITY_GROWTH.md` |
   82: | 기본 병영·특수병단·Tier 3 | `design/APPROVED_PLAYER_TEN_UNIT_LINEAGES_POC_V1.md`, `design/APPROVED_BARRACKS_AND_SPECIAL_CORPS_UNIT_TREE_V5.md`, `design/APPROVED_BARRACKS_TIER2_TIER3_INTEGRATED_TREE_V2.md` |
   83: | 전투 계산·키워드·상태이상·비행 | `design/APPROVED_COMMON_COMBAT_AND_RANK_BUDGET_POC_V1.md`, `design/APPROVED_COMBAT_KEYWORDS_STATUS_EFFECTS_AND_FLIGHT.md` |
   84: | 건설·경제·전문화·전술·용병 | `design/APPROVED_BUILDINGS_TACTICAL_MERCENARY_POC_V1.md`, `design/APPROVED_STAGE_ECONOMY_AND_BUILDING_COST_BASELINE_V1.md` |
  105: 베일종
  106: ```
  107: 
  108: 레거시 명칭 `Roulettebound`, `율비`, `경계의 율`, `은종성채`, `무명야`는 과거 변경 이력 외에는 사용하지 않는다.
  109: 
  110: ## 핵심 책임 원본
  111: 
```

### `docs/GODOT_PROJECT_STRUCTURE.md` — REFERENCE_OR_OTHER / 456 lines

Hits: `roulette` [30, 38, 45, 261, 285, 365], `룰렛` [77, 94, 391, 406], `spin` [365]

```text
   27:   battle/
   28:   buildings/
   29:   units/
   30:   roulette/
   31:   waves/
   32:   ui/
   33: scripts/
   35:   battle/
   36:   buildings/
   37:   units/
   38:   roulette/
   39:   waves/
   40:   ui/
   41: resources/
   42:   units/
   43:   buildings/
   44:   battlefields/
   45:   roulette/
   46:   waves/
   47:   presentation/
   48: data/
   74: - 상·중·하 라인과 전장 객체 조합.
   75: - 본진, 성문, 중간거점, 중앙 접전지, 건설 노드, 우회 출입구 배치.
   76: - 전투 시간과 월드 이벤트 진행.
   77: - 병종·건물·룰렛 데이터의 책임 원본이 되지 않는다.
   78: 
   79: ### Unit
   80: 
   91: 
   92: - 현재 체력, Tier, 건설·활성·비활성·업그레이드·폐허 상태.
   93: - 생산 타이머, 수입과 공격 같은 런타임 행동.
   94: - BuildingProfile과 룰렛 토큰 규칙을 중복 소유하지 않는다.
   95: - 중간거점 소유권 변화에 따른 사용 가능 상태를 반영한다.
   96: 
   97: ### UI
  258:  ├─ CombatClock
  259:  ├─ EconomyService
  260:  ├─ BuildingRegistry
  261:  ├─ RouletteService
  262:  ├─ DeploymentService
  263:  ├─ CaptureService
  264:  ├─ WaveService
  282: 
  283: HUD
  284:  ├─ Resource display
  285:  ├─ Roulette panel
  286:  ├─ Bench/deployment panel
  287:  ├─ World objective status
  288:  ├─ Wave telegraph
  362: ```gdscript
  363: signal build_requested(node_id: StringName, building_id: StringName)
  364: signal deployment_requested(reserve_id: int, lane_id: StringName)
  365: signal roulette_spin_requested(reference_row: int)
  366: signal strongpoint_owner_changed(strongpoint_id: StringName, owner_team_id: int)
  367: signal gate_destroyed(gate_id: StringName)
  368: signal unit_spawn_requested(archetype_id: StringName, owner_team_id: int, visual_faction_id: StringName)
  388: ## 13. UI 원칙
  389: 
  390: - HUD는 `Control`, `Container`, `Theme`로 구성한다.
  391: - 룰렛 칸, 대기 유닛 카드, 건설 상품과 징조 행처럼 반복 단위만 재사용 Scene으로 분리한다.
  392: - 전장 목표 상태는 실제 위치에 표시한다.
  393: - 미니맵은 만들지 않는다.
  394: - 한국어 확장, 1920×1080과 1280×720을 검증한다.
  403: - UnitArchetypeProfile·Tier·Rank와 공통 전투 데이터.
  404: - FactionVisualProfile·AnimationContract 스키마.
  405: - BattlefieldProfile.
  406: - 룰렛 확률·경제·웨이브 책임 원본.
  407: - 저장 스키마가 생긴 이후 저장 코드.
  408: 
  409: 변경 시 호출 위치, 데이터 호환, headless 결과와 실제 플레이를 함께 검증한다.
```

### `docs/HANDOFF_CONTEXT.md` — ACTIVE / 309 lines

Hits: `룰렛` [17, 18, 66, 73, 84, 101, 103, 224], `이동권` [85]

```text
   14: 
   15: ## 1. 가장 먼저 알아야 할 것
   16: 
   17: 1. 오멘워드는 건물로 룰렛의 토큰·확률과 증원 체계를 설계하고 세 전선을 지휘하는 판타지 전략 오토배틀 게임이다.
   18: 2. 저장소에는 Phase 0 기술 기준선과 수직 슬라이스 구성요소가 존재하지만 승인 룰렛·전투 목적·코어 UX는 미완결이다.
   19: 3. 과거 Phase 0 Work Order의 `구현 전`과 README의 과도한 `수직 슬라이스 완료`를 현재 상태로 재사용하지 않는다.
   20: 4. 새 Codex 채팅은 `PROJECT_CORE.md`, `CURRENT_IMPLEMENTATION_STATUS.md`, 실제 main, validation 문서와 Issue·PR을 대조한 뒤 다음 최소 변경을 제안한다.
   21: 5. 아군과 적군은 별도 병종 전투 데이터를 만들지 않고 공용 10병종에 서로 다른 FactionVisualProfile을 연결한다.
   63: 
   64: ### 한 문장
   65: 
   66: > 예고된 세 전선의 위협을 읽고, 제한된 건물 노드로 룰렛 확률을 설계한 뒤, 당첨된 증원을 어느 전선에 투입할지 결정해 전황을 뒤집는 실시간 전략 오토배틀 게임.
   67: 
   68: ### 핵심 루프
   69: 
   70: ```text
   71: 베일의 징조 확인
   72: → 건물·토큰·전술 선택
   73: → 3×3 룰렛
   74: → 병력 획득·라인 배치
   75: → 3라인 교전
   76: → 접전지·중간거점 공방
   81: ### 첫 10분 약속
   82: 
   83: - 세계관과 전선 위기를 짧게 이해한다.
   84: - 건설→룰렛→배치→역전을 두 번 체험한다.
   85: - 이동권과 상위 등급의 강함을 체감한다.
   86: - 방패병·대검전사·암살자 중 첫 전문화를 선택한다.
   87: - 설명보다 행동과 즉시 반응으로 시스템을 학습한다.
   88: 
   98: + HUMAN_QA_NOT_RUN
   99: ```
  100: 
  101: 현재 Godot 프로젝트는 기술·데이터 그레이박스와 여러 수직 슬라이스 구성요소를 포함한다. 그러나 승인 룰렛 판정, 전투 상태 기반 승패, 접전지·거점·성문 연결과 승인 UX 6종이 닫히지 않았으므로 “핵심 수직 슬라이스 완료”로 부르지 않는다.
  102: 
  103: 다음 순서는 정본 복구 뒤 승인 룰렛 계약, 전투 목적 루프, 코어 UX, 사람 플레이 검증이다.
  104: 
  105: ## 4. 전장 불변 구조
  106: 
  221: - 이미지 안의 맵 연결, 요새·거점 배치와 건설 노드 수.
  222: - 좌하단 전장 요약 UI와 미니맵처럼 보이는 구성.
  223: 
  224: 현재 전장·경제·룰렛 책임 문서가 이미지보다 우선한다.
  225: 
  226: ## 9. 애니메이션·연출 계약
  227: 
```

### `docs/OMENWARD_GAME_DESIGN.md` — ACTIVE / 778 lines

Hits: `룰렛` [7, 18, 22, 28, 37, 46, 83, 225, 451, 463, 476, 490, 551, 581, 625, 647, 684, 704, 712], `중앙 가로줄` [479], `럭키` [491], `결과 보관` [492], `이동권` [583]

```text
    4: - 문서 버전: **v0.20**
    5: - 갱신일: 2026-07-22
    6: - 상태: **프리프로덕션 계약 승인 / Godot 기술·데이터 기준선 구현 / 핵심 수직 슬라이스 부분 구현·코어 루프 미검증**
    7: - 장르: 실시간 3라인 전략 오토배틀 + 건물 기반 3×3 룰렛 빌드
    8: - 플랫폼: Windows PC / 마우스·키보드 / 싱글플레이 PvE
    9: - 엔진: Godot + GDScript
   10: - 프로젝트 코어: **CORE_CONFIRMED / CORE_LOCKED** (2026-07-22 사용자 확인)
   15: 
   16: ## 1. 한 문장 소개
   17: 
   18: 플레이어는 루메른 왕국 트리븐 전선의 지휘관이 되어 건물로 룰렛 확률과 증원 체계를 설계하고, 베일의 법칙에 따라 베일런 황야에서 넘어오는 공세를 세 전선에서 막아낸다.
   19: 
   20: ## 2. 핵심 재미
   21: 
   22: > **다음 공세를 미리 읽고 건물과 룰렛 확률을 설계한 뒤, 당첨된 병력과 전술로 무너지는 전선을 간발의 차이로 뒤집는다.**
   23: 
   24: 핵심 감정:
   25: 
   26: - 예고된 위험을 읽는 긴장.
   27: - 제한된 자원으로 빌드를 선택하는 고민.
   28: - 룰렛 결과를 조작해 원하는 병력을 얻는 기대.
   29: - 적절한 라인 배치와 건설이 즉시 전황을 바꾸는 만족.
   30: - 거점과 성문을 밀고 당기는 전선 변화.
   31: 
   34: ```text
   35: 베일의 징조 확인
   36: → 건물·토큰·전술 선택
   37: → 3×3 룰렛 회전·조작
   38: → 병력 획득·대기
   39: → 상·중·하 라인 배치
   40: → 자동 교전과 전술 개입
   43: → 다음 공세 준비
   44: ```
   45: 
   46: - 첫 10분 안에 건설→룰렛→배치→역전 루프를 두 번 체험한다.
   47: - 새 시스템은 한 문장 설명, 한 번의 행동, 즉시 결과로 10~30초 안에 기본 사용법을 익힌다.
   48: - 정보는 설명 메뉴보다 실제 전장, 벨루, 베일의 징조와 조작 미리보기로 전달한다.
   49: 
   80: | 괴물 분류 | 베일종 |
   81: | 안내자 | 벨루 |
   82: 
   83: 룰렛은 UI 용어다. 세계관에서는 전술판, 전력 표식, 증원 경로로 가볍게 번역한다. 병사는 자신이 룰렛에서 생성됐다고 인식하지 않는다.
   84: 
   85: ## 5. 벨루
   86: 
  222: ### 진영별로 달라지는 것
  223: 
  224: - 소유 팀과 적대 관계.
  225: - 아군 배치·룰렛 생산 또는 적 웨이브 출격.
  226: - 스프라이트·초상화·아이콘·팔레트.
  227: - 세계관 표시명과 설명.
  228: 
  448: | 비행병 | 240초 | 4 |
  449: | 거인 | 300초 | 6 |
  450: 
  451: 건물 Tier는 생산량, 속도, 품질과 전투 성능을 강화하지만 룰렛 토큰 수는 **완공된 건물 개수**로 계산한다.
  452: 
  453: ---
  454: 
  460: 기본 수입 20초마다 +5
  461: 중앙 접전지 60초마다 소유 지점당 +4
  462: 중간거점 30초마다 소유 지점당 +2
  463: 기본 룰렛 20
  464: ```
  465: 
  466: - 중간거점 점령 시 생산과 건설권이 점령 진영으로 이전된다.
  473: 
  474: ---
  475: 
  476: ## 14. 룰렛
  477: 
  478: - 3×3 보드.
  479: - 중앙 가로줄 기본 판정.
  480: - 같은 비-X 심벌 3개가 완성된 줄만 보상.
  481: 
  482: | 완성 줄 | 등급 |
  487: | 8·9칸 동일 | 전설 |
  488: 
  489: - 전설은 스테이지당 1회, 이후 9칸 동일은 영웅 2기.
  490: - 상위 등급은 룰렛으로만 생성.
  491: - 럭키 기본 12%, 실패당 +8%p, 6회 실패 뒤 확정.
  492: - 결과 보관함 기본 3칸.
  493: - 금화 1/2/3줄 이상은 15/40/100 지급.
  494: - 금화 장기 평균 지급 목표는 회전비의 30% 이하.
  495: 
  548: 
  549: - 전역 쿨다운은 없다.
  550: - 용병 상점은 일반·엘리트만 판매하고 토큰을 추가하지 않는다.
  551: - 용병은 룰렛 실패 복구와 특수 지원을 담당한다.
  552: - 바리케이드는 도로를 영구 완전 봉쇄하지 않는다.
  553: 
  554: ---
  578: 튜토리얼은 다음을 포함한다.
  579: 
  580: - 첫 병영과 일반 검사.
  581: - 첫 룰렛과 라인 배치.
  582: - 포탑 또는 바리케이드 역전.
  583: - 이동권으로 엘리트 생성.
  584: - 방패병·대검전사·암살자 중 첫 Tier 2 선택.
  585: - 중간거점 점령과 건설·생산권 변화.
  586: - 암살자 안개 우회로.
  622: - 기본 전략 줌에서 양측 본진과 세 라인 전체 확인.
  623: - 별도 미니맵 없음.
  624: - 실제 전장 위 성문·중간거점·접전지 상태 표시.
  625: - 룰렛 좌하단, 건설·용병·전술 하단, 벨루 우하단.
  626: 
  627: ### 아트
  628: 
  644: 
  645: - 기본 전투는 낮은 현악·타악·종 모티프.
  646: - W5·W10·W15에서 밀도 증가.
  647: - 룰렛 등급별 음정 단계 상승.
  648: - 벨루는 맑은 두 음 종 모티프.
  649: - 숫자 정보는 음성보다 HUD 우선.
  650: 
  681: - 모든 아키타입에 양 진영 Visual Set 존재.
  682: - 양 Visual Set의 상태·프레임·피벗·이벤트 호환.
  683: - 같은 아키타입·Tier·등급의 양 진영 전투 결과 동일.
  684: - 100,000시드 룰렛 분포.
  685: - 경제·할인·환불·식량·거점 생산.
  686: - 동일 시드 전투 재현.
  687: - 60초 공세와 W5·W10·W15·W20 이정표.
  701: - 3라인 전장·성문·중간거점·접전지·우회로.
  702: - 공용 10병종과 Tier·등급 계보.
  703: - 아군·적군 공용 데이터와 진영 이미지 분리.
  704: - 건물·경제·룰렛·웨이브·전술·용병.
  705: - 튜토리얼·약 3시간 캠페인·절차 생성.
  706: - HUD·아트·애니메이션·오디오·성능 계약.
  707: - 수직 슬라이스 전장 초기값.
  709: ### PoC 조정 가능
  710: 
  711: - 모든 세부 전투·생산·경제·Threat 수치.
  712: - 룰렛 가중치와 등장 확률.
  713: - 점령·성문·암살자 시간.
  714: - 애니메이션 FPS와 프레임 수.
  715: - 카메라 흔들림·히트 스톱.
```

### `docs/OMENWARD_ROADMAP.md` — ACTIVE / 368 lines

Hits: `룰렛` [5, 24, 48, 68, 135, 192, 227, 248, 287, 362, 363, 368], `완성선` [68], `판정 줄` [68], `결과 보관` [227], `럭키` [288]

```text
    2: 
    3: - 갱신일: 2026-07-22
    4: - 기준: `docs/PROJECT_CORE.md`, `docs/CURRENT_IMPLEMENTATION_STATUS.md`, `docs/HANDOFF_CONTEXT.md`
    5: - 현재 상태: **C0 정본·프로젝트 코어 확정·잠금 완료 / C1 승인 룰렛 계약 복구 착수**
    6: - 현재 조사 입력: `docs/work_orders/0002-current-main-audit-and-next-iteration-plan-mode.md`
    7: - 사전 기술 추천안: `docs/design/proposals/0001-phase-0-godot-bootstrap.md`
    8: - 모든 코드·Scene·Resource·데이터 구조 변경은 Codex Plan Mode 제안서와 사용자 승인 후 시작한다.
   21: → Phase 0 기술·데이터 기준선 구현
   22: → 수직 슬라이스 구성요소 부분 구현
   23: → 정본·프로젝트 코어 확정·잠금 완료
   24: → [현재] 승인 룰렛 계약 복구
   25: → 전투 목적 루프 연결
   26: → 승인 코어 UX 6종
   27: → 10~15분 코어 플레이테스트
   45: 
   46: 오멘워드의 완료는 기능 수가 아니라 다음 경험과 품질을 만족하는 상태다.
   47: 
   48: - 플레이어가 다음 공세를 읽고 건물·룰렛·배치로 대응한다.
   49: - 세 라인의 성문·중간거점·접전지 상태를 미니맵 없이 파악한다.
   50: - 공용 10병종 데이터가 아군·적군 이미지 세트에서 동일하게 동작한다.
   51: - 암살자 우회와 거점·성문 공방이 빌드 선택을 바꾼다.
   65: | P0 프리프로덕션 | 제품·전장·공용 데이터·아트·연출 계약 | 완료 | 정본 유지 |
   66: | P1 기술 기준선 | 실행·데이터·결정론·검증 골격 | **구현됨 / 최신 runtime 재검증 필요** | C0 정본 일치 |
   67: | C0 정본·코어 복구 | 프로젝트 코어·상태·로드맵 일치 | **완료 / CORE_LOCKED** | 정본 PR 병합 |
   68: | C1 룰렛 계약 복구 | 중앙 판정 줄·완성선·등급·단일 보상 | **현재** | 승인 계약 테스트 PASS |
   69: | C2 전투 목적 루프 | 접전지·거점·성문·승패·경제 연결 | 부분 구현 | End-to-End 전투 PASS |
   70: | C3 코어 UX | 승인 UX 6종을 실제 데이터와 연결 | 미시작 | 이해도·가독성 기준 |
   71: | C4 코어 플레이테스트 | 10~15분 핵심 재미와 학습 검증 | 미실행 | 사람 플레이 기준 충족 |
  132: 
  133: - `project.godot`, Scene, GDScript, Resource, 데이터, 테스트 생성·수정.
  134: - 구현 브랜치·커밋·PR.
  135: - 실제 전투·룰렛·건설 구현.
  136: 
  137: ### 종료 기준
  138: 
  189: ### 제외
  190: 
  191: - 실제 전투·이동·AI.
  192: - 룰렛·건설·경제·점령·성문·웨이브.
  193: - 3라인 실제 Battle Scene.
  194: - 최종 아트·VFX·오디오.
  195: 
  224: - 성문 6개.
  225: - 중간거점·접전지·전방 2·후방 1 노드.
  226: - 최소 건물 2종.
  227: - 최소 3×3 룰렛과 결과 보관·배치.
  228: - 공용 아키타입 대표 3~5종.
  229: - 같은 아키타입의 아군·적군 이미지 생성.
  230: - 암살자 우회 또는 공성 역할.
  245: ```text
  246: 징조 확인
  247: → 건설
  248: → 룰렛
  249: → 배치
  250: → 3라인 교전
  251: → 거점 점령
  284: 
  285: ### 확률·경제
  286: 
  287: - 룰렛 최소 100,000시드 시뮬레이션.
  288: - 금화 EV와 럭키 확률.
  289: - 시작 경제·시장·접전지·거점 수입 시간축.
  290: - 식량 부족과 생산 대기.
  291: 
  359: 
  360: ```text
  361: 프로젝트 코어 확정·잠금 반영과 정본 PR 병합
  362: → 최신 main에서 승인 룰렛 책임 원본·구현·테스트 전수 감사
  363: → 룰렛 계약 복구 Plan
  364: → Build
  365: → Adversarial Review·Validation Review·Integration Review
  366: ```
  367: 
  368: 다음 기능 변경은 승인 룰렛 계약 복구로 제한한다. 전투 목적 루프·코어 UX·신규 콘텐츠는 같은 PR에 섞지 않는다.
```

### `docs/PHASE_0_VALIDATION.md` — ACTIVE / 23 lines

Hits: `roulette` [19]

```text
   16: 
   17: 1. Run `scenes/main/main.tscn` at 1920×1080 and confirm the status panel and three lane probe are fully visible.
   18: 2. Run at 1280×720 and confirm viewport/keep/integer scaling leaves intentional letterboxing rather than non-integer pixel scaling.
   19: 3. Confirm the probe only communicates Phase 0 data readiness; it must not simulate combat, roulette, capture, gates, waves, or enemy-specific units.
   20: 
   21: ## Goal 0002 handoff
   22: 
```

### `docs/PROJECT_CORE.md` — ACTIVE / 170 lines

Hits: `룰렛` [15, 24, 31, 33, 48, 49, 58, 59, 78, 89, 90, 91, 102, 103, 119, 134], `완성선` [58, 91, 122], `중앙 가로줄` [58], `결과 보관` [59], `판정 줄` [90, 122]

```text
   12: 
   13: ## 1. 정체성 한 문장
   14: 
   15: > **예고된 세 전선의 위협을 읽고, 제한된 건물 노드로 룰렛 확률을 설계한 뒤, 당첨된 증원을 어느 전선에 투입할지 결정해 전황을 뒤집는 실시간 전략 오토배틀 게임.**
   16: 
   17: ## 2. 플레이어 판타지
   18: 
   21: 플레이어가 책임지는 것은 다음 세 가지다.
   22: 
   23: 1. **예측** — 베일의 징조로 다음 공세의 라인·병종·수량·특수 행동을 읽는다.
   24: 2. **확률 설계** — 제한된 건물 노드와 전문화로 룰렛 토큰·출처·확률을 바꾼다.
   25: 3. **전선 커밋** — 얻은 병력을 어느 라인에 배치할지 결정하고 다른 라인의 위험을 감수한다.
   26: 
   27: ## 3. 핵심 루프
   28: 
   29: ```text
   30: 베일의 징조 확인
   31: → 건설 후보와 룰렛 확률 변화 비교
   32: → 제한 노드에 건설·전문화
   33: → 3×3 룰렛 회전·허용된 조작·최종 확정
   34: → 생성된 보상 확인
   35: → 상·중·하 라인 중 하나에 배치
   36: → 자동 교전·접전지·중간거점·성문 공방
   45: ### 4.1 기획 코어
   46: 
   47: - 적 공세는 대응 가능한 시간 전에 공개된다.
   48: - 건설은 경제·방어뿐 아니라 미래 룰렛 결과의 확률과 출처를 바꾼다.
   49: - 룰렛은 결과를 직접 선택하는 장치가 아니라, 플레이어가 사전에 설계한 확률을 시험하는 장치다.
   50: - 당첨 결과를 어느 라인에 투입할지가 최종 전술 선택이다.
   51: - 자동전투 결과는 거점·성문·승패와 다음 경제에 실제 영향을 준다.
   52: - 실패 원인이 다음 건설·확률·배치 판단으로 환류한다.
   55: 
   56: - 좌우 대칭의 독립된 상·중·하 3라인.
   57: - 제한된 후방·전방 건설 노드와 점령에 따른 건설권·생산권 이전.
   58: - 3×3 룰렛의 중앙 가로줄 기본 판정과 동일 심벌 완성선 기반 등급.
   59: - 룰렛 결과 보관·배치와 재회전 잠금.
   60: - 공용 병종 데이터와 진영별 표현 분리.
   61: - 접전지·중간거점·성문·같은 라인 암살자 우회.
   62: - 결정론적 시드·입력 로그·재현 가능한 핵심 결과.
   75: 
   76: | 분류 | 현재 포함 요소 | 변경 원칙 |
   77: |---|---|---|
   78: | `PROJECT_CORE` | 공세 예측, 건물 기반 확률 설계, 룰렛 판정, 라인 투입, 자동전투 결과, 원인 피드백 | 제거·대체 시 사용자 승인과 코어 재개 필요 |
   79: | `CORE_SUPPORT` | 거점·성문·암살자 우회, 확률 미리보기, 토큰 장부, 상성·사거리 표시, 라인 원인 보고 | 코어 인과를 약화시키지 않는 범위에서 조정 |
   80: | `MVP_SUPPORT` | 대표 병종 3~5개, 최소 건물 2~4개, 튜토리얼, 짧은 공세 묶음, 회색상자 UI | 코어 검증에 필요한 최소치로 제한 |
   81: | `CONTENT_VARIANT` | 공용 10병종 전체, Tier 3 세부 분화, W1~W20 전체, 다수 보스·캠페인 | 코어 플레이테스트 뒤 확장 |
   86: 
   87: 1. 세 라인은 일반 유닛이 자유롭게 횡단하지 않는 독립 전선이다.
   88: 2. 기본·일반 난이도에서는 치명적 공세 정보를 숨겨 난이도를 만들지 않는다.
   89: 3. 건물 선택은 룰렛 토큰·확률·출처 중 하나 이상에 관찰 가능한 영향을 준다.
   90: 4. 기본 룰렛 보상은 중앙 판정 줄이 성립할 때만 생성된다.
   91: 5. 룰렛 등급은 승인된 동일 심벌 완성선 규칙으로 계산한다.
   92: 6. 자동전투는 거점·성문·승패 상태를 실제로 변화시킨다.
   93: 7. 플레이어가 얻은 보상을 어느 라인에 투입할지 결정한다.
   94: 8. 패배와 손실의 핵심 원인을 플레이어가 확인할 수 있다.
   99: 
  100: | 제거·대체 가정 | 결과 | 판정 |
  101: |---|---|---|
  102: | 룰렛 제거 | 일반적인 3라인 디펜스가 됨 | 코어 훼손 |
  103: | 건물이 확률에 영향 없음 | 건설과 룰렛이 분리된 장르 혼합이 됨 | 코어 훼손 |
  104: | 공세 전조 제거 | 대응 설계가 불가능하고 RNG 의존이 커짐 | 코어 훼손 |
  105: | 라인 선택 제거 | 플레이어의 마지막 전술 책임이 사라짐 | 코어 훼손 |
  106: | 거점·성문 제거 | 세 라인이 세 개의 체력 막대로 축소됨 | 핵심 지원 훼손 |
  116: - 프로젝트 코어, GDD, 로드맵, 현재 상태와 Issue가 같은 구현 단계를 가리킨다.
  117: - `구현 전`, `완료`, `검증됨`을 실제 증거 없이 사용하지 않는다.
  118: 
  119: ### C1 — 룰렛 인과
  120: 
  121: - 건물 전후 확률 변화가 실제 판정 데이터와 일치한다.
  122: - 중앙 판정 줄·완성선·등급·출처·보상이 승인 계약대로 동작한다.
  123: - 같은 시드와 확정 보드는 같은 결과를 만든다.
  124: 
  125: ### C2 — 전장 목적 루프
  131: ### C3 — 코어 UX
  132: 
  133: - 건설 전 확률 미리보기.
  134: - 룰렛 토큰 장부.
  135: - T-30/T-15/T-5 공세 전조.
  136: - 상성·사거리·타기팅 오버레이.
  137: - 웨이브 종료 후 라인별 원인 보고.
```

### `docs/REFERENCE_REPOSITORIES.md` — REFERENCE_OR_OTHER / 208 lines

Hits: `룰렛` [156, 175, 192]

```text
  153: - `Control`, `Container`, `Theme`, 재사용 Scene.
  154: - 표시 데이터를 입력받고 사용자 의도를 Signal로 반환.
  155: - UI가 전역 게임 상태와 규칙을 직접 수정하지 않음.
  156: - 룰렛 칸, 결과 카드, 건설 상품, 웨이브 징조 같은 반복 단위만 컴포넌트화.
  157: - 별도 CSS·가상 DOM·범용 데이터 바인딩 프레임워크를 만들지 않음.
  158: 
  159: ### 검증 순서
  172: - UnitArchetype·Tier·Rank·공통 전투 데이터.
  173: - FactionVisual·AnimationContract 스키마.
  174: - BattlefieldProfile.
  175: - 룰렛·경제·웨이브 원본.
  176: - 저장 스키마가 생긴 뒤 저장 코드.
  177: 
  178: ## urban-legend에서 그대로 가져오지 않음
  189: - 다수 유닛의 3라인 이동·타기팅·교전.
  190: - 라인별 성문과 중간거점 권한 이전.
  191: - 건물 노드·폐허·생산·토큰 연쇄.
  192: - 건물 개수 기반 룰렛 풀.
  193: - 공용 archetype 기반 적 웨이브.
  194: - 암살자 오프맵 우회.
  195: - 공격 판정과 AnimationContract 이벤트 동기화.
```

### `docs/VERTICAL_SLICE_VALIDATION.md` — ACTIVE / 21 lines

Hits: `roulette` [14, 19]

```text
   11: git diff --check
   12: ```
   13: 
   14: The suite covers shared-unit data, three isolated lanes, gates, outposts, economy, building nodes, roulette, deployment, tutorial unlock, W1–W20 scheduling, and assassin bypass timing.
   15: 
   16: ## Manual QA still required
   17: 
   18: 1. Complete the tutorial, then start the regular stage.
   19: 2. Check roulette, build, deploy, and retry controls at 1920×1080.
   20: 3. Repeat at 1280×720 and confirm HUD, gates, outposts, and lane state remain readable.
   21: 4. Play through W1–W20 and inspect W15 legendary and W20 mythic milestones.
```

### `docs/base/SKILL_EXECUTION_REPORT.md` — REFERENCE_OR_OTHER / 36 lines

Hits: `룰렛` [26]

```text
   23: | 요청 | 예상 Mode | 필수 Skill |
   24: |---|---|---|
   25: | PR 누락·중복 적대적 검토 | REVIEW | project-intake, validation-review, integration-review |
   26: | 룰렛 확률 기획 | PLAN | project-intake, game-design |
   27: | Godot 성능 버그 수정 | BUILD | project-intake, engineering |
   28: | HUD UI 아트 시각 감사 | REVIEW | validation-review, integration-review, ui-art-audit |
   29: | 수직 슬라이스 MVP 설계 | PLAN | game-design, engineering, production-pm 이후 vertical-slice |
```

### `docs/base/SKILL_REGISTRY.json` — REFERENCE_OR_OTHER / 238 lines

Hits: `룰렛` [113]

```text
  110:       "id": "discipline.game-design",
  111:       "category": "disciplines",
  112:       "path": "skills/disciplines/02-game-design/SKILL.md",
  113:       "triggers": ["게임 규칙", "룰렛", "전투", "경제", "병종", "밸런스"],
  114:       "depends_on": ["foundation.project-intake"],
  115:       "priority": 70
  116:     },
```

### `docs/benchmarks/0001-core-game-benchmark-proposal.md` — REFERENCE_OR_OTHER / 428 lines

Hits: `룰렛` [8, 24, 25, 28, 29, 60, 107, 119, 140, 144, 232, 294, 303, 333, 340, 352, 386, 388], `roulette` [9, 36, 78, 88, 105, 136, 162, 177, 193, 220, 249, 274, 303, 326], `럭키` [58, 82, 329, 349, 407], `slot` [64, 418]

```text
    5: 
    6: ## 1. 목적
    7: 
    8: - 해결할 문제: 슬롯·룰렛, 실시간 3라인 전투, 건설 경제, 병종 상성, 적 웨이브 전조를 결합할 때 각 시스템의 장점이 서로를 가리지 않고 하나의 명확한 플레이 경험으로 이어져야 한다.
    9: - 플레이어/개발 가치: 이미 반응이 검증된 게임의 강점과 불만 요소를 분리해, Roulettebound가 표면적인 장르 혼합에 머무르지 않고 `건물로 확률을 설계하고 예고된 전선 위협에 대응한다`는 독자적인 핵심을 빠르게 검증한다.
   10: 
   11: ## 2. 현재 상태와 근거
   12: 
   21: 
   22: ### 현재 프로젝트 핵심
   23: 
   24: - 건물 개수가 3개 릴의 룰렛 토큰 수와 확률을 결정한다.
   25: - 플레이어는 룰렛 보상을 대기칸에 보관하고 상·중·하 라인으로 배치한다.
   26: - 본진 후방 안전 노드 6개와 라인별 위험 노드 3개가 경제·생산·포탑 사이에서 경쟁한다.
   27: - 라인 중앙 접전지 점령 중에는 추가 금화를 얻는다.
   28: - 적은 룰렛을 사용하지 않고 건물 기반 웨이브를 생산하며 약 30초 전에 라인과 병종을 예고한다.
   29: - 건물은 티어 상승으로 생산량, 생산속도, 유닛 품질, 포탑 성능을 강화한다. 티어는 룰렛 토큰 수를 늘리지 않는다.
   30: 
   31: ### 조사 방법
   32: 
   33: - 공식 Steam 상점 설명과 사용자 평가 집계
   34: - 개발사 공식 소개
   35: - 주요 비평 기사에서 반복적으로 언급된 장점과 문제
   36: - Roulettebound 기획과의 직접 연결성
   37: 
   38: 정확한 리뷰 비율은 조사 시점의 스냅샷이며 이후 변동할 수 있다.
   39: 
   55: 
   56: - 한 판 목표 시간
   57: - 웨이브 수량 정보를 정확히 공개할지 범위로 공개할지
   58: - 럭키 찬스를 순수 확률로 둘지 실패 누적 보정으로 둘지
   59: - 건물 티어를 고정 성장으로 할지 선택형 분기로 할지
   60: - 상대 건물로 추정되는 룰렛 확률을 정확한 수치로 공개할지 정성 정보로 공개할지
   61: 
   62: ## 4. 벤치마킹 매트릭스
   63: 
   64: ### 4.1 Slotbound
   65: 
   66: **관찰 요소**
   67: 
   75: - 조사 시점에 출시 예정이며 Steam 사용자 리뷰가 없다.
   76: - 가장 가까운 표면 비교 대상이지만 시장 반응이 검증된 게임으로 보기는 어렵다.
   77: 
   78: **Roulettebound 적용 후보**
   79: 
   80: - 슬롯 결과가 즉시 전장에 영향을 주는 강한 연출
   81: - 확률을 바꾼 원인이 무엇인지 보여주는 코어형 효과 표현
   82: - 실패가 계속될 때 기대감이 축적되는 럭키 찬스 연출
   83: 
   84: **그대로 가져오지 않을 것**
   85: 
   86: - 흡수·진화, 랜덤 능력치, 각인, 아이템, 코어를 MVP에 동시에 넣는 구조
   87: - 고유 유닛명, 진화 계보, UI 배치와 슬롯 연출 복제
   88: - Roulettebound의 건물 기반 토큰 구성보다 유닛 흡수 성장을 우선하는 구조
   89: 
   90: ### 4.2 Luck be a Landlord
   91: 
  102: - 반면 일부 평가는 구조가 경직되고 로그라이크의 진행감이 약하다고 지적했다.
  103: - 슬롯 외형 때문에 실제 화폐 도박이나 소액결제가 없음에도 플랫폼 등급·정책 문제를 겪었다.
  104: 
  105: **Roulettebound 적용 후보**
  106: 
  107: 1. **룰렛 토큰 장부**
  108:    - 현재 토큰 개수, 각 토큰의 칸당 확률, X 토큰 수를 항상 확인할 수 있게 한다.
  109: 2. **건설 전 확률 변화 미리보기**
  110:    - 건설 버튼에 `검사 25% → 40%, X 25% → 0%`처럼 결과를 미리 표시한다.
  116: **그대로 가져오지 않을 것**
  117: 
  118: - 수백 개 심벌을 빠르게 추가하는 콘텐츠 중심 확장
  119: - 룰렛 결과만으로 승패가 끝나는 점수 게임 구조
  120: - 도박을 연상시키는 실제 화폐형 연출과 상점 표현
  121: 
  122: ### 4.3 Commander Quest
  133: - 독특한 장르 결합과 상성·전장 시너지는 강점이다.
  134: - 90% 이상 평가를 받은 비교작보다 반응이 낮다는 점은 장르 결합 자체가 자동으로 높은 만족도를 만들지는 않으며, 명확성·완성도·밸런스가 중요하다는 위험 신호로 본다.
  135: 
  136: **Roulettebound 적용 후보**
  137: 
  138: - 병종 초상에 역할과 상성 아이콘을 고정 표시한다.
  139: - 적 웨이브 카드에서 `주요 위협 → 추천 대응 → 현재 보유 대응 병력`을 한 화면에 연결한다.
  140: - 룰렛 당첨 유닛을 어느 라인에 배치할지 결정할 때 예상 상성 결과를 간단히 미리 보여준다.
  141: 
  142: **그대로 가져오지 않을 것**
  143: 
  144: - 카드 덱과 룰렛 토큰 덱을 동시에 별도 시스템으로 운영하는 이중 덱빌딩
  145: - 전투·이벤트·메타 해금 구조를 첫 수직 슬라이스에 포함
  146: - 지나치게 많은 병종 친화도와 숨은 시너지
  147: 
  159: - 비평에서는 낮은 조작 부담으로 높은 전략 깊이를 제공하고, 패배 원인의 인과가 비교적 명확하다는 점이 강점으로 언급됐다.
  160: - 최근 평가가 장기 평가보다 낮은 점은 지속적인 밸런스와 콘텐츠 운영이 반응을 흔들 수 있음을 보여준다.
  161: 
  162: **Roulettebound 적용 후보**
  163: 
  164: 1. **공개 정보 기반 대응**
  165:    - 적 건물, 건물 티어, 다음 웨이브 구성과 출격 라인을 플레이어가 조사할 수 있게 한다.
  174: 
  175: - PvP 중심의 수십 개 유닛과 다수 기술 분기
  176: - 시즌 메타와 랭크 운영
  177: - Roulettebound의 실시간 경제보다 라운드별 완전 정지 준비를 우선하는 흐름
  178: 
  179: ### 4.5 Thronefall
  180: 
  190: - 조사 시점 Steam 영문 리뷰 10,800개와 최근 리뷰 169개 모두 95% 긍정으로 `압도적으로 긍정적`이다.
  191: - 최소한의 시스템으로 경제와 방어의 깊이를 만든 점, 짧은 플레이와 반복성이 높은 평가를 받았다.
  192: 
  193: **Roulettebound 적용 후보**
  194: 
  195: - 노드를 누르면 농장·시장·훈련소·포탑의 핵심 차이를 한 줄로 비교하는 즉시 건설 메뉴를 연다.
  196: - 건물 티어 상승은 긴 트리가 아니라 한 번에 이해할 수 있는 2가지 선택형 분기를 우선 검토한다.
  217: - 조사 시점 Steam 영문 리뷰 12,530개 중 93% 긍정, 최근 100개 중 91% 긍정이다.
  218: - 적 행동을 투명하게 공개하면서도 높은 전략성을 유지하는 대표 사례다.
  219: 
  220: **Roulettebound 적용 후보**
  221: 
  222: - 30초 전조를 세 단계로 나눈다.
  223:   - T-30: 라인, 병종, 수량, 특수 속성 공개
  229: **그대로 가져오지 않을 것**
  230: 
  231: - 모든 전투 결과가 완전히 결정적인 턴제 퍼즐 구조
  232: - 룰렛 결과까지 완전 예측 가능하게 만드는 것
  233: - 한 칸 단위 전술 이동 중심 게임으로 변경하는 것
  234: 
  235: ### 4.7 The King is Watching
  246: - 핵심 제한 메커니즘과 타협의 긴장이 호평을 받았다.
  247: - 무작위 건물 획득이 플레이어를 적응시키는 장점과 원하는 빌드를 막는 좌절을 동시에 만든다는 평가가 있었다.
  248: 
  249: **Roulettebound 적용 후보**
  250: 
  251: - 이미 확정한 `후방 6노드 + 라인별 3노드`를 단순 공간 제한이 아니라 게임의 핵심 타협 메커니즘으로 유지한다.
  252: - 모든 건물을 짓게 하는 확장보다, 노드가 모자라 기존 시설을 철거하거나 위험한 전방에 짓게 만드는 방향을 우선한다.
  271: - 2025년 얼리 액세스 첫 달 40만 장 판매가 보도됐으며, 짧고 단순하지만 깊은 조합성이 강점으로 평가됐다.
  272: - 후속 업데이트에서는 높은 난이도의 RNG 의존도를 낮추고 적 선택권을 늘리는 방향이 강조됐다.
  273: 
  274: **Roulettebound 적용 후보**
  275: 
  276: - MVP 이후 `지휘관 교리` 1개를 선택해 특정 병종 또는 건물 빌드에 방향성을 주는 실험
  277: - 고난도에서 무작위성을 더 키우기보다, 상대 웨이브와 제한 자원을 더 어렵게 조정
  291: 
  292: **적용 결론**
  293: 
  294: - MVP는 네 병종, 네 핵심 건물, 세 라인과 하나의 룰렛 계약에 집중한다.
  295: - 사제·마법사·도적, 스킬북, 장비, 영웅은 기본 인과관계가 보인 뒤 추가한다.
  296: 
  297: ### 5.2 운을 없애지 않고 운을 설계하게 함
  300: 
  301: **적용 결론**
  302: 
  303: - Roulettebound에서는 `건물 배치가 룰렛 덱빌딩`이라는 문장을 UI와 튜토리얼의 중심으로 둔다.
  304: - 확률 수치를 숨기지 않고 건설·파괴 전후 변화를 보여준다.
  305: 
  306: ### 5.3 패배 원인이 보여야 재도전이 생김
  323: 
  324: ## 6. 반복된 불만과 위험
  325: 
  326: | 위험 | 관찰된 문제 | Roulettebound 대응 |
  327: |---|---|---|
  328: | 장르 혼합 과부하 | 시스템이 많지만 각 선택의 효과가 희미함 | MVP 병종·건물 수 제한, 한 화면에서 인과 연결 |
  329: | RNG 좌절 | 원하는 빌드가 나오지 않아 대응 불가능 | 건물로 확률 직접 구성, 정확한 확률 공개, 럭키 찬스 보정 실험 |
  330: | 정답 반복 | 한 번 강한 빌드가 만들어지면 이후 판단이 사라짐 | 적 웨이브 전조와 라인 분산으로 매번 대응 요구 |
  331: | 결과 불명확 | 전투가 화려하지만 패배 이유를 모름 | 사거리·상성 오버레이와 라인 결과 요약 |
  332: | 경제 스노우볼 | 앞선 플레이어가 접전지 수입으로 계속 앞섬 | 접전 중 수입 중단, 보상 상한과 회복 수단 테스트 |
  333: | UI 과밀 | 룰렛, 전투, 건설, 웨이브 정보가 동시에 경쟁 | 경고 우선순위, 선택 시 상세, 기본 화면은 핵심 수치만 |
  334: | 도박 이미지 정책 | 실제 결제와 무관해도 슬롯 외형이 등급 문제를 유발 | 실제 화폐 연출 배제, 확률 공개, 구매형 랜덤 보상 금지 |
  335: 
  336: ## 7. 우선 적용 제안
  337: 
  338: ### A. 수직 슬라이스에 포함할 핵심
  339: 
  340: 1. **건설 전 룰렛 확률 미리보기**
  341: 2. **현재 토큰 장부와 X 토큰 표시**
  342: 3. **T-30/T-15/T-5 적 웨이브 전조**
  343: 4. **병종 상성·사거리 오버레이**
  346: 
  347: ### B. 수직 슬라이스 이후 실험
  348: 
  349: 1. 럭키 찬스를 실패 누적 보정으로 전환하거나 보완
  350: 2. 건물 티어의 `고정 성장` 대 `2갈래 선택형 성장` A/B 테스트
  351: 3. 전방 생산시설의 생산 보너스 여부
  352: 4. 룰렛 토큰 집중 또는 일시 비활성화 도구
  353: 5. 런 종료 빌드 요약과 재도전 프리셋
  354: 
  355: ### C. 초기 제외
  383: 
  384: ## 9. 검증 제안
  385: 
  386: - 정적 검사: 룰렛 토큰 수와 표시 확률의 합이 항상 일치하는지 확인
  387: - Godot headless: 건물 추가·파괴에 따른 토큰 풀, 전조 타이머, 접전지 수입 테스트
  388: - Scene 실행: 세 라인, 건설 노드, 웨이브 경고와 룰렛 UI가 동시에 읽히는지 확인
  389: - 수동 플레이 확인:
  390:   - 플레이어가 10초 안에 다음 위협 라인과 추천 병종을 말할 수 있는가
  391:   - 건설 전 확률 변화와 식량·금화 영향을 예측할 수 있는가
  404: 1. A 항목 6개를 수직 슬라이스 필수 UX로 채택할지
  405: 2. 건물 티어를 초기부터 선택형 2갈래로 설계할지, MVP는 고정 성장으로 시작할지
  406: 3. 적 웨이브 수량을 정확히 공개할지, `소수/다수` 범위로 공개할지
  407: 4. 럭키 찬스를 순수 확률로 유지할지, 실패 누적 보정을 넣을지
  408: 5. 전방 생산시설에 별도 생산 보너스를 줄지, 짧은 합류 거리만 보상으로 둘지
  409: 
  410: ## 12. 승인 게이트
  415: 
  416: ## 13. 주요 출처
  417: 
  418: - Slotbound Steam: https://store.steampowered.com/app/4459590/
  419: - Luck be a Landlord Steam: https://store.steampowered.com/app/1404850/
  420: - Commander Quest 공식 소개: https://flywaygames.com/en/game/commander-quest
  421: - Commander Quest Steam: https://store.steampowered.com/app/2697930/_Commander_Quest/
```

### `docs/benchmarks/APPLICATION_CANDIDATES.md` — REFERENCE_OR_OTHER / 31 lines

Hits: `룰렛` [7], `럭키` [16]

```text
    4: 
    5: ## A — 수직 슬라이스 필수 후보
    6: 
    7: - 건설 전 룰렛 확률 변화 미리보기
    8: - 현재 토큰 장부와 X 토큰 표시
    9: - T-30/T-15/T-5 적 웨이브 전조
   10: - 상성·사거리·타기팅 오버레이
   13: 
   14: ## B — 이후 실험 후보
   15: 
   16: - 럭키 찬스 실패 누적 보정
   17: - 건물 티어 2갈래 선택형 업그레이드
   18: - 전방 생산시설 생산 보너스
   19: - 토큰 집중 또는 일시 비활성화 수단
```

### `docs/benchmarks/PLAYER_REACTION_THEMES.md` — REFERENCE_OR_OTHER / 27 lines

Hits: `roulette` [21], `룰렛` [23]

```text
   18: - 메타 성장이나 해금이 기본 재미를 늦게 여는 문제
   19: - 확률과 슬롯 외형에서 발생하는 도박 이미지 및 등급 정책 위험
   20: 
   21: ## Roulettebound 검증 질문
   22: 
   23: - 플레이어가 건물과 룰렛 확률의 연결을 즉시 이해하는가?
   24: - 다음 웨이브를 보고 실제로 다른 건물·병종·라인 선택을 하는가?
   25: - 포탑과 경제시설의 노드 경쟁이 의미 있는 고민을 만드는가?
   26: - 패배 후 원인을 한 문장으로 설명할 수 있는가?
```

### `docs/benchmarks/README.md` — REFERENCE_OR_OTHER / 25 lines

Hits: `roulette` [3]

```text
    1: # 벤치마킹 문서
    2: 
    3: 벤치마킹은 참고 게임을 복제하기 위한 문서가 아니라, 플레이어 반응과 시스템 문제를 분석해 Roulettebound에 적용하거나 제외할 결론을 남기는 공간이다.
    4: 
    5: ## 현재 상태
    6: 
```

### `docs/benchmarks/REVIEW_CHECKLIST.md` — REFERENCE_OR_OTHER / 9 lines

Hits: `럭키` [6]

```text
    3: - [ ] A 항목 6개를 수직 슬라이스 필수 UX로 채택할지 결정
    4: - [ ] 건물 티어 고정 성장/선택형 분기 결정
    5: - [ ] 적 웨이브 수량 정확 공개/범위 공개 결정
    6: - [ ] 럭키 찬스 순수 확률/실패 누적 보정 결정
    7: - [ ] 전방 생산시설 추가 보너스 여부 결정
    8: - [ ] 승인된 항목만 `GAME_DESIGN.md`에 반영
    9: - [ ] 구현 전 별도 Codex Plan Mode 제안서 작성
```

### `docs/benchmarks/SOURCE_SNAPSHOT_2026-07-14.md` — REFERENCE_OR_OTHER / 16 lines

Hits: `slot` [7]

```text
    4: 
    5: | 게임 | 확인 상태 |
    6: |---|---|
    7: | Slotbound | 출시 예정, Steam 사용자 리뷰 없음 |
    8: | Luck be a Landlord | 영문 5,317개 중 94% 긍정, 전체 구매 리뷰 10,171개 매우 긍정적 |
    9: | Commander Quest | 구매 리뷰 1,562개 중 73% 긍정, 대체로 긍정적 |
   10: | Mechabellum | 영문 9,509개 중 84% 긍정, 최근 155개 중 74% 긍정 |
```

### `docs/design/APPROVED_15_WAVE_STAGE_CLOCK_AND_OVERTIME_V2.md` — ACTIVE / 254 lines

Hits: `룰렛` [178, 242]

```text
  175: 
  176: 신화급은 적군 전용이다.
  177: 
  178: - 플레이어 룰렛 등급에 신화를 추가하지 않는다.
  179: - 전설 스킬 구조 위에 신화 특성 1개와 다중 페이즈를 추가한다.
  180: - 직선 돌파·광역 공격·구조물 파괴와 높은 군중제어 저항을 가진다.
  181: - 시간이 지날수록 공격·이동 성능이 상승해 장기 교착을 끝낸다.
  239: 5. 20웨이브의 최종 압박은 적군 전용 신화 최종보스다.
  240: 6. 이전 공세가 남아 있어도 다음 공세가 예정대로 합류한다.
  241: 7. 16~19웨이브는 파밍이 아닌 누적 실패 압박으로 느껴진다.
  242: 8. 신화급은 플레이어 룰렛이나 플레이어 등급표에 추가되지 않는다.
  243: 
  244: ---
  245: 
```

### `docs/design/APPROVED_BARRACKS_AND_SPECIAL_CORPS_UNIT_TREE_V5.md` — ACTIVE / 342 lines

Hits: `룰렛` [53, 59, 74, 173, 174, 177, 198, 221, 225, 228, 239, 272, 320, 325]

```text
   50: - 기병 / 기병 토큰
   51: ```
   52: 
   53: - 자동생산과 룰렛 토큰을 함께 제공한다.
   54: - 자동생산은 일반 등급만 생성한다.
   55: - 한 병영은 전문화 하나만 선택한다.
   56: - 업그레이드 중에는 이전 생산과 토큰을 유지한다.
   57: - 생산 진행률은 보존한다.
   58: - 완료 뒤 첫 완성분부터 새 병종을 생산한다.
   59: - 토큰 변경은 다음 룰렛 회전부터 적용한다.
   60: 
   61: ---
   62: 
   71: 
   72: Tier 1 효과:
   73: 
   74: - 룰렛 토큰 없음.
   75: - 유닛 자동생산 없음.
   76: - 공격·방어·경제·식량 오라 없음.
   77: - 완공 뒤 활성 전투 시간에 따라 Tier 2 전문화 금화 비용 감소.
  170: 
  171: 토큰 규칙:
  172: 
  173: - 다음 룰렛 회전부터 선택 계열 토큰을 적용한다.
  174: - 진행 중인 룰렛 스냅샷은 변경하지 않는다.
  175: - 각 릴에 선택 계열 토큰 1개를 제공하는 것을 초기값으로 사용한다.
  176: - 건물이 파괴되면 다음 회전부터 해당 토큰을 제거한다.
  177: - 이미 확정된 룰렛 결과는 유지한다.
  178: 
  179: ---
  180: 
  195: - 일시정지 중에는 멈춘다.
  196: - 피격 중에도 생산은 계속된다.
  197: - 자동생산은 일반 등급만 생성한다.
  198: - 엘리트·영웅·전설은 룰렛 결과로만 획득한다.
  199: - 식량이 부족하면 `완성 대기` 상태가 된다.
  200: - 완성 대기 중에는 다음 생산 사이클을 시작하지 않는다.
  201: - 식량이 확보되면 즉시 출격 대기열에 넣고 다음 생산을 시작한다.
  218: 
  219: - 최대 할인 경로는 정확히 150초 늦게 Tier 2를 시작한다.
  220: - 최대 할인 경로도 15분 스테이지 안에서 최소 한 번 이상의 자동생산 기회를 가져야 한다.
  221: - 10웨이브 이후 새 특수병단은 자동생산보다 토큰과 즉시 룰렛 기대값이 주목적이 된다.
  222: 
  223: ---
  224: 
  225: ## 9. 룰렛과 자동생산의 역할 분리
  226: 
  227: ```text
  228: 룰렛
  229: - 즉시 획득
  230: - 일반·엘리트·영웅·전설 가능
  231: - 확률 조작과 고점 보상
  236: - 장기 투자 실패 방지
  237: ```
  238: 
  239: 특수병단은 룰렛만 의존하는 도박 건물도, 자동생산만으로 특수병종을 쌓는 확정 생산 건물도 아니다. 두 획득 경로를 동시에 제공하되 시간과 등급으로 역할을 분리한다.
  240: 
  241: ---
  242: 
  269: - 현재 생산 진행률과 남은 활성 전투 시간.
  270: - 자동생산 병종과 식량.
  271: - 완성 대기 여부.
  272: - 룰렛에 적용 중인 토큰과 릴별 확률.
  273: 
  274: ---
  275: 
  317: 4. 할인은 Tier 2 금화 비용에만 적용한다.
  318: 5. 업그레이드 시작 뒤 할인율이 바뀌지 않는다.
  319: 6. Tier 2 완료 전에는 특수 토큰과 자동생산이 활성화되지 않는다.
  320: 7. Tier 2 완료 뒤 다음 룰렛부터 토큰이 적용된다.
  321: 8. 특수병종 자동생산은 일반 등급만 생성한다.
  322: 9. 자동생산 간격은 사제 180·마법사 210·비행병 240·거인 300초다.
  323: 10. 식량 부족 완성 대기 중에는 다음 생산이 진행되지 않는다.
  324: 11. 건물 파괴 시 진행 중 생산과 건물 안 완성 대기 유닛이 사라진다.
  325: 12. 이미 인도된 유닛과 확정된 룰렛 결과는 유지된다.
  326: 13. 즉시 전문화는 빠른 전력, 준비 할인은 낮은 투자비라는 서로 다른 이점을 가진다.
  327: 14. 기본 병영은 특수병단보다 빠른 일반 전력 누적을 유지한다.
  328: 
```

### `docs/design/APPROVED_BARRACKS_TIER2_TIER3_INTEGRATED_TREE_V2.md` — ACTIVE / 397 lines

Hits: `룰렛` [6, 44, 47, 208, 242, 249], `roulette` [58, 101, 139, 360]

```text
    3: - 상태: **Tier 2 전문화 목록·토큰 계열·Tier 3 역할 분기 승인 / 절대 수치·최종 명칭은 PoC 조정 대상**
    4: - 작성일: 2026-07-15
    5: - 대체 문서: `APPROVED_BARRACKS_TIER2_TIER3_INTEGRATED_TREE_V1.md`
    6: - 적용 범위: 기본 병영 자동생산, 룰렛 토큰, 일반 병종 진화, Tier 패시브, Tier 3 운영 교리, 식량·생산시간 초기 가설
    7: - 연결 문서:
    8:   - `docs/design/APPROVED_PRIEST_HEAL_AND_COMMAND_SUPPORT_CLASS_V1.md`
    9:   - `docs/design/APPROVED_UNIT_GRADE_AND_ABILITY_GROWTH.md`
   41: - 서로 다른 병영은 서로 다른 전문화를 선택할 수 있다.
   42: - Tier 3에서는 상위 세부 병종 하나와 운영 교리 하나를 함께 선택한다.
   43: - 자동생산은 모든 Tier에서 일반 등급만 생산한다.
   44: - 엘리트·영웅·전설은 룰렛 결과로만 획득한다.
   45: - 업그레이드 중에는 이전 Tier 생산과 토큰을 유지한다.
   46: - 생산 진행률은 보존하며 완료 뒤 첫 완성분부터 새 병종을 생산한다.
   47: - 진행 중인 룰렛은 유지하고 다음 회전부터 새 토큰을 반영한다.
   48: 
   49: ---
   50: 
   55: ```text
   56: base_unit_family = warrior
   57: specialization_t2_id = greatblade_warrior
   58: roulette_token_family = warrior
   59: ```
   60: 
   61: Tier 2 대검전사의 기본 역할:
   98: ```text
   99: base_unit_family = priest_support
  100: specialization_t2_id = priest_support
  101: roulette_token_family = priest
  102: ```
  103: 
  104: Tier 2 사제는 회복과 격려를 모두 약하게 가진 혼합 지원형이다.
  136: ```text
  137: base_unit_family = assassin
  138: specialization_t2_id = assassin
  139: roulette_token_family = assassin
  140: ```
  141: 
  142: Tier 2 암살자의 역할:
  205: 기병 토큰 → 기병 계열
  206: ```
  207: 
  208: 기존 군기 토큰은 활성 토큰 목록에서 제거한다. 전쟁사제의 룰렛 결과는 사제 토큰으로 판정한다.
  209: 
  210: ---
  211: 
  239: 
  240: - 자동생산 간격 감소.
  241: - 초기 가설: 생산시간 -20%.
  242: - 등급과 룰렛 상위 등급 확률은 바꾸지 않는다.
  243: 
  244: ### 정예훈련
  245: 
  246: - 해당 병영에서 새로 생산되는 일반 유닛만 강화한다.
  247: - 초기 전투병 가설: 최대 체력 +8%, 피해 +8%.
  248: - 사제 초기 가설: 최대 체력 +8%, 회복·버프 효과량 +5%.
  249: - 이미 생산된 유닛·룰렛 유닛·용병에는 적용하지 않는다.
  250: - 엘리트 등급을 생성하지 않는다.
  251: 
  252: ### 토큰 증폭
  357: specialization_t2_id = greatsword
  358: specialization_t2_id = berserker
  359: specialization_t2_id = banner_support
  360: roulette_token_family = banner
  361: ```
  362: 
  363: 기존 데이터가 존재할 경우 새 통합 ID로 마이그레이션한다.
```

### `docs/design/APPROVED_BARRACKS_TIER3_EVOLUTION_AND_GRADE_SKILLS.md` — ACTIVE / 444 lines

Hits: `roulette` [7], `룰렛` [20, 23, 175, 228, 250, 253, 260, 288, 340, 344, 352, 366, 428, 429]

```text
    4: - 승인일: 2026-07-14
    5: - 우선순위: 이 문서는 기존 병영 Tier, 전사 토큰, 유닛 패시브·스킬 성장 문구 중 충돌하는 부분을 대체한다.
    6: - 연결 문서:
    7:   - `docs/design/APPROVED_ROULETTE_CORE_RULES.md`
    8:   - `docs/design/APPROVED_UNIT_GRADE_AND_ABILITY_GROWTH.md`
    9:   - `docs/design/APPROVED_BUILDING_SPECIALIZATION_AND_TACTICAL_COMMANDS.md`
   10:   - `docs/design/APPROVED_COMBAT_KEYWORDS_STATUS_EFFECTS_AND_FLIGHT.md`
   17:    - 상위 Tier 병영은 더 전문화된 일반 병종을 생산한다.
   18: 2. **병영 Tier와 핵심 패시브 성장**
   19:    - Tier가 높을수록 패시브가 추가되고 기존 패시브도 강화된다.
   20: 3. **룰렛 등급과 일반 스킬 성장**
   21:    - 일반 / 엘리트 / 영웅 / 전설 등급이 스킬 강화와 신규 스킬 해금을 결정한다.
   22: 
   23: 병영 Tier는 룰렛 등급을 올리지 않는다. 룰렛 등급은 병영의 병종 전문화 선택을 바꾸지 않는다.
   24: 
   25: ## 2. 병영 Tier 구조
   26: 
  172: 
  173: ## 5. 계열별 공유 토큰
  174: 
  175: 같은 기본 병종 계열은 Tier와 세부 병종이 달라도 동일 룰렛 심벌을 사용한다.
  176: 
  177: ### 전사 토큰
  178: 
  225: passive_progression_id
  226: ```
  227: 
  228: 건물 완공·파괴·업그레이드는 다음 룰렛 회전 스냅샷부터 반영한다.
  229: 
  230: ## 6. 병종 후보 확률 추첨
  231: 
  247: 
  248: 기병·궁병·창병처럼 Tier 1 후보가 없는 계열은 Tier 2와 Tier 3만 비교한다. 기존 1:2 비율을 사용할 수도 있으며 최종 수치는 플레이테스트로 결정한다.
  249: 
  250: ## 7. 룰렛 보상 판정 순서
  251: 
  252: ```text
  253: 1. 최종 룰렛 보드 확정
  254: 2. 동일 심벌 완성 줄 수로 등급 확정
  255: 3. 당첨 계열의 병영 출처 후보 풀 생성
  256: 4. 가중치 기반으로 실제 훈련 출처 선택
  257: 5. 일반이면 선택 출처의 실제 병종 생성
  258: 6. 엘리트·영웅·전설이면 계열별 고정 등급 유닛 생성
  259: 7. 출처 Tier에 따른 패시브 생성·강화 적용
  260: 8. 룰렛 등급에 따른 스킬 생성·강화 적용
  261: ```
  262: 
  263: 같은 시드, 회전 스냅샷과 최종 보드는 같은 결과를 만든다.
  285: 
  286: Tier 3 세부 병종 선택은 고정 상위 등급 유닛의 정체성·외형·기본 스킬 세트를 바꾸지 않는다. 다만 선택된 출처 Tier는 패시브 수와 강화 단계를 결정한다.
  287: 
  288: ## 10. 일반 스킬 성장은 룰렛 등급이 결정
  289: 
  290: ### 일반
  291: 
  337: - Tier 2: 선택한 일반 전문 병종
  338: - Tier 3: 선택한 일반 상위 세부 병종
  339: 
  340: 자동 생산에는 룰렛 등급 판정을 적용하지 않는다. 운영 교리는 자동 생산 유닛에 적용할 수 있지만 등급을 엘리트·영웅·전설로 바꾸지 않는다.
  341: 
  342: ## 13. UI 계약
  343: 
  344: ### 룰렛 토큰 장부
  345: 
  346: - 계열별 공유 토큰 수
  347: - Tier별 병영 수
  349: - 선택될 훈련 출처 Tier 확률
  350: - Tier별 패시브 해금·강화 단계
  351: 
  352: ### 룰렛 결과 화면
  353: 
  354: - 기본 병종 계열
  355: - 완성 줄 수와 생성 등급
  363: - 전문 병종 역할 비교
  364: - 자동 생산 병종 변화
  365: - 공유 토큰 유지 또는 변경
  366: - 일반 룰렛 후보 확률 변화
  367: - 새 패시브와 기존 패시브 강화 내용
  368: - 운영 교리 효과
  369: 
  425: - 엘리트는 1스킬을 강화한다.
  426: - 영웅은 강화된 1스킬을 유지하고 2스킬을 생성한다.
  427: - 전설은 영웅 효과를 유지하고 2스킬을 강화하며 3스킬을 생성한다.
  428: - 병영 Tier는 룰렛 등급을 올리지 않는다.
  429: - 자동 생산은 상위 룰렛 등급을 생성하지 않는다.
  430: - 건물 파괴 뒤에도 생성된 유닛의 병종·등급·패시브·스킬 구성은 유지된다.
  431: 
  432: ## 16. 아직 미확정인 세부
```

### `docs/design/APPROVED_BELLU_MASCOT_AND_GUIDE_CONTRACT.md` — ACTIVE / 213 lines

Hits: `룰렛` [5, 34, 36, 107, 121, 164]

```text
    2: 
    3: - 상태: **캐릭터·역할·최종 시안·일시 상태 표현 승인됨**
    4: - 승인일: 2026-07-15
    5: - 적용 범위: 튜토리얼, HUD, 베일의 징조, 룰렛 결과, 전투 경보, 세계관 설명, 마케팅
    6: - 세계관 명칭: `docs/design/APPROVED_OMENWARD_WORLD_AND_NAMING.md`
    7: 
    8: 이 문서는 기존 `율비` 명칭을 사용하는 마스코트 문서보다 우선한다.
   31: 2. 적 라인·병종·수량·도착 시간 보고
   32: 3. 최초 시스템 조작 안내
   33: 4. 위험과 가능한 대응 선택지 요약
   34: 5. 룰렛·토큰·건물의 세계관식 번역
   35: 6. 베일의 법칙과 과거 사건 암시
   36: 7. 룰렛 등급·방어 성공·패배 반응
   37: 
   38: 정찰관과 부관은 기능성 안내 NPC로 두지 않는다.
   39: 
  104: → 기본형 복귀
  105: ```
  106: 
  107: - 위험·보스·축복·저주·룰렛 등급은 영구 변신이 아니다.
  108: - 반응은 경고등처럼 약 0.5~2초만 나타난다.
  109: - 종료 뒤 0.2~0.4초 동안 기본형으로 복귀한다.
  110: - 지속 효과와 남은 시간은 HUD 아이콘·타이머·웨이브 카드가 담당한다.
  118: > 웨이브 임박
  119: > 필수 튜토리얼
  120: > 축복·저주
  121: > 룰렛 등급 반응
  122: > 세계관 기억 반응
  123: > 일반 감정 반응
  124: ```
  161: - 말풍선: 최대 두 줄
  162: - 기본 노출: 2.5~4초
  163: - 클릭과 드래그 입력을 가로채지 않음
  164: - 전장·룰렛 결과·건설 노드·웨이브 타이머를 가리지 않음
  165: - 음성 없이도 텍스트·아이콘·색으로 같은 필수 정보를 전달
  166: - 애니메이션 감소 옵션에서는 큰 움직임 없이 색·문양 점등만 사용
  167: 
```

### `docs/design/APPROVED_BELLU_SINGLE_GUIDE_AND_FIRST_10_MINUTE_FLOW.md` — ACTIVE / 260 lines

Hits: `룰렛` [27, 82, 85, 90, 105, 179, 238, 246, 257, 258, 259], `이동권` [31, 216], `roulette` [260]

```text
   24: | 0:00~0:15 | 실베른 성채·지휘관 역할·첫 베일의 징조 |
   25: | 0:15~0:35 | 일시정지 계획 모드 |
   26: | 0:35~1:05 | 첫 병영·전사 토큰 |
   27: | 1:05~1:45 | 고정 1줄 룰렛·일반 검사 |
   28: | 1:45~2:15 | 상단 배치 |
   29: | 2:15~3:10 | 첫 경계 공세·횡베기 |
   30: | 3:10~5:15 | 포탑 또는 바리케이드·첫 역전 |
   31: | 5:15~5:45 | 이동권으로 1줄→2줄·엘리트 |
   32: | 5:45~7:20 | 엘리트 강화·두 번째 핵심 루프 |
   33: | 7:20~8:10 | Tier 2 전사 병종 선택 |
   34: | 8:10~9:20 | 선택 병종 즉시 체험 |
   79: |---|---|
   80: | 시작 | 일시정지, 징조 카드 |
   81: | 첫 건설 | 건설 메뉴, 병영 |
   82: | 병영 완공 | 룰렛, 전사 토큰 간단 표시 |
   83: | 첫 병력 획득 | 대기칸, 라인 배치 |
   84: | 두 번째 공세 | 포탑, 바리케이드 |
   85: | 두 번째 룰렛 | 행·열 이동, 되돌리기, 확정 |
   86: | 첫 역전 뒤 | 병영 Tier 2 업그레이드 |
   87: 
   88: 상점 전체, Tier 3, 상태이상 상세, 제약과 결전 웨이브는 첫 10분에 접힌 상태로 둔다.
   89: 
   90: ## 5. 첫 병영과 룰렛
   91: 
   92: 벨루:
   93: 
  102: 
  103: > 전술판에 전사 표식이 연결됐어요.
  104: 
  105: 첫 룰렛 보드:
  106: 
  107: ```text
  108: X     전사   X
  176: 
  177: > 좋아요! 지휘관의 선택이 통했어요.
  178: 
  179: ## 8. 두 번째 룰렛과 엘리트
  180: 
  181: 이동 전:
  182: 
  213: 
  214: - 결과 미리보기
  215: - 확정 전 되돌리기
  216: - 이동권은 확정할 때 소비
  217: - 2줄은 엘리트
  218: 
  219: ## 9. Tier 2 전사 체험
  235: - 첫 교전 전 무료 라인 재배치
  236: - 첫 공세 실패 시 본진 피해 상한과 비상 검사 제공
  237: - 두 번째 공세 대응 실패 시 비상 바리케이드 제공
  238: - 두 번째 룰렛 확정 전 무제한 되돌리기
  239: - Tier 2 선택 중 전투 시간 정지
  240: 
  241: 실패 복구는 플레이어가 잘했다고 속이지 않고, `비상 증원`, `성채 방어 사격`처럼 원인을 명확하게 표시한다.
  243: ## 11. PoC 목표
  244: 
  245: - 첫 병영: 1분 10초 이내
  246: - 첫 룰렛: 1분 45초 이내
  247: - 첫 배치: 2분 15초 이내
  248: - 첫 역전: 5분 15초 이내
  249: - 첫 엘리트: 5분 45초 이내
  254: 
  255: - 첫 30초에 실베른 성채, 베일런 황야와 지휘관 역할을 이해한다.
  256: - 모든 기능성 안내는 벨루와 HUD가 담당한다.
  257: - 첫 룰렛은 1줄 일반 검사로 결정론적으로 끝난다.
  258: - 두 번째 룰렛은 플레이어 조작으로 2줄 엘리트가 된다.
  259: - 첫 10분 안에 건설→룰렛→배치→방어 루프를 두 번 경험한다.
  260: - 신규 대사와 UI에 `율비`, `경계의 율`, `Roulettebound`를 사용하지 않는다.
```

### `docs/design/APPROVED_BENCHMARK_DECISIONS.md` — ACTIVE / 84 lines

Hits: `룰렛` [13, 16, 80], `럭키` [51, 53]

```text
   10: 
   11: 다음 6개를 첫 플레이 가능한 수직 슬라이스의 필수 UX로 채택한다.
   12: 
   13: 1. **건설 전 룰렛 확률 미리보기**
   14:    - 건설 또는 철거로 토큰 풀이 어떻게 변하는지 실행 전에 보여준다.
   15:    - 예: `검사 25% → 40%`, `X 25% → 0%`.
   16: 2. **룰렛 토큰 장부**
   17:    - 토큰 종류별 개수, 각 릴의 구성, 등장 확률과 X 토큰 수를 확인할 수 있다.
   18: 3. **적 웨이브 3단계 전조**
   19:    - T-30: 라인, 병종, 정확한 수량.
   48: - 정보 은폐나 수량 범위 표시는 고난도 변형으로만 검토한다.
   49: - 치명적인 특수 공격과 후방 침투는 반드시 사전 경고한다.
   50: 
   51: ## 4. 럭키 찬스
   52: 
   53: - 럭키 찬스는 **기본 발생 확률 + 실패 누적 보정** 구조를 채택한다.
   54: - 연속 실패할수록 다음 회전의 발생 확률이 올라가며, 성공하면 누적 보정이 초기화된다.
   55: - 정확한 기본 확률, 증가량과 확정 상한은 밸런스 데이터로 결정한다.
   56: - 목표는 운을 제거하는 것이 아니라 장기간 아무 조작 기회도 얻지 못하는 좌절을 방지하는 것이다.
   77: 
   78: ```text
   79: 건물 구성·전문화
   80: → 룰렛 토큰과 확률 변화
   81: → 예고된 적 웨이브에 맞는 유닛 획득
   82: → 3라인 배치와 접전지·시설 공방
   83: → 결과 원인 확인과 다음 건설 결정
```

### `docs/design/APPROVED_BUILDINGS_TACTICAL_MERCENARY_POC_V1.md` — ACTIVE / 149 lines

Hits: `룰렛` [28, 149], `이동권` [65], `결과 보관` [114]

```text
   25: 
   26: - Tier 1 준비 할인은 Tier 3 비용에 적용하지 않는다.
   27: - Tier 3 완료 뒤 자동생산 병종과 토큰 계열은 부모를 유지한다.
   28: - 상위 등급 룰렛 템플릿은 계열 고정이며 T3 분기는 패시브 방향만 반영한다.
   29: 
   30: ## 3. 농장 성장
   31: 
   62: | 부모 | 분기 | 비용 | 효과 |
   63: |---|---|---:|---|
   64: | 교역소 | 왕립 거래소 | 110 | 20초마다 +9, 두 번째 이후 시장 비용 증가량 -5 |
   65: | 군수상회 | 전선 보급소 | 110 | 전술 명령 비용 -25%, 이동권 가격 증가량 절반 |
   66: 
   67: - 시장 할인은 strongest-only다.
   68: - 시장 자체 수입은 각 건물별로 합산한다.
  111: - 수동 갱신 비용 15, 갱신마다 +5, 최대 40.
  112: - 일반·엘리트만 판매한다. 영웅·전설은 판매하지 않는다.
  113: - 용병은 건물 토큰을 추가하지 않는다.
  114: - 구매한 용병은 결과 보관함으로 들어가며 식량은 배치 시 사용한다.
  115: 
  116: ### 가격 가설
  117: 
  146: - 포탑 중심 빌드도 병력·전술 없이 적 본진을 파괴할 수 없다.
  147: - 특수병단 T3는 기본 병영 T3보다 강하지만 투자·생산·식량 부담이 더 크다.
  148: - 전술 명령 반복으로 보스를 영구 지연할 수 없다.
  149: - 용병은 룰렛 실패 복구 수단이지만 상위 등급 획득의 주 경로가 아니다.
```

### `docs/design/APPROVED_BUILDING_SPECIALIZATION_AND_TACTICAL_COMMANDS.md` — ACTIVE / 310 lines

Hits: `룰렛` [31, 51, 101, 109, 140, 166, 274, 291], `reel` [219]

```text
   28: - 적 존재는 새 건설·업그레이드 명령을 차단하지 않는다.
   29: - 피격 중에도 진행도는 계속 증가한다.
   30: - 건설 중 구조물은 공격받아 완공 전에 파괴될 수 있다.
   31: - 건설 중에는 생산·수입·식량·룰렛 토큰·포탑 공격 기능이 없다.
   32: - 완공되는 시뮬레이션 틱부터 정상 기능이 활성화된다.
   33: - 완공 전에 체력 0이면 건설 실패, 환불 없음, 폐허 상태로 전환한다.
   34: - 건설 중 체력 곡선과 충돌 범위는 별도 데이터로 조정한다.
   48: - Tier 2 선택은 해당 건물이 파괴·철거될 때까지 유지한다.
   49: - 한 건물에서 서로 다른 Tier 2 방향을 동시에 섞지 않는다.
   50: - 전문화 변경은 철거 후 재건설을 기본 방식으로 한다.
   51: - 업그레이드 중에는 이전 Tier 기능과 기존 룰렛 토큰을 유지한다.
   52: - 완료 순간 전문화 효과와 변경된 토큰을 적용한다.
   53: - 선택 전에 비용·시간·기능·토큰·확률 변화를 표시한다.
   54: 
   98: - 세 릴의 검사 토큰이 선택 병종 토큰으로 교체된다.
   99: - 이미 생산된 검사와 출전 유닛은 변경되지 않는다.
  100: - 생산 진행률은 보존하고 완료 뒤 처음 완성되는 유닛부터 새 병종을 적용한다.
  101: - 진행 중인 룰렛은 유지하고 다음 회전부터 새 토큰을 적용한다.
  102: 
  103: ### Tier 3 교리 후보
  104: 
  106: - 정예훈련: 해당 시설에서 새로 생산되는 일반 등급 유닛에게 영구 능력치 버프
  107: - 토큰 증폭: 선택 병종 토큰을 결정된 릴 하나에 추가
  108: 
  109: 정예훈련은 엘리트 등급 유닛을 생성하지 않는다. 상위 등급은 룰렛 결과로 생성한다. 정확한 수치는 후속 승인 대상이다.
  110: 
  111: ## 6. 포탑 전문화
  112: 
  137: 
  138: - 해금된 기본 명령은 HUD에 항상 표시한다.
  139: - 상점 무작위 1회권 대신 사용할 때마다 금화를 직접 지불한다.
  140: - 식량·건설 노드·룰렛 토큰을 사용하지 않는다.
  141: - 적 본진·생산시설을 직접 파괴하는 주력 수단으로 만들지 않는다.
  142: - 효과 범위·지속시간·예상 효과·비용을 실행 전에 표시한다.
  143: - 시장 빌드가 금화를 전장 대응력으로 바꾸는 주요 소비처다.
  163: 
  164: 일시정지 중에는 전술 명령뿐 아니라 다음 행동도 정상적으로 확정할 수 있다.
  165: 
  166: - 룰렛 회전과 결과 처리
  167: - 건설·업그레이드·철거
  168: - 유닛 생산·배치·이동 명령
  169: - 건물 전문화 선택
  216: specialization_id
  217: production_unit_id
  218: base_token_id
  219: bonus_token_reel
  220: production_modifiers
  221: spawned_unit_modifiers
  222: turret_modifiers
  271: - 포탑 전문화별 범위·사거리·내구도 표시
  272: - 전술 명령 비용·범위·효과·개별 쿨다운 표시
  273: - 전역 쿨다운·예약 금액·예약 상태는 표시하지 않음
  274: - 일시정지 중에도 실행 가능한 명령·룰렛·배치·건설 버튼을 정상 활성화
  275: - 일시정지 중 확정된 행동의 자원 차감과 대기 상태를 즉시 표시
  276: - 시전시간·지속시간·쿨다운·생산·건설 시간 바는 일시정지 동안 고정
  277: - 보스의 가로축·세로축 공격 범위를 서로 다른 형태로 표시
  288: - 한 명령이 다른 명령을 전역 쿨다운으로 잠그지 않는다.
  289: - 일시정지 중 명령 사용은 정상 확정되고 금화가 즉시 차감된다.
  290: - 일시정지 중에는 시전시간·지속시간·개별 쿨다운이 진행되지 않는다.
  291: - 일시정지 중 룰렛·건설·업그레이드·유닛 배치 등 허용 조작이 정상 작동한다.
  292: - 예약 객체·예약 금액·예약 개수 제한은 생성되지 않는다.
  293: - 바리케이드는 일반 적을 지연하지만 길 뚫기 보스를 영구 봉쇄하지 않는다.
  294: - 직선 돌파와 크게 베기는 전조 뒤 고정된 범위에 적용된다.
```

### `docs/design/APPROVED_DOPAMINE_DRIVEN_DESIGN_AND_FIRST_10_MINUTES.md` — ACTIVE / 164 lines

Hits: `룰렛` [11, 16, 75, 85, 109, 117, 132, 145, 161], `이동권` [111], `roulette` [164]

```text
    8: 
    9: ## 1. 핵심 재미
   10: 
   11: > 건물을 지어 룰렛 확률을 바꾸고, 당첨된 병력으로 예고된 위기를 뒤집는다.
   12: 
   13: ```text
   14: 베일의 징조
   15: → 건물·토큰 선택
   16: → 룰렛 기대와 결과
   17: → 필요한 라인 배치
   18: → 상성과 전술로 역전
   19: → 다음 성장 기대
   72: 
   73: 1. 긴장 — 베일의 징조로 위험 라인이 보임
   74: 2. 판단 — 건물·병종·전술을 고름
   75: 3. 기대 — 룰렛 결과를 기다림
   76: 4. 보상 — 줄 완성·등급·유닛 등장
   77: 5. 통제감 — 자신의 건물과 이동이 결과에 영향을 줌
   78: 6. 역전 — 병력·포탑·전술 명령이 전선을 바꿈
   82: 
   83: ### 확률을 건설한다
   84: 
   85: 건물이 생산뿐 아니라 룰렛 토큰과 확률을 바꾼다.
   86: 
   87: ### 30초 뒤 위기를 보고 준비한다
   88: 
  106: - 벨루와 베일의 징조를 통한 적 정보 이해
  107: - 일시정지 계획 모드 체험
  108: - 병영과 전사 토큰 연결 확인
  109: - 첫 룰렛·병력 획득·라인 배치
  110: - 포탑 또는 바리케이드로 첫 역전
  111: - 이동권으로 일반 결과를 엘리트로 개선
  112: - 방패병·대검병·광전사 중 하나 선택
  113: - 핵심 루프 두 번 경험
  114: 
  115: 첫 10분 종료 시 플레이어가 다음을 설명할 수 있어야 한다.
  116: 
  117: > 나는 실베른 성채의 지휘관이고, 베일의 징조를 본 뒤 건물과 룰렛으로 병력을 준비해 막는다.
  118: 
  119: ## 8. 벨루의 역할
  120: 
  129: ## 9. 첫 10분 시간 목표
  130: 
  131: - 첫 병영: 1분 10초 이내
  132: - 첫 룰렛: 1분 45초 이내
  133: - 첫 배치: 2분 15초 이내
  134: - 첫 역전: 5분 15초 이내
  135: - 첫 엘리트: 5분 45초 이내
  142: - 한 말풍선 최대 두 줄
  143: - 새 개념은 한 번에 하나
  144: - 자세한 확률표·상태이상·Tier 3·제약·결전 공세는 필요 전까지 접음
  145: - 룰렛 후보 확률과 패시브·스킬 정보는 요약과 상세 패널로 분리
  146: 
  147: ## 11. 추가 기능 검수 질문
  148: 
  158: ## 12. 검증 계약
  159: 
  160: - 첫 플레이어가 각 핵심 시스템의 기본 사용법을 10~30초 안에 이해한다.
  161: - 첫 10분 안에 건설→룰렛→배치→역전을 두 번 경험한다.
  162: - 벨루의 설명 없이도 HUD가 필수 숫자를 전달한다.
  163: - 설명을 건너뛰어도 플레이가 막히지 않는다.
  164: - 신규 문서와 대사에 `Roulettebound`, `율비`, `경계의 율`을 사용하지 않는다.
```

### `docs/design/APPROVED_OMENWARD_WORLD_AND_NAMING.md` — ACTIVE / 184 lines

Hits: `roulette` [7, 41, 174], `룰렛` [122, 162]

```text
    4: - 승인일: 2026-07-15
    5: - 적용 범위: 게임명, 세계관, 튜토리얼, HUD, 웨이브 카드, 대사, 마케팅, 이후 모든 기획 문서
    6: 
    7: 이 문서는 기존 `Roulettebound`, `경계의 율`, `율비`, `은종성채`, `무명야` 등 임시·이전 명칭보다 우선한다.
    8: 
    9: ## 1. 게임 공식 명칭
   10: 
   38: Build the odds. Hold the frontier.
   39: ```
   40: 
   41: 저장소명과 내부 코드명 `roulettebound-prototype`은 구현 마이그레이션 계획이 승인될 때까지 유지한다.
   42: 
   43: ## 2. 명명 원칙
   44: 
  119: 플레이어는 실베른 성채에 부임한 최전선 지휘관이다.
  120: 
  121: - 세 라인의 위험을 읽는다.
  122: - 건물과 룰렛 확률을 설계한다.
  123: - 병력과 포탑을 배치한다.
  124: - 전술 명령으로 위기를 지연하거나 역전한다.
  125: - 괴물이 루미엔 영토로 넘어가지 못하게 한다.
  159: 
  160: ```text
  161: 세 갈래 전선
  162: + 3×3 룰렛 문양
  163: + 경계종의 빛 고리
  164: + 베일의 균열
  165: + 작은 감시 정령 벨루
  171: 
  172: | 이전 명칭 | 공식 명칭 |
  173: |---|---|
  174: | Roulettebound | 오멘워드 / OMENWARD |
  175: | 경계의 율 | 베일의 법칙 |
  176: | 율 | 법칙 / 베일 |
  177: | 율비 | 벨루 |
```

### `docs/design/APPROVED_PERFORMANCE_DATA_TEST_READINESS_POC_V1.md` — ACTIVE / 469 lines

Hits: `룰렛` [368, 375, 396, 418, 446, 465], `럭키` [373]

```text
  365: - 식량 부족 완성 대기.
  366: - 점령 중 거점 생산 정지와 안정화 뒤 재개.
  367: 
  368: ### 룰렛
  369: 
  370: - 최소 100,000시드 분포.
  371: - 전설 1회 제한.
  372: - 금화 EV 30% 이하.
  373: - 럭키 6회 실패 뒤 확정.
  374: - 건물 파괴·비활성 후 다음 회전 토큰 제거.
  375: - 룰렛 결과가 공용 archetype ID를 생성.
  376: 
  377: ### 웨이브
  378: 
  393: ## 8. 플레이테스트 계측
  394: 
  395: - 첫 건물 건설 시간.
  396: - 첫 룰렛과 첫 보상 시간.
  397: - 웨이브별 남은 Threat.
  398: - 라인별 병력·건물 투자.
  399: - 금화 수입원·지출원 비율.
  415: 1. 최신 Handoff, GDD, Roadmap과 Documentation Map 존재.
  416: 2. 공용 10병종과 진영별 Visual Set 책임 문서 존재.
  417: 3. 전장·거점·성문·우회로 초기값 존재.
  418: 4. 공통 전투·등급·룰렛·경제·웨이브·UI 필드 정의.
  419: 5. 애니메이션 상태·판정 이벤트 계약 존재.
  420: 6. 구현을 수직 슬라이스로 제한.
  421: 
  443: - 한 맵, 3라인.
  444: - 성문·중간거점·접전지.
  445: - 최소 건물 2종.
  446: - 최소 3×3 룰렛과 일반·엘리트.
  447: - 첫 네 공세.
  448: - 공용 archetype 대표 3~5종.
  449: - 각 대표 archetype의 아군·적군 이미지 세트.
  462: - 양 진영 이미지의 프레임·피벗·이벤트가 어긋남.
  463: - 문서에 없는 하드코딩 수치가 핵심 규칙을 결정.
  464: - 일시정지 중 active_combat_time이 진행.
  465: - 룰렛·웨이브·전투가 시드로 재현되지 않음.
  466: - Tier·Rank 비교가 데이터가 아니라 진영별 코드 분기로 처리됨.
  467: - 최대 객체 상한에서 30fps 아래로 장시간 하락.
  468: - 전조와 판정, 공격 모션과 실제 피해가 인지 가능한 수준으로 불일치.
```

### `docs/design/APPROVED_PREPRODUCTION_POC_BASELINE_V1.md` — ACTIVE / 217 lines

Hits: `룰렛` [16, 31, 43, 79, 155, 168, 209], `럭키` [16], `roulette` [31]

```text
   13: 3. 좌우 대칭 독립 3라인·성문·중간거점·접전지·암살자 우회.
   14: 4. 공용 10병종의 생산·Tier·등급·능력 계보.
   15: 5. 아군·적군 공용 전투 데이터와 진영별 이미지 세트 분리.
   16: 6. 룰렛 가중치·목표 확률·럭키·금화 기대값.
   17: 7. 공용 아키타입 기반 W1~20 웨이브와 보스 패키지.
   18: 8. 건물 Tier 3·경제·전술 명령·용병.
   19: 9. 튜토리얼·약 3시간 캠페인·절차 생성.
   28: - 공통 전투: `docs/design/APPROVED_COMMON_COMBAT_AND_RANK_BUDGET_POC_V1.md`
   29: - 공용 병종 데이터·진영 이미지: `docs/design/APPROVED_SHARED_UNIT_ARCHETYPE_AND_FACTION_VISUAL_DATA_V1.md`
   30: - 병종 능력 계보: `docs/design/APPROVED_PLAYER_TEN_UNIT_LINEAGES_POC_V1.md`
   31: - 룰렛: `docs/design/APPROVED_ROULETTE_PROBABILITY_TARGETS_POC_V1.md`
   32: - W1~20 웨이브·보스: `docs/design/APPROVED_SHARED_ARCHETYPE_WAVE_1_20_POC_V1.md`
   33: - 건물·전술·용병: `docs/design/APPROVED_BUILDINGS_TACTICAL_MERCENARY_POC_V1.md`
   34: - 튜토리얼·캠페인·절차 생성: `docs/design/APPROVED_TUTORIAL_CAMPAIGN_PROCEDURAL_POC_V1.md`
   40: 
   41: ## 3. 변경 시 사용자 승인이 필요한 구조
   42: 
   43: - 핵심 루프 `징조 → 건설·룰렛 → 배치 → 3라인 공방`.
   44: - 좌우 대칭 독립 3라인.
   45: - 라인별 성문 3개.
   46: - 중간거점 전방 2·후방 1 건설 노드.
   76: 분리:
   77: 
   78: - 팀과 적대 관계.
   79: - 룰렛·생산 또는 웨이브 출격.
   80: - 스프라이트·초상화·아이콘·팔레트·표시명.
   81: 
   82: 금지:
  152: 기본 수입 +5 / 20초
  153: 중앙 접전지 +4 / 60초
  154: 중간거점 +2 / 30초
  155: 룰렛 20
  156: ```
  157: 
  158: - 60초마다 공세 충돌.
  165: - 유닛 HP·공격력·방어·사거리·쿨다운.
  166: - 생산시간·식량·Threat.
  167: - 건물 비용과 Tier 3 효과.
  168: - 룰렛 릴 가중치와 목표 분포.
  169: - 웨이브 수량과 예산.
  170: - 전술 명령 피해·지속.
  171: - 성능 상한과 갱신 주기.
  206: 
  207: ## 11. 남은 프리프로덕션·PoC 작업
  208: 
  209: - 룰렛 최소 100,000시드 확률 시뮬레이션.
  210: - 기본·시장·접전지·중간거점 경제 시간축 검증.
  211: - 전투·생산·웨이브 표 계산.
  212: - 대표 아키타입의 아군·적군 이미지 프레임 호환 테스트.
```

### `docs/design/APPROVED_PRIEST_HEAL_AND_COMMAND_SUPPORT_CLASS_V1.md` — ACTIVE / 346 lines

Hits: `roulette` [21, 296, 312], `룰렛` [66, 199, 344]

```text
   18: 병종 표시명: 사제
   19: base_unit_family = priest_support
   20: combat_archetype_id = priest_line_support
   21: roulette_token_family = priest
   22: ```
   23: 
   24: 공통 태그:
   63: - 별도 사원이나 지휘소를 기본 생산 건물로 요구하지 않는다.
   64: - 업그레이드 중에는 검사 생산과 전사 토큰을 유지한다.
   65: - 생산 진행률을 보존하고 완료 뒤 첫 완성분부터 사제를 생산한다.
   66: - 진행 중 룰렛은 유지하고 다음 회전부터 사제 토큰을 적용한다.
   67: - 기존 군기 토큰은 활성 토큰 목록에서 제거한다.
   68: 
   69: ---
  196: 
  197: ## 7. 등급별 스킬 성장
  198: 
  199: 사제도 공통 룰렛 등급 계약을 따른다.
  200: 
  201: ```text
  202: 일반: 1스킬 기본형
  293: 
  294: ```text
  295: base_unit_family = priest_support
  296: roulette_token_family = priest
  297: healing_profile_id
  298: healing_target_priority_id
  299: support_focus_id
  309: 
  310: ```text
  311: base_unit_family = banner_commander
  312: roulette_token_family = banner
  313: specialization_t2_id = banner_support
  314: ```
  315: 
  341: - 버프 효과량·오라 반경·대상 수.
  342: - 엘리트·영웅·전설 사제 이름과 스킬.
  343: - 적군 대응 지원 유닛의 공식 이름과 외형.
  344: - 사제 생산·식량·룰렛 가중치 최종값.
  345: 
  346: 이 문서는 지원 계열 구조를 승인하지만 Godot 구현 승인을 뜻하지 않는다. 구현은 별도 Codex Plan Mode 제안서와 사용자 승인을 거친다.
```

### `docs/design/APPROVED_ROULETTE_CORE_RULES.md` — ACTIVE / 270 lines

Hits: `룰렛` [1, 46, 65, 70, 112, 119, 221, 227, 235], `판정 줄` [10, 12, 14, 15, 19, 31, 42, 44, 112, 177, 268], `럭키` [194, 205, 209], `이동권` [207, 209, 211, 212, 214, 268], `결과 보관` [217, 220, 267], `중앙 가로줄` [247]

```text
    1: # 승인된 룰렛 핵심 규칙
    2: 
    3: - 상태: **핵심 구조·등급 생성·병영 출처 추첨 승인됨 / 세부 확률 일부 미확정**
    4: - 승인일: 2026-07-14
    7:   - `docs/design/APPROVED_BARRACKS_TIER3_EVOLUTION_AND_GRADE_SKILLS.md`
    8:   - `docs/design/APPROVED_ECONOMY_BUILDS_AND_TERMINAL_ASSAULT.md`
    9: 
   10: ## 1. 기본 판정 줄
   11: 
   12: - 기본 판정 줄은 3×3 보드의 가운데 가로줄이다.
   13: - 상단·하단 판정은 아이템 효과가 있을 때만 허용한다.
   14: - 판정 줄은 회전 비용을 지불한 시점에 고정한다.
   15: - 회전 도중 장비·아이템 변경으로 판정 줄을 바꾸지 못한다.
   16: 
   17: ## 2. 기본 보상 조건
   18: 
   19: 판정 줄의 세 칸이 동일한 비-X 심벌일 때만 보상을 판정한다.
   20: 
   21: ```text
   22: [전사, 전사, 전사] → 전사 계열 보상
   28: 
   29: - 동일 심벌 1개 또는 2개는 보상 없음
   30: - 혼합 심벌은 종류별 분할 보상 없음
   31: - 판정 줄이 일치하면 해당 심벌을 판정 심벌로 고정하고 전체 8개 줄을 검사한다.
   32: 
   33: ## 3. 완성 줄 계산
   34: 
   39: - 대각선 2줄
   40: 
   41: - 판정 심벌과 같은 심벌로 완성된 줄만 센다.
   42: - 판정 줄도 완성 줄 하나에 포함한다.
   43: - 겹치는 줄도 각각 계산한다.
   44: - 판정 줄이 완성되지 않았다면 다른 위치의 완성 줄도 무시한다.
   45: 
   46: ## 4. 룰렛 등급
   47: 
   48: | 동일 심벌 완성 줄 | 생성 등급 |
   49: |---:|---|
   62: 
   63: - 전설은 한 스테이지 전체에서 병종과 관계없이 1회만 생성한다.
   64: - 전설 생성 뒤 다시 9칸 동일 유닛 심벌이 나오면 해당 계열 영웅 2명으로 변환한다.
   65: - 전설 사용 여부를 룰렛 UI에 항상 표시한다.
   66: - 금화 심벌은 전설 등급을 만들지 않는다.
   67: 
   68: ## 6. 계열별 공유 병종 토큰
   69: 
   70: 같은 기본 병종 계열은 Tier와 세부 병종이 달라도 동일한 룰렛 심벌을 사용한다.
   71: 
   72: ### 전사 토큰
   73: 
  109: ## 7. 유닛 보상 결정 순서
  110: 
  111: ```text
  112: 1. 룰렛 보드와 판정 줄 확정
  113: 2. 동일 심벌 완성 줄 수로 등급 확정
  114: 3. 당첨된 기본 병종 계열의 병영 출처 후보 풀 생성
  115: 4. 가중치로 실제 훈련 출처 병영 선택
  116: 5. 일반 등급이면 선택 출처의 실제 병종 생성
  117: 6. 엘리트·영웅·전설이면 계열별 고정 유닛 생성
  118: 7. 선택된 병영 Tier로 핵심 패시브 생성·강화 적용
  119: 8. 룰렛 등급으로 일반 스킬 생성·강화 적용
  120: ```
  121: 
  122: ## 8. 병영 출처 가중치
  174: 
  175: ## 11. 금화 심벌
  176: 
  177: 판정 줄이 금화 3개일 때 전체 금화 완성 줄 수를 센다.
  178: 
  179: | 금화 완성 줄 | 보상 |
  180: |---:|---:|
  191: - 금화에는 유닛 등급을 적용하지 않음
  192: - 별도 회전 충전, 웨이브당 횟수 제한과 연속 감쇠 없음
  193: 
  194: ## 12. 럭키 찬스
  195: 
  196: ```text
  197: 기본 확률: 12%
  202: 
  203: - 현재 확률과 확정까지 남은 실패 횟수 공개
  204: - 행운 아이템은 초기 기본 확률 상승
  205: - 자연 럭키 찬스는 무료 행·열 이동 1회 제공
  206: 
  207: ## 13. 이동권
  208: 
  209: - 자연 럭키 찬스와 이동권을 같은 회전에 함께 사용 가능
  210: - 회전당 이동 횟수 상한 없음
  211: - 실제 상한은 보유 이동권 수
  212: - 이동권 1장당 행 또는 열 1칸 순환 이동 1회
  213: - 여러 이동을 계획하고 되돌리기·전체 초기화 가능
  214: - 최종 확정 시에만 이동권 소비
  215: - 최종 확정 뒤 보상은 한 번만 계산
  216: 
  217: ## 14. 결과 보관함
  218: 
  219: - 유닛 보상은 공간 부족으로 사라지지 않는다.
  220: - 결과 보관함에 생성한 뒤 대기칸 이동·즉시 출전·판매로 정리한다.
  221: - 보관함이 남아 있으면 다음 룰렛 회전만 차단한다.
  222: - 전투·건설·카메라·기존 유닛 배치는 계속 가능하다.
  223: - 금화 보상은 즉시 지급한다.
  224: 
  225: ## 15. UI 계약
  226: 
  227: 룰렛 전:
  228: 
  229: - 릴별 공유 토큰 수
  230: - 병종 계열별 당첨 확률
  232: - Tier별 훈련 출처 확률
  233: - Tier별 패시브 해금·강화 내용
  234: 
  235: 룰렛 결과:
  236: 
  237: - 완성 줄 수와 등급
  238: - 기본 병종 계열
  244: 
  245: ## 16. 검증 계약
  246: 
  247: - 기본 상태에서는 중앙 가로줄만 판정한다.
  248: - 혼합 줄과 2개 일치는 보상을 만들지 않는다.
  249: - 1/2/3~7/8줄은 일반/엘리트/영웅/전설로 매핑된다.
  250: - Tier 1 검사와 Tier 2 이상 전사 계열은 전사 토큰을 공유한다.
  264: - 전사 계열 Tier 2 병종의 최종 명칭과 수치
  265: - Tier별 정확한 후보 가중치
  266: - 계열별 고정 엘리트·영웅·전설 유닛
  267: - 대기칸 기본 용량과 결과 보관함 UI
  268: - 판정 줄 변경·행운·이동권 아이템 세부
  269: 
  270: 이 문서는 핵심 구조를 승인하지만 개별 수치를 구현 승인으로 간주하지 않는다. Godot 구현은 별도 Codex Plan Mode 제안서와 사용자 승인을 거쳐야 한다.
```

<details><summary>Full content</summary>

```text
# 승인된 룰렛 핵심 규칙

- 상태: **핵심 구조·등급 생성·병영 출처 추첨 승인됨 / 세부 확률 일부 미확정**
- 승인일: 2026-07-14
- 관련 문서:
  - `docs/design/APPROVED_UNIT_GRADE_AND_ABILITY_GROWTH.md`
  - `docs/design/APPROVED_BARRACKS_TIER3_EVOLUTION_AND_GRADE_SKILLS.md`
  - `docs/design/APPROVED_ECONOMY_BUILDS_AND_TERMINAL_ASSAULT.md`

## 1. 기본 판정 줄

- 기본 판정 줄은 3×3 보드의 가운데 가로줄이다.
- 상단·하단 판정은 아이템 효과가 있을 때만 허용한다.
- 판정 줄은 회전 비용을 지불한 시점에 고정한다.
- 회전 도중 장비·아이템 변경으로 판정 줄을 바꾸지 못한다.

## 2. 기본 보상 조건

판정 줄의 세 칸이 동일한 비-X 심벌일 때만 보상을 판정한다.

```text
[전사, 전사, 전사] → 전사 계열 보상
[기병, 기병, 기병] → 기병 계열 보상
[기병, 기병, 궁병] → 보상 없음
[기병, 기병, X] → 보상 없음
[X, X, X] → 보상 없음
```

- 동일 심벌 1개 또는 2개는 보상 없음
- 혼합 심벌은 종류별 분할 보상 없음
- 판정 줄이 일치하면 해당 심벌을 판정 심벌로 고정하고 전체 8개 줄을 검사한다.

## 3. 완성 줄 계산

검사 대상:

- 가로 3줄
- 세로 3줄
- 대각선 2줄

- 판정 심벌과 같은 심벌로 완성된 줄만 센다.
- 판정 줄도 완성 줄 하나에 포함한다.
- 겹치는 줄도 각각 계산한다.
- 판정 줄이 완성되지 않았다면 다른 위치의 완성 줄도 무시한다.

## 4. 룰렛 등급

| 동일 심벌 완성 줄 | 생성 등급 |
|---:|---|
| 1줄 | 일반 |
| 2줄 | 엘리트 |
| 3~7줄 | 영웅 |
| 8줄·9칸 전체 동일 | 전설 |

- `신화` 명칭은 사용하지 않고 `전설` 명칭을 유지한다.
- 등급은 최종 보드 확정 시점에 결정한다.
- 수동 합성, 처치 경험치와 건물 Tier로 등급이 상승하지 않는다.
- 병영 Tier는 일반·엘리트·영웅·전설 확률에 영향을 주지 않는다.
- 병영 자동 생산은 항상 일반 등급이다.

## 5. 전설 제한

- 전설은 한 스테이지 전체에서 병종과 관계없이 1회만 생성한다.
- 전설 생성 뒤 다시 9칸 동일 유닛 심벌이 나오면 해당 계열 영웅 2명으로 변환한다.
- 전설 사용 여부를 룰렛 UI에 항상 표시한다.
- 금화 심벌은 전설 등급을 만들지 않는다.

## 6. 계열별 공유 병종 토큰

같은 기본 병종 계열은 Tier와 세부 병종이 달라도 동일한 룰렛 심벌을 사용한다.

### 전사 토큰

- Tier 1 검사
- Tier 2 방패병·대검병·광전사 후보
- 향후 Tier 3 전사 계열 세부 병종

### 기병 토큰

- Tier 2 기병
- Tier 3 중기병·충격기병

### 궁병 토큰

- Tier 2 궁병
- Tier 3 석궁병·대공궁병·연사궁병

### 창병 토큰

- Tier 2 창병
- 향후 Tier 3 창병 세부 병종

병영 한 동은 기본적으로 각 릴에 해당 공유 토큰 1개를 제공한다.

```text
Tier 1 검사 병영 1개
Tier 2 방패병 병영 1개
→ 각 릴 전사 토큰 총 2개
```

```text
Tier 2 기병 병영 1개
Tier 3 중기병 병영 1개
→ 각 릴 기병 토큰 총 2개
```

토큰 외형은 같지만 내부 원장에는 출처 병영 ID·Tier·세부 병종·후보 가중치와 패시브 성장 단계를 보존한다.

## 7. 유닛 보상 결정 순서

```text
1. 룰렛 보드와 판정 줄 확정
2. 동일 심벌 완성 줄 수로 등급 확정
3. 당첨된 기본 병종 계열의 병영 출처 후보 풀 생성
4. 가중치로 실제 훈련 출처 병영 선택
5. 일반 등급이면 선택 출처의 실제 병종 생성
6. 엘리트·영웅·전설이면 계열별 고정 유닛 생성
7. 선택된 병영 Tier로 핵심 패시브 생성·강화 적용
8. 룰렛 등급으로 일반 스킬 생성·강화 적용
```

## 8. 병영 출처 가중치

핵심 원칙:

- 높은 Tier 일반 병종 후보가 낮은 Tier 후보보다 높은 가중치를 가진다.
- 낮은 Tier 병영이 남아 있으면 낮은 Tier 병종도 계속 등장할 수 있다.
- 정확한 가중치는 데이터로 조정한다.

초기 추천:

| 출처 | 추천 가중치 |
|---|---:|
| Tier 1 | 1 |
| Tier 2 | 2 |
| Tier 3 | 3 |

기병·궁병·창병처럼 Tier 1 후보가 없는 계열은 Tier 2와 Tier 3만 비교한다. 이 경우 기존 1:2 비율도 후보이며 최종 값은 플레이테스트로 결정한다.

## 9. 일반 등급 결과

일반 등급에서는 선택된 병영 출처의 실제 생산 병종을 생성한다.

- Tier 1 전사 출처 → 일반 검사
- Tier 2 전사 출처 → 선택한 일반 전사 병종
- Tier 2 기병 출처 → 일반 기병
- Tier 3 중기병 출처 → 일반 중기병
- Tier 3 충격기병 출처 → 일반 충격기병
- Tier 3 대공궁병 출처 → 일반 대공궁병

## 10. 고정 상위 등급 결과

엘리트·영웅·전설은 기본 병종 계열별 고정 유닛이다.

- 전사 계열 상위 등급은 전사 계열 고정 템플릿
- 기병 계열 상위 등급은 중기병·충격기병 선택과 무관하게 고정
- 궁병 계열 상위 등급은 석궁병·대공궁병·연사궁병 선택과 무관하게 고정
- 창병 계열 상위 등급은 창병 계열 고정 템플릿

선택된 훈련 출처 Tier는 고정 상위 등급 유닛의 패시브 단계에 적용한다.

### 병영 Tier별 패시브

- Tier 1: 패시브 1 기본형
- Tier 2: 패시브 1 강화 + 패시브 2 생성
- Tier 3: 패시브 1 추가 강화 + 패시브 2 강화 + 패시브 3 생성

### 등급별 일반 스킬

- 일반: 1스킬
- 엘리트: 1스킬 강화
- 영웅: 강화된 1스킬 + 2스킬 생성
- 전설: 영웅 효과 + 2스킬 강화 + 3스킬 생성

## 11. 금화 심벌

판정 줄이 금화 3개일 때 전체 금화 완성 줄 수를 센다.

| 금화 완성 줄 | 보상 |
|---:|---:|
| 1줄 | 실제 회전 비용의 75% |
| 2줄 | 실제 회전 비용의 200% |
| 3줄 이상 | 실제 회전 비용의 500% |

```text
금화 보상 = floor(실제 지불한 회전 비용 × 환급 비율)
```

- 할인 후 실제 지불액 기준
- 금화 9칸 전체 동일도 500% 상한
- 금화에는 유닛 등급을 적용하지 않음
- 별도 회전 충전, 웨이브당 횟수 제한과 연속 감쇠 없음

## 12. 럭키 찬스

```text
기본 확률: 12%
자연 실패 1회당: +8%p
6회 연속 자연 실패 후 다음 회전: 100%
자연 발생 뒤: 현재 기본 확률로 초기화
```

- 현재 확률과 확정까지 남은 실패 횟수 공개
- 행운 아이템은 초기 기본 확률 상승
- 자연 럭키 찬스는 무료 행·열 이동 1회 제공

## 13. 이동권

- 자연 럭키 찬스와 이동권을 같은 회전에 함께 사용 가능
- 회전당 이동 횟수 상한 없음
- 실제 상한은 보유 이동권 수
- 이동권 1장당 행 또는 열 1칸 순환 이동 1회
- 여러 이동을 계획하고 되돌리기·전체 초기화 가능
- 최종 확정 시에만 이동권 소비
- 최종 확정 뒤 보상은 한 번만 계산

## 14. 결과 보관함

- 유닛 보상은 공간 부족으로 사라지지 않는다.
- 결과 보관함에 생성한 뒤 대기칸 이동·즉시 출전·판매로 정리한다.
- 보관함이 남아 있으면 다음 룰렛 회전만 차단한다.
- 전투·건설·카메라·기존 유닛 배치는 계속 가능하다.
- 금화 보상은 즉시 지급한다.

## 15. UI 계약

룰렛 전:

- 릴별 공유 토큰 수
- 병종 계열별 당첨 확률
- 일반 등급의 실제 세부 병종별 확률
- Tier별 훈련 출처 확률
- Tier별 패시브 해금·강화 내용

룰렛 결과:

- 완성 줄 수와 등급
- 기본 병종 계열
- 실제 생성 유닛
- 선택된 출처 병영과 Tier
- 생성·강화된 핵심 패시브
- 생성·강화된 일반 스킬
- 전설 사용 여부

## 16. 검증 계약

- 기본 상태에서는 중앙 가로줄만 판정한다.
- 혼합 줄과 2개 일치는 보상을 만들지 않는다.
- 1/2/3~7/8줄은 일반/엘리트/영웅/전설로 매핑된다.
- Tier 1 검사와 Tier 2 이상 전사 계열은 전사 토큰을 공유한다.
- 같은 계열의 Tier 2·Tier 3 병영은 같은 심벌을 제공한다.
- 높은 Tier 후보의 가중치는 낮은 Tier보다 높다.
- 일반 등급은 선택 출처의 실제 병종을 생성한다.
- 엘리트·영웅·전설은 세부 병종과 무관한 고정 계열 유닛을 생성한다.
- 선택 출처 Tier가 패시브 해금과 강화 단계를 결정한다.
- 엘리트는 1스킬을 강화한다.
- 영웅은 2스킬을 생성한다.
- 전설은 2스킬을 강화하고 3스킬을 생성한다.
- 전설은 한 스테이지 1회 제한을 지킨다.
- 같은 시드·회전 스냅샷·최종 보드는 같은 결과를 만든다.

## 17. 미확정 세부

- 전사 계열 Tier 2 병종의 최종 명칭과 수치
- Tier별 정확한 후보 가중치
- 계열별 고정 엘리트·영웅·전설 유닛
- 대기칸 기본 용량과 결과 보관함 UI
- 판정 줄 변경·행운·이동권 아이템 세부

이 문서는 핵심 구조를 승인하지만 개별 수치를 구현 승인으로 간주하지 않는다. Godot 구현은 별도 Codex Plan Mode 제안서와 사용자 승인을 거쳐야 한다.

```

</details>

### `docs/design/APPROVED_ROULETTE_PROBABILITY_TARGETS_POC_V1.md` — ACTIVE / 138 lines

Hits: `룰렛` [1, 5], `럭키` [5, 49, 52, 60, 67, 68, 129], `이동권` [5, 23, 38, 49, 68, 71, 73, 76, 128], `중앙 가로줄` [9], `결과 보관` [114]

```text
    1: # 승인된 룰렛 확률·등급·금화 기대값 PoC V1
    2: 
    3: - 상태: **확률 구조·목표 분포 승인 / 릴 가중치는 첫 시뮬레이션 가설**
    4: - 작성일: 2026-07-16
    5: - 적용 범위: 3×3 룰렛, 건물 토큰, X·금화·이동권, 럭키 찬스, 등급 결과
    6: 
    7: ## 1. 기본 판정
    8: 
    9: - 중앙 가로줄은 기본 판정줄이다.
   10: - 같은 비-X 심벌 3개가 완성된 판정줄만 보상한다.
   11: - 완성 줄 수 1/2/3~7/8은 일반/엘리트/영웅/전설이다.
   12: - 9칸 동일은 첫 전설 획득 전에는 전설, 이후에는 영웅 2기다.
   20: 기본 고정 가중치 / 릴
   21: X 6
   22: 금화 2
   23: 이동권 1
   24: 
   25: 활성 생산시설
   26: 기본 병영 계열 토큰 +3
   35: 
   36: ## 3. 목표 자연 확률
   37: 
   38: 이동권 사용 전 1회 회전의 목표 범위다.
   39: 
   40: | 결과 | 목표 확률 |
   41: |---|---:|
   46: | 전설 8줄 이상 | 0.10~0.35% |
   47: 
   48: - 첫 10회 안에 자연 일반 결과를 90% 이상 경험해야 한다.
   49: - 이동권과 럭키 찬스를 포함한 건강한 플레이에서 스테이지당 영웅 1~3기, 전설 0~1기를 목표로 한다.
   50: - 전설 미획득 상태에서 15웨이브까지 전설 확률 누적 목표는 25~45%다.
   51: 
   52: ## 4. 럭키 찬스
   53: 
   54: ```text
   55: 시작 12%
   57: 6회 실패 뒤 다음 회전 확정
   58: ```
   59: 
   60: 럭키 발동 시:
   61: 
   62: 1. 회전 결과를 생성한다.
   63: 2. 완성 줄이 없으면 현재 토큰 풀에서 가장 가까운 1줄 후보를 선택한다.
   64: 3. 한 칸을 결정론적으로 교체해 최소 일반 1줄을 만든다.
   65: 4. 이미 보상이 있으면 결과를 바꾸지 않는다.
   66: 
   67: - 럭키는 등급을 직접 올리지 않는다.
   68: - 럭키로 만들어진 줄도 일반 판정과 동일하게 이동권 조작이 가능하다.
   69: - 성공 후 확률은 12%로 초기화한다.
   70: 
   71: ## 5. 이동권
   72: 
   73: - 자연 이동과 아이템 이동권을 한 회전에 함께 사용할 수 있다.
   74: - 첫 PoC 보유 상한은 5개다.
   75: - 상점 기본 가격은 18금화, 같은 스테이지 구매마다 +4금화다.
   76: - 행·열 이동 1회마다 이동권 1개를 사용한다.
   77: - 결과 확정 전에는 되돌리기 1회를 무료로 허용하되 다른 이동을 확정하면 무료 되돌리기는 사라진다.
   78: 
   79: ## 6. 금화 기대값
  111: - 엘리트 이상은 계열 고정 템플릿을 생성하고 선택 출처 Tier의 패시브 단계만 적용한다.
  112: - 파괴된 건물은 다음 회전부터 후보에서 제거한다.
  113: 
  114: ## 8. 결과 보관함
  115: 
  116: - 기본 보관 슬롯 3개.
  117: - 보관 중인 결과는 식량을 사용하지 않는다.
  125: 
  126: - 건물 1/2/4/6동 조합.
  127: - 기본 병영만, 특수병단 혼합, 시장 중심 빌드.
  128: - 이동권 0/2/5개.
  129: - 럭키 없음/기본/최대 실패 보호.
  130: - 금화 평균 지급과 파산 확률.
  131: - 영웅·전설 최초 획득 웨이브 분포.
  132: 
```

<details><summary>Full content</summary>

```text
# 승인된 룰렛 확률·등급·금화 기대값 PoC V1

- 상태: **확률 구조·목표 분포 승인 / 릴 가중치는 첫 시뮬레이션 가설**
- 작성일: 2026-07-16
- 적용 범위: 3×3 룰렛, 건물 토큰, X·금화·이동권, 럭키 찬스, 등급 결과

## 1. 기본 판정

- 중앙 가로줄은 기본 판정줄이다.
- 같은 비-X 심벌 3개가 완성된 판정줄만 보상한다.
- 완성 줄 수 1/2/3~7/8은 일반/엘리트/영웅/전설이다.
- 9칸 동일은 첫 전설 획득 전에는 전설, 이후에는 영웅 2기다.
- 전설은 한 스테이지당 1회다.

## 2. 릴 가중치 구조

각 릴은 고정 유틸리티 가중치와 활성 건물 토큰 가중치를 합친다.

```text
기본 고정 가중치 / 릴
X 6
금화 2
이동권 1

활성 생산시설
기본 병영 계열 토큰 +3
특수병단 계열 토큰 +2
토큰 증폭 교리 +1
```

- 한 건물의 전문화 전환은 다음 회전부터 기존 토큰을 새 토큰으로 교체한다.
- 같은 계열 건물이 여러 동이면 가중치를 합산한다.
- 특수 토큰은 강한 결과이므로 기본 병영보다 낮은 가중치로 시작한다.
- 세 릴은 같은 계열 구성을 사용하되 정지 순서와 시드는 독립이다.

## 3. 목표 자연 확률

이동권 사용 전 1회 회전의 목표 범위다.

| 결과 | 목표 확률 |
|---|---:|
| 보상 없음 | 55~68% |
| 일반 1줄 | 22~32% |
| 엘리트 2줄 | 5~9% |
| 영웅 3줄 이상 | 1.0~2.5% |
| 전설 8줄 이상 | 0.10~0.35% |

- 첫 10회 안에 자연 일반 결과를 90% 이상 경험해야 한다.
- 이동권과 럭키 찬스를 포함한 건강한 플레이에서 스테이지당 영웅 1~3기, 전설 0~1기를 목표로 한다.
- 전설 미획득 상태에서 15웨이브까지 전설 확률 누적 목표는 25~45%다.

## 4. 럭키 찬스

```text
시작 12%
실패마다 +8%p
6회 실패 뒤 다음 회전 확정
```

럭키 발동 시:

1. 회전 결과를 생성한다.
2. 완성 줄이 없으면 현재 토큰 풀에서 가장 가까운 1줄 후보를 선택한다.
3. 한 칸을 결정론적으로 교체해 최소 일반 1줄을 만든다.
4. 이미 보상이 있으면 결과를 바꾸지 않는다.

- 럭키는 등급을 직접 올리지 않는다.
- 럭키로 만들어진 줄도 일반 판정과 동일하게 이동권 조작이 가능하다.
- 성공 후 확률은 12%로 초기화한다.

## 5. 이동권

- 자연 이동과 아이템 이동권을 한 회전에 함께 사용할 수 있다.
- 첫 PoC 보유 상한은 5개다.
- 상점 기본 가격은 18금화, 같은 스테이지 구매마다 +4금화다.
- 행·열 이동 1회마다 이동권 1개를 사용한다.
- 결과 확정 전에는 되돌리기 1회를 무료로 허용하되 다른 이동을 확정하면 무료 되돌리기는 사라진다.

## 6. 금화 기대값

기본 회전비 20 기준:

| 금화 줄 | 지급 |
|---|---:|
| 1줄 | 15 |
| 2줄 | 40 |
| 3줄 이상 | 100 |

목표:

```text
장기 평균 금화 지급 <= 회전당 6
순 회전 비용 >= 평균 14
```

- 무료 회전도 지급 기준 회전비 20을 사용한다.
- 금화 결과로 자동 재회전하지 않는다.
- 금화 가중치는 시장·난이도와 무관하게 기본 2로 시작한다.

## 7. 출처 후보

같은 토큰 계열의 여러 건물이 있을 때:

| 출처 | 가중치 |
|---|---:|
| T1 | 1 |
| T2 | 2 |
| T3 | 3 |

- 일반 결과는 선택된 실제 건물 병종을 생성한다.
- 엘리트 이상은 계열 고정 템플릿을 생성하고 선택 출처 Tier의 패시브 단계만 적용한다.
- 파괴된 건물은 다음 회전부터 후보에서 제거한다.

## 8. 결과 보관함

- 기본 보관 슬롯 3개.
- 보관 중인 결과는 식량을 사용하지 않는다.
- 동일 결과를 2개 이상 합성해 승급할 수 없다.
- 슬롯 초과 시 새 결과를 버리거나 기존 결과와 교체한다.
- 전설 결과는 버리기 전에 한 번의 확인을 요구한다.

## 9. 시뮬레이션 검증

구현 전 또는 구현 초기에 최소 100,000시드를 검증한다.

- 건물 1/2/4/6동 조합.
- 기본 병영만, 특수병단 혼합, 시장 중심 빌드.
- 이동권 0/2/5개.
- 럭키 없음/기본/최대 실패 보호.
- 금화 평균 지급과 파산 확률.
- 영웅·전설 최초 획득 웨이브 분포.

실패 기준:

- 무조작 전설 확률이 0.5%를 초과.
- 금화 평균 지급이 6을 초과.
- 건물을 늘릴수록 원하는 토큰의 절대 확률이 비정상적으로 감소.
- 특수병단 하나가 모든 기본 토큰보다 자주 등장.

```

</details>

### `docs/design/APPROVED_SHARED_UNIT_ARCHETYPE_AND_FACTION_VISUAL_DATA_V1.md` — ACTIVE / 274 lines

Hits: `룰렛` [40, 150]

```text
   37: - 공격 준비·판정·회복 시간, 애니메이션 상태 ID와 이벤트 프레임도 공용 계약을 사용한다.
   38: - 아군과 적군은 사용 이미지, 초상화, 아이콘, 팔레트와 진영 표시만 다르게 만든다.
   39: - 첫 PoC에서는 진영별 VFX·오디오도 별도 복제하지 않고 공용 사용을 기본으로 한다.
   40: - 적군은 룰렛을 사용하지 않고 웨이브와 생산시설을 통해 같은 아키타입을 출격시킨다.
   41: 
   42: ## 2. 공용 10병종
   43: 
  147: 분리:
  148: 
  149: - 팀과 적대 관계.
  150: - 룰렛·생산·웨이브 등 출격 경로.
  151: - 배치 권한과 AI 명령 출처.
  152: - 스프라이트·초상화·아이콘·팔레트.
  153: - 세계관 표시명과 설명 문구.
```

### `docs/design/APPROVED_SPECIAL_CORPS_BUILDING_AND_GIANT_CLASS_V3.md` — ACTIVE / 256 lines

Hits: `roulette` [53], `룰렛` [100, 101, 112]

```text
   50: 병종 표시명: 거인
   51: base_unit_family = large_giant
   52: combat_archetype_id = giant_siege_frontliner
   53: roulette_token_family = giant
   54: ```
   55: 
   56: 공통 태그:
   97: 
   98: 거인 획득 경로:
   99: 
  100: - 룰렛 일반 결과.
  101: - 룰렛 엘리트·영웅·전설 결과.
  102: - 거인단의 일반 등급 자동생산.
  103: 
  104: 고정 경계:
  109: - 식량 부족 시 완성 대기하며 다음 생산은 멈춘다.
  110: - 거인단 파괴 시 진행 중 생산과 건물 안 완성 대기 거인은 사라진다.
  111: - 이미 전장이나 대기칸에 인도된 거인은 유지된다.
  112: - 거인단 파괴 시 다음 룰렛부터 거인 토큰을 제거한다.
  113: - 플레이어 신화급 거인은 없다.
  114: 
  115: ---
```

### `docs/design/APPROVED_STAGE_ECONOMY_AND_BUILDING_COST_BASELINE_V1.md` — ACTIVE / 533 lines

Hits: `룰렛` [5, 13, 35, 44, 60, 63, 158, 187, 280, 315, 364, 378, 393, 403, 414, 425, 435, 503, 511, 527], `결과 보관` [259], `spin` [283, 456], `roulette` [456]

```text
    2: 
    3: - 상태: **경제 구조 승인 / 금화·비용·시간은 첫 PoC 기준값으로 승인하며 플레이테스트 조정 가능**
    4: - 작성일: 2026-07-15
    5: - 적용 범위: 정규 15분 스테이지 시작 자원, 시간 수입, 접전지 수입, 룰렛 비용, 기본 건물 비용, 기본 병영 Tier 2 비용, 시장, 농장, 전술 명령 비용, 환불 규칙
    6: - 연결 문서:
    7:   - `docs/design/APPROVED_BARRACKS_AND_SPECIAL_CORPS_UNIT_TREE_V5.md`
    8:   - `docs/design/APPROVED_SPECIAL_CORPS_BUILDING_AND_GIANT_CLASS_V3.md`
   10:   - `docs/design/APPROVED_15_WAVE_STAGE_CLOCK_AND_OVERTIME_V2.md`
   11: - 우선순위:
   12:   - 특수병단 비용·시간은 V5/V3 계약을 그대로 사용한다.
   13:   - 금화 룰렛 지급률은 기존 75%/200%/500% 계약을 유지한다.
   14:   - 튜토리얼은 필요하면 별도 시작 자원·무료 회전을 사용하며 이 문서는 정규 스테이지 기준이다.
   15: 
   16: ---
   32: 특수병단 준비 전환
   33: → 150초를 기다려 Tier 2 비용 최대 50% 절감
   34: 
   35: 룰렛 집중
   36: → 건물을 최소화하고 즉시 병력·상위 등급 기대값 확보
   37: ```
   38: 
   41: - 적 개별 처치 금화를 제공하지 않는다.
   42: - 웨이브 클리어 고정 금화를 제공하지 않는다.
   43: - 주요 확정 수입은 기본 시간 수입, 시장, 중앙 접전지 통제다.
   44: - 확률 수입은 금화 룰렛 결과로 제한한다.
   45: - 약한 적을 남겨두고 파밍하거나 공세를 의도적으로 지연해 이득을 얻을 수 없어야 한다.
   46: - 일시정지 중에는 모든 시간 기반 수입과 생산이 멈춘다.
   47: 
   57: starting_food_used = 배치된 초기 유닛 기준
   58: ```
   59: 
   60: - 시작 금화는 기본 병영·포탑·농장을 지은 뒤 룰렛 2회를 돌릴 수 있는 수준이다.
   61: - 특수병단 즉시 전문화도 가능하지만 다른 방어 투자를 크게 포기해야 한다.
   62: - 캠페인 제약·난이도는 시작 금화를 데이터로 변경할 수 있다.
   63: - 튜토리얼의 강제 건설·무료 룰렛은 별도 TutorialManifest가 우선한다.
   64: 
   65: ---
   66: 
  155: - 업그레이드 중에는 Tier 1 검사 생산과 전사 토큰을 유지한다.
  156: - 생산 진행률을 보존한다.
  157: - 완료 뒤 처음 완성되는 생산분부터 선택 병종으로 변경한다.
  158: - 진행 중인 룰렛은 유지하고 다음 회전부터 토큰 변경을 적용한다.
  159: - 자동생산은 일반 등급만 생성한다.
  160: - 기본 병영 Tier 3 비용은 상위 병종과 운영 교리 수치 확정 뒤 별도 승인한다.
  161: 
  184: - 업그레이드 시간은 할인하지 않는다.
  185: - 전문화 시작 순간 할인율과 실제 지불액을 고정한다.
  186: - 자동생산은 일반 등급만 생성한다.
  187: - 상위 등급 특수 유닛은 룰렛으로만 획득한다.
  188: 
  189: ---
  190: 
  256: 
  257: - 시작 식량 한도는 12다.
  258: - 전장에 배치되거나 출격 대기열에서 인도된 유닛만 식량을 점유한다.
  259: - 결과 보관함과 아직 배치하지 않은 대기 결과는 식량을 사용하지 않는다.
  260: - 유닛 사망·영구 퇴각 시 식량을 반환한다.
  261: - 식량 한도를 초과해도 기존 유닛은 유지한다.
  262: - 신규 자동생산·배치만 차단한다.
  277: 
  278: ---
  279: 
  280: ## 10. 룰렛 비용과 금화 결과
  281: 
  282: ```text
  283: base_spin_cost = 20 gold
  284: ```
  285: 
  286: - 정규 스테이지 기본 회전비는 고정 20금화다.
  312: ≤ 실제 회전비의 30%
  313: ```
  314: 
  315: 즉 기본 기준으로 평균 지급 목표는 회전당 6금화 이하다. 정확한 토큰 구성과 확률은 룰렛 확률 모델에서 검증한다.
  316: 
  317: - 금화 결과는 회전비를 기준으로 계산하므로 비용 할인·무료 회전의 예외를 명시해야 한다.
  318: - 무료 회전에서 금화 심벌이 완성되면 `기준 회전비 20`을 지급 기준으로 사용한다.
  361: 
  362: ## 13. 15분 총수입 기준
  363: 
  364: 금화 룰렛 지급과 건물 지출을 제외한 총 가용 금화다.
  365: 
  366: | 시나리오 | 5분 | 10분 | 15분 |
  367: |---|---:|---:|---:|
  375: - 시작 금화 160 포함.
  376: - 기본 시간 수입 포함.
  377: - 시장 1개는 0:00에 건설을 시작해 0:25에 완공.
  378: - 금화 룰렛 수입은 제외.
  379: - 건설비·회전비 지출은 제외한 총 유입량이다.
  380: 
  381: 첫 시장은 15분 시점에 약 172금화를 생산하고 건설비 50을 제외하면 약 122금화 순증가한다. 대신 초반 전투 건물 한 동과 후방 노드 하나를 포기한다.
  390: 기본 병영 40
  391: + 포탑 35
  392: + 농장 35
  393: + 룰렛 2회 40
  394: = 150
  395: 잔여 10
  396: ```
  400: ```text
  401: 기본 병영 2동 80
  402: + 농장 35
  403: + 룰렛 2회 40
  404: = 155
  405: 잔여 5
  406: ```
  411: 기본 병영 40
  412: + 시장 50
  413: + 농장 35
  414: + 룰렛 1회 20
  415: = 145
  416: 잔여 15
  417: ```
  422: 기본 병영 40
  423: + 특수병단 Tier 1 40
  424: + 사제단 즉시 업그레이드 60
  425: + 룰렛 1회 20
  426: = 160
  427: 잔여 0
  428: ```
  432: ```text
  433: 특수병단 Tier 1 40
  434: + 거인단 즉시 업그레이드 100
  435: + 룰렛 1회 20
  436: = 160
  437: 잔여 0
  438: ```
  453: - base_income_interval_seconds
  454: - control_income_amount_per_point
  455: - control_income_interval_seconds
  456: - roulette_base_spin_cost
  457: - gold_result_reward_multipliers
  458: - building_cost_profiles
  459: - building_time_profiles
  500: 2. 기본 수입은 활성 전투 시간 20초마다 5금화다.
  501: 3. 중앙 접전지는 60초마다 아군 점령 한 곳당 4금화를 지급한다.
  502: 4. 적 처치와 웨이브 클리어는 기본 금화를 지급하지 않는다.
  503: 5. 기본 룰렛 회전비는 20금화다.
  504: 6. 시작 금화만으로 안정 방어·시장·사제단 즉시 전환 중 하나를 선택할 수 있다.
  505: 7. 거인단 즉시 전환은 가능하지만 기본 방어 투자를 거의 포기한다.
  506: 8. 시장 첫 동의 비용 회수시간은 완공 후 약 4분 10초다.
  508: 10. 특수병단 준비 할인은 150초·50%가 최대다.
  509: 11. 자동생산과 시간 수입은 일시정지 중 멈춘다.
  510: 12. 식량 부족은 기존 유닛을 제거하지 않고 신규 인도만 막는다.
  511: 13. 금화 룰렛의 장기 평균 지급 목표는 실제 회전비의 30% 이하다.
  512: 14. 건설 취소·업그레이드 취소·철거·파괴의 환불률이 서로 다르다.
  513: 15. 적 파괴에는 환불이 없다.
  514: 16. 15분 평균 통제 시나리오에서 총 유입 475금화 전후를 첫 기준으로 사용한다.
  524: - 농장 Tier 3 전문화.
  525: - 포탑 Tier 2·3 전투 효과와 비용.
  526: - 기본 병영 Tier 3 상위 병종·교리 비용.
  527: - 룰렛 실제 확률과 금화 심벌 기대값.
  528: - 용병·아이템·장비 가격.
  529: - 난이도별 시작 금화·수입·비용 배율.
  530: - 전술 명령 정확한 효과 대비 비용.
```

### `docs/design/APPROVED_TUTORIAL_CAMPAIGN_PROCEDURAL_POC_V1.md` — ACTIVE / 176 lines

Hits: `룰렛` [46, 129]

```text
   43: 
   44: ### 고정 흐름
   45: 1. 기본 병영 무료 또는 강제 건설.
   46: 2. 첫 룰렛 중앙 1줄 일반 검사.
   47: 3. 상단 제1공세 방어.
   48: 4. 포탑 또는 바리케이드로 첫 역전.
   49: 5. 두 번째 회전에서 상단 행 왼쪽 이동으로 엘리트 전사.
  126: | 특수 위협 동시 수 | 최대 1 | 최대 2 | 최대 3 |
  127: 
  128: - 난이도는 숨은 명중·회피나 플레이어 조작 불가 확률을 변경하지 않는다.
  129: - 룰렛 기본 확률은 난이도로 직접 낮추지 않는다.
  130: 
  131: ## 8. 베일의 축복·저주·선언 초안
  132: 
```

### `docs/design/APPROVED_TUTORIAL_FIRST_FOUR_WAVES_BALANCE_V1.md` — ACTIVE / 599 lines

Hits: `룰렛` [23, 53, 197, 303, 386, 471, 489]

```text
   20: ```text
   21: 1공세: 일반 검사 한 명의 배치·자동전투·횡베기
   22: 2공세: 두 라인 판단·포탑 또는 바리케이드에 의한 역전
   23: 3공세: 룰렛 조작으로 얻은 엘리트의 확실한 전투력 상승
   24: 4공세: Tier 2 전사 선택과 세 라인 종합 판단
   25: ```
   26: 
   50: - `공세 충돌 시점`은 적 선두가 중앙 접전지 주 교전 반경에 들어오는 순간이다.
   51: - 베일의 징조는 기본적으로 충돌 30초 전에 표시한다.
   52: - 적 출격은 충돌 약 10~12초 전에 적 중간요새에서 시작한다.
   53: - 출격 전 18~20초 동안 플레이어는 편성·건설·룰렛·배치를 준비한다.
   54: 
   55: ### 2.3 초기 피해 모델
   56: 
  194: 
  195: ## 5. 병영 자동생산 보장
  196: 
  197: 첫 룰렛 외에도 병영이 일반 병력을 자동생산한다는 사실을 자연스럽게 보여준다.
  198: 
  199: ### 5.1 Tier 1 병영 생산
  200: 
  300: 
  301: 사용 가능 병력:
  302: 
  303: - 룰렛으로 얻은 일반 검사 A 1기
  304: 
  305: 의도한 결과:
  306: 
  383: 
  384: - 생존 일반 검사들
  385: - 병영 자동생산 검사 C
  386: - 두 번째 룰렛의 엘리트 전사 1기
  387: 
  388: 권장 배치:
  389: 
  468: | 포탑·바리케이드 선택 강조 | 제2공세 충돌 23초 전 |
  469: | 제2공세 충돌 | 3:38~3:45 |
  470: | 제2공세 종료·첫 역전 | 3:55~4:15 |
  471: | 두 번째 룰렛·엘리트 확정 | 5:15~5:45 |
  472: | 제3공세 충돌 | 6:15~6:30 |
  473: | 제3공세 종료 | 6:25~6:45 |
  474: | Tier 2 선택 완료 | 7:50~8:00 |
  486: 정확한 시작 금화와 건물 비용은 별도 경제 표에서 확정한다. 다만 두 번째 공세 시작 시 다음 조건을 반드시 만족해야 한다.
  487: 
  488: ```text
  489: 병영·필수 룰렛 비용을 지불한 뒤에도
  490: 포탑 건설 또는 바리케이드 사용 중 하나는 항상 가능
  491: ```
  492: 
```

### `docs/design/APPROVED_UI_ART_AUDIO_POC_BIBLE_V1.md` — ACTIVE / 267 lines

Hits: `룰렛` [28, 34, 35, 89, 95, 216], `럭키` [28], `이동권` [28, 93, 96], `결과 보관` [95]

```text
   25: 상단 좌측: 금화·식량·수입 타이머
   26: 상단 중앙: 공세 시계·베일의 징조·보스 체력
   27: 상단 우측: 일시정지·속도·설정
   28: 좌측 하단: 3×3 룰렛·이동권·럭키 찬스
   29: 하단 중앙: 건설·용병·대기 결과·전술 명령
   30: 우측 하단: 벨루·최대 두 줄 대사
   31: ```
   32: 
   33: - 중앙 전장 가시영역을 최대한 보존한다.
   34: - 룰렛·건설 조작 중에도 웨이브 경고와 본진 위험을 가리지 않는다.
   35: - 전설 룰렛 결과 연출은 1.2초 이내에 전투 화면으로 복귀한다.
   36: - 목표 상태는 축소 지도에 복제하지 않고 실제 월드 위치에 표시한다.
   37: 
   38: ## 3. 정보 우선순위
   86: - 위험 공격의 붉은 전조와 혼동되지 않는 회색·청회색 연무를 사용한다.
   87: - 선택 해제·침투 종료 뒤 다시 가린다.
   88: 
   89: ## 5. 룰렛 UX
   90: 
   91: - 각 심벌은 실루엣 아이콘과 작은 계열 문양을 함께 사용한다.
   92: - 현재 판정줄은 얇은 금속 프레임으로 표시한다.
   93: - 이동권 사용 전 예상 완성 줄을 실시간 미리보기한다.
   94: - 등급 결과는 프레임 형태, 길이와 음향 단계로 구분한다.
   95: - 결과 보관함 3칸은 룰렛 옆에 명확히 배치한다.
   96: - 금화·이동권 부족은 비활성 버튼과 정확한 부족량으로 표시한다.
   97: - 결과 유닛은 공용 `archetype_id`와 Rank를 나타내며 진영별 별도 병종 ID를 사용하지 않는다.
   98: 
   99: ## 6. 공용 10병종과 진영 이미지
  213: 
  214: ### 효과음
  215: 
  216: - 룰렛 일반·엘리트·영웅·전설은 1·2·3·4단계 음정 상승.
  217: - 벨루는 맑은 두 음 종 모티프.
  218: - 방패·대검·창·궁·마법·비행·거인은 공용 아키타입별 재질·저역 차이를 사용한다.
  219: - 아군·적군 이미지가 다르다는 이유로 별도 효과음을 대량 복제하지 않는다.
```

### `docs/design/APPROVED_UNIT_ANIMATION_AND_BATTLE_PRESENTATION_GUIDE_V1.md` — ACTIVE / 539 lines

Hits: `룰렛` [74, 421]

```text
   71: 
   72: | 상태 | 필수 여부 | 목적 |
   73: |---|---|---|
   74: | `deploy` | 필수 | 생산·룰렛 결과 뒤 전장 배치 |
   75: | `idle` | 필수 | 대기와 병종 정체성 |
   76: | `move` | 필수 | 라인 전진·후퇴·목표 접근 |
   77: | `attack_basic` | 필수 | 기본 공격 판정 표시 |
  418: 
  419: - 자동 카메라가 3라인 시야를 빼앗지 않는다.
  420: - 보스 등장·성문 붕괴·승리는 짧은 중심 이동을 우선한다.
  421: - 룰렛·건설·배치 중 포인터 대상이 자동 연출로 움직이지 않게 한다.
  422: 
  423: ---
  424: 
```

### `docs/design/APPROVED_UNIT_GRADE_AND_ABILITY_GROWTH.md` — ACTIVE / 240 lines

Hits: `roulette` [6], `룰렛` [10, 12, 28, 37, 82, 198, 220]

```text
    3: - 상태: **핵심 구조 승인됨 / 병종별 세부 능력과 수치 조정 대기**
    4: - 승인일: 2026-07-14
    5: - 연결 문서:
    6:   - `docs/design/APPROVED_ROULETTE_CORE_RULES.md`
    7:   - `docs/design/APPROVED_BARRACKS_TIER3_EVOLUTION_AND_GRADE_SKILLS.md`
    8:   - `docs/design/APPROVED_COMBAT_KEYWORDS_STATUS_EFFECTS_AND_FLIGHT.md`
    9: 
   10: 이 문서는 룰렛 등급과 능력 성장 구조를 정의한다. 병영 Tier별 병종 진화·공유 토큰·패시브 강화는 `APPROVED_BARRACKS_TIER3_EVOLUTION_AND_GRADE_SKILLS.md`가 우선한다.
   11: 
   12: ## 1. 등급은 룰렛로만 결정한다
   13: 
   14: | 동일 심벌 완성 줄 | 생성 등급 |
   15: |---:|---|
   25: - 전설은 한 스테이지 전체에서 병종과 관계없이 1회만 생성한다.
   26: - 전설 생성 뒤 다시 9칸 동일 결과가 나오면 해당 계열 영웅 2명으로 변환한다.
   27: 
   28: ## 2. 병영 Tier와 룰렛 등급은 독립 축이다
   29: 
   30: 병영 Tier가 결정하는 것:
   31: 
   34: - 기존 핵심 패시브의 강화 단계
   35: - 자동 생산 병종
   36: 
   37: 룰렛 등급이 결정하는 것:
   38: 
   39: - 일반·엘리트·영웅·전설 고정 등급
   40: - 일반 스킬의 강화·생성 단계
   79: 
   80: ## 4. 등급별 일반 스킬 성장
   81: 
   82: 핵심 패시브와 별도로 일반 스킬은 룰렛 등급에 따라 성장한다.
   83: 
   84: ### 일반
   85: 
  195: 
  196: ## 9. UI 계약
  197: 
  198: 룰렛 결과 화면:
  199: 
  200: - 기본 병종 계열
  201: - 실제 생성 유닛
  217: ## 10. 검증 계약
  218: 
  219: - 1/2/3~7/8줄은 일반/엘리트/영웅/전설로 매핑된다.
  220: - 병영 Tier가 룰렛 등급 결과를 바꾸지 않는다.
  221: - Tier 1은 패시브 1 기본형을 적용한다.
  222: - Tier 2는 패시브 1 강화와 패시브 2 생성을 적용한다.
  223: - Tier 3는 패시브 1 추가 강화, 패시브 2 강화와 패시브 3 생성을 적용한다.
```

### `docs/design/APPROVED_WARRIOR_FAMILY_TIER2_ABILITIES.md` — ACTIVE / 318 lines

Hits: `룰렛` [237, 305]

```text
  234: 
  235: 각 타격은 별도 적중으로 처리하지만 `피의 갈증`은 자체 내부 쿨타임을 따른다.
  236: 
  237: ## 6. 병영 Tier와 룰렛 등급의 적용 순서
  238: 
  239: 일반 전사 계열 보상:
  240: 
  302: - Tier 2 광전사는 광전 본능·피의 갈증·광란 연격을 가진다.
  303: - Tier 2 병종은 패시브 1 강화와 패시브 2 생성을 모두 받는다.
  304: - 향후 Tier 3 후손은 패시브 1 추가 강화·패시브 2 강화·패시브 3 생성을 받는다.
  305: - Tier 2 일반 병종의 고유 1스킬은 룰렛 상위 등급 고정 전사의 스킬로 자동 승계되지 않는다.
  306: - 비행 유닛은 세 병종의 기본 근접 공격과 지상 전용 1스킬의 대상이 아니다.
  307: - 건물 파괴 뒤에도 생성된 유닛의 패시브와 스킬은 유지된다.
  308: 
```

### `docs/design/DESIGN_FREEZE_CHECKLIST.md` — REFERENCE_OR_OTHER / 198 lines

Hits: `룰렛` [21, 25, 101, 109, 114, 116, 124, 135, 198], `럭키` [114], `결과 보관` [114], `이동권` [114, 117]

```text
   18: ## A. 제품·한 판 구조
   19: 
   20: - [x] Windows PC, 마우스·키보드, 싱글플레이 PvE.
   21: - [x] 핵심 루프 `징조 → 건설·룰렛 → 배치 → 3라인 공방`.
   22: - [x] 활성 전투 시간 기준 60초 공세 시계.
   23: - [x] W5 엘리트, W10 영웅, W15 전설 보스, W20 신화 보스.
   24: - [x] W15 보스 처치 표준 승리, 이전 적 본진 파괴 조기 승리.
   25: - [x] 일시정지 중 건설·룰렛·배치·전술 계획 가능, active combat time 정지.
   26: - [~] 정규 전투 약 15분과 약 3시간 첫 캠페인.
   27: - [ ] 재시작·포기·결과 화면의 정확한 입력 흐름.
   28: 
   98: 
   99: - [x] 건물은 지정 노드에만 배치하고 도로를 막지 않음.
  100: - [x] 상태: Planned→Constructing→Active→Upgrading→Inactive/Ruins.
  101: - [x] 완공 건물 개수가 룰렛 토큰 수를 결정.
  102: - [x] 자동생산은 일반 Rank만 생성.
  103: - [x] 특수병단 T1 준비 할인 최대 50%.
  104: - [x] 중간거점 소유권과 건설·생산 권한 연동.
  106: - [~] Tier 2·3 효과와 생산 주기.
  107: - [ ] 건물 상태와 거점 이벤트의 실제 Signal·데이터 흐름.
  108: 
  109: ## G. 경제·룰렛
  110: 
  111: - [x] 시작 금화 160, 식량 12.
  112: - [x] 기본 +5/20초, 접전지 +4/60초, 중간거점 +2/30초.
  113: - [x] 적 처치·웨이브 클리어 고정 금화 없음.
  114: - [x] 룰렛 3×3, 건물 토큰, 결과 보관함, 이동권, 럭키.
  115: - [x] Rank 결과 1/2/3~7/8줄 = 일반/엘리트/영웅/전설.
  116: - [x] 룰렛 결과는 공용 archetype ID와 Rank를 생성.
  117: - [~] X·금화·이동권·병종 토큰 가중치.
  118: - [~] 자연 Rank 확률과 금화 EV.
  119: - [ ] 최소 100,000시드 시뮬레이션.
  120: - [ ] 경제 시간축 검증.
  121: 
  122: ## H. 적 웨이브·보스
  123: 
  124: - [x] 적은 플레이어 룰렛이 아니라 StageManifest·웨이브로 출격.
  125: - [x] spawn entry는 공용 archetype, Tier, Rank, count, lane, time, team, Visual Set 참조.
  126: - [x] T-30/T-15/T-5 징조.
  127: - [x] 생산시설 파괴가 예약 출격분 감소.
  132: 
  133: ## I. 입력·UI·접근성
  134: 
  135: - [x] 룰렛 좌하단, 건설·전술 하단, 벨루 우하단.
  136: - [x] 성문·거점·접전지 상태를 실제 월드 위치에 표시.
  137: - [x] 미니맵 없음.
  138: - [x] 암살자 선택 중 우회 경로·도착 구역 표시.
  195: 
  196: - 현재 코드를 수정할 수 없음.
  197: - Plan Mode 제안서 승인 전 구현 브랜치·커밋·PR 금지.
  198: - 새 대형 시스템보다 공용 데이터·전장·룰렛·연출의 구현과 계측이 우선.
```

### `docs/design/notes/0005a-construction-durability-and-doctrine-recommendation.md` — REFERENCE_OR_OTHER / 244 lines

Hits: `룰렛` [194]

```text
  191: - 파괴되어도 식량 기여가 즉시 사라지지 않고 비상 비축 시간 뒤 제거된다.
  192: - Tier 2 12초, Tier 3 20초 초기 가설
  193: - 비축 시간 동안 새 농장을 완공하면 식량 초과를 피할 수 있다.
  194: - 건물이 파괴됐으므로 룰렛 토큰·기타 기능은 즉시 제거한다.
  195: 
  196: ## 10. 포탑 전문화 추천
  197: 
```

### `docs/design/notes/0006a-tactical-command-economy-and-cooldown-recommendation.md` — REFERENCE_OR_OTHER / 218 lines

Hits: `룰렛` [121]

```text
  118: - 시전 지연 1.5초
  119: - 지속시간 18초 또는 체력 0까지
  120: - 체력: 해당 스테이지 Tier 1 포탑의 약 70%
  121: - 공격·식량·건설 노드·룰렛 토큰 없음
  122: - 개별 쿨다운 30초 초기값
  123: 
  124: 바리케이드는 일반 웨이브와 돌격을 지연하지만 보스를 영구 봉쇄하지 않는다.
```

### `docs/design/notes/roulette-proposal-review-questions.md` — REFERENCE_OR_OTHER / 34 lines

Hits: `룰렛` [1, 32], `roulette` [5, 6], `lucky` [6], `중앙 가로줄` [10], `판정 줄` [10, 11], `럭키` [16, 17, 18], `이동권` [18, 26]

```text
    1: # 룰렛 제안서 검토 질문
    2: 
    3: 책임 원본:
    4: 
    5: - `docs/design/APPROVED_ROULETTE_CORE_RULES.md`
    6: - `docs/design/proposals/0004-roulette-resolution-bench-and-lucky-chance.md`
    7: 
    8: ## 승인됨
    9: 
   10: - 기본 판정 줄은 중앙 가로줄이며 아이템 효과로만 변경한다.
   11: - 판정 줄 3칸이 같은 비-X 심벌일 때만 보상한다.
   12: - 완성 줄 1개는 1성 병사, 2개는 엘리트, 3~7개는 영웅이다.
   13: - 유닛 9칸 전체 동일은 전설이며 한 판당 한 번만 소환한다.
   14: - 전설 소환 뒤 다시 9칸 동일이 나오면 같은 병종 영웅 2명을 지급한다.
   15: - 금화는 1줄 75%, 2줄 200%, 3줄 이상 500%를 지급한다.
   16: - 럭키 찬스는 12% 시작, 실패당 +8%p, 6회 실패 뒤 다음 회전 확정 구조다.
   17: - 행운 아이템으로 럭키 찬스 초기 확률을 높인다.
   18: - 자연 럭키 찬스와 이동권을 함께 사용할 수 있다.
   19: - 회전당 이동 횟수 제한을 두지 않는다.
   20: 
   21: ## 다음 조정이 필요한 항목
   23: 1. 줄 변경 아이템의 형태와 지속시간
   24: 2. 행운 아이템 1개당 증가량과 중첩 상한
   25: 3. 대기칸 기본 용량과 수동 합성 포함 여부
   26: 4. 이동권 보유 상한과 가격
   27: 5. 전설의 영웅 대비 능력치·스킬 관계
   28: 6. 금화 200%·500%를 유지하면서 무한 회전을 막을 안전장치
   29: 
   30: ### 금화 안전장치 추천
   31: 
   32: 현재 추천은 룰렛 회전에 금화와 별개의 **시간 기반 회전 충전** 1개를 요구하는 방식이다. 금화 보상이 높아도 충전 속도보다 빠르게 연속 회전할 수 없으며, 전투 중 룰렛 템포도 제어할 수 있다.
   33: 
   34: 승인 전에는 Godot 구현을 시작하지 않는다.
```

<details><summary>Full content</summary>

```text
# 룰렛 제안서 검토 질문

책임 원본:

- `docs/design/APPROVED_ROULETTE_CORE_RULES.md`
- `docs/design/proposals/0004-roulette-resolution-bench-and-lucky-chance.md`

## 승인됨

- 기본 판정 줄은 중앙 가로줄이며 아이템 효과로만 변경한다.
- 판정 줄 3칸이 같은 비-X 심벌일 때만 보상한다.
- 완성 줄 1개는 1성 병사, 2개는 엘리트, 3~7개는 영웅이다.
- 유닛 9칸 전체 동일은 전설이며 한 판당 한 번만 소환한다.
- 전설 소환 뒤 다시 9칸 동일이 나오면 같은 병종 영웅 2명을 지급한다.
- 금화는 1줄 75%, 2줄 200%, 3줄 이상 500%를 지급한다.
- 럭키 찬스는 12% 시작, 실패당 +8%p, 6회 실패 뒤 다음 회전 확정 구조다.
- 행운 아이템으로 럭키 찬스 초기 확률을 높인다.
- 자연 럭키 찬스와 이동권을 함께 사용할 수 있다.
- 회전당 이동 횟수 제한을 두지 않는다.

## 다음 조정이 필요한 항목

1. 줄 변경 아이템의 형태와 지속시간
2. 행운 아이템 1개당 증가량과 중첩 상한
3. 대기칸 기본 용량과 수동 합성 포함 여부
4. 이동권 보유 상한과 가격
5. 전설의 영웅 대비 능력치·스킬 관계
6. 금화 200%·500%를 유지하면서 무한 회전을 막을 안전장치

### 금화 안전장치 추천

현재 추천은 룰렛 회전에 금화와 별개의 **시간 기반 회전 충전** 1개를 요구하는 방식이다. 금화 보상이 높아도 충전 속도보다 빠르게 연속 회전할 수 없으며, 전투 중 룰렛 템포도 제어할 수 있다.

승인 전에는 Godot 구현을 시작하지 않는다.

```

</details>

### `docs/design/proposals/0001-match-structure-and-start-conditions.md` — HISTORICAL / 348 lines

Hits: `룰렛` [21, 23, 38, 84, 109, 114, 115, 116, 123, 127, 146, 148, 183, 208, 229, 292, 299, 300, 313]

```text
   18: | 정식 한 판 목표 | 25~35분 |
   19: | 수직 슬라이스 목표 | 10~15분 |
   20: | 시작 건물 | 본진 핵심 건물만 존재, 전용 건설 노드는 모두 비어 있음 |
   21: | 시작 룰렛 | 실제 토큰 0개, 각 릴 X 토큰 3개 |
   22: | 시작 경제 | 본진 기본 금화 수입과 기본 보급 한도 제공 |
   23: | 시작 금화 기준 | `Tier 1 경제시설 1개 + 기본 훈련소 1개 + 룰렛 2회`가 가능한 예산 |
   24: | 첫 웨이브 | 게임 시작 시 즉시 전조 공개, 60초 후 한 라인으로 출격 |
   25: | 접전지 활성화 | 첫 웨이브 출격 시점에 활성화 |
   26: | 기본 속도 | 1배속 |
   35: ### 3.1 MVP 모드
   36: 
   37: - 싱글플레이 PvE만 제공한다.
   38: - 적은 플레이어 룰렛을 복제하지 않고 기존 기획대로 건물·티어 기반 웨이브를 사용한다.
   39: - 멀티플레이, 협동, 랭크, 영구 메타 성장은 제외한다.
   40: - 첫 MVP는 난이도 `일반` 하나만 제공한다.
   41: 
   81: ### 5.2 시작 시 존재하지 않는 것
   82: 
   83: - 플레이어 시장, 농장, 훈련소, 포탑
   84: - 룰렛 실제 토큰
   85: - 출전 유닛
   86: - 점령 중인 접전지
   87: - 런 전용 장비·스킬북
  106: 시작 금화 목표
  107: = Tier 1 경제시설 1개
  108: + 기본 병종 훈련소 1개
  109: + 기본 룰렛 2회
  110: ```
  111: 
  112: 플레이어는 다음과 같은 선택을 할 수 있어야 한다.
  113: 
  114: - 시장 + 훈련소 + 룰렛: 경제와 병력의 균형
  115: - 농장 + 훈련소 + 룰렛: 빠른 병력 확장 준비
  116: - 훈련소 2개 + 적은 룰렛: 병종 특화와 높은 초반 압박
  117: - 전방 포탑 + 훈련소: 안전한 첫 방어
  118: 
  119: 시작 예산으로 주요 시설 3개와 다수 회전을 동시에 확보할 수는 없어야 한다.
  120: 
  121: 기본 보급 한도는 Tier 1 병사 약 4~6기를 운용할 수 있는 수준을 목표로 한다. 농장이 없어도 첫 웨이브 대응은 가능하지만 중반 확장은 막히도록 한다.
  122: 
  123: ## 7. 시작 룰렛 상태
  124: 
  125: - 실제 토큰은 0개로 시작한다.
  126: - 각 릴은 X 토큰 3개로 채워진다.
  127: - 실제 토큰이 없는 상태에서도 룰렛 버튼은 보이지만, 회전 시 보상이 없다는 사실을 명확히 표시한다.
  128: - 기본 권장안은 실제 토큰이 0개일 때 유료 회전을 차단하는 것이다.
  129: - 첫 건물이 완공된 순간 토큰 장부와 확률 미리보기가 갱신된다.
  130: 
  143: ### T=0~30
  144: 
  145: - 플레이어가 첫 건물을 선택
  146: - 건설 전 룰렛 확률 변화를 확인
  147: - 첫 훈련소 또는 시장·농장 건설
  148: - 건물이 완공되면 룰렛과 자동 생산 사용 가능
  149: 
  150: ### T=30
  151: 
  180: - 대략 웨이브 1~2
  181: - 한 라인 중심 공격
  182: - Tier 1 유닛과 건물
  183: - 플레이어가 건설→토큰→룰렛→배치를 학습
  184: 
  185: ### 중반 단계
  186: 
  205: 
  206: - `Space`로 일시정지·재개
  207: - 일시정지 중 카메라 이동, 정보 확인, 건물·유닛 선택 가능
  208: - 건설, 업그레이드, 출전, 룰렛 기준 줄 선택 같은 명령은 예약 가능
  209: - 예약 명령의 자원은 즉시 차감하거나 명확하게 예약 표시
  210: - 게임 시간, 건설, 생산, 이동, 공격, 점령과 웨이브 타이머는 정지
  211: 
  226: 
  227: ### 10.3 패널 동작
  228: 
  229: - 룰렛, 건설, 업그레이드와 웨이브 정보는 전장을 덮는 전체화면 모달이 아니라 패널로 표시
  230: - 패널을 연다고 자동으로 시간이 멈추지 않음
  231: - 플레이어가 원하면 직접 일시정지
  232: - 확인 대화상자와 결과 화면만 자동 정지
  289: - 새 시드로 재도전
  290: - 메인 메뉴
  291: 
  292: 같은 시나리오 재도전은 적 웨이브 일정과 맵 구성을 유지한다. 룰렛 난수까지 같은 시드로 재현하는 기능은 디버그 옵션에서 제공하고 일반 플레이에서는 새 룰렛 결과를 기본으로 한다.
  293: 
  294: ## 13. 튜토리얼 첫 판 예외
  295: 
  296: 첫 튜토리얼에서는 다음을 순차적으로 개방한다.
  297: 
  298: 1. 후방 시장 또는 훈련소 건설
  299: 2. 건설 전 룰렛 확률 변화 확인
  300: 3. 룰렛 회전
  301: 4. 대기 유닛을 첫 웨이브 라인에 배치
  302: 5. 첫 웨이브 방어
  303: 6. 중앙 접전지 활성화와 점령
  310: | 상황 | 추천 처리 |
  311: |---|---|
  312: | 시작 후 아무 건물도 짓지 않음 | 첫 웨이브 전조는 계속 진행. 본진 약한 방어만 존재하며 패배 가능 |
  313: | 실제 룰렛 토큰 0개 | 회전 차단, 이유와 첫 건물 추천 표시 |
  314: | 첫 웨이브 전에 2배속 사용 | 허용하되 T-10에 자동 1배속 |
  315: | 일시정지 중 명령 예약 후 자원 부족 | 예약 시점에 자원 검증·차감하여 재개 시 실패하지 않게 함 |
  316: | 승패 판정 직전에 일시정지 | 승패 조건이 충족된 틱이면 결과 판정 우선 |
```

### `docs/design/proposals/0001-phase-0-godot-bootstrap.md` — HISTORICAL / 1138 lines

Hits: `룰렛` [195, 286, 341, 357, 827], `roulette` [333, 927]

```text
  192: 
  193: 단점:
  194: 
  195: - 3라인 전체 전장, 룰렛, 건설·전술, 벨루와 상태 표시를 동시에 보여주기에는 논리 픽셀 공간이 작음.
  196: - 고해상도 픽셀 재질과 긴 한국어 UI의 정보 밀도가 과도해질 수 있음.
  197: 
  198: 결론: 960×540을 추천한다. Phase 0 수동 검증에서 1280×720 레터박스가 제품 기준에 맞지 않거나 HUD가 더 작은 논리 해상도를 요구할 때만 640×360 전환안을 다시 제출한다.
  283: 
  284: - Battle Scene.
  285: - 별도 Ally Unit / Enemy Unit Scene.
  286: - 룰렛·건물·거점·성문·웨이브 Scene.
  287: - 메뉴·결과·설정 Scene.
  288: 
  289: 이 Scene들은 Phase 0 기준선이 검증된 뒤 Issue #32에서 실제 경로와 책임을 제안한다.
  330: 
  331: ```text
  332: master_seed
  333: ├─ roulette stream
  334: ├─ wave stream
  335: ├─ combat stream
  336: └─ presentation stream
  338: 
  339: - 전역 `rand*` 호출을 게임 규칙에서 사용하지 않는다.
  340: - stream 이름과 master seed에서 안정적인 정수 seed를 생성한다.
  341: - stream 분리는 룰렛의 호출 수 변화가 웨이브·전투 결과를 흔들지 않게 한다.
  342: - 프레젠테이션 오프셋은 별도 stream을 사용하고 전투 규칙에 영향을 주지 않는다.
  343: 
  344: 입력 로그 최소 항목:
  354:   payload_ids
  355: ```
  356: 
  357: - Phase 0에서는 배치·건설·룰렛의 실제 명령을 구현하지 않는다.
  358: - `bootstrap_toggle_planning`과 같은 테스트 명령으로 기록→재생→동일 tick 결과를 검증한다.
  359: - 저장 위치는 테스트에서는 메모리, 디버그 출력이 필요한 경우 `user://replays/`를 사용한다.
  360: - 실제 캠페인 저장 스키마는 Phase 0 범위 밖이다.
  824: 
  825: - 실제 전투와 피해 계산 실행.
  826: - 유닛 이동·타기팅·공격 AI.
  827: - 룰렛·건설·생산·경제·점령·성문·웨이브 구현.
  828: - 3라인 전장 Scene과 Camera2D 조작.
  829: - 암살자 우회 동작.
  830: - 실제 Unit Scene과 UnitInstance 전투 상태 머신.
  924: - planning 상태에서 active tick 정지.
  925: - planning 해제 뒤 같은 순서로 진행.
  926: - 같은 seed와 log 결과 동일.
  927: - roulette stream 추가 호출이 wave stream 결과에 영향 없음.
  928: 
  929: #### Visual contract
  930: 
```

### `docs/design/proposals/0002-prototype-campaign-and-stage-generator.md` — HISTORICAL / 341 lines

Hits: `룰렛` [28], `slot` [173]

```text
   25: 
   26: | 스테이지 | 목표 시간 | 학습·검증 역할 | 생성 복잡도 제한 |
   27: |---|---:|---|---|
   28: | 1. 개전 | 20~25분 | 건설, 토큰, 첫 룰렛, 한 라인 배치 | 한 라인 중심, 기본 검사 웨이브 |
   29: | 2. 상성 | 25~30분 | 검사·기병·창병·궁병 상성 | 최대 두 라인 동시 압박 |
   30: | 3. 삼중 전선 | 25~30분 | 세 라인과 접전지 경제 | 세 라인 활성, 특수 웨이브 없음 |
   31: | 4. 전방 거점 | 30~35분 | 전방 시설 위험과 적 건물 파괴 | 시설 공격 웨이브 허용 |
  170: required_enemy_buildings
  171: forbidden_combinations
  172: telegraph_type
  173: unit_slots
  174: lane_rules
  175: ```
  176: 
```

### `docs/design/proposals/0003-constraint-system.md` — HISTORICAL / 250 lines

Hits: `룰렛` [98, 102, 106], `럭키` [103, 104], `이동권` [104]

```text
   95: | 봉쇄된 후방 | 3 | 후방 안전 노드 1개 사용 불가 |
   96: | 제한 방어 | 2 | 라인당 포탑 건설 수 제한 |
   97: 
   98: ### 5.3 룰렛 제약
   99: 
  100: | 이름 | 점수 | 효과 |
  101: |---|---:|---|
  102: | 비싼 회전 | 1 | 룰렛 비용 증가 |
  103: | 불운한 흐름 | 2 | 럭키 찬스 실패 누적 증가량 감소 |
  104: | 흐린 예감 | 2 | 럭키 찬스 이동권 상점 등장 빈도 감소 |
  105: 
  106: 룰렛 결과를 조작하거나 플레이어가 알 수 없는 확률 보정은 사용하지 않는다.
  107: 
  108: ### 5.4 전장·접전지 제약
  109: 
```

### `docs/design/proposals/0004-roulette-resolution-bench-and-lucky-chance.md` — HISTORICAL / 345 lines

Hits: `룰렛` [1, 10, 36, 166, 198, 203, 211, 303, 345], `럭키` [1, 19, 31, 32, 33, 86, 218, 241, 256, 258, 275, 291, 309, 328], `roulette` [5], `이동권` [19, 33, 237, 257, 258, 259, 261, 276, 277, 279, 292, 293, 313, 328, 329, 330, 339], `판정 줄` [24, 25, 26, 63, 65, 68, 69, 84, 101, 114, 128, 145, 155, 175, 286, 294, 305, 336], `spin` [41], `lucky` [43], `결과 보관` [58, 205, 209, 211, 297, 316], `중앙 가로줄` [65, 149, 159, 320]

```text
    1: # 제안서 0004 — 룰렛 결과 판정, 대기칸 부족과 럭키 찬스
    2: 
    3: - 상태: **핵심 규칙 사용자 승인 / 세부 안전장치와 수치 검토 중**
    4: - 구현 상태: 별도 Codex Plan Mode 승인 전 구현 금지
    5: - 책임 원본: `docs/design/APPROVED_ROULETTE_CORE_RULES.md`
    6: - 관련 문서: `docs/GAME_DESIGN.md`, `docs/DECISIONS_PENDING.md`, `docs/design/DESIGN_FREEZE_CHECKLIST.md`
    7: 
    8: ## 1. 목적
    9: 
   10: 룰렛 회전 시작부터 보상 정리까지의 상태 전이와 예외 처리를 고정한다.
   11: 
   12: 이번 사용자 조정으로 기존 제안의 다음 부분은 폐기한다.
   13: 
   16: - 혼합 심벌 종류별 동시 보상
   17: - 같은 심벌 수에 따른 1성·2성·3성 보상
   18: - 금화 최대 환급률 100% 미만 계약
   19: - 자연 럭키 찬스와 이동권 중 하나만 사용
   20: - 회전당 이동 1회 제한
   21: 
   22: ## 2. 승인된 핵심 결정
   23: 
   24: 1. 기본 판정 줄은 **가운데 가로줄**로 고정한다.
   25: 2. 판정 줄 변경은 아이템 효과로만 가능하다.
   26: 3. 판정 줄의 세 심벌이 완전히 같아야 보상이 발생한다.
   27: 4. 판정 심벌의 전체 완성 줄 수가 유닛 등급 또는 금화 배율을 결정한다.
   28: 5. 유닛 9칸 전체 동일은 전설이며 한 판당 한 번만 소환한다.
   29: 6. 두 번째 이후 9칸 동일 유닛 결과는 같은 병종 영웅 2명으로 변환한다.
   30: 7. 금화는 1줄 75%, 2줄 200%, 3줄 이상 500%를 지급한다.
   31: 8. 럭키 찬스는 12% 시작, 실패당 +8%p, 6회 실패 뒤 다음 회전 확정 구조를 유지한다.
   32: 9. `행운` 아이템이 럭키 찬스 초기 확률을 높인다.
   33: 10. 자연 럭키 찬스와 이동권을 같은 회전에 함께 사용할 수 있다.
   34: 11. 회전당 이동 횟수 상한을 두지 않는다.
   35: 
   36: ## 3. 룰렛 상태 전이
   37: 
   38: ```text
   39: Idle
   40: → CostPaidAndSnapshotLocked
   41: → Spinning
   42: → BoardRevealed
   43: → NaturalLuckyRoll
   44: → ManipulationPhase 또는 ResultLock
   45: → ResultLock
   46: → LineResolution
   55: 
   56: - 현재 릴에 실제 토큰이 1개 이상 존재한다.
   57: - 회전 비용을 지불할 수 있다.
   58: - 이전 회전의 결과 보관함이 비어 있다.
   59: - 다른 회전이나 보드 조작이 진행 중이지 않다.
   60: 
   61: 실제 토큰이 0개라 각 릴이 `[X, X, X]`인 상태에서는 회전 버튼을 비활성화한다.
   62: 
   63: ### 3.2 판정 줄 고정
   64: 
   65: - 기본 판정 줄 인덱스는 중앙 가로줄이다.
   66: - 아이템 효과가 없다면 플레이어에게 줄 선택 UI를 표시하지 않는다.
   67: - 줄 변경 아이템이 적용된 경우에만 상단·하단 또는 선택 가능 상태를 제공한다.
   68: - 비용을 지불한 순간 현재 판정 줄과 아이템 효과를 스냅샷으로 고정한다.
   69: - 회전 도중 아이템 장착·해제로 판정 줄을 바꾸지 못한다.
   70: 
   71: 정확한 줄 변경 아이템의 소비 여부와 지속시간은 아이템 설계에서 확정한다.
   72: 
   81: - 부족분 X 토큰
   82: - 실제 지불한 회전 비용
   83: - 난수 시드와 회전 번호
   84: - 현재 판정 줄
   85: - 줄 변경 아이템 효과
   86: - 행운 아이템으로 보정된 럭키 찬스 기본 확률
   87: 
   88: 회전 도중 건물이 완공·파괴·철거돼도 현재 판에는 영향을 주지 않는다.
   89: 
   98: 
   99: ## 5. 기본 보상 조건
  100: 
  101: 판정 줄의 세 칸이 동일한 비-X 심벌일 때만 보상 판정을 시작한다.
  102: 
  103: ```text
  104: [검사, 검사, 검사] → 검사 보상 판정
  111: - 같은 심벌 1개 또는 2개는 보상을 주지 않는다.
  112: - 혼합 심벌을 각각 보상하지 않는다.
  113: - 세 칸이 같으면 해당 심벌을 `판정 심벌`로 고정한다.
  114: - 판정 줄이 완성되지 않았다면 보드 다른 곳의 완성 줄은 무시한다.
  115: 
  116: ## 6. 완성 줄 계산
  117: 
  125: ```
  126: 
  127: - 판정 심벌과 같은 심벌 3개로 완성된 줄만 센다.
  128: - 판정 줄도 1줄로 센다.
  129: - 교차하거나 겹치는 줄은 각각 센다.
  130: - 다른 심벌의 완성 줄은 무시한다.
  131: 
  142: 
  143: ```text
  144: 검사 / 기병 / 궁병
  145: 검사 / 검사 / 검사  ← 기본 판정 줄
  146: 검사 / 금화 / 마법사
  147: ```
  148: 
  149: 중앙 가로줄과 왼쪽 세로줄이 완성돼 **엘리트 검사 1명**을 지급한다.
  150: 
  151: ### 7.2 3줄 예시
  152: 
  153: ```text
  154: 검사 / 검사 / 기병
  155: 검사 / 검사 / 검사  ← 기본 판정 줄
  156: 검사 / 검사 / 궁병
  157: ```
  158: 
  159: 중앙 가로줄, 왼쪽 세로줄, 가운데 세로줄이 완성돼 **영웅 검사 1명**을 지급한다.
  160: 
  161: ### 7.3 전설
  162: 
  163: - 유닛 심벌이 9칸 전체를 채우면 8개 줄이 모두 완성된다.
  164: - 한 스테이지/한 판에서 전설은 병종과 관계없이 한 번만 소환할 수 있다.
  165: - 이미 전설을 소환했다면 이후 9칸 동일 유닛 결과는 해당 병종 영웅 2명으로 바꾼다.
  166: - 전설 사용 여부는 룰렛 패널과 결과 화면에 표시한다.
  167: - 전설의 정확한 능력치와 고유 스킬은 후속 유닛 설계에서 결정한다.
  168: 
  169: ### 7.4 합성
  172: 
  173: ## 8. 금화 보상
  174: 
  175: 판정 줄이 `[금화, 금화, 금화]`일 때 전체 보드의 금화 완성 줄 수를 센다.
  176: 
  177: | 금화 완성 줄 수 | 환급 비율 |
  178: |---:|---:|
  195: 
  196: 사용자 배율을 유지하면서 구현하려면 다음 중 하나를 구현 전에 확정해야 한다.
  197: 
  198: 1. 룰렛 회전에 금화와 별개의 시간 충전 자원을 요구한다.
  199: 2. 금화 고배율 보상에 재사용 대기시간 또는 웨이브당 지급 횟수 제한을 둔다.
  200: 3. 금화 보상 기준액과 실제 회전 비용을 분리하고 연속 회전 비용을 상승시킨다.
  201: 4. 동일 금화 토큰의 연속 지급에 감쇠를 적용한다.
  202: 
  203: 현재 추천은 **시간 기반 회전 충전 1개 + 금화 비용** 방식이다. 금화가 많아도 충전 속도보다 빠르게 무한 회전할 수 없고, 룰렛 템포도 제어할 수 있다. 최종 선택은 미확정이다.
  204: 
  205: ## 9. 결과 보관함과 대기칸
  206: 
  207: 혼합 보상을 제거했으므로 한 회전당 유닛 보상 묶음은 최대 1개다.
  208: 
  209: - 유닛 보상은 공간 부족으로 사라지지 않고 결과 보관함에 생성한다.
  210: - 금화 보상은 보관함을 사용하지 않고 즉시 지급한다.
  211: - 결과 보관함이 남아 있으면 다음 룰렛 회전만 차단한다.
  212: - 전투, 건설, 카메라, 기존 유닛 배치는 계속 진행한다.
  213: - 보상은 대기칸 이동, 즉시 출전 또는 판매로 정리한다.
  214: - 판매는 항상 가능하게 해 소프트락을 막는다.
  215: 
  216: 대기칸 8칸 기본안은 아직 추천값이며 사용자 최종 승인을 받지 않았다.
  217: 
  218: ## 10. 럭키 찬스
  219: 
  220: ### 10.1 확률
  221: 
  234: 
  235: - 현재 확률과 확정까지 남은 실패 횟수를 UI에 공개한다.
  236: - 자연 발생을 사용하지 않고 넘겨도 성공으로 보고 누적을 초기화한다.
  237: - 이동권 사용 여부는 자연 발생 판정 결과를 바꾸지 않는다.
  238: 
  239: ### 10.2 행운 아이템
  240: 
  241: `행운` 아이템은 럭키 찬스의 초기 기본 확률을 올린다.
  242: 
  243: ```text
  244: 적용 기본 확률 = 12% + 행운 아이템 보너스 합계
  253: 
  254: ### 11.1 이동 자원
  255: 
  256: - 자연 럭키 찬스 성공은 무료 이동 크레딧 1회를 제공한다.
  257: - 이동권 1장은 추가 이동 1회를 제공한다.
  258: - 이동권은 자연 럭키 찬스 발생 여부와 관계없이 사용할 수 있다.
  259: - 무료 이동과 이동권을 같은 회전에 함께 사용할 수 있다.
  260: - 회전당 이동 횟수 제한은 없다.
  261: - 실제 이동 가능 횟수는 무료 크레딧과 보유 이동권 수로만 제한된다.
  262: 
  263: ### 11.2 허용 이동
  264: 
  272: ### 11.3 계획·미리보기·소비
  273: 
  274: - 이동을 적용할 때마다 현재 보드, 완성 줄 수와 예상 보상을 갱신한다.
  275: - 자연 럭키 찬스가 있다면 첫 이동은 무료 크레딧을 우선 사용한다.
  276: - 추가 이동마다 이동권 1장을 예약한다.
  277: - 이동권은 최종 판을 확정할 때만 실제 소비한다.
  278: - 확정 전 `한 단계 되돌리기`와 `원본으로 초기화`를 제공한다.
  279: - 되돌린 이동에 예약된 이동권은 소비하지 않는다.
  280: - 최종 확정 뒤에는 되돌릴 수 없고 보상을 한 번만 계산한다.
  281: - 별도 결정 시간 제한은 두지 않는다. 전투는 계속되며 일시정지를 사용할 수 있다.
  282: 
  283: ## 12. 전체 회전 예시
  284: 
  285: ```text
  286: 1. 기본 중앙 판정 줄과 적용 아이템 스냅샷
  287: 2. 회전 비용 지불
  288: 3. 완공 건물과 토큰 풀 스냅샷
  289: 4. 세 릴 독립 셔플·3개 추출
  290: 5. 3×3 판 공개
  291: 6. 자연 럭키 찬스 판정
  292: 7. 무료 이동과 이동권으로 0회 이상 행·열 순환
  293: 8. 최종 판 확정 및 이동권 소비
  294: 9. 판정 줄 3개 동일 여부 확인
  295: 10. 동일하면 판정 심벌의 전체 완성 줄 계산
  296: 11. 유닛 등급 또는 금화 배율 결정
  297: 12. 금화 즉시 지급 또는 유닛 결과 보관함 생성
  298: 13. 보관함 정리 후 다음 회전 가능
  299: ```
  300: 
  301: ## 13. UI 필수 정보
  302: 
  303: 룰렛 패널:
  304: 
  305: - 현재 판정 줄과 줄 변경 아이템 효과
  306: - 현재 회전 비용
  307: - 현재 토큰 수와 칸당 확률
  308: - X 토큰 수
  309: - 자연 럭키 찬스 현재 확률
  310: - 확정까지 남은 실패 횟수
  311: - 행운 아이템 총 보너스
  312: - 무료 이동 크레딧 수
  313: - 보유·예약 이동권 수
  314: - 현재 완성 줄 수와 예상 보상
  315: - 전설 소환 가능 여부
  316: - 결과 보관함 상태와 회전 불가 이유
  317: 
  318: ## 14. 검증 시나리오
  319: 
  320: - 아이템이 없으면 중앙 가로줄만 판정한다.
  321: - 중앙줄 `[검사, 검사, 기병]`은 보상이 없다.
  322: - 중앙줄 `[검사, 검사, 검사]`와 다른 검사 완성 줄이 없으면 1성 병사 검사를 지급한다.
  323: - 승인된 2줄 예시는 엘리트 검사를 지급한다.
  325: - 첫 9칸 동일 유닛 결과는 전설을 지급한다.
  326: - 같은 판에서 전설을 이미 소환한 뒤 9칸 동일 결과는 영웅 2명을 지급한다.
  327: - 금화 1줄·2줄·3줄 이상은 75%·200%·500%를 지급한다.
  328: - 자연 럭키 찬스와 여러 이동권을 같은 회전에 사용할 수 있다.
  329: - 이동권은 최종 확정 전에는 소비되지 않는다.
  330: - 여러 이동을 되돌린 뒤 예상 보상과 예약 이동권 수가 정확히 복구된다.
  331: - 최종 판 보상은 정확히 한 번만 지급된다.
  332: - 대기칸이 가득 차도 유닛 보상이 사라지지 않는다.
  333: 
  334: ## 15. 남은 사용자 결정
  335: 
  336: 1. 판정 줄 변경 아이템의 형태, 지속시간과 소비 여부
  337: 2. 대기칸 초기 용량과 수동 합성 제외 여부
  338: 3. 행운 아이템 1개당 확률 증가량과 중첩 상한
  339: 4. 이동권 보유 상한과 상점 가격
  340: 5. 전설 유닛의 전투력과 영웅 대비 관계
  341: 6. 200%·500% 금화 배율을 유지하기 위한 무한 회전 안전장치
  342: 
  343: ## 16. 승인 게이트
  344: 
  345: 핵심 룰렛 판정과 이동 구조는 승인됐다. 위 남은 항목과 금화 안전장치를 확정한 뒤 `GAME_DESIGN.md`, 데이터 계약과 Codex Plan Mode 구현 제안서에 최종 프리즈한다.
```

<details><summary>Full content</summary>

```text
# 제안서 0004 — 룰렛 결과 판정, 대기칸 부족과 럭키 찬스

- 상태: **핵심 규칙 사용자 승인 / 세부 안전장치와 수치 검토 중**
- 구현 상태: 별도 Codex Plan Mode 승인 전 구현 금지
- 책임 원본: `docs/design/APPROVED_ROULETTE_CORE_RULES.md`
- 관련 문서: `docs/GAME_DESIGN.md`, `docs/DECISIONS_PENDING.md`, `docs/design/DESIGN_FREEZE_CHECKLIST.md`

## 1. 목적

룰렛 회전 시작부터 보상 정리까지의 상태 전이와 예외 처리를 고정한다.

이번 사용자 조정으로 기존 제안의 다음 부분은 폐기한다.

- 회전 전 상·중·하 기준 줄 자유 선택
- 같은 심벌 1개 또는 2개 보상
- 혼합 심벌 종류별 동시 보상
- 같은 심벌 수에 따른 1성·2성·3성 보상
- 금화 최대 환급률 100% 미만 계약
- 자연 럭키 찬스와 이동권 중 하나만 사용
- 회전당 이동 1회 제한

## 2. 승인된 핵심 결정

1. 기본 판정 줄은 **가운데 가로줄**로 고정한다.
2. 판정 줄 변경은 아이템 효과로만 가능하다.
3. 판정 줄의 세 심벌이 완전히 같아야 보상이 발생한다.
4. 판정 심벌의 전체 완성 줄 수가 유닛 등급 또는 금화 배율을 결정한다.
5. 유닛 9칸 전체 동일은 전설이며 한 판당 한 번만 소환한다.
6. 두 번째 이후 9칸 동일 유닛 결과는 같은 병종 영웅 2명으로 변환한다.
7. 금화는 1줄 75%, 2줄 200%, 3줄 이상 500%를 지급한다.
8. 럭키 찬스는 12% 시작, 실패당 +8%p, 6회 실패 뒤 다음 회전 확정 구조를 유지한다.
9. `행운` 아이템이 럭키 찬스 초기 확률을 높인다.
10. 자연 럭키 찬스와 이동권을 같은 회전에 함께 사용할 수 있다.
11. 회전당 이동 횟수 상한을 두지 않는다.

## 3. 룰렛 상태 전이

```text
Idle
→ CostPaidAndSnapshotLocked
→ Spinning
→ BoardRevealed
→ NaturalLuckyRoll
→ ManipulationPhase 또는 ResultLock
→ ResultLock
→ LineResolution
→ RewardResolution
→ RewardsPending 또는 Idle
→ Idle
```

### 3.1 회전 시작 조건

다음을 모두 만족해야 한다.

- 현재 릴에 실제 토큰이 1개 이상 존재한다.
- 회전 비용을 지불할 수 있다.
- 이전 회전의 결과 보관함이 비어 있다.
- 다른 회전이나 보드 조작이 진행 중이지 않다.

실제 토큰이 0개라 각 릴이 `[X, X, X]`인 상태에서는 회전 버튼을 비활성화한다.

### 3.2 판정 줄 고정

- 기본 판정 줄 인덱스는 중앙 가로줄이다.
- 아이템 효과가 없다면 플레이어에게 줄 선택 UI를 표시하지 않는다.
- 줄 변경 아이템이 적용된 경우에만 상단·하단 또는 선택 가능 상태를 제공한다.
- 비용을 지불한 순간 현재 판정 줄과 아이템 효과를 스냅샷으로 고정한다.
- 회전 도중 아이템 장착·해제로 판정 줄을 바꾸지 못한다.

정확한 줄 변경 아이템의 소비 여부와 지속시간은 아이템 설계에서 확정한다.

## 4. 판 생성 규칙

### 4.1 회전 스냅샷

비용 지불 순간 다음을 고정한다.

- 완공된 토큰 제공 건물 목록
- 릴별 실제 토큰 구성
- 부족분 X 토큰
- 실제 지불한 회전 비용
- 난수 시드와 회전 번호
- 현재 판정 줄
- 줄 변경 아이템 효과
- 행운 아이템으로 보정된 럭키 찬스 기본 확률

회전 도중 건물이 완공·파괴·철거돼도 현재 판에는 영향을 주지 않는다.

### 4.2 릴별 추출

각 세로 릴은 같은 구성의 독립 풀을 사용한다.

1. 완공 건물 기준 실제 토큰을 구성한다.
2. 실제 토큰이 3개 미만이면 X를 채운다.
3. 릴별로 독립 셔플한다.
4. 셔플된 풀에서 앞의 3개를 비복원 추출해 위·중간·아래 칸에 표시한다.

## 5. 기본 보상 조건

판정 줄의 세 칸이 동일한 비-X 심벌일 때만 보상 판정을 시작한다.

```text
[검사, 검사, 검사] → 검사 보상 판정
[검사, 검사, 기병] → 보상 없음
[검사, 기병, 궁병] → 보상 없음
[검사, 검사, X] → 보상 없음
[X, X, X] → 보상 없음
```

- 같은 심벌 1개 또는 2개는 보상을 주지 않는다.
- 혼합 심벌을 각각 보상하지 않는다.
- 세 칸이 같으면 해당 심벌을 `판정 심벌`로 고정한다.
- 판정 줄이 완성되지 않았다면 보드 다른 곳의 완성 줄은 무시한다.

## 6. 완성 줄 계산

전체 8개 줄을 검사한다.

```text
가로 3줄
+ 세로 3줄
+ 대각선 2줄
= 총 8줄
```

- 판정 심벌과 같은 심벌 3개로 완성된 줄만 센다.
- 판정 줄도 1줄로 센다.
- 교차하거나 겹치는 줄은 각각 센다.
- 다른 심벌의 완성 줄은 무시한다.

## 7. 유닛 보상

| 완성 줄 수 | 최종 보상 |
|---:|---|
| 1줄 | 해당 병종 1성 병사 1명 |
| 2줄 | 해당 병종 엘리트 1명 |
| 3~7줄 | 해당 병종 영웅 1명 |
| 8줄 | 해당 병종 전설 1명 |

### 7.1 2줄 예시

```text
검사 / 기병 / 궁병
검사 / 검사 / 검사  ← 기본 판정 줄
검사 / 금화 / 마법사
```

중앙 가로줄과 왼쪽 세로줄이 완성돼 **엘리트 검사 1명**을 지급한다.

### 7.2 3줄 예시

```text
검사 / 검사 / 기병
검사 / 검사 / 검사  ← 기본 판정 줄
검사 / 검사 / 궁병
```

중앙 가로줄, 왼쪽 세로줄, 가운데 세로줄이 완성돼 **영웅 검사 1명**을 지급한다.

### 7.3 전설

- 유닛 심벌이 9칸 전체를 채우면 8개 줄이 모두 완성된다.
- 한 스테이지/한 판에서 전설은 병종과 관계없이 한 번만 소환할 수 있다.
- 이미 전설을 소환했다면 이후 9칸 동일 유닛 결과는 해당 병종 영웅 2명으로 바꾼다.
- 전설 사용 여부는 룰렛 패널과 결과 화면에 표시한다.
- 전설의 정확한 능력치와 고유 스킬은 후속 유닛 설계에서 결정한다.

### 7.4 합성

수동·자동 유닛 합성은 아직 승인되지 않았다. 첫 프로토타입 제외를 추천하되 별도 결정으로 유지한다.

## 8. 금화 보상

판정 줄이 `[금화, 금화, 금화]`일 때 전체 보드의 금화 완성 줄 수를 센다.

| 금화 완성 줄 수 | 환급 비율 |
|---:|---:|
| 1줄 | 실제 지불 비용의 75% |
| 2줄 | 실제 지불 비용의 200% |
| 3줄 이상 | 실제 지불 비용의 500% |

```text
금화 보상 = floor(실제 지불한 회전 비용 × 환급 비율)
```

- 할인 후 실제 지불액을 기준으로 한다.
- 금화는 즉시 지급한다.
- 9칸 전체 금화도 3줄 이상 구간으로 처리해 500% 상한을 적용한다.
- 금화에는 유닛 등급을 적용하지 않는다.

### 8.1 필수 안전장치 검토

이 배율을 그대로 적용하면 시장 3개만 존재하는 구성에서 9칸 금화가 결정론적으로 발생할 수 있고, 매 회전 500% 환급으로 즉시 무한 경제가 가능하다.

사용자 배율을 유지하면서 구현하려면 다음 중 하나를 구현 전에 확정해야 한다.

1. 룰렛 회전에 금화와 별개의 시간 충전 자원을 요구한다.
2. 금화 고배율 보상에 재사용 대기시간 또는 웨이브당 지급 횟수 제한을 둔다.
3. 금화 보상 기준액과 실제 회전 비용을 분리하고 연속 회전 비용을 상승시킨다.
4. 동일 금화 토큰의 연속 지급에 감쇠를 적용한다.

현재 추천은 **시간 기반 회전 충전 1개 + 금화 비용** 방식이다. 금화가 많아도 충전 속도보다 빠르게 무한 회전할 수 없고, 룰렛 템포도 제어할 수 있다. 최종 선택은 미확정이다.

## 9. 결과 보관함과 대기칸

혼합 보상을 제거했으므로 한 회전당 유닛 보상 묶음은 최대 1개다.

- 유닛 보상은 공간 부족으로 사라지지 않고 결과 보관함에 생성한다.
- 금화 보상은 보관함을 사용하지 않고 즉시 지급한다.
- 결과 보관함이 남아 있으면 다음 룰렛 회전만 차단한다.
- 전투, 건설, 카메라, 기존 유닛 배치는 계속 진행한다.
- 보상은 대기칸 이동, 즉시 출전 또는 판매로 정리한다.
- 판매는 항상 가능하게 해 소프트락을 막는다.

대기칸 8칸 기본안은 아직 추천값이며 사용자 최종 승인을 받지 않았다.

## 10. 럭키 찬스

### 10.1 확률

```text
기본 확률: 12%
자연 실패 1회당: +8%p
6회 연속 자연 실패 후 다음 회전: 100%
자연 발생 시: 현재 기본 확률로 초기화
```

예시:

```text
12% → 20% → 28% → 36% → 44% → 52% → 100%
```

- 현재 확률과 확정까지 남은 실패 횟수를 UI에 공개한다.
- 자연 발생을 사용하지 않고 넘겨도 성공으로 보고 누적을 초기화한다.
- 이동권 사용 여부는 자연 발생 판정 결과를 바꾸지 않는다.

### 10.2 행운 아이템

`행운` 아이템은 럭키 찬스의 초기 기본 확률을 올린다.

```text
적용 기본 확률 = 12% + 행운 아이템 보너스 합계
현재 자연 확률 = 적용 기본 확률 + 실패 횟수 × 8%p
```

- 자연 발생 후에는 12%가 아니라 아이템 보정이 적용된 기본 확률로 돌아간다.
- 6회 실패 뒤 다음 회전 확정 구조는 유지한다.
- 아이템당 증가량, 중첩 상한과 희귀도는 미확정이다.

## 11. 다중 행·열 이동

### 11.1 이동 자원

- 자연 럭키 찬스 성공은 무료 이동 크레딧 1회를 제공한다.
- 이동권 1장은 추가 이동 1회를 제공한다.
- 이동권은 자연 럭키 찬스 발생 여부와 관계없이 사용할 수 있다.
- 무료 이동과 이동권을 같은 회전에 함께 사용할 수 있다.
- 회전당 이동 횟수 제한은 없다.
- 실제 이동 가능 횟수는 무료 크레딧과 보유 이동권 수로만 제한된다.

### 11.2 허용 이동

- 가로줄 하나를 왼쪽으로 1칸 순환
- 가로줄 하나를 오른쪽으로 1칸 순환
- 세로줄 하나를 위로 1칸 순환
- 세로줄 하나를 아래로 1칸 순환

여러 행·열 이동을 순서대로 조합할 수 있다.

### 11.3 계획·미리보기·소비

- 이동을 적용할 때마다 현재 보드, 완성 줄 수와 예상 보상을 갱신한다.
- 자연 럭키 찬스가 있다면 첫 이동은 무료 크레딧을 우선 사용한다.
- 추가 이동마다 이동권 1장을 예약한다.
- 이동권은 최종 판을 확정할 때만 실제 소비한다.
- 확정 전 `한 단계 되돌리기`와 `원본으로 초기화`를 제공한다.
- 되돌린 이동에 예약된 이동권은 소비하지 않는다.
- 최종 확정 뒤에는 되돌릴 수 없고 보상을 한 번만 계산한다.
- 별도 결정 시간 제한은 두지 않는다. 전투는 계속되며 일시정지를 사용할 수 있다.

## 12. 전체 회전 예시

```text
1. 기본 중앙 판정 줄과 적용 아이템 스냅샷
2. 회전 비용 지불
3. 완공 건물과 토큰 풀 스냅샷
4. 세 릴 독립 셔플·3개 추출
5. 3×3 판 공개
6. 자연 럭키 찬스 판정
7. 무료 이동과 이동권으로 0회 이상 행·열 순환
8. 최종 판 확정 및 이동권 소비
9. 판정 줄 3개 동일 여부 확인
10. 동일하면 판정 심벌의 전체 완성 줄 계산
11. 유닛 등급 또는 금화 배율 결정
12. 금화 즉시 지급 또는 유닛 결과 보관함 생성
13. 보관함 정리 후 다음 회전 가능
```

## 13. UI 필수 정보

룰렛 패널:

- 현재 판정 줄과 줄 변경 아이템 효과
- 현재 회전 비용
- 현재 토큰 수와 칸당 확률
- X 토큰 수
- 자연 럭키 찬스 현재 확률
- 확정까지 남은 실패 횟수
- 행운 아이템 총 보너스
- 무료 이동 크레딧 수
- 보유·예약 이동권 수
- 현재 완성 줄 수와 예상 보상
- 전설 소환 가능 여부
- 결과 보관함 상태와 회전 불가 이유

## 14. 검증 시나리오

- 아이템이 없으면 중앙 가로줄만 판정한다.
- 중앙줄 `[검사, 검사, 기병]`은 보상이 없다.
- 중앙줄 `[검사, 검사, 검사]`와 다른 검사 완성 줄이 없으면 1성 병사 검사를 지급한다.
- 승인된 2줄 예시는 엘리트 검사를 지급한다.
- 승인된 3줄 예시는 영웅 검사를 지급한다.
- 첫 9칸 동일 유닛 결과는 전설을 지급한다.
- 같은 판에서 전설을 이미 소환한 뒤 9칸 동일 결과는 영웅 2명을 지급한다.
- 금화 1줄·2줄·3줄 이상은 75%·200%·500%를 지급한다.
- 자연 럭키 찬스와 여러 이동권을 같은 회전에 사용할 수 있다.
- 이동권은 최종 확정 전에는 소비되지 않는다.
- 여러 이동을 되돌린 뒤 예상 보상과 예약 이동권 수가 정확히 복구된다.
- 최종 판 보상은 정확히 한 번만 지급된다.
- 대기칸이 가득 차도 유닛 보상이 사라지지 않는다.

## 15. 남은 사용자 결정

1. 판정 줄 변경 아이템의 형태, 지속시간과 소비 여부
2. 대기칸 초기 용량과 수동 합성 제외 여부
3. 행운 아이템 1개당 확률 증가량과 중첩 상한
4. 이동권 보유 상한과 상점 가격
5. 전설 유닛의 전투력과 영웅 대비 관계
6. 200%·500% 금화 배율을 유지하기 위한 무한 회전 안전장치

## 16. 승인 게이트

핵심 룰렛 판정과 이동 구조는 승인됐다. 위 남은 항목과 금화 안전장치를 확정한 뒤 `GAME_DESIGN.md`, 데이터 계약과 Codex Plan Mode 구현 제안서에 최종 프리즈한다.

```

</details>

### `docs/design/proposals/0005-building-tiers-lifecycle-and-economy.md` — HISTORICAL / 316 lines

Hits: `reel` [141], `룰렛` [178, 196, 245]

```text
  138: `토큰 증폭` 전문화는 다음 효과를 추가한다.
  139: 
  140: ```text
  141: 기본: Reel 1 +1 / Reel 2 +1 / Reel 3 +1
  142: 전문화: 위 기본 구성 + 랜덤으로 정해진 릴 하나에 +1
  143: ```
  144: 
  175: ### 9.1 Tier 1 공통 병영
  176: 
  177: - 생산 병종: 검사
  178: - 룰렛 토큰: 검사
  179: - 역할: 초반 전열과 공통 시작점
  180: 
  181: ### 9.2 Tier 2 병종 전환
  193: - 다음 생산부터 선택 병종을 생산한다.
  194: - 기존 검사 토큰 3개를 선택 병종 토큰 3개로 교체한다.
  195: - 이미 생산된 검사는 그대로 유지한다.
  196: - 현재 룰렛 스냅샷은 유지하고 다음 회전부터 반영한다.
  197: - 생산 진행률은 보존하고 업그레이드 완료 뒤 처음 완성되는 유닛부터 새 병종을 적용한다.
  198: 
  199: ### 9.3 Tier 3 교리 추천
  242: - 금화 토큰 증폭
  243: - 전술 명령 또는 상점 소비 효율 강화
  244: 
  245: 시장 전문화가 금화 룰렛과 용병·전술 명령을 모두 과도하게 강화하지 않도록 한 건물에서 모든 경제 효과를 동시에 얻지 못하게 한다.
  246: 
  247: ## 12. 생산 완료와 Ready 슬롯
  248: 
```

### `docs/design/proposals/0006-tactical-commands.md` — HISTORICAL / 262 lines

Hits: `룰렛` [31, 74, 168]

```text
   28: - 해금된 기본 명령은 HUD에 항상 표시한다.
   29: - 기본 명령은 상점 무작위 1회권으로 구매하지 않는다.
   30: - 발동마다 금화를 직접 소비한다.
   31: - 식량·건설 노드·룰렛 토큰을 사용하지 않는다.
   32: - 전역 쿨다운을 사용하지 않는다.
   33: - 일시정지 예약 상태와 예약 금액을 사용하지 않는다.
   34: - 전술 명령은 게임이 진행 중일 때만 실행 확정할 수 있다.
   71: - 아군은 통과하고 적만 차단한다.
   72: - 라인당 동시에 하나만 활성화한다.
   73: - 적의 공격 대상이며 체력 0 또는 지속시간 종료 시 사라진다.
   74: - 공격 기능, 식량, 건설 노드와 룰렛 토큰이 없다.
   75: - 보스·지휘관형 적은 예고된 길 뚫기 패턴으로 대응할 수 있다.
   76: 
   77: 초기 수치 가설:
  165: - 금화 부족 시 버튼 비활성화와 부족량 표시
  166: - 시장·아이템 할인은 별도 상한 적용 가능
  167: 
  168: 금화가 많은 플레이어가 여러 명령을 연속 사용해 위기를 넘기는 것은 의도된 시장 빌드 보상이다. 그 대가로 건설·룰렛·용병에 사용할 금화가 줄어든다.
  169: 
  170: ## 10. Command Power
  171: 
```

### `docs/goals/0001-engine-selection-and-bootstrap.md` — HISTORICAL / 28 lines

Hits: `룰렛` [22]

```text
   19: 
   20: ## 제외된 범위
   21: 
   22: - 실제 전투, 이동, 타기팅, 공격, 룰렛, 건설, 점령, 성문, 웨이브.
   23: - 최종 스프라이트·애니메이션·VFX·오디오.
   24: - 적군 전용 병종 데이터와 별도 전투 Scene.
   25: 
```

### `docs/goals/0002-core-vertical-slice.md` — HISTORICAL / 275 lines

Hits: `룰렛` [15, 125, 127, 129, 209, 210, 232], `결과 보관` [128]

```text
   12: ```text
   13: 베일의 징조
   14: → 건설
   15: → 룰렛
   16: → 병력 배치
   17: → 3라인 교전
   18: → 중앙 접전지·중간거점 점령
  122: - 같은 병종 다수의 루프는 결정론적 프레임 오프셋을 사용한다.
  123: - 웨이브 정리는 짧은 무기 정리만, 최종 승리에만 2.5~4초 시퀀스.
  124: 
  125: ### 룰렛·공세
  126: 
  127: - 완공 건물 토큰이 반영되는 최소 3×3 룰렛.
  128: - 결과 보관과 라인 배치.
  129: - 룰렛 결과는 공용 archetype ID와 Rank를 생성.
  130: - 베일의 징조 뒤 공용 archetype 기반 최소 적 웨이브.
  131: - 디버그 표시: 라인, 팀, archetype, Visual Set, Unit 수, 사거리, 점령, 성문, 우회, 애니메이션 상태와 판정 이벤트.
  132: 
  206: ## 프로젝트 불변 조건
  207: 
  208: - 기본 포탑 한 기가 중간거점과 중앙 접전지 사이 전체를 단독으로 덮지 않는다.
  209: - 완공된 건물 개수가 룰렛 토큰 수를 결정한다.
  210: - 적은 플레이어 룰렛을 돌리지 않고 웨이브로 출격한다.
  211: - 상·중·하 일반 이동 그래프는 연결되지 않는다.
  212: - 중간거점은 전방 2·후방 1 노드를 유지한다.
  213: - 점령된 중간거점의 건설권·생산권은 점령 팀으로 이전된다.
  229: - 전체 양 진영 Visual Set 완성.
  230: - 암살자 탐지 건물.
  231: - 성문 수리·재건.
  232: - 최종 룰렛 확률.
  233: - 저장·불러오기, 멀티플레이.
  234: - C#, GDExtension, 외부 ECS.
  235: 
```

### `docs/images/VISUAL_REFERENCE_INDEX.md` — REFERENCE_OR_OTHER / 108 lines

Hits: `룰렛` [82]

```text
   79: | `오멘워드 유닛 도감.png` | 과거 병종 도감 | **VR-002와 동일 계열 / 형식 폐기** | 병종·등급 관계만 보존 |
   80: | `battle_map_tool.html` | 전장 설계 도구 | **MIGRATION_PENDING** | `tools/battle_map/` 또는 승인된 도구 경로에 HTML·사용법·버전 기록 |
   81: | `전장_맵_툴_사용법.txt` | 도구 사용법 | **MIGRATION_PENDING** | 맵 툴과 함께 보존 |
   82: | `룰렛바운드_게임기획서_v0.7.docx` | 레거시 GDD | **ARCHIVE_SOURCE** | 최신 GDD와 충돌하지 않게 `archive/` 또는 변경 이력 인덱스로만 보존 |
   83: | `붙여넣은 텍스트 (1).txt` | 게임 제작 방법 메모 | **BASE_CANDIDATE** | 프로젝트 사양이 아니라 Base 공용 기획 방법 자료로 정리 |
   84: 
   85: `urban-legend`의 저승역 UI·캐릭터 아트 제안서는 다른 프로젝트 자료다. 오멘워드의 파일 누락 감사 대상이나 활성 아트 기준에 포함하지 않는다. 범용 방법만 Base에서 별도로 추출할 수 있다.
```

### `docs/issues/0001.md` — HISTORICAL / 47 lines

Hits: `룰렛` [42]

```text
   39: 
   40: ## 제외 범위
   41: 
   42: - 실제 전투, 룰렛, 건설, 점령, 성문, 웨이브, 최종 자산·오디오.
   43: - 적군 전용 병종 데이터·Scene·모션 상태 머신.
   44: 
   45: ## 다음 단계
```

### `docs/issues/0002.md` — HISTORICAL / 60 lines

Hits: `roulette` [28, 42], `slot` [28], `룰렛` [42], `럭키` [49]

```text
   25: 
   26: ## 목표
   27: 
   28: Slotbound, Luck be a Landlord, Commander Quest, Mechabellum, Thronefall, Into the Breach, The King is Watching, 9 Kings의 시스템과 반응을 비교해 Roulettebound 수직 슬라이스에 적용할 요소와 제외할 요소를 확정한다.
   29: 
   30: ## 책임 문서
   31: 
   39: 
   40: - 높은 반응을 얻은 비교작은 단순한 기본 규칙, 깊은 조합, 명확한 인과관계, 제한된 건설 공간, 빠른 재시도를 공통적으로 가진다.
   41: - 반복된 위험은 장르 혼합 과부하, 통제 불가능한 RNG, 전투 결과 불명확, 경제 스노우볼, UI 과밀이다.
   42: - Roulettebound의 차별점은 `건물이 룰렛 토큰 풀을 구성하고, 예고된 적 웨이브에 3라인으로 대응한다`는 연결로 유지한다.
   43: 
   44: ## 검토할 핵심 결정
   45: 
   46: 1. 수직 슬라이스 필수 UX 6개 채택 여부
   47: 2. 건물 티어 고정 성장 또는 선택형 2갈래 분기
   48: 3. 적 웨이브 수량 정확 공개 또는 범위 공개
   49: 4. 럭키 찬스 순수 확률 또는 실패 누적 보정
   50: 5. 전방 생산시설 추가 생산 보너스 여부
   51: 
   52: ## 승인 후 작업
```

### `docs/issues/0006.md` — HISTORICAL / 34 lines

Hits: `roulette` [3, 16, 19, 28], `룰렛` [24]

```text
    1: ---
    2: issue_number: 6
    3: title: "[Archived][Roulette] 초기 공유 토큰·등급 판정 초안"
    4: state: closed
    5: github_url: "https://github.com/alsdmlals4-eng/omenward/issues/6"
    6: github_updated_at: "2026-07-15T20:07:23Z"
   13: - docs/design/APPROVED_COMMON_COMBAT_AND_RANK_BUDGET_POC_V1.md
   14: - docs/design/APPROVED_PLAYER_TEN_UNIT_LINEAGES_POC_V1.md
   15: - docs/design/APPROVED_PREPRODUCTION_POC_BASELINE_V1.md
   16: - docs/design/APPROVED_ROULETTE_PROBABILITY_TARGETS_POC_V1.md
   17: ---
   18: 
   19: # Issue #6: [Archived][Roulette] 초기 공유 토큰·등급 판정 초안
   20: 
   21: ## Issue body
   22: ## 종료 상태
   23: 
   24: 초기 전사·기병·궁병·창병 중심 공유 토큰 설명은 현재 플레이어 10병종과 룰렛 확률·계보 문서로 통합되어 이 Issue를 종료한다.
   25: 
   26: 현재 책임 원본:
   27: 
   28: - `docs/design/APPROVED_ROULETTE_PROBABILITY_TARGETS_POC_V1.md`
   29: - `docs/design/APPROVED_PLAYER_TEN_UNIT_LINEAGES_POC_V1.md`
   30: - `docs/design/APPROVED_COMMON_COMBAT_AND_RANK_BUDGET_POC_V1.md`
   31: - `docs/design/APPROVED_PREPRODUCTION_POC_BASELINE_V1.md`
```

### `docs/issues/0011.md` — HISTORICAL / 34 lines

Hits: `roulette` [16, 30], `룰렛` [24]

```text
   13: - docs/design/APPROVED_COMMON_COMBAT_AND_RANK_BUDGET_POC_V1.md
   14: - docs/design/APPROVED_PLAYER_TEN_UNIT_LINEAGES_POC_V1.md
   15: - docs/design/APPROVED_PREPRODUCTION_POC_BASELINE_V1.md
   16: - docs/design/APPROVED_ROULETTE_PROBABILITY_TARGETS_POC_V1.md
   17: ---
   18: 
   19: # Issue #11: [Archived][Units] 초기 등급·Tier 성장 초안
   21: ## Issue body
   22: ## 종료 상태
   23: 
   24: 초기 전사 후보와 등급 성장 설명은 현재 플레이어 10병종 계보·공통 전투 예산·룰렛 확률 기준으로 통합되어 이 Issue를 종료한다.
   25: 
   26: 현재 책임 원본:
   27: 
   28: - `docs/design/APPROVED_COMMON_COMBAT_AND_RANK_BUDGET_POC_V1.md`
   29: - `docs/design/APPROVED_PLAYER_TEN_UNIT_LINEAGES_POC_V1.md`
   30: - `docs/design/APPROVED_ROULETTE_PROBABILITY_TARGETS_POC_V1.md`
   31: - `docs/design/APPROVED_PREPRODUCTION_POC_BASELINE_V1.md`
   32: - Issue #30
   33: 
```

### `docs/issues/0013.md` — HISTORICAL / 34 lines

Hits: `roulette` [16, 31]

```text
   13: - docs/design/APPROVED_BARRACKS_AND_SPECIAL_CORPS_UNIT_TREE_V5.md
   14: - docs/design/APPROVED_BARRACKS_TIER2_TIER3_INTEGRATED_TREE_V2.md
   15: - docs/design/APPROVED_PLAYER_TEN_UNIT_LINEAGES_POC_V1.md
   16: - docs/design/APPROVED_ROULETTE_PROBABILITY_TARGETS_POC_V1.md
   17: ---
   18: 
   19: # Issue #13: [Archived][Barracks] 초기 전사 계열·Tier 후보 확률
   28: - `docs/design/APPROVED_BARRACKS_AND_SPECIAL_CORPS_UNIT_TREE_V5.md`
   29: - `docs/design/APPROVED_BARRACKS_TIER2_TIER3_INTEGRATED_TREE_V2.md`
   30: - `docs/design/APPROVED_PLAYER_TEN_UNIT_LINEAGES_POC_V1.md`
   31: - `docs/design/APPROVED_ROULETTE_PROBABILITY_TARGETS_POC_V1.md`
   32: - Issue #27·#30
   33: 
   34: 이전 상세 내용은 GitHub 변경 이력에서 확인한다.
```

### `docs/issues/0015.md` — HISTORICAL / 85 lines

Hits: `이동권` [55], `룰렛` [78]

```text
   52: | 1:45~2:15 | 상단 배치 |
   53: | 2:15~3:10 | 첫 경계 공세 |
   54: | 3:10~5:15 | 포탑·바리케이드 역전 |
   55: | 5:15~5:45 | 이동권으로 2줄 엘리트 |
   56: | 5:45~7:20 | 엘리트 강화 |
   57: | 7:20~8:10 | Tier 2 전사 선택 |
   58: | 8:10~10:00 | 선택 병종 체험과 다음 성장 예고 |
   75: ## PoC 목표
   76: 
   77: - 첫 병영 1분 10초
   78: - 첫 룰렛 1분 45초
   79: - 첫 배치 2분 15초
   80: - 첫 역전 5분 15초
   81: - 첫 엘리트 5분 45초
```

### `docs/issues/0017.md` — HISTORICAL / 90 lines

Hits: `룰렛` [43]

```text
   40: - 시스템 튜토리얼
   41: - 전술 선택지 요약
   42: - 베일의 법칙 암시
   43: - 룰렛·역전 감정 반응
   44: 
   45: 정찰관·부관 별도 안내 UI와 음성은 사용하지 않는다.
   46: 
```

### `docs/issues/0020.md` — HISTORICAL / 66 lines

Hits: `roulette` [56], `룰렛` [62]

```text
   53: 
   54: ## 보류 범위
   55: 
   56: 저장소명과 내부 코드명 `roulettebound-prototype`은 구현 마이그레이션 계획이 별도로 승인될 때까지 유지한다.
   57: 
   58: 로고 키워드:
   59: 
   60: ```text
   61: 세 갈래 전선
   62: + 3×3 룰렛 문양
   63: + 경계종의 빛 고리
   64: + 베일의 균열
   65: + 벨루
```

### `docs/issues/0022.md` — HISTORICAL / 72 lines

Hits: `룰렛` [48, 70]

```text
   45: 
   46: 1. 첫 배치와 기본 교전.
   47: 2. 포탑 또는 바리케이드로 첫 역전.
   48: 3. 룰렛 상위 등급의 강함 체감.
   49: 4. 세 라인 분배와 Tier 2 선택.
   50: 
   51: ## 초기 수치 가설
   67: 3. 중간거점·성문·우회로 튜토리얼 도입 시점.
   68: 4. 포탑 완공과 첫 역전 타이밍.
   69: 5. 세 Tier 2 선택의 방어 성공률.
   70: 6. 시작 금화·병영·포탑·룰렛 비용 정합성.
   71: 
   72: 별도 Codex Plan Mode 제안서와 사용자 승인 전에는 Unit·Wave·Building Resource와 전투 코드를 구현하지 않는다.
```

### `docs/issues/0026.md` — HISTORICAL / 85 lines

Hits: `룰렛` [53, 73, 74]

```text
   50: - 최대 할인 총금화 70.
   51: - 첫 사제도 전체 180초 생산시간 사용.
   52: - 자동생산은 일반 등급만 생성.
   53: - 룰렛은 일반·엘리트·영웅·전설 사제를 생성.
   54: - 초기 식량 3.
   55: - 플레이어 신화급 사제 없음.
   56: 
   70: - 일시정지 중 중단.
   71: - 식량 부족 시 한 기 완성 대기, 다음 생산 중단.
   72: - 사제단 파괴 시 진행 중 생산과 건물 안 완성 대기 사제 소멸.
   73: - 이미 인도된 사제와 확정 룰렛 결과는 유지.
   74: - 다음 룰렛부터 사제 토큰 제거.
   75: 
   76: ## 남은 작업
   77: 
```

### `docs/issues/0027.md` — HISTORICAL / 124 lines

Hits: `룰렛` [98, 100, 103]

```text
   95: 거인단 → 거인 토큰 + 일반 거인 자동생산
   96: ```
   97: 
   98: - 다음 룰렛부터 선택 토큰 적용.
   99: - 자동생산은 일반 등급만 생성.
  100: - 엘리트·영웅·전설은 룰렛 전용.
  101: - 식량 부족 시 한 기 완성 대기, 다음 생산 정지.
  102: - 건물 파괴 시 진행 중 생산과 건물 안 완성 대기 유닛 소멸.
  103: - 이미 인도된 유닛과 확정 룰렛 결과는 유지.
  104: 
  105: ## 전략적 의미
  106: 
```

### `docs/issues/0028.md` — HISTORICAL / 136 lines

Hits: `룰렛` [71, 106]

```text
   68: - 즉시 총금화 140, 최대 할인 총금화 90.
   69: - 첫 자동생산도 300초 전체 시간이 필요하다.
   70: - 자동생산은 Normal Rank만 생성한다.
   71: - 룰렛은 Normal·Elite·Hero·Legendary Rank를 생성한다.
   72: - 플레이어 Mythic Rank는 없다.
   73: 
   74: ## 전투 정체성
  103: - 식량 부족 시 한 기 완성 대기, 다음 생산 중단.
  104: - 거인단 파괴 시 진행 중 생산과 건물 안 완성 대기 유닛 소멸.
  105: - 이미 인도된 UnitInstance는 유지.
  106: - 다음 룰렛부터 거인 archetype 토큰 제거.
  107: 
  108: ## 기본 공격·연출
  109: 
```

### `docs/issues/0029.md` — HISTORICAL / 105 lines

Hits: `룰렛` [3, 20, 25, 43, 72, 75, 102]

```text
    1: ---
    2: issue_number: 29
    3: title: "[Approved Economy][Stage] 시작 경제·건물·룰렛·거점 생산 기준 V1"
    4: state: open
    5: github_url: "https://github.com/alsdmlals4-eng/omenward/issues/29"
    6: github_updated_at: "2026-07-15T20:15:01Z"
   17: - docs/design/APPROVED_STAGE_ECONOMY_AND_BUILDING_COST_BASELINE_V1.md
   18: ---
   19: 
   20: # Issue #29: [Approved Economy][Stage] 시작 경제·건물·룰렛·거점 생산 기준 V1
   21: 
   22: ## Issue body
   23: ## 현재 상태
   24: 
   25: **정규 스테이지 시작 경제·접전지 수입·중간거점 생산·건물비·룰렛비·환불률의 첫 PoC 기준 승인 / 플레이테스트 조정 가능 / 구현 금지**
   26: 
   27: ## 책임 문서
   28: 
   40: 기본 수입 20초마다 +5
   41: 중앙 접전지 60초마다 아군 점령 한 곳당 +4
   42: 소유 중간거점 30초마다 한 곳당 +2
   43: 기본 룰렛 1회 20
   44: ```
   45: 
   46: - 적 개별 처치 금화 없음.
   69: 
   70: 건물은 지정 노드에만 배치하며 도로를 막지 않는다.
   71: 
   72: ## 룰렛·전술 명령
   73: 
   74: ```text
   75: 룰렛 20
   76: 금화 1줄 15
   77: 금화 2줄 40
   78: 금화 3줄 이상 100
   99: - 중간거점 생산이 기본 수입·시장·접전지 수입과 만드는 전체 경제 속도.
  100: - 기본 병영 자동생산 간격.
  101: - 시장·농장·포탑 Tier 2·3 효과와 비용.
  102: - 룰렛 실제 확률·금화 기대값.
  103: - 용병·아이템 가격과 난이도별 경제 배율.
  104: 
  105: 별도 Plan Mode 승인 전에는 경제·건물·거점 생산 시스템 구현을 시작하지 않는다.
```

### `docs/issues/0030.md` — HISTORICAL / 120 lines

Hits: `룰렛` [101]

```text
   98: 2. 세계관·벨루.
   99: 3. 전장·점령·성문·우회로.
  100: 4. 공용 10병종 Tier·Rank·능력 계보.
  101: 5. 룰렛·경제·건물·전술·용병.
  102: 6. 공용 아키타입 기반 W1~20·보스.
  103: 7. 튜토리얼·약 3시간 캠페인·절차 생성.
  104: 8. HUD·아트·애니메이션·오디오.
```

### `docs/issues/0032.md` — HISTORICAL / 137 lines

Hits: `룰렛` [37, 106]

```text
   34: ```text
   35: 베일의 징조
   36: → 건설
   37: → 룰렛
   38: → 공용 병종 배치
   39: → 3라인 교전
   40: → 접전지·중간거점 점령
  103: 3. 중간거점·접전지 상태 머신.
  104: 4. 전방 2·후방 1 노드와 최소 건물 2종.
  105: 5. 거점 건설권·생산권 이전.
  106: 6. 최소 3×3 룰렛과 공용 archetype 결과 생성.
  107: 7. 공용 Unit Scene과 대표 archetype 3~5종.
  108: 8. 대표 archetype의 아군·적군 Visual Set.
  109: 9. 최소 적 웨이브와 베일의 징조.
```

### `docs/proposals/0002-benchmarking-review-gate.md` — HISTORICAL / 16 lines

Hits: `럭키` [13]

```text
   10: 1. 수직 슬라이스 필수 UX 6개
   11: 2. 건물 티어의 고정 성장 또는 선택형 분기
   12: 3. 적 웨이브 정보의 정확도
   13: 4. 럭키 찬스 실패 누적 보정 여부
   14: 5. 전방 생산시설 추가 보너스 여부
   15: 
   16: 사용자가 승인하면 `GAME_DESIGN.md`, `ROADMAP.md`, `DECISIONS_PENDING.md`에 승인된 항목만 반영한다.
```

### `docs/proposals/0002-issue-body.md` — HISTORICAL / 30 lines

Hits: `roulette` [9], `slot` [9], `럭키` [23]

```text
    6: 
    7: ## 목표
    8: 
    9: Slotbound, Luck be a Landlord, Commander Quest, Mechabellum, Thronefall, Into the Breach, The King is Watching, 9 Kings의 시스템과 반응을 비교해 Roulettebound 수직 슬라이스에 적용할 요소와 제외할 요소를 확정한다.
   10: 
   11: ## 책임 문서
   12: 
   20: 1. 수직 슬라이스 필수 UX 6개
   21: 2. 건물 티어 고정 성장/선택형 분기
   22: 3. 웨이브 수량 정확 공개/범위 공개
   23: 4. 럭키 찬스 순수 확률/실패 누적 보정
   24: 5. 전방 생산시설 추가 보너스 여부
   25: 
   26: ## 승인 후 작업
```

### `docs/superpowers/plans/2026-07-16-vertical-slice-stage-progression.md` — REFERENCE_OR_OTHER / 297 lines

Hits: `roulette` [7, 130, 133, 134, 139, 142, 146, 152, 156, 158, 160, 164, 166, 244], `unitspawndefinition` [31, 88, 139, 140, 160], `spin` [139, 146, 156, 160, 244]

```text
    4: 
    5: **Goal:** Build a playable tutorial and a W1–W20 regular stage using the existing shared-archetype Godot bootstrap, then validate and merge it into `main`.
    6: 
    7: **Architecture:** `GameSession` composes a `StageRun`, which loads deterministic stage definitions, owns the clock/economy/roulette/wave flow, and sends commands to three isolated lane simulators. One `Unit` scene receives an archetype, tier, rank, team, and faction visual profile; static stage definitions are `.tres` Resources while the resolved runtime manifest and input log are JSON.
    8: 
    9: **Tech Stack:** Godot 4.7.1 Standard, GDScript, Compatibility renderer, headless GDScript tests, Python unittest for Issue mirror validation.
   10: 
   28: **Interfaces:**
   29: - Produces `StageDefinition.build_manifest(seed: int) -> StageManifest`.
   30: - Produces `WaveDefinition` with `wave_number`, `omen_lead_seconds`, `spawns`, `boss_kind`, and `is_overtime`.
   31: - Produces `UnitSpawnDefinition` with `archetype_id`, `tier_id`, `rank_id`, `owner_team_id`, `visual_faction_id`, `lane_id`, and `spawn_delay_seconds`.
   32: - Extends `StageManifest.to_json()` with resolved stage, economy, wave, and input-log fields.
   33: 
   34: - [ ] **Step 1: Write the failing data-contract test**
   85: - Test: `tests/headless/battle_simulation_test.gd`
   86: 
   87: **Interfaces:**
   88: - `BattleSimulator.spawn_unit(spawn: UnitSpawnDefinition) -> UnitInstance`
   89: - `BattleSimulator.advance(delta: float) -> void`
   90: - `BattleSimulator.snapshot() -> Dictionary`
   91: - `LaneState` stores no references to another lane's ordinary unit list.
  127: 
  128: Commit: `feat: simulate shared units across three lanes`
  129: 
  130: ### Task 3: Add economy, node construction, roulette, and deployment services
  131: 
  132: **Files:**
  133: - Create: `scripts/core/stage_economy.gd`, `scripts/buildings/building_state.gd`, `scripts/buildings/building_service.gd`, `scripts/roulette/roulette_service.gd`, `scripts/units/deployment_service.gd`
  134: - Test: `tests/headless/economy_roulette_test.gd`
  135: 
  136: **Interfaces:**
  137: - `StageEconomy.try_spend_gold(amount: int) -> bool`
  138: - `BuildingService.try_construct(outpost_id: StringName, node_id: StringName, building_id: StringName) -> bool`
  139: - `RouletteService.spin(seed_input: Dictionary) -> Array[UnitSpawnDefinition]`
  140: - `DeploymentService.deploy(card: UnitSpawnDefinition, lane_id: StringName, position: float) -> bool`
  141: 
  142: - [ ] **Step 1: Write failing economy and roulette tests**
  143: 
  144: ```gdscript
  145: _expect(economy.gold == 160, "regular stage starts at 160 gold", failures)
  146: _expect(roulette.spin({"seed": 12}).size() > 0, "a valid spin produces deployable shared units", failures)
  147: _expect(not buildings.try_construct(&"enemy_top", &"front_a", &"tower"), "enemy-owned node rejects player building", failures)
  148: ```
  149: 
  150: - [ ] **Step 2: Run the test and confirm it fails**
  151: 
  152: Run: `Godot_v4.7.1-stable_win64_console.exe --headless --path . -s res://tests/headless/economy_roulette_test.gd`
  153: 
  154: - [ ] **Step 3: Implement minimal services**
  155: 
  156: Implement 160 starting gold, 12 food cap, +5 per active 20 seconds, +4 per controlled clash zone every 60 seconds, +2 per stable owned outpost every 30 seconds, 20 gold spin, and only `tower` plus `farm` building definitions. Completed buildings contribute roulette tokens; construction requires a currently owned, stabilized node and is locked during capture.
  157: 
  158: - [ ] **Step 4: Implement deterministic 3×3 roulette and hand/deploy flow**
  159: 
  160: Use the determinism service RNG and append each spin/build/deploy command to `StageManifest.input_log`. Roulette outputs only `UnitSpawnDefinition` objects with shared archetype IDs and player visual faction. Reject deployment when food capacity is exceeded; never charge food for un-deployed results.
  161: 
  162: - [ ] **Step 5: Run tests and commit**
  163: 
  164: Run: `Godot_v4.7.1-stable_win64_console.exe --headless --path . -s res://tests/headless/economy_roulette_test.gd`
  165: 
  166: Commit: `feat: add stage economy roulette and deployment`
  167: 
  168: ### Task 4: Add waves, omen, boss, assassin bypass, and stage state machine
  169: 
  241: 
  242: - [ ] **Step 3: Implement scenes with intentionally minimal controls**
  243: 
  244: The stage-select view starts tutorial and locks the regular button until victory. The HUD exposes gold, food, current wave, omen, roulette spin, pending result cards, three lane deploy buttons, two building buttons, pause, retry, and result overlay. The battlefield view draws all lanes, gates, outposts, node state, clash ownership, bypass warning, and simple shared unit silhouettes from `FactionVisualProfile.palette_color`.
  245: 
  246: - [ ] **Step 4: Wire shared-unit state presentation**
  247: 
```

### `docs/superpowers/specs/2026-07-16-vertical-slice-stage-progression-design.md` — REFERENCE_OR_OTHER / 50 lines

Hits: `roulette` [9, 17, 24]

```text
    6: 
    7: ## Scope
    8: 
    9: The tutorial contains four deterministic waves and teaches construction, the 3×3 roulette, lane deployment, one defensive building, and the first archetype choice. Completion unlocks the regular stage.
   10: 
   11: The regular stage shares the same systems and has twenty deterministic waves: baseline pressure in W1–W4, elite at W5, hero at W10, legendary boss and standard victory target at W15, overtime pressure at W16–W19, and a mythic boss at W20. A player may win through the W15 objective or enemy-base destruction; surviving beyond W15 enables the overtime sequence.
   12: 
   14: 
   15: `GameSession` remains the composition root and owns a `StageRun` state machine. A `StageManifest` defines tutorial and regular-stage data, including economy, unlocked actions, omen schedule, wave compositions, lane assignments, and victory rules. A single shared `Unit` scene is instantiated from the existing public archetype, tier, rank, team, and visual-faction contract; enemies differ only by team, visual set, and wave spawn instruction.
   16: 
   17: The battle scene owns three isolated lane simulators. Each lane contains its own gates, middle outposts, clash zone, unit spatial list, and assassin bypass route. It exposes observable state to HUD/debug controls but has no cross-lane movement graph. Economy, building nodes, roulette results, deploy queue, wave clock, combat results, stage result, and deterministic input log are independent services coordinated by `StageRun`.
   18: 
   19: ## Playable Loop
   20: 
   21: ```text
   22: stage selection
   23: → omen
   24: → build / roulette / deploy
   25: → three-lane simulation
   26: → capture / bypass / gate siege
   27: → wave result and next omen
```

### `docs/work_orders/0001-phase-0-codex-plan-mode.md` — HISTORICAL / 390 lines

Hits: `룰렛` [92, 99, 336, 372]

```text
   89: 
   90: ### 한 문장
   91: 
   92: > 건물을 지어 룰렛 확률과 증원 체계를 설계하고, 예고된 공세를 세 전선에서 뒤집는 판타지 전략 오토배틀 게임.
   93: 
   94: ### 핵심 루프
   95: 
   96: ```text
   97: 베일의 징조
   98: → 건물·토큰 선택
   99: → 3×3 룰렛
  100: → 병력 획득·라인 배치
  101: → 3라인 교전
  102: → 접전지·중간거점 공방
  333: 
  334: - 실제 Godot 파일 생성·수정.
  335: - 실제 전투·이동·타기팅·공격 AI.
  336: - 룰렛·건설·경제·점령·성문·웨이브 구현.
  337: - 3라인 Battle Scene.
  338: - 실제 Unit 전투 Scene.
  339: - 최종 아트·VFX·오디오.
  369: - 실제 저장소에 예상하지 못한 Godot 구현이 이미 존재한다.
  370: - 공용 병종 계약을 지키기 위해 데이터 분리가 필요하다고 판단된다.
  371: - 공식 stable 버전 또는 CLI가 사전 추천과 다르다.
  372: - Phase 0 범위 안에서 전투·룰렛·건설 구현이 필수라고 판단된다.
  373: - 외부 애드온이 없으면 테스트가 불가능하다고 판단된다.
  374: - 960×540 정책이 제품 요구를 만족할 수 없다고 판단된다.
  375: 
```

### `docs/work_orders/0002-current-main-audit-and-next-iteration-plan-mode.md` — HISTORICAL / 202 lines

Hits: `룰렛` [67]

```text
   64: - 공용 UnitArchetype, Tier, Rank, Attack, AnimationContract, FactionVisual 데이터.
   65: - 적군 전용 중복 병종 데이터·Scene·스킬·모션 계약이 생겼는지.
   66: - 독립 3라인, 성문, 중간거점, 접전지, 건설 노드와 암살자 우회 구현.
   67: - 룰렛, 배치, 경제, 웨이브와 승패 흐름.
   68: - 튜토리얼 4웨이브와 일반 W1~W20 데이터·실행 가능 범위.
   69: - headless 테스트와 수동 QA가 실제로 통과했는지.
   70: - 문서가 현재 코드 상태와 충돌하는 부분.
```

### `scenes/ui/stage_hud.tscn` — ACTIVE / 121 lines

Hits: `spin` [40, 46, 115]

```text
   37: offset_bottom = 62.0
   38: theme_override_colors/font_color = Color(0.94, 0.8, 0.42, 1)
   39: 
   40: [node name="SpinButton" type="Button" parent="."]
   41: layout_mode = 0
   42: offset_left = 20.0
   43: offset_top = 450.0
   44: offset_right = 132.0
   45: offset_bottom = 486.0
   46: text = "Spin (20)"
   47: 
   48: [node name="TowerButton" type="Button" parent="."]
   49: layout_mode = 0
  112: offset_bottom = 294.0
  113: text = "Retry Stage"
  114: 
  115: [connection signal="pressed" from="SpinButton" to="." method="_on_spin_pressed"]
  116: [connection signal="pressed" from="TowerButton" to="." method="_on_tower_pressed"]
  117: [connection signal="pressed" from="FarmButton" to="." method="_on_farm_pressed"]
  118: [connection signal="pressed" from="DeployTop" to="." method="_on_deploy_pressed" binds= [&"top"]]
```

### `scripts/battle/battle_simulator.gd` — ACTIVE / 139 lines

Hits: `unitspawndefinition` [39]

```text
   36: 	}
   37: 
   38: 
   39: func spawn_unit(spawn: UnitSpawnDefinition) -> Variant:
   40: 	if spawn == null or not registry.archetypes.has(str(spawn.archetype_id)) or not lanes.has(spawn.lane_id):
   41: 		return null
   42: 	var unit: Variant = UnitInstanceScript.new(spawn, registry, _next_unit_id, _rng.randi_range(0, 9999))
```

<details><summary>Full content</summary>

```text
class_name BattleSimulator
extends RefCounted

const UnitInstanceScript = preload("res://scripts/battle/unit_instance.gd")
const LaneStateScript = preload("res://scripts/battle/lane_state.gd")
const GateStateScript = preload("res://scripts/battle/gate_state.gd")
const ClashZoneStateScript = preload("res://scripts/battle/clash_zone_state.gd")
const AssassinBypassStateScript = preload("res://scripts/battle/assassin_bypass_state.gd")

const FIXED_STEP_SECONDS := 0.1
const LANE_IDS := [&"top", &"middle", &"bottom"]

var registry: DataRegistry
var seed := 0
var lanes := {}
var gates := {}
var clash_zones := {}
var bypasses: Array = []

var _rng := RandomNumberGenerator.new()
var _accumulator := 0.0
var _tick := 0
var _next_unit_id := 1


func _init(assigned_registry: DataRegistry, seed_value: int = 0) -> void:
	registry = assigned_registry
	seed = seed_value
	_rng.seed = seed
	for lane_id in LANE_IDS:
		lanes[lane_id] = LaneStateScript.new(lane_id)
		clash_zones[lane_id] = ClashZoneStateScript.new(lane_id)
	gates = {
		"lumern": {"top": GateStateScript.new(), "middle": GateStateScript.new(), "bottom": GateStateScript.new()},
		"veil": {"top": GateStateScript.new(), "middle": GateStateScript.new(), "bottom": GateStateScript.new()},
	}


func spawn_unit(spawn: UnitSpawnDefinition) -> Variant:
	if spawn == null or not registry.archetypes.has(str(spawn.archetype_id)) or not lanes.has(spawn.lane_id):
		return null
	var unit: Variant = UnitInstanceScript.new(spawn, registry, _next_unit_id, _rng.randi_range(0, 9999))
	_next_unit_id += 1
	if not lanes[spawn.lane_id].add_unit(unit):
		return null
	return unit


func request_lane_move(unit: Variant, requested_lane_id: StringName) -> bool:
	return unit != null and unit.lane_id == requested_lane_id


func request_assassin_bypass(unit: Variant, enemy_outpost_position: float) -> bool:
	if unit == null or unit.archetype_id != &"assassin" or not lanes.has(unit.lane_id):
		return false
	var lane: Variant = lanes[unit.lane_id]
	if not lane.remove_unit(unit):
		return false
	bypasses.append({
		"unit": unit,
		"state": AssassinBypassStateScript.new(unit.lane_id, enemy_outpost_position),
	})
	return true


func advance(delta: float) -> void:
	_accumulator += maxf(0.0, delta)
	while _accumulator + 0.000001 >= FIXED_STEP_SECONDS:
		_accumulator -= FIXED_STEP_SECONDS
		_advance_fixed_step()


func snapshot() -> Dictionary:
	var lane_snapshots: Array = []
	var unit_snapshots: Array = []
	var zone_snapshots: Array = []
	var gate_snapshots := {}
	for lane_id in LANE_IDS:
		var lane: Variant = lanes[lane_id]
		lane_snapshots.append(lane.snapshot())
		for unit in lane.ordered_units():
			unit_snapshots.append(unit.to_snapshot())
		zone_snapshots.append(clash_zones[lane_id].snapshot())
	for team_id in [&"lumern", &"veil"]:
		var team_gates := {}
		for lane_id in LANE_IDS:
			team_gates[str(lane_id)] = gates[str(team_id)][str(lane_id)].snapshot()
		gate_snapshots[str(team_id)] = team_gates
	return {
		"seed": seed,
		"tick": _tick,
		"accumulator": _accumulator,
		"lanes": lane_snapshots,
		"units": unit_snapshots,
		"gates": gate_snapshots,
		"clash_zones": zone_snapshots,
		"bypasses": bypasses.map(func(entry: Dictionary) -> Dictionary: return entry["state"].snapshot()),
	}


func _advance_fixed_step() -> void:
	_tick += 1
	_advance_bypasses(FIXED_STEP_SECONDS)
	for lane_id in LANE_IDS:
		var lane: Variant = lanes[lane_id]
		for unit in lane.ordered_units():
			if not unit.is_alive():
				continue
			var target: Variant = lane.find_target(unit)
			unit.target_unit_id = target.unit_id if target != null else -1
			if target == null:
				unit.state = "idle"
				continue
			if unit.distance_to(target) > float(unit.combat_stats()["attack_range"]):
				unit.move_toward(target, FIXED_STEP_SECONDS)
				continue
			var damage: float = unit.advance_attack(FIXED_STEP_SECONDS)
			if damage > 0.0:
				target.receive_damage(damage)
		lane.remove_dead_units()
		clash_zones[lane_id].advance(FIXED_STEP_SECONDS)
	for team_id in [&"lumern", &"veil"]:
		for lane_id in LANE_IDS:
			gates[str(team_id)][str(lane_id)].advance(FIXED_STEP_SECONDS)


func _advance_bypasses(delta: float) -> void:
	var active_bypasses: Array = []
	for entry: Dictionary in bypasses:
		var bypass: Variant = entry["state"]
		bypass.advance(delta)
		if not bypass.is_complete():
			active_bypasses.append(entry)
			continue
		var unit: Variant = entry["unit"]
		unit.lane_position = bypass.exit_position
		unit.state = "bypass_exit"
		lanes[unit.lane_id].add_unit(unit)
	bypasses = active_bypasses

```

</details>

### `scripts/battle/unit_instance.gd` — ACTIVE / 139 lines

Hits: `unitspawndefinition` [24]

```text
   21: var _recovery_seconds := 0.1
   22: 
   23: 
   24: func _init(spawn: UnitSpawnDefinition, registry: DataRegistry, assigned_unit_id: int, animation_offset: int) -> void:
   25: 	unit_id = assigned_unit_id
   26: 	archetype_id = spawn.archetype_id
   27: 	tier_id = spawn.tier_id
```

<details><summary>Full content</summary>

```text
class_name UnitInstance
extends RefCounted

var unit_id := 0
var archetype_id: StringName
var tier_id: StringName
var rank_id: StringName
var owner_team_id: StringName
var visual_faction_id: StringName
var lane_id: StringName
var lane_position := 0.0
var target_unit_id := -1
var state := "idle"
var health := 0.0
var cooldown_remaining := 0.0
var deterministic_animation_offset := 0

var _stats := {}
var _preparation_seconds := 0.1
var _hit_seconds := 0.1
var _recovery_seconds := 0.1


func _init(spawn: UnitSpawnDefinition, registry: DataRegistry, assigned_unit_id: int, animation_offset: int) -> void:
	unit_id = assigned_unit_id
	archetype_id = spawn.archetype_id
	tier_id = spawn.tier_id
	rank_id = spawn.rank_id
	owner_team_id = spawn.owner_team_id
	visual_faction_id = spawn.visual_faction_id
	lane_id = spawn.lane_id
	lane_position = 0.0 if owner_team_id == &"lumern" else 100.0
	deterministic_animation_offset = animation_offset
	_stats = _build_combat_stats(registry)
	health = float(_stats["max_health"])
	_load_attack_timing(registry)


func combat_stats() -> Dictionary:
	return _stats.duplicate()


func is_alive() -> bool:
	return health > 0.0


func distance_to(other: UnitInstance) -> float:
	return absf(lane_position - other.lane_position)


func move_toward(other: UnitInstance, delta: float) -> void:
	state = "move"
	var direction := signf(other.lane_position - lane_position)
	lane_position += direction * float(_stats["move_speed"]) * delta


func advance_attack(delta: float) -> float:
	if state == "idle" or state == "move":
		state = "attack_preparation"
		cooldown_remaining = _preparation_seconds
		return 0.0
	if state == "attack_preparation":
		cooldown_remaining -= delta
		if cooldown_remaining <= 0.0:
			state = "attack_hit"
			cooldown_remaining = _hit_seconds
		return 0.0
	if state == "attack_hit":
		state = "attack_recovery"
		cooldown_remaining = _recovery_seconds
		return float(_stats["attack"])
	if state == "attack_recovery":
		cooldown_remaining -= delta
		if cooldown_remaining <= 0.0:
			state = "idle"
		return 0.0
	return 0.0


func receive_damage(raw_damage: float) -> float:
	var mitigated := raw_damage * 100.0 / (100.0 + float(_stats["armor"]))
	health = maxf(0.0, health - mitigated)
	if health <= 0.0:
		state = "dead"
		target_unit_id = -1
	return mitigated


func to_snapshot() -> Dictionary:
	return {
		"unit_id": unit_id,
		"archetype_id": str(archetype_id),
		"tier_id": str(tier_id),
		"rank_id": str(rank_id),
		"owner_team_id": str(owner_team_id),
		"visual_faction_id": str(visual_faction_id),
		"lane_id": str(lane_id),
		"lane_position": lane_position,
		"target_unit_id": target_unit_id,
		"state": state,
		"health": health,
		"cooldown_remaining": cooldown_remaining,
		"deterministic_animation_offset": deterministic_animation_offset,
	}


func _build_combat_stats(registry: DataRegistry) -> Dictionary:
	var profile: Variant = registry.archetypes.get(str(archetype_id))
	if profile == null or profile.base_stats.is_empty():
		push_error("unknown shared archetype: %s" % archetype_id)
		return {}
	var multiplier := _tier_multiplier(registry) * _rank_multiplier(registry)
	var result := {}
	for key in profile.base_stats:
		result[key] = float(profile.base_stats[key]) * multiplier
	return result


func _tier_multiplier(registry: DataRegistry) -> float:
	for profile in registry.catalog.tier_profiles:
		if profile.tier_id == tier_id:
			return profile.stat_multiplier
	return 1.0


func _rank_multiplier(registry: DataRegistry) -> float:
	for profile in registry.catalog.rank_profiles:
		if profile.rank_id == rank_id:
			return profile.stat_multiplier
	return 1.0


func _load_attack_timing(registry: DataRegistry) -> void:
	for profile in registry.catalog.attack_profiles:
		if profile.profile_id == archetype_id:
			_preparation_seconds = float(profile.preparation_ms) / 1000.0
			_hit_seconds = float(profile.hit_ms) / 1000.0
			_recovery_seconds = float(profile.recovery_ms) / 1000.0
			return

```

</details>

### `scripts/buildings/building_service.gd` — ACTIVE / 97 lines

Hits: `roulette` [50, 55, 92]

```text
   47: 	return true
   48: 
   49: 
   50: func roulette_archetype_ids() -> Array[StringName]:
   51: 	var tokens: Array[StringName] = []
   52: 	for key in _buildings:
   53: 		var state: Variant = _buildings[key]
   54: 		if _outpost_is_active_for_player(state.outpost_id) and _building_matches_current_capture(state):
   55: 			tokens.append(state.definition.roulette_archetype_id)
   56: 	if tokens.is_empty():
   57: 		tokens.append(&"shield_guard")
   58: 	return tokens
   89: 	definition.building_id = building_id
   90: 	definition.gold_cost = gold_cost
   91: 	definition.food_cap_bonus = food_cap_bonus
   92: 	definition.roulette_archetype_id = token_id
   93: 	return definition
   94: 
   95: 
```

<details><summary>Full content</summary>

```text
class_name BuildingService
extends RefCounted

const PLAYER_TEAM_ID := &"lumern"
const BuildingDefinitionScript = preload("res://scripts/data/building_definition.gd")
const BuildingStateScript = preload("res://scripts/buildings/building_state.gd")

var economy: Variant
var manifest: Variant
var definitions := {}
var _outposts := {}
var _nodes := {}
var _buildings := {}


func _init(assigned_economy: Variant, assigned_manifest: Variant) -> void:
	economy = assigned_economy
	manifest = assigned_manifest
	definitions = {
		&"tower": _definition(&"tower", 35, 0, &"shield_guard"),
		&"farm": _definition(&"farm", 35, 6, &"archer"),
	}


func register_outpost(outpost_id: StringName, outpost: Variant, node_ids: Array) -> void:
	_outposts[outpost_id] = outpost
	_nodes[outpost_id] = node_ids.duplicate()


func try_construct(outpost_id: StringName, node_id: StringName, building_id: StringName) -> bool:
	if not definitions.has(building_id) or not _node_is_available(outpost_id, node_id):
		return false
	var definition: Variant = definitions[building_id]
	if not economy.try_spend_gold(definition.gold_cost):
		return false
	var outpost: Variant = _outposts[outpost_id]
	var state: Variant = BuildingStateScript.new(outpost_id, node_id, definition, outpost.capture_revision)
	_buildings[_key(outpost_id, node_id)] = state
	if definition.food_cap_bonus > 0:
		economy.add_food_cap(definition.food_cap_bonus)
	manifest.input_log.append({
		"action": "build",
		"outpost_id": str(outpost_id),
		"node_id": str(node_id),
		"building_id": str(building_id),
	})
	return true


func roulette_archetype_ids() -> Array[StringName]:
	var tokens: Array[StringName] = []
	for key in _buildings:
		var state: Variant = _buildings[key]
		if _outpost_is_active_for_player(state.outpost_id) and _building_matches_current_capture(state):
			tokens.append(state.definition.roulette_archetype_id)
	if tokens.is_empty():
		tokens.append(&"shield_guard")
	return tokens


func _node_is_available(outpost_id: StringName, node_id: StringName) -> bool:
	if not _nodes.has(outpost_id) or not (_nodes[outpost_id] as Array).has(node_id):
		return false
	var key := _key(outpost_id, node_id)
	if _buildings.has(key):
		var state: Variant = _buildings[key]
		if _building_matches_current_capture(state):
			return false
		_buildings.erase(key)
	return _outpost_is_active_for_player(outpost_id)


func _outpost_is_active_for_player(outpost_id: StringName) -> bool:
	if not _outposts.has(outpost_id):
		return false
	var outpost: Variant = _outposts[outpost_id]
	return outpost.owner_team_id == PLAYER_TEAM_ID and outpost.state == outpost.STABLE and not outpost.construction_locked


func _building_matches_current_capture(state: Variant) -> bool:
	if not _outposts.has(state.outpost_id):
		return false
	var outpost: Variant = _outposts[state.outpost_id]
	return state.capture_revision == outpost.capture_revision


func _definition(building_id: StringName, gold_cost: int, food_cap_bonus: int, token_id: StringName) -> Variant:
	var definition: Variant = BuildingDefinitionScript.new()
	definition.building_id = building_id
	definition.gold_cost = gold_cost
	definition.food_cap_bonus = food_cap_bonus
	definition.roulette_archetype_id = token_id
	return definition


func _key(outpost_id: StringName, node_id: StringName) -> String:
	return "%s:%s" % [outpost_id, node_id]

```

</details>

### `scripts/core/determinism_service.gd` — ACTIVE / 24 lines

Hits: `roulette` [21, 22, 23, 24], `spin` [21, 23]

```text
   18: 	return manifest
   19: 
   20: 
   21: func create_roulette_rng(spin_seed: int) -> RandomNumberGenerator:
   22: 	var roulette_rng := RandomNumberGenerator.new()
   23: 	roulette_rng.seed = seed ^ spin_seed
   24: 	return roulette_rng
```

<details><summary>Full content</summary>

```text
class_name DeterminismService
extends RefCounted

var seed: int
var _rng := RandomNumberGenerator.new()

func _init(seed_value: int) -> void:
	seed = seed_value
	_rng.seed = seed

func create_stage_manifest(stage_id: String, archetype_ids: Array[String]) -> StageManifest:
	var manifest := StageManifest.new()
	manifest.stage_id = stage_id
	manifest.seed = seed
	manifest.archetype_ids = archetype_ids.duplicate()
	manifest.archetype_ids.sort()
	manifest.random_roll = _rng.randi()
	return manifest


func create_roulette_rng(spin_seed: int) -> RandomNumberGenerator:
	var roulette_rng := RandomNumberGenerator.new()
	roulette_rng.seed = seed ^ spin_seed
	return roulette_rng

```

</details>

### `scripts/core/stage_run.gd` — ACTIVE / 120 lines

Hits: `roulette` [8, 25, 58, 65, 66], `spin` [65, 66], `unitspawndefinition` [76, 79, 107]

```text
    5: const CombatClockScript = preload("res://scripts/core/combat_clock.gd")
    6: const StageEconomyScript = preload("res://scripts/core/stage_economy.gd")
    7: const BuildingServiceScript = preload("res://scripts/buildings/building_service.gd")
    8: const RouletteServiceScript = preload("res://scripts/roulette/roulette_service.gd")
    9: const DeploymentServiceScript = preload("res://scripts/units/deployment_service.gd")
   10: const WaveDirectorScript = preload("res://scripts/waves/wave_director.gd")
   11: const BattleSimulatorScript = preload("res://scripts/battle/battle_simulator.gd")
   22: var clock: Variant
   23: var economy: Variant
   24: var buildings: Variant
   25: var roulette: Variant
   26: var deployment: Variant
   27: var wave_director: Variant
   28: var battle: Variant
   55: 	buildings = BuildingServiceScript.new(economy, manifest)
   56: 	_home_outpost = OutpostStateScript.new(&"lumern")
   57: 	buildings.register_outpost(&"home", _home_outpost, [&"front_a", &"front_b"])
   58: 	roulette = RouletteServiceScript.new(economy, buildings, manifest, &"lumern")
   59: 	deployment = DeploymentServiceScript.new(economy, manifest)
   60: 	wave_director = WaveDirectorScript.new(stage)
   61: 	battle = BattleSimulatorScript.new(_registry, seed)
   62: 	result_state = RUNNING
   63: 
   64: 
   65: func spin_roulette(seed_input: Dictionary) -> Array:
   66: 	return roulette.spin(seed_input) if roulette != null else []
   67: 
   68: 
   69: func construct_home(building_id: StringName) -> bool:
   73: 	return buildings.try_construct(&"home", node_id, building_id)
   74: 
   75: 
   76: func deploy_card(card: UnitSpawnDefinition, lane_id: StringName) -> bool:
   77: 	if deployment == null or battle == null or not deployment.deploy(card, lane_id, 10.0):
   78: 		return false
   79: 	var deployed := card.duplicate() as UnitSpawnDefinition
   80: 	deployed.lane_id = lane_id
   81: 	return battle.spawn_unit(deployed) != null
   82: 
  104: 	for wave in wave_director.advance(delta):
  105: 		current_wave = wave.wave_number
  106: 		for spawn in wave.spawns:
  107: 			battle.spawn_unit(spawn.duplicate() as UnitSpawnDefinition)
  108: 		manifest.input_log.append({"action": "wave", "wave_number": current_wave})
  109: 	battle.advance(delta)
  110: 
```

<details><summary>Full content</summary>

```text
class_name StageRun
extends RefCounted

const DataRegistryScript = preload("res://scripts/core/data_registry.gd")
const CombatClockScript = preload("res://scripts/core/combat_clock.gd")
const StageEconomyScript = preload("res://scripts/core/stage_economy.gd")
const BuildingServiceScript = preload("res://scripts/buildings/building_service.gd")
const RouletteServiceScript = preload("res://scripts/roulette/roulette_service.gd")
const DeploymentServiceScript = preload("res://scripts/units/deployment_service.gd")
const WaveDirectorScript = preload("res://scripts/waves/wave_director.gd")
const BattleSimulatorScript = preload("res://scripts/battle/battle_simulator.gd")
const StageProgressionScript = preload("res://scripts/core/stage_progression.gd")
const OutpostStateScript = preload("res://scripts/battle/outpost_state.gd")

const RUNNING := &"running"
const VICTORY := &"victory"
const DEFEAT := &"defeat"

var progression: Variant
var stage: Variant
var manifest: Variant
var clock: Variant
var economy: Variant
var buildings: Variant
var roulette: Variant
var deployment: Variant
var wave_director: Variant
var battle: Variant
var current_wave := 0
var result_state: StringName = &""

var _registry: Variant
var _home_outpost: Variant


func _init(assigned_progression: Variant = null) -> void:
	progression = assigned_progression if assigned_progression != null else StageProgressionScript.new()


func start(assigned_stage: Variant, seed: int) -> void:
	stage = assigned_stage
	current_wave = 0
	result_state = &""
	if stage == null or not progression.can_start(stage):
		return
	_registry = DataRegistryScript.new()
	var errors: PackedStringArray = _registry.load_bootstrap_catalog("res://data/bootstrap_catalog.tres")
	if not errors.is_empty():
		result_state = DEFEAT
		return
	manifest = stage.build_manifest(seed)
	clock = CombatClockScript.new()
	clock.is_planning = false
	economy = StageEconomyScript.new(manifest)
	buildings = BuildingServiceScript.new(economy, manifest)
	_home_outpost = OutpostStateScript.new(&"lumern")
	buildings.register_outpost(&"home", _home_outpost, [&"front_a", &"front_b"])
	roulette = RouletteServiceScript.new(economy, buildings, manifest, &"lumern")
	deployment = DeploymentServiceScript.new(economy, manifest)
	wave_director = WaveDirectorScript.new(stage)
	battle = BattleSimulatorScript.new(_registry, seed)
	result_state = RUNNING


func spin_roulette(seed_input: Dictionary) -> Array:
	return roulette.spin(seed_input) if roulette != null else []


func construct_home(building_id: StringName) -> bool:
	if buildings == null:
		return false
	var node_id := &"front_a" if building_id == &"tower" else &"front_b"
	return buildings.try_construct(&"home", node_id, building_id)


func deploy_card(card: UnitSpawnDefinition, lane_id: StringName) -> bool:
	if deployment == null or battle == null or not deployment.deploy(card, lane_id, 10.0):
		return false
	var deployed := card.duplicate() as UnitSpawnDefinition
	deployed.lane_id = lane_id
	return battle.spawn_unit(deployed) != null


func submit_command(command: Dictionary) -> bool:
	if result_state != RUNNING:
		return false
	match command.get("action", ""):
		"stage_victory":
			result_state = VICTORY
			progression.record_victory(stage)
		"stage_defeat":
			result_state = DEFEAT
		_:
			return false
	manifest.input_log.append(command.duplicate(true))
	return true


func advance(delta: float) -> void:
	if result_state != RUNNING:
		return
	clock.advance(delta)
	economy.advance(delta, 0, _stable_owned_outpost_count())
	for wave in wave_director.advance(delta):
		current_wave = wave.wave_number
		for spawn in wave.spawns:
			battle.spawn_unit(spawn.duplicate() as UnitSpawnDefinition)
		manifest.input_log.append({"action": "wave", "wave_number": current_wave})
	battle.advance(delta)


func _stable_owned_outpost_count() -> int:
	if battle == null:
		return 0
	var count := 0
	for lane_id in battle.LANE_IDS:
		var outpost: Variant = battle.clash_zones[lane_id].outpost
		if outpost.owner_team_id == &"lumern" and outpost.state == outpost.STABLE:
			count += 1
	return count

```

</details>

### `scripts/data/building_definition.gd` — ACTIVE / 7 lines

Hits: `roulette` [7]

```text
    4: @export var building_id: StringName
    5: @export var gold_cost: int
    6: @export var food_cap_bonus: int
    7: @export var roulette_archetype_id: StringName
```

<details><summary>Full content</summary>

```text
class_name BuildingDefinition
extends Resource

@export var building_id: StringName
@export var gold_cost: int
@export var food_cap_bonus: int
@export var roulette_archetype_id: StringName

```

</details>

### `scripts/data/unit_spawn_definition.gd` — ACTIVE / 24 lines

Hits: `unitspawndefinition` [1]

```text
    1: class_name UnitSpawnDefinition
    2: extends Resource
    3: 
    4: @export var archetype_id: StringName
```

<details><summary>Full content</summary>

```text
class_name UnitSpawnDefinition
extends Resource

@export var archetype_id: StringName
@export var tier_id: StringName = &"tier_1"
@export var rank_id: StringName = &"common"
@export var owner_team_id: StringName
@export var visual_faction_id: StringName
@export var lane_id: StringName
@export var spawn_delay_seconds: float = 0.0
@export var food_cost: int = 1


func to_dictionary() -> Dictionary:
	return {
		"archetype_id": str(archetype_id),
		"tier_id": str(tier_id),
		"rank_id": str(rank_id),
		"owner_team_id": str(owner_team_id),
		"visual_faction_id": str(visual_faction_id),
		"lane_id": str(lane_id),
		"spawn_delay_seconds": spawn_delay_seconds,
		"food_cost": food_cost,
	}

```

</details>

### `scripts/data/wave_definition.gd` — ACTIVE / 20 lines

Hits: `unitspawndefinition` [4, 8]

```text
    1: class_name WaveDefinition
    2: extends Resource
    3: 
    4: const UnitSpawnDefinition = preload("res://scripts/data/unit_spawn_definition.gd")
    5: 
    6: @export var wave_number: int
    7: @export var omen_lead_seconds: float = 5.0
    8: @export var spawns: Array[UnitSpawnDefinition] = []
    9: @export var boss_kind: StringName
   10: @export var is_overtime := false
   11: 
```

<details><summary>Full content</summary>

```text
class_name WaveDefinition
extends Resource

const UnitSpawnDefinition = preload("res://scripts/data/unit_spawn_definition.gd")

@export var wave_number: int
@export var omen_lead_seconds: float = 5.0
@export var spawns: Array[UnitSpawnDefinition] = []
@export var boss_kind: StringName
@export var is_overtime := false


func to_dictionary() -> Dictionary:
	return {
		"wave_number": wave_number,
		"omen_lead_seconds": omen_lead_seconds,
		"spawns": spawns.map(func(spawn): return spawn.to_dictionary()),
		"boss_kind": str(boss_kind),
		"is_overtime": is_overtime,
	}

```

</details>

### `scripts/roulette/roulette_service.gd` — ACTIVE / 40 lines

Hits: `roulette` [1, 26, 27, 36], `spin` [4, 22, 23, 25, 26], `unitspawndefinition` [6, 22, 30]

```text
    1: class_name RouletteService
    2: extends RefCounted
    3: 
    4: const SPIN_COST := 20
    5: const BOARD_SIZE := 9
    6: const UnitSpawnDefinitionScript = preload("res://scripts/data/unit_spawn_definition.gd")
    7: const DeterminismServiceScript = preload("res://scripts/core/determinism_service.gd")
    8: 
    9: var economy: Variant
   19: 	player_team_id = assigned_player_team_id
   20: 
   21: 
   22: func spin(seed_input: Dictionary) -> Array[UnitSpawnDefinition]:
   23: 	if not economy.try_spend_gold(SPIN_COST):
   24: 		return []
   25: 	var spin_seed := int(seed_input.get("seed", manifest.seed))
   26: 	var rng: RandomNumberGenerator = DeterminismServiceScript.new(manifest.seed).create_roulette_rng(spin_seed)
   27: 	var token_ids: Array[StringName] = buildings.roulette_archetype_ids()
   28: 	var cards: Array = []
   29: 	for index in BOARD_SIZE:
   30: 		var card: Variant = UnitSpawnDefinitionScript.new()
   31: 		card.archetype_id = token_ids[rng.randi_range(0, token_ids.size() - 1)]
   32: 		card.owner_team_id = player_team_id
   33: 		card.visual_faction_id = player_team_id
   34: 		cards.append(card)
   35: 	manifest.input_log.append({
   36: 		"action": "roulette",
   37: 		"seed": rng.seed,
   38: 		"cards": cards.map(func(card): return card.to_dictionary()),
   39: 	})
```

<details><summary>Full content</summary>

```text
class_name RouletteService
extends RefCounted

const SPIN_COST := 20
const BOARD_SIZE := 9
const UnitSpawnDefinitionScript = preload("res://scripts/data/unit_spawn_definition.gd")
const DeterminismServiceScript = preload("res://scripts/core/determinism_service.gd")

var economy: Variant
var buildings: Variant
var manifest: Variant
var player_team_id: StringName


func _init(assigned_economy: Variant, assigned_buildings: Variant, assigned_manifest: Variant, assigned_player_team_id: StringName) -> void:
	economy = assigned_economy
	buildings = assigned_buildings
	manifest = assigned_manifest
	player_team_id = assigned_player_team_id


func spin(seed_input: Dictionary) -> Array[UnitSpawnDefinition]:
	if not economy.try_spend_gold(SPIN_COST):
		return []
	var spin_seed := int(seed_input.get("seed", manifest.seed))
	var rng: RandomNumberGenerator = DeterminismServiceScript.new(manifest.seed).create_roulette_rng(spin_seed)
	var token_ids: Array[StringName] = buildings.roulette_archetype_ids()
	var cards: Array = []
	for index in BOARD_SIZE:
		var card: Variant = UnitSpawnDefinitionScript.new()
		card.archetype_id = token_ids[rng.randi_range(0, token_ids.size() - 1)]
		card.owner_team_id = player_team_id
		card.visual_faction_id = player_team_id
		cards.append(card)
	manifest.input_log.append({
		"action": "roulette",
		"seed": rng.seed,
		"cards": cards.map(func(card): return card.to_dictionary()),
	})
	return cards

```

</details>

### `scripts/ui/stage_hud.gd` — ACTIVE / 71 lines

Hits: `spin` [13, 26, 29, 30], `roulette` [30]

```text
   10: 
   11: var run: Variant
   12: var _pending_cards: Array = []
   13: var _spin_index := 0
   14: 
   15: 
   16: func bind_run(assigned_run: Variant) -> void:
   23: 	_update_display()
   24: 
   25: 
   26: func _on_spin_pressed() -> void:
   27: 	if run == null:
   28: 		return
   29: 	_spin_index += 1
   30: 	_pending_cards = run.spin_roulette({"seed": _spin_index})
   31: 	_update_display()
   32: 
   33: 
```

<details><summary>Full content</summary>

```text
class_name StageHud
extends Control

@onready var _resource_label: Label = $ResourceLabel
@onready var _wave_label: Label = $WaveLabel
@onready var _omen_label: Label = $OmenLabel
@onready var _cards_label: Label = $CardsLabel
@onready var _result_label: Label = $ResultLabel
@onready var _retry_button: Button = $RetryButton

var run: Variant
var _pending_cards: Array = []
var _spin_index := 0


func bind_run(assigned_run: Variant) -> void:
	run = assigned_run
	_pending_cards.clear()
	_update_display()


func _process(_delta: float) -> void:
	_update_display()


func _on_spin_pressed() -> void:
	if run == null:
		return
	_spin_index += 1
	_pending_cards = run.spin_roulette({"seed": _spin_index})
	_update_display()


func _on_tower_pressed() -> void:
	if run != null:
		run.construct_home(&"tower")
	_update_display()


func _on_farm_pressed() -> void:
	if run != null:
		run.construct_home(&"farm")
	_update_display()


func _on_deploy_pressed(lane_id: StringName) -> void:
	if run == null or _pending_cards.is_empty():
		return
	var card: Variant = _pending_cards.front()
	if run.deploy_card(card, lane_id):
		_pending_cards.pop_front()
	_update_display()


func _on_retry_pressed() -> void:
	var session := get_node_or_null("../../GameSession")
	if session != null:
		session.retry_stage()


func _update_display() -> void:
	if run == null or run.economy == null:
		return
	_resource_label.text = "Gold %d   Food %d/%d" % [run.economy.gold, run.economy.food_used, run.economy.food_cap]
	_wave_label.text = "Wave %d" % run.current_wave
	var omen: float = float(run.wave_director.omen_seconds_remaining()) if run.wave_director != null else 0.0
	_omen_label.text = "Next omen %.0fs" % omen
	_cards_label.text = "Cards: %s" % ", ".join(_pending_cards.map(func(card: Variant) -> String: return str(card.archetype_id)))
	_result_label.visible = run.result_state != run.RUNNING
	_result_label.text = "Stage %s" % str(run.result_state).capitalize()
	_retry_button.visible = run.result_state != run.RUNNING

```

</details>

### `scripts/units/deployment_service.gd` — ACTIVE / 28 lines

Hits: `unitspawndefinition` [8, 16, 19]

```text
    5: 
    6: var economy: Variant
    7: var manifest: Variant
    8: var deployed_cards: Array[UnitSpawnDefinition] = []
    9: 
   10: 
   11: func _init(assigned_economy: Variant, assigned_manifest: Variant) -> void:
   13: 	manifest = assigned_manifest
   14: 
   15: 
   16: func deploy(card: UnitSpawnDefinition, lane_id: StringName, position: float) -> bool:
   17: 	if card == null or not LANE_IDS.has(lane_id) or not economy.try_reserve_food(card.food_cost):
   18: 		return false
   19: 	var deployed := card.duplicate() as UnitSpawnDefinition
   20: 	deployed.lane_id = lane_id
   21: 	deployed_cards.append(deployed)
   22: 	manifest.input_log.append({
```

<details><summary>Full content</summary>

```text
class_name DeploymentService
extends RefCounted

const LANE_IDS := [&"top", &"middle", &"bottom"]

var economy: Variant
var manifest: Variant
var deployed_cards: Array[UnitSpawnDefinition] = []


func _init(assigned_economy: Variant, assigned_manifest: Variant) -> void:
	economy = assigned_economy
	manifest = assigned_manifest


func deploy(card: UnitSpawnDefinition, lane_id: StringName, position: float) -> bool:
	if card == null or not LANE_IDS.has(lane_id) or not economy.try_reserve_food(card.food_cost):
		return false
	var deployed := card.duplicate() as UnitSpawnDefinition
	deployed.lane_id = lane_id
	deployed_cards.append(deployed)
	manifest.input_log.append({
		"action": "deploy",
		"lane_id": str(lane_id),
		"position": position,
		"card": deployed.to_dictionary(),
	})
	return true

```

</details>

### `skills/disciplines/02-game-design/SKILL.md` — REFERENCE_OR_OTHER / 35 lines

Hits: `룰렛` [9]

```text
    6: - Shared contract: `skills/SHARED_EXECUTION_CONTRACT.md`
    7: 
    8: ## 사용 조건
    9: - 게임 규칙·룰렛·전투
   10: - 경제·병종·밸런스
   11: 
   12: ## 사용하지 않는 조건
```

### `tests/README.md` — ACTIVE / 3 lines

Hits: `룰렛` [3]

```text
    1: # Tests placeholder
    2: 
    3: 엔진 선정 후 헤드리스 시뮬레이션, 데이터 검증, 상성 테스트, 룰렛 확률 테스트의 실행 방법을 이곳에 기록합니다.
```

<details><summary>Full content</summary>

```text
# Tests placeholder

엔진 선정 후 헤드리스 시뮬레이션, 데이터 검증, 상성 테스트, 룰렛 확률 테스트의 실행 방법을 이곳에 기록합니다.

```

</details>

### `tests/headless/battle_simulation_test.gd` — ACTIVE / 163 lines

Hits: `unitspawndefinition` [4, 143, 144]

```text
    1: extends SceneTree
    2: 
    3: const DataRegistry = preload("res://scripts/core/data_registry.gd")
    4: const UnitSpawnDefinition = preload("res://scripts/data/unit_spawn_definition.gd")
    5: 
    6: const BOOTSTRAP_CATALOG_PATH := "res://data/bootstrap_catalog.tres"
    7: const BATTLE_SIMULATOR_PATH := "res://scripts/battle/battle_simulator.gd"
  140: 	return registry
  141: 
  142: 
  143: func _spawn(visual_faction_id: StringName, lane_id: StringName, archetype_id: StringName = &"shield_guard") -> UnitSpawnDefinition:
  144: 	var spawn := UnitSpawnDefinition.new()
  145: 	spawn.archetype_id = archetype_id
  146: 	spawn.owner_team_id = visual_faction_id
  147: 	spawn.visual_faction_id = visual_faction_id
```

<details><summary>Full content</summary>

```text
extends SceneTree

const DataRegistry = preload("res://scripts/core/data_registry.gd")
const UnitSpawnDefinition = preload("res://scripts/data/unit_spawn_definition.gd")

const BOOTSTRAP_CATALOG_PATH := "res://data/bootstrap_catalog.tres"
const BATTLE_SIMULATOR_PATH := "res://scripts/battle/battle_simulator.gd"
const GATE_STATE_PATH := "res://scripts/battle/gate_state.gd"
const OUTPOST_STATE_PATH := "res://scripts/battle/outpost_state.gd"


func _init() -> void:
	var failures := PackedStringArray()
	var simulator_script := load(BATTLE_SIMULATOR_PATH)
	var gate_script := load(GATE_STATE_PATH)
	var outpost_script := load(OUTPOST_STATE_PATH)
	_expect(simulator_script != null, "battle simulator script exists", failures)
	_expect(gate_script != null, "gate state script exists", failures)
	_expect(outpost_script != null, "outpost state script exists", failures)
	if simulator_script != null:
		_test_shared_stats_and_lane_isolation(simulator_script, failures)
		_test_fixed_seed_snapshot_repeatability(simulator_script, failures)
	if gate_script != null:
		_test_gate_multipliers_and_collapse(gate_script, failures)
	if outpost_script != null:
		_test_outpost_capture_sequence(outpost_script, failures)
		_test_outpost_capture_power_scaling(outpost_script, failures)
		_test_outpost_capture_power_requires_discrete_values(outpost_script, failures)
		_test_outpost_exit_hold_and_reversion(outpost_script, failures)
	_finish(failures)


func _test_shared_stats_and_lane_isolation(simulator_script: GDScript, failures: PackedStringArray) -> void:
	var simulator: Variant = simulator_script.new(_registry(), 91)
	var registry: Variant = _registry()
	for archetype in registry.catalog.archetypes:
		var public_stats: Variant = archetype.get("base_stats")
		_expect(public_stats is Dictionary and not public_stats.is_empty(), "%s exposes public base combat stats" % archetype.archetype_id, failures)
		var lumern: Variant = simulator.spawn_unit(_spawn(&"lumern", &"top", archetype.archetype_id))
		var veil: Variant = simulator.spawn_unit(_spawn(&"veil", &"top", archetype.archetype_id))
		_expect(lumern.combat_stats() == veil.combat_stats(), "%s visual faction does not alter combat stats" % archetype.archetype_id, failures)
		if public_stats is Dictionary:
			_expect(lumern.combat_stats() == public_stats, "%s unit stats derive from public profile data" % archetype.archetype_id, failures)
	var lumern: Variant = simulator.lanes[&"top"].units[0]
	_expect(not simulator.request_lane_move(lumern, &"middle"), "ordinary top lane units cannot move to middle", failures)
	_expect(lumern.lane_id == &"top", "rejected lane movement preserves the original lane", failures)
	_expect(simulator.lanes[&"middle"].units.is_empty(), "middle lane does not own top lane units", failures)


func _test_gate_multipliers_and_collapse(gate_script: GDScript, failures: PackedStringArray) -> void:
	var gate: Variant = gate_script.new()
	var expected_normal := 1000.0 * 0.4 * 100.0 / 180.0
	_expect(is_equal_approx(gate.apply_damage(1000.0, false), expected_normal), "normal damage uses the 0.4 structure multiplier and 80 resistance", failures)
	var expected_siege := 1000.0 * 2.0 * 100.0 / 180.0
	_expect(is_equal_approx(gate.apply_damage(1000.0, true), expected_siege), "siege damage uses the 2.0 structure multiplier and 80 resistance", failures)
	gate.apply_damage(100000.0, true)
	_expect(gate.is_collapsing(), "destroyed gate enters the two-second collapse state", failures)
	gate.advance(1.9)
	_expect(not gate.is_collapsed(), "gate does not collapse before two seconds", failures)
	gate.advance(0.1)
	_expect(gate.is_collapsed(), "gate collapses after two seconds", failures)


func _test_outpost_capture_sequence(outpost_script: GDScript, failures: PackedStringArray) -> void:
	var outpost: Variant = outpost_script.new(&"veil", true)
	outpost.begin_capture(&"lumern", 2.0)
	_expect(outpost.construction_locked and not outpost.existing_buildings_enabled, "capture start locks construction and disables existing buildings", failures)
	outpost.advance(5.0)
	_expect(outpost.state == outpost.CAPTURING, "power two neutralizes an outpost in five seconds", failures)
	outpost.advance(5.0)
	_expect(outpost.owner_team_id == &"lumern", "capture completion assigns the new owner", failures)
	_expect(outpost.prior_building_ruined, "capture completion ruins the prior building", failures)
	_expect(outpost.state == outpost.STABILIZING, "capture completion begins stabilization", failures)
	outpost.advance(5.0)
	_expect(outpost.state == outpost.STABLE and not outpost.construction_locked, "five-second stabilization unlocks new-owner construction", failures)


func _test_outpost_capture_power_scaling(outpost_script: GDScript, failures: PackedStringArray) -> void:
	var zero_power: Variant = outpost_script.new(&"veil")
	zero_power.begin_capture(&"lumern", 0.0)
	zero_power.advance(20.0)
	_expect(zero_power.state == zero_power.NEUTRALIZING, "capture power zero does not progress neutralization", failures)
	_expect(is_equal_approx(float(zero_power.snapshot().get("capture_progress", -1.0)), 0.0), "capture power zero keeps progress at zero", failures)
	var one_power: Variant = outpost_script.new(&"veil")
	one_power.begin_capture(&"lumern", 1.0)
	one_power.advance(9.9)
	_expect(one_power.state == one_power.NEUTRALIZING, "capture power one has not neutralized before ten seconds", failures)
	one_power.advance(0.1)
	_expect(one_power.state == one_power.CAPTURING, "capture power one neutralizes in ten seconds", failures)
	var two_power: Variant = outpost_script.new(&"veil")
	two_power.begin_capture(&"lumern", 2.0)
	two_power.advance(4.9)
	_expect(two_power.state == two_power.NEUTRALIZING, "capture power two has not neutralized before five seconds", failures)
	two_power.advance(0.1)
	_expect(two_power.state == two_power.CAPTURING, "capture power two neutralizes in five seconds", failures)


func _test_outpost_capture_power_requires_discrete_values(outpost_script: GDScript, failures: PackedStringArray) -> void:
	for invalid_power in [0.5, 1.5]:
		var outpost: Variant = outpost_script.new(&"veil")
		outpost.begin_capture(&"lumern", invalid_power)
		outpost.advance(20.0)
		_expect(is_equal_approx(float(outpost.snapshot().get("capture_power", -1.0)), 0.0), "capture power %s normalizes to zero" % invalid_power, failures)
		_expect(is_equal_approx(float(outpost.snapshot().get("capture_progress", -1.0)), 0.0), "capture power %s does not drive capture progress" % invalid_power, failures)


func _test_outpost_exit_hold_and_reversion(outpost_script: GDScript, failures: PackedStringArray) -> void:
	var outpost: Variant = outpost_script.new(&"veil", true)
	outpost.begin_capture(&"lumern", 2.0)
	outpost.advance(5.0)
	outpost.lose_capture_power(20.0)
	_expect(outpost.state == outpost.CAPTURING, "capturer exit does not immediately discard capture progress", failures)
	_expect(is_equal_approx(float(outpost.snapshot().get("capture_progress", -1.0)), 1.0), "capturer exit preserves capture progress during the hold", failures)
	outpost.advance(3.0)
	_expect(is_equal_approx(float(outpost.snapshot().get("capture_progress", -1.0)), 1.0), "capture progress remains frozen for the three-second exit hold", failures)
	outpost.advance(1.0)
	_expect(is_equal_approx(float(outpost.snapshot().get("capture_progress", -1.0)), 0.9), "capture progress reverts at ten percent per second after the hold", failures)
	outpost.advance(9.0)
	_expect(outpost.state == outpost.STABLE and outpost.owner_team_id == &"veil", "fully reverted capture restores the previous stable owner", failures)
	_expect(not outpost.construction_locked and outpost.existing_buildings_enabled, "fully reverted capture restores the previous stable building state", failures)


func _test_fixed_seed_snapshot_repeatability(simulator_script: GDScript, failures: PackedStringArray) -> void:
	var first: Variant = simulator_script.new(_registry(), 314159)
	var second: Variant = simulator_script.new(_registry(), 314159)
	for simulator in [first, second]:
		simulator.spawn_unit(_spawn(&"lumern", &"top"))
		simulator.spawn_unit(_spawn(&"veil", &"top"))
		simulator.spawn_unit(_spawn(&"lumern", &"bottom", &"archer"))
		for _step in 20:
			simulator.advance(0.1)
	_expect(JSON.stringify(first.snapshot()) == JSON.stringify(second.snapshot()), "identical seeds and inputs reproduce the same battle snapshot", failures)


func _registry() -> Variant:
	var registry: Variant = DataRegistry.new()
	var errors: PackedStringArray = registry.load_bootstrap_catalog(BOOTSTRAP_CATALOG_PATH)
	if not errors.is_empty():
		push_error("battle test registry failed to load: %s" % errors)
	return registry


func _spawn(visual_faction_id: StringName, lane_id: StringName, archetype_id: StringName = &"shield_guard") -> UnitSpawnDefinition:
	var spawn := UnitSpawnDefinition.new()
	spawn.archetype_id = archetype_id
	spawn.owner_team_id = visual_faction_id
	spawn.visual_faction_id = visual_faction_id
	spawn.lane_id = lane_id
	return spawn


func _expect(condition: bool, message: String, failures: PackedStringArray) -> void:
	if not condition:
		failures.append(message)


func _finish(failures: PackedStringArray) -> void:
	if failures.is_empty():
		print("Battle simulation checks passed")
		quit(0)
	else:
		printerr("Battle simulation failures:\n%s" % "\n".join(failures))
		quit(1)

```

</details>

### `tests/headless/economy_roulette_test.gd` — ACTIVE / 128 lines

Hits: `unitspawndefinition` [5, 74, 75, 93, 114], `roulette` [12, 16, 23, 24, 62, 69, 70, 71, 72, 74, 76, 83, 84, 85, 86, 124, 127], `spin` [70, 71, 72, 84]

```text
    2: 
    3: const StageManifest = preload("res://scripts/core/stage_manifest.gd")
    4: const OutpostState = preload("res://scripts/battle/outpost_state.gd")
    5: const UnitSpawnDefinition = preload("res://scripts/data/unit_spawn_definition.gd")
    6: 
    7: 
    8: func _init() -> void:
    9: 	var failures := PackedStringArray()
   10: 	var economy_script := load("res://scripts/core/stage_economy.gd")
   11: 	var building_service_script := load("res://scripts/buildings/building_service.gd")
   12: 	var roulette_script := load("res://scripts/roulette/roulette_service.gd")
   13: 	var deployment_script := load("res://scripts/units/deployment_service.gd")
   14: 	_expect(economy_script != null, "stage economy service exists", failures)
   15: 	_expect(building_service_script != null, "building service exists", failures)
   16: 	_expect(roulette_script != null, "roulette service exists", failures)
   17: 	_expect(deployment_script != null, "deployment service exists", failures)
   18: 	if economy_script != null:
   19: 		_test_stage_economy(economy_script, failures)
   20: 	if economy_script != null and building_service_script != null:
   21: 		_test_building_ownership_and_capture_lock(economy_script, building_service_script, failures)
   22: 		_test_stabilized_capture_allows_rebuilding(economy_script, building_service_script, failures)
   23: 	if economy_script != null and building_service_script != null and roulette_script != null:
   24: 		_test_deterministic_nine_cell_roulette(economy_script, building_service_script, roulette_script, failures)
   25: 	if economy_script != null and deployment_script != null:
   26: 		_test_deployment_food_limit(economy_script, deployment_script, failures)
   27: 	_finish(failures)
   59: 	_expect(buildings.try_construct(&"captured_top", &"front_a", &"farm"), "a captured outpost accepts new construction after stabilization", failures)
   60: 
   61: 
   62: func _test_deterministic_nine_cell_roulette(economy_script: GDScript, building_service_script: GDScript, roulette_script: GDScript, failures: PackedStringArray) -> void:
   63: 	var first_manifest := _manifest()
   64: 	var first_economy: Variant = economy_script.new(first_manifest)
   65: 	var first_buildings: Variant = building_service_script.new(first_economy, first_manifest)
   66: 	var outpost := OutpostState.new(&"lumern")
   67: 	first_buildings.register_outpost(&"player_top", outpost, [&"front_a"])
   68: 	first_buildings.try_construct(&"player_top", &"front_a", &"tower")
   69: 	var first_roulette: Variant = roulette_script.new(first_economy, first_buildings, first_manifest, &"lumern")
   70: 	var first_result: Array = first_roulette.spin({"seed": 12})
   71: 	_expect(first_result.size() == 9, "a paid roulette spin resolves a deterministic 3x3 board", failures)
   72: 	_expect(first_economy.gold == 105, "tower construction and one roulette spin charge their approved costs", failures)
   73: 	for card in first_result:
   74: 		_expect(card is UnitSpawnDefinition, "roulette produces only shared unit spawn definitions", failures)
   75: 		if card is UnitSpawnDefinition:
   76: 			_expect(card.owner_team_id == &"lumern" and card.visual_faction_id == &"lumern", "roulette cards use the player shared-unit faction contract", failures)
   77: 	var second_manifest := _manifest()
   78: 	var second_economy: Variant = economy_script.new(second_manifest)
   79: 	var second_buildings: Variant = building_service_script.new(second_economy, second_manifest)
   80: 	var second_outpost := OutpostState.new(&"lumern")
   81: 	second_buildings.register_outpost(&"player_top", second_outpost, [&"front_a"])
   82: 	second_buildings.try_construct(&"player_top", &"front_a", &"tower")
   83: 	var second_roulette: Variant = roulette_script.new(second_economy, second_buildings, second_manifest, &"lumern")
   84: 	var second_result: Array = second_roulette.spin({"seed": 12})
   85: 	_expect(_cards_json(first_result) == _cards_json(second_result), "identical roulette seed and build inputs reproduce identical cards", failures)
   86: 	_expect(first_manifest.input_log.size() == 2, "construction and roulette commands are recorded in the manifest", failures)
   87: 
   88: 
   89: func _test_deployment_food_limit(economy_script: GDScript, deployment_script: GDScript, failures: PackedStringArray) -> void:
   90: 	var manifest := _manifest()
   91: 	var economy: Variant = economy_script.new(manifest)
   92: 	var deployment: Variant = deployment_script.new(economy, manifest)
   93: 	var card := UnitSpawnDefinition.new()
   94: 	card.archetype_id = &"shield_guard"
   95: 	card.owner_team_id = &"lumern"
   96: 	card.visual_faction_id = &"lumern"
  111: 
  112: 
  113: func _cards_json(cards: Array) -> String:
  114: 	return JSON.stringify(cards.map(func(card: UnitSpawnDefinition) -> Dictionary: return card.to_dictionary()))
  115: 
  116: 
  117: func _expect(condition: bool, message: String, failures: PackedStringArray) -> void:
  121: 
  122: func _finish(failures: PackedStringArray) -> void:
  123: 	if failures.is_empty():
  124: 		print("Economy, construction, roulette, and deployment checks passed")
  125: 		quit(0)
  126: 	else:
  127: 		printerr("Economy, construction, roulette, and deployment failures:\n%s" % "\n".join(failures))
  128: 		quit(1)
```

<details><summary>Full content</summary>

```text
extends SceneTree

const StageManifest = preload("res://scripts/core/stage_manifest.gd")
const OutpostState = preload("res://scripts/battle/outpost_state.gd")
const UnitSpawnDefinition = preload("res://scripts/data/unit_spawn_definition.gd")


func _init() -> void:
	var failures := PackedStringArray()
	var economy_script := load("res://scripts/core/stage_economy.gd")
	var building_service_script := load("res://scripts/buildings/building_service.gd")
	var roulette_script := load("res://scripts/roulette/roulette_service.gd")
	var deployment_script := load("res://scripts/units/deployment_service.gd")
	_expect(economy_script != null, "stage economy service exists", failures)
	_expect(building_service_script != null, "building service exists", failures)
	_expect(roulette_script != null, "roulette service exists", failures)
	_expect(deployment_script != null, "deployment service exists", failures)
	if economy_script != null:
		_test_stage_economy(economy_script, failures)
	if economy_script != null and building_service_script != null:
		_test_building_ownership_and_capture_lock(economy_script, building_service_script, failures)
		_test_stabilized_capture_allows_rebuilding(economy_script, building_service_script, failures)
	if economy_script != null and building_service_script != null and roulette_script != null:
		_test_deterministic_nine_cell_roulette(economy_script, building_service_script, roulette_script, failures)
	if economy_script != null and deployment_script != null:
		_test_deployment_food_limit(economy_script, deployment_script, failures)
	_finish(failures)


func _test_stage_economy(economy_script: GDScript, failures: PackedStringArray) -> void:
	var economy: Variant = economy_script.new(_manifest())
	_expect(economy.gold == 160, "regular stage starts at 160 gold", failures)
	_expect(economy.food_cap == 12, "regular stage starts with 12 food", failures)
	economy.advance(60.0, 1, 1)
	_expect(economy.gold == 183, "active combat grants base, controlled clash, and stable outpost income on their exact intervals", failures)


func _test_building_ownership_and_capture_lock(economy_script: GDScript, building_service_script: GDScript, failures: PackedStringArray) -> void:
	var economy: Variant = economy_script.new(_manifest())
	var buildings: Variant = building_service_script.new(economy, _manifest())
	var enemy_outpost := OutpostState.new(&"veil")
	var player_outpost := OutpostState.new(&"lumern")
	buildings.register_outpost(&"enemy_top", enemy_outpost, [&"front_a"])
	buildings.register_outpost(&"player_top", player_outpost, [&"front_a", &"front_b"])
	_expect(not buildings.try_construct(&"enemy_top", &"front_a", &"tower"), "enemy-owned node rejects player building", failures)
	_expect(buildings.try_construct(&"player_top", &"front_a", &"tower"), "owned stabilized outpost accepts a tower", failures)
	player_outpost.begin_capture(&"veil", 1.0)
	_expect(not buildings.try_construct(&"player_top", &"front_b", &"farm"), "capture locks construction nodes", failures)


func _test_stabilized_capture_allows_rebuilding(economy_script: GDScript, building_service_script: GDScript, failures: PackedStringArray) -> void:
	var economy: Variant = economy_script.new(_manifest())
	var buildings: Variant = building_service_script.new(economy, _manifest())
	var outpost := OutpostState.new(&"veil", true)
	buildings.register_outpost(&"captured_top", outpost, [&"front_a"])
	outpost.begin_capture(&"lumern", 1.0)
	outpost.advance(20.0)
	outpost.advance(5.0)
	_expect(buildings.try_construct(&"captured_top", &"front_a", &"farm"), "a captured outpost accepts new construction after stabilization", failures)


func _test_deterministic_nine_cell_roulette(economy_script: GDScript, building_service_script: GDScript, roulette_script: GDScript, failures: PackedStringArray) -> void:
	var first_manifest := _manifest()
	var first_economy: Variant = economy_script.new(first_manifest)
	var first_buildings: Variant = building_service_script.new(first_economy, first_manifest)
	var outpost := OutpostState.new(&"lumern")
	first_buildings.register_outpost(&"player_top", outpost, [&"front_a"])
	first_buildings.try_construct(&"player_top", &"front_a", &"tower")
	var first_roulette: Variant = roulette_script.new(first_economy, first_buildings, first_manifest, &"lumern")
	var first_result: Array = first_roulette.spin({"seed": 12})
	_expect(first_result.size() == 9, "a paid roulette spin resolves a deterministic 3x3 board", failures)
	_expect(first_economy.gold == 105, "tower construction and one roulette spin charge their approved costs", failures)
	for card in first_result:
		_expect(card is UnitSpawnDefinition, "roulette produces only shared unit spawn definitions", failures)
		if card is UnitSpawnDefinition:
			_expect(card.owner_team_id == &"lumern" and card.visual_faction_id == &"lumern", "roulette cards use the player shared-unit faction contract", failures)
	var second_manifest := _manifest()
	var second_economy: Variant = economy_script.new(second_manifest)
	var second_buildings: Variant = building_service_script.new(second_economy, second_manifest)
	var second_outpost := OutpostState.new(&"lumern")
	second_buildings.register_outpost(&"player_top", second_outpost, [&"front_a"])
	second_buildings.try_construct(&"player_top", &"front_a", &"tower")
	var second_roulette: Variant = roulette_script.new(second_economy, second_buildings, second_manifest, &"lumern")
	var second_result: Array = second_roulette.spin({"seed": 12})
	_expect(_cards_json(first_result) == _cards_json(second_result), "identical roulette seed and build inputs reproduce identical cards", failures)
	_expect(first_manifest.input_log.size() == 2, "construction and roulette commands are recorded in the manifest", failures)


func _test_deployment_food_limit(economy_script: GDScript, deployment_script: GDScript, failures: PackedStringArray) -> void:
	var manifest := _manifest()
	var economy: Variant = economy_script.new(manifest)
	var deployment: Variant = deployment_script.new(economy, manifest)
	var card := UnitSpawnDefinition.new()
	card.archetype_id = &"shield_guard"
	card.owner_team_id = &"lumern"
	card.visual_faction_id = &"lumern"
	card.food_cost = 12
	_expect(deployment.deploy(card, &"top", 10.0), "deployment reserves available food", failures)
	_expect(not deployment.deploy(card, &"top", 20.0), "deployment rejects cards that exceed the food cap", failures)
	_expect(economy.food_used == 12, "rejected deployment does not spend additional food", failures)
	_expect(manifest.input_log.size() == 1, "only accepted deployment commands are recorded", failures)


func _manifest() -> StageManifest:
	var manifest := StageManifest.new()
	manifest.stage_id = "regular_stage"
	manifest.seed = 101
	manifest.starting_gold = 160
	manifest.starting_food_cap = 12
	return manifest


func _cards_json(cards: Array) -> String:
	return JSON.stringify(cards.map(func(card: UnitSpawnDefinition) -> Dictionary: return card.to_dictionary()))


func _expect(condition: bool, message: String, failures: PackedStringArray) -> void:
	if not condition:
		failures.append(message)


func _finish(failures: PackedStringArray) -> void:
	if failures.is_empty():
		print("Economy, construction, roulette, and deployment checks passed")
		quit(0)
	else:
		printerr("Economy, construction, roulette, and deployment failures:\n%s" % "\n".join(failures))
		quit(1)

```

</details>

### `tests/headless/stage_data_contract_test.gd` — ACTIVE / 152 lines

Hits: `unitspawndefinition` [6, 140]

```text
    3: const BootstrapValidator = preload("res://scripts/core/bootstrap_validator.gd")
    4: const DataRegistry = preload("res://scripts/core/data_registry.gd")
    5: const StageDefinition = preload("res://scripts/data/stage_definition.gd")
    6: const UnitSpawnDefinition = preload("res://scripts/data/unit_spawn_definition.gd")
    7: 
    8: const BOOTSTRAP_CATALOG_PATH := "res://data/bootstrap_catalog.tres"
    9: const TUTORIAL_STAGE_PATH := "res://data/stages/tutorial_stage.tres"
  137: 	if not load_errors.is_empty():
  138: 		return load_errors
  139: 	var regular: StageDefinition = registry.stage_definition(&"regular_stage").duplicate(true) as StageDefinition
  140: 	var spawn: UnitSpawnDefinition = regular.waves[0].spawns[0].duplicate() as UnitSpawnDefinition
  141: 	spawn.archetype_id = archetype_id
  142: 	spawn.visual_faction_id = visual_faction_id
  143: 	spawn.owner_team_id = owner_team_id
```

<details><summary>Full content</summary>

```text
extends SceneTree

const BootstrapValidator = preload("res://scripts/core/bootstrap_validator.gd")
const DataRegistry = preload("res://scripts/core/data_registry.gd")
const StageDefinition = preload("res://scripts/data/stage_definition.gd")
const UnitSpawnDefinition = preload("res://scripts/data/unit_spawn_definition.gd")

const BOOTSTRAP_CATALOG_PATH := "res://data/bootstrap_catalog.tres"
const TUTORIAL_STAGE_PATH := "res://data/stages/tutorial_stage.tres"
const REGULAR_STAGE_PATH := "res://data/stages/regular_stage.tres"
const MANIFEST_SEED := 20260716


func _init() -> void:
	var failures := PackedStringArray()
	var tutorial := ResourceLoader.load(TUTORIAL_STAGE_PATH)
	var regular := ResourceLoader.load(REGULAR_STAGE_PATH)

	_expect(tutorial != null, "tutorial stage resource must load", failures)
	_expect(regular != null, "regular stage resource must load", failures)
	if tutorial != null:
		_expect(_waves(tutorial).size() == 4, "tutorial has four waves", failures)
	if regular != null:
		var waves := _waves(regular)
		_expect(waves.size() == 20, "regular stage has W1 through W20", failures)
		if waves.size() >= 20:
			_expect(waves[14].boss_kind == &"legendary", "W15 is legendary", failures)
			_expect(waves[19].boss_kind == &"mythic", "W20 is mythic", failures)
		_assert_regular_manifest_contract(regular as StageDefinition, failures)

	_expect_spawn_is_rejected(
		&"enemy_only",
		&"veil",
		&"veil",
		&"top",
		"unknown spawn archetype IDs are rejected",
		"unknown spawn archetype_id: enemy_only",
		failures,
	)
	_expect_spawn_is_rejected(
		&"shield_guard",
		&"other",
		&"veil",
		&"top",
		"non-lumern/veil visual factions are rejected",
		"invalid spawn visual_faction_id: other",
		failures,
	)
	_expect_spawn_is_rejected(
		&"shield_guard",
		&"veil",
		&"other",
		&"top",
		"non-lumern/veil owner teams are rejected",
		"invalid spawn owner_team_id: other",
		failures,
	)
	_expect_spawn_is_rejected(
		&"shield_guard",
		&"veil",
		&"veil",
		&"side",
		"lane IDs outside top/middle/bottom are rejected",
		"invalid spawn lane_id: side",
		failures,
	)

	if failures.is_empty():
		print("Stage data contract checks passed")
		quit(0)
	else:
		printerr("Stage data contract failures:\n%s" % "\n".join(failures))
		quit(1)


func _waves(stage: Resource) -> Array:
	return stage.get("waves") as Array


func _assert_regular_manifest_contract(regular: StageDefinition, failures: PackedStringArray) -> void:
	var parsed: Variant = JSON.parse_string(regular.build_manifest(MANIFEST_SEED).to_json())
	_expect(parsed is Dictionary, "regular manifest JSON parses into an object", failures)
	if not parsed is Dictionary:
		return
	var manifest: Dictionary = parsed
	_expect(manifest.get("stage_id") == "regular_stage", "manifest includes the regular stage ID", failures)
	_expect(manifest.get("seed") == MANIFEST_SEED, "manifest includes the supplied seed", failures)
	_expect(manifest.get("starting_gold") == 160, "manifest includes starting gold", failures)
	_expect(manifest.get("starting_food_cap") == 12, "manifest includes starting food cap", failures)
	_expect(manifest.get("tutorial_stage") == false, "manifest identifies the regular stage", failures)
	_expect(manifest.get("wave_count") == 20, "manifest includes the regular wave count", failures)

	var manifest_waves: Array = manifest.get("waves", []) as Array
	_expect(manifest_waves.size() == 20, "manifest includes resolved waves", failures)
	if manifest_waves.is_empty():
		return
	var first_wave: Dictionary = manifest_waves[0] as Dictionary
	_expect(first_wave.has("wave_number"), "manifest wave includes a wave number", failures)
	_expect(first_wave.has("omen_lead_seconds"), "manifest wave includes omen lead time", failures)
	_expect(first_wave.has("boss_kind"), "manifest wave includes boss kind", failures)
	_expect(first_wave.has("is_overtime"), "manifest wave includes overtime state", failures)
	var spawns: Array = first_wave.get("spawns", []) as Array
	_expect(not spawns.is_empty(), "manifest wave includes resolved spawns", failures)
	if spawns.is_empty():
		return
	var first_spawn: Dictionary = spawns[0] as Dictionary
	for field in ["archetype_id", "tier_id", "rank_id", "owner_team_id", "visual_faction_id", "lane_id", "spawn_delay_seconds"]:
		_expect(first_spawn.has(field), "manifest spawn includes %s" % field, failures)

	var input_log: Variant = manifest.get("input_log")
	_expect(input_log is Array, "manifest includes an input log array", failures)
	if input_log is Array:
		_expect((input_log as Array).is_empty(), "new manifests begin with an empty input log", failures)


func _expect_spawn_is_rejected(
	archetype_id: StringName,
	visual_faction_id: StringName,
	owner_team_id: StringName,
	lane_id: StringName,
	message: String,
	expected_error: String,
	failures: PackedStringArray,
) -> void:
	var errors := _invalid_spawn_errors(archetype_id, visual_faction_id, owner_team_id, lane_id)
	_expect(errors.has(expected_error), "%s: %s" % [message, errors], failures)


func _invalid_spawn_errors(
	archetype_id: StringName,
	visual_faction_id: StringName,
	owner_team_id: StringName,
	lane_id: StringName,
) -> PackedStringArray:
	var registry: DataRegistry = DataRegistry.new()
	var load_errors := registry.load_bootstrap_catalog(BOOTSTRAP_CATALOG_PATH)
	if not load_errors.is_empty():
		return load_errors
	var regular: StageDefinition = registry.stage_definition(&"regular_stage").duplicate(true) as StageDefinition
	var spawn: UnitSpawnDefinition = regular.waves[0].spawns[0].duplicate() as UnitSpawnDefinition
	spawn.archetype_id = archetype_id
	spawn.visual_faction_id = visual_faction_id
	spawn.owner_team_id = owner_team_id
	spawn.lane_id = lane_id
	regular.waves[0].spawns[0] = spawn
	registry.stages[str(regular.stage_id)] = regular
	return BootstrapValidator.new().validate_registry(registry)


func _expect(condition: bool, message: String, failures: PackedStringArray) -> void:
	if not condition:
		failures.append(message)

```

</details>

### `tests/headless/stage_run_test.gd` — ACTIVE / 109 lines

Hits: `unitspawndefinition` [4, 71]

```text
    1: extends SceneTree
    2: 
    3: const DataRegistry = preload("res://scripts/core/data_registry.gd")
    4: const UnitSpawnDefinition = preload("res://scripts/data/unit_spawn_definition.gd")
    5: 
    6: const BOOTSTRAP_CATALOG_PATH := "res://data/bootstrap_catalog.tres"
    7: const TUTORIAL_STAGE_PATH := "res://data/stages/tutorial_stage.tres"
   68: 
   69: func _test_assassin_bypass_leaves_and_returns_to_same_lane(battle_script: GDScript, failures: PackedStringArray) -> void:
   70: 	var battle: Variant = battle_script.new(_registry(), 11)
   71: 	var assassin := UnitSpawnDefinition.new()
   72: 	assassin.archetype_id = &"assassin"
   73: 	assassin.owner_team_id = &"lumern"
   74: 	assassin.visual_faction_id = &"lumern"
```

<details><summary>Full content</summary>

```text
extends SceneTree

const DataRegistry = preload("res://scripts/core/data_registry.gd")
const UnitSpawnDefinition = preload("res://scripts/data/unit_spawn_definition.gd")

const BOOTSTRAP_CATALOG_PATH := "res://data/bootstrap_catalog.tres"
const TUTORIAL_STAGE_PATH := "res://data/stages/tutorial_stage.tres"
const REGULAR_STAGE_PATH := "res://data/stages/regular_stage.tres"


func _init() -> void:
	var failures := PackedStringArray()
	var stage_run_script := load("res://scripts/core/stage_run.gd")
	var progression_script := load("res://scripts/core/stage_progression.gd")
	var wave_director_script := load("res://scripts/waves/wave_director.gd")
	var bypass_script := load("res://scripts/battle/assassin_bypass_state.gd")
	var battle_script := load("res://scripts/battle/battle_simulator.gd")
	_expect(stage_run_script != null, "stage run service exists", failures)
	_expect(progression_script != null, "stage progression service exists", failures)
	_expect(wave_director_script != null, "wave director service exists", failures)
	_expect(bypass_script != null, "assassin bypass state exists", failures)
	if stage_run_script != null and progression_script != null:
		_test_tutorial_unlock_and_regular_wave_progression(stage_run_script, progression_script, failures)
	if bypass_script != null:
		_test_assassin_bypass_timing(bypass_script, failures)
	if bypass_script != null and battle_script != null:
		_test_assassin_bypass_leaves_and_returns_to_same_lane(battle_script, failures)
	_finish(failures)


func _test_tutorial_unlock_and_regular_wave_progression(stage_run_script: GDScript, progression_script: GDScript, failures: PackedStringArray) -> void:
	var tutorial: Resource = ResourceLoader.load(TUTORIAL_STAGE_PATH)
	var regular: Resource = ResourceLoader.load(REGULAR_STAGE_PATH)
	var progression: Variant = progression_script.new()
	var run: Variant = stage_run_script.new(progression)
	run.start(tutorial, 1001)
	_expect(run.result_state == &"running", "stage run begins with the tutorial", failures)
	_advance_waves(run, 4)
	_expect(run.current_wave == 4, "tutorial reaches W4 from its declared data", failures)
	run.submit_command({"action": "stage_victory"})
	_expect(progression.regular_unlocked, "tutorial victory unlocks the regular stage for this session", failures)
	run.start(regular, 1001)
	_advance_waves(run, 15)
	_expect(run.current_wave == 15, "regular progression reaches W15", failures)
	_expect(run.wave_director.current_wave().boss_kind == &"legendary", "W15 uses the existing legendary wave definition", failures)
	_advance_waves(run, 16)
	for wave_number in range(16, 20):
		_expect(run.wave_director.wave_at(wave_number).is_overtime, "W%s is marked as overtime" % wave_number, failures)
	_advance_waves(run, 20)
	_expect(run.current_wave == 20, "regular progression reaches W20", failures)
	_expect(run.wave_director.current_wave().boss_kind == &"mythic", "W20 uses the existing mythic wave definition", failures)


func _test_assassin_bypass_timing(bypass_script: GDScript, failures: PackedStringArray) -> void:
	var bypass: Variant = bypass_script.new(&"middle", 500.0)
	_expect(bypass.capture_power == 0.0, "assassin bypass never contributes capture power", failures)
	bypass.advance(1.0)
	_expect(bypass.state == &"travel", "assassin enters travel after one second of windup", failures)
	bypass.advance(6.49)
	_expect(not bypass.warning_active, "warning remains hidden until 2.5 seconds before arrival", failures)
	bypass.advance(0.01)
	_expect(bypass.warning_active, "warning activates exactly 2.5 seconds before arrival", failures)
	bypass.advance(2.5)
	_expect(bypass.state == &"recovery" and bypass.exit_position == 620.0, "assassin arrives in the same lane 120 units behind the enemy outpost", failures)
	bypass.advance(0.6)
	_expect(bypass.is_complete(), "assassin completes the required 0.6 second arrival recovery", failures)


func _test_assassin_bypass_leaves_and_returns_to_same_lane(battle_script: GDScript, failures: PackedStringArray) -> void:
	var battle: Variant = battle_script.new(_registry(), 11)
	var assassin := UnitSpawnDefinition.new()
	assassin.archetype_id = &"assassin"
	assassin.owner_team_id = &"lumern"
	assassin.visual_faction_id = &"lumern"
	assassin.lane_id = &"bottom"
	var unit: Variant = battle.spawn_unit(assassin)
	_expect(battle.request_assassin_bypass(unit, 100.0), "assassin can start its same-lane bypass", failures)
	battle.advance(1.0)
	_expect(battle.lanes[&"bottom"].units.is_empty(), "assassin is removed from the lane during travel", failures)
	battle.advance(9.6)
	_expect(battle.lanes[&"bottom"].units.size() == 1, "assassin returns to its original lane after recovery", failures)
	_expect(is_equal_approx(float(battle.lanes[&"bottom"].units[0].lane_position), 220.0), "assassin returns behind the same lane enemy outpost", failures)


func _advance_waves(run: Variant, target_wave: int) -> void:
	while run.current_wave < target_wave:
		run.advance(60.0)


func _registry() -> Variant:
	var registry := DataRegistry.new()
	var errors: PackedStringArray = registry.load_bootstrap_catalog(BOOTSTRAP_CATALOG_PATH)
	if not errors.is_empty():
		push_error("stage run test registry failed to load: %s" % errors)
	return registry


func _expect(condition: bool, message: String, failures: PackedStringArray) -> void:
	if not condition:
		failures.append(message)


func _finish(failures: PackedStringArray) -> void:
	if failures.is_empty():
		print("Stage run, wave, progression, and assassin bypass checks passed")
		quit(0)
	else:
		printerr("Stage run, wave, progression, and assassin bypass failures:\n%s" % "\n".join(failures))
		quit(1)

```

</details>

### `tests/python/test_skill_routing_contract.py` — ACTIVE / 63 lines

Hits: `룰렛` [30]

```text
   27:         self.assertEqual(len(selected), len(set(selected)))
   28: 
   29:     def test_game_design_plan(self) -> None:
   30:         result = route("룰렛 확률과 보상 규칙을 기획해줘", REGISTRY)
   31:         self.assertEqual(result["mode"], "PLAN")
   32:         self.assertIn("discipline.game-design", ids(result))
   33: 
```

<details><summary>Full content</summary>

```text
from __future__ import annotations

import pathlib
import sys
import unittest

ROOT = pathlib.Path(__file__).resolve().parents[2]
sys.path.insert(0, str(ROOT / "tools"))

from route_skills import load_registry, route  # noqa: E402

REGISTRY = load_registry(ROOT / "docs" / "base" / "SKILL_REGISTRY.json")


def ids(result: dict) -> list[str]:
    return [item["id"] for item in result["skills"]]


class SkillRoutingTests(unittest.TestCase):
    def test_review_forces_adversarial_stack(self) -> None:
        result = route("PR의 누락과 중복을 적대적으로 검토하고 레드팀 검증해줘", REGISTRY)
        selected = ids(result)
        self.assertEqual(result["mode"], "REVIEW")
        self.assertIn("foundation.project-intake", selected)
        self.assertIn("foundation.validation-review", selected)
        self.assertIn("discipline.integration-review", selected)
        self.assertEqual(len(selected), len(set(selected)))

    def test_game_design_plan(self) -> None:
        result = route("룰렛 확률과 보상 규칙을 기획해줘", REGISTRY)
        self.assertEqual(result["mode"], "PLAN")
        self.assertIn("discipline.game-design", ids(result))

    def test_engineering_build(self) -> None:
        result = route("Godot GDScript 성능 버그를 수정해줘", REGISTRY)
        self.assertEqual(result["mode"], "BUILD")
        self.assertIn("discipline.engineering", ids(result))

    def test_ui_art_audit_selects_specialist(self) -> None:
        result = route("HUD UI 아트의 화면 가독성을 시각 감사해줘", REGISTRY)
        selected = ids(result)
        self.assertEqual(result["mode"], "REVIEW")
        self.assertIn("specialist.ui-art-audit", selected)
        self.assertTrue({"discipline.ux-ui-accessibility", "discipline.art"} & set(selected))

    def test_dependencies_precede_dependents(self) -> None:
        result = route("수직 슬라이스 MVP를 설계해줘", REGISTRY)
        selected = ids(result)
        specialist_index = selected.index("specialist.vertical-slice")
        for dependency in ("discipline.game-design", "discipline.engineering", "discipline.production-pm"):
            self.assertLess(selected.index(dependency), specialist_index)

    def test_generic_request_does_not_enable_specialists(self) -> None:
        result = route("프로젝트 작업을 정리해줘", REGISTRY)
        self.assertFalse(any(skill_id.startswith("specialist.") for skill_id in ids(result)))

    def test_unknown_manual_override_is_rejected(self) -> None:
        with self.assertRaises(ValueError):
            route("검토", REGISTRY, forced_skills=["specialist.does-not-exist"])


if __name__ == "__main__":
    unittest.main()

```

</details>

### `tools/_collect_c1_roulette_audit.py` — ACTIVE / 241 lines

Hits: `roulette` [10, 11, 18, 19, 27, 76, 94, 97, 99, 104, 105, 109, 113, 115, 164, 169, 182, 191, 232, 240, 241], `룰렛` [19, 178, 182, 189], `spin` [19], `reroll` [19], `re-roll` [19], `reel` [19], `slot` [19], `unitspawndefinition` [20], `lucky` [20], `럭키` [20], `완성선` [20], `중앙 가로줄` [20], `중앙 줄` [21], `판정 줄` [21], `result storage` [21], `결과 보관` [21], `이동권` [21]

```text
    7: from collections import defaultdict
    8: 
    9: ROOT = pathlib.Path(__file__).resolve().parents[1]
   10: REPORT_MD = ROOT / "docs" / "_C1_ROULETTE_AUDIT_INPUT.md"
   11: REPORT_JSON = ROOT / "docs" / "_C1_ROULETTE_AUDIT_INPUT.json"
   12: 
   13: TEXT_SUFFIXES = {
   14:     ".md", ".gd", ".tscn", ".tres", ".json", ".py", ".yml", ".yaml",
   15:     ".txt", ".cfg", ".godot", ".csv", ".ini", ".toml", ".ps1", ".sh",
   16: }
   17: SKIP_PARTS = {".git", ".godot", "assets", "vendor", "third_party"}
   18: ROULETTE_TERMS = (
   19:     "roulette", "룰렛", "spin", "reroll", "re-roll", "reel", "slot",
   20:     "unitspawndefinition", "lucky", "럭키", "완성선", "중앙 가로줄",
   21:     "중앙 줄", "판정 줄", "result storage", "결과 보관", "이동권",
   22: )
   23: STALE_TERMS = (
   24:     "0001-phase-0-codex-plan-mode", "phase 0 plan mode", "구현 전",
   25:     "existing_core_identified", "core_lock_pending_user_confirmation",
   26:     "pending_user_confirmation", "codex/phase-0-godot-bootstrap",
   27:     "roulettebound", "율비", "경계의 율", "은종성채", "무명야",
   28:     "플레이 가능한 수직 슬라이스 구현 완료",
   29: )
   30: ACTIVE_PREFIXES = (
   73: 
   74: 
   75: files: list[dict[str, object]] = []
   76: roulette_files: list[dict[str, object]] = []
   77: stale_files: list[dict[str, object]] = []
   78: references: list[dict[str, object]] = []
   79: broken_links: list[dict[str, object]] = []
   91:     item = {"path": relative, "category": category(relative), "lines": len(lines), "bytes": len(text.encode("utf-8"))}
   92:     files.append(item)
   93: 
   94:     roulette_hits: dict[str, list[int]] = defaultdict(list)
   95:     stale_hits: dict[str, list[int]] = defaultdict(list)
   96:     for index, lowered in enumerate(lower_lines):
   97:         for term in ROULETTE_TERMS:
   98:             if term.casefold() in lowered:
   99:                 roulette_hits[term].append(index + 1)
  100:         for term in STALE_TERMS:
  101:             if term.casefold() in lowered:
  102:                 stale_hits[term].append(index + 1)
  103: 
  104:     if roulette_hits:
  105:         all_indexes = {line - 1 for values in roulette_hits.values() for line in values}
  106:         include_full = (
  107:             len(lines) <= 420
  108:             and (
  109:                 "roulette" in relative.casefold()
  110:                 or relative.startswith(("scripts/", "tests/", "data/", "resources/"))
  111:             )
  112:         )
  113:         roulette_files.append({
  114:             **item,
  115:             "hits": dict(roulette_hits),
  116:             "context": context(lines, all_indexes, 3),
  117:             "full_content": text if include_full else None,
  118:         })
  161:     "base_commit": subprocess.check_output(["git", "rev-parse", "HEAD"], cwd=ROOT, text=True).strip(),
  162:     "counts": {
  163:         "text_files": len(files),
  164:         "roulette_related_files": len(roulette_files),
  165:         "stale_term_files": len(stale_files),
  166:         "markdown_references": len(references),
  167:         "broken_links": len(broken_links),
  168:     },
  169:     "roulette_files": roulette_files,
  170:     "stale_files": stale_files,
  171:     "broken_links": broken_links,
  172:     "inbound_references": dict(sorted(inbound.items())),
  175: REPORT_JSON.write_text(json.dumps(payload, ensure_ascii=False, indent=2) + "\n", encoding="utf-8")
  176: 
  177: md: list[str] = [
  178:     "# C1 승인 룰렛 계약 복구 — 기계 수집 감사 입력",
  179:     "",
  180:     f"- 기준 커밋: `{payload['base_commit']}`",
  181:     f"- 텍스트 파일: {len(files)}",
  182:     f"- 룰렛 관련 파일: {len(roulette_files)}",
  183:     f"- 구형 상태·명칭 후보 파일: {len(stale_files)}",
  184:     f"- 마크다운 내부 참조: {len(references)}",
  185:     f"- 깨진 내부 링크: {len(broken_links)}",
  186:     "",
  187:     "이 파일은 기계 수집 입력이다. ACTIVE/HISTORICAL 판정과 삭제 여부는 후속 사람이 검증한다.",
  188:     "",
  189:     "## 룰렛 관련 파일",
  190: ]
  191: for entry in roulette_files:
  192:     md.extend([
  193:         "",
  194:         f"### `{entry['path']}` — {entry['category']} / {entry['lines']} lines",
  229:     md.append(f"- `{item['path']}` — {item['category']} / {item['lines']} lines / {item['bytes']} bytes")
  230: REPORT_MD.write_text("\n".join(md) + "\n", encoding="utf-8")
  231: 
  232: for relative in ("tools/_collect_c1_roulette_audit.py", ".github/workflows/collect-c1-roulette-audit-once.yml"):
  233:     path = ROOT / relative
  234:     if path.exists():
  235:         path.unlink()
  237: run("git", "config", "user.name", "github-actions[bot]")
  238: run("git", "config", "user.email", "41898282+github-actions[bot]@users.noreply.github.com")
  239: run("git", "add", "-A")
  240: run("git", "commit", "-m", "collect C1 roulette and stale-reference audit input")
  241: run("git", "push", "origin", "HEAD:agent/c1-approved-roulette-contract-recovery")
```

<details><summary>Full content</summary>

```text
from __future__ import annotations

import json
import pathlib
import re
import subprocess
from collections import defaultdict

ROOT = pathlib.Path(__file__).resolve().parents[1]
REPORT_MD = ROOT / "docs" / "_C1_ROULETTE_AUDIT_INPUT.md"
REPORT_JSON = ROOT / "docs" / "_C1_ROULETTE_AUDIT_INPUT.json"

TEXT_SUFFIXES = {
    ".md", ".gd", ".tscn", ".tres", ".json", ".py", ".yml", ".yaml",
    ".txt", ".cfg", ".godot", ".csv", ".ini", ".toml", ".ps1", ".sh",
}
SKIP_PARTS = {".git", ".godot", "assets", "vendor", "third_party"}
ROULETTE_TERMS = (
    "roulette", "룰렛", "spin", "reroll", "re-roll", "reel", "slot",
    "unitspawndefinition", "lucky", "럭키", "완성선", "중앙 가로줄",
    "중앙 줄", "판정 줄", "result storage", "결과 보관", "이동권",
)
STALE_TERMS = (
    "0001-phase-0-codex-plan-mode", "phase 0 plan mode", "구현 전",
    "existing_core_identified", "core_lock_pending_user_confirmation",
    "pending_user_confirmation", "codex/phase-0-godot-bootstrap",
    "roulettebound", "율비", "경계의 율", "은종성채", "무명야",
    "플레이 가능한 수직 슬라이스 구현 완료",
)
ACTIVE_PREFIXES = (
    "README.md", "AGENTS.md", "docs/ACTIVE_CONTEXT.md", "docs/HANDOFF_CONTEXT.md",
    "docs/DOCUMENTATION_MAP.md", "docs/OMENWARD_GAME_DESIGN.md",
    "docs/OMENWARD_ROADMAP.md", "docs/DECISIONS_PENDING.md",
    "docs/CURRENT_IMPLEMENTATION_STATUS.md", "docs/PROJECT_CORE.md",
    "docs/design/APPROVED_", "docs/PHASE_0_VALIDATION.md",
    "docs/VERTICAL_SLICE_VALIDATION.md", "scripts/", "scenes/", "data/",
    "resources/", "tests/", ".github/", "tools/",
)
HISTORICAL_PARTS = ("/archive/", "/work_orders/", "/proposals/", "/issues/", "/goals/")
LINK_RE = re.compile(r"\[[^\]]*\]\(([^)]+)\)")


def run(*args: str) -> None:
    subprocess.run(args, cwd=ROOT, check=True)


def is_text(path: pathlib.Path) -> bool:
    relative = path.relative_to(ROOT)
    if any(part in SKIP_PARTS for part in relative.parts):
        return False
    return path.is_file() and (path.suffix.lower() in TEXT_SUFFIXES or path.name in {"README", "LICENSE"})


def category(relative: str) -> str:
    normalized = "/" + relative.replace("\\", "/")
    if any(part in normalized for part in HISTORICAL_PARTS):
        return "HISTORICAL"
    if relative.startswith(ACTIVE_PREFIXES):
        return "ACTIVE"
    return "REFERENCE_OR_OTHER"


def context(lines: list[str], indexes: set[int], radius: int = 2) -> list[dict[str, object]]:
    selected: list[dict[str, object]] = []
    emitted: set[int] = set()
    for index in sorted(indexes):
        for line_index in range(max(0, index - radius), min(len(lines), index + radius + 1)):
            if line_index in emitted:
                continue
            emitted.add(line_index)
            selected.append({"line": line_index + 1, "text": lines[line_index]})
    return selected


files: list[dict[str, object]] = []
roulette_files: list[dict[str, object]] = []
stale_files: list[dict[str, object]] = []
references: list[dict[str, object]] = []
broken_links: list[dict[str, object]] = []

for path in sorted(ROOT.rglob("*")):
    if not is_text(path):
        continue
    relative = path.relative_to(ROOT).as_posix()
    try:
        text = path.read_text(encoding="utf-8")
    except UnicodeDecodeError:
        continue
    lines = text.splitlines()
    lower_lines = [line.casefold() for line in lines]
    item = {"path": relative, "category": category(relative), "lines": len(lines), "bytes": len(text.encode("utf-8"))}
    files.append(item)

    roulette_hits: dict[str, list[int]] = defaultdict(list)
    stale_hits: dict[str, list[int]] = defaultdict(list)
    for index, lowered in enumerate(lower_lines):
        for term in ROULETTE_TERMS:
            if term.casefold() in lowered:
                roulette_hits[term].append(index + 1)
        for term in STALE_TERMS:
            if term.casefold() in lowered:
                stale_hits[term].append(index + 1)

    if roulette_hits:
        all_indexes = {line - 1 for values in roulette_hits.values() for line in values}
        include_full = (
            len(lines) <= 420
            and (
                "roulette" in relative.casefold()
                or relative.startswith(("scripts/", "tests/", "data/", "resources/"))
            )
        )
        roulette_files.append({
            **item,
            "hits": dict(roulette_hits),
            "context": context(lines, all_indexes, 3),
            "full_content": text if include_full else None,
        })

    if stale_hits:
        all_indexes = {line - 1 for values in stale_hits.values() for line in values}
        stale_files.append({
            **item,
            "hits": dict(stale_hits),
            "context": context(lines, all_indexes, 2),
        })

    if path.suffix.lower() == ".md":
        for line_number, line in enumerate(lines, 1):
            for raw_target in LINK_RE.findall(line):
                target = raw_target.split("#", 1)[0].strip()
                if not target or "://" in target or target.startswith(("mailto:", "#")):
                    continue
                resolved = (path.parent / target).resolve()
                try:
                    resolved.relative_to(ROOT.resolve())
                except ValueError:
                    continue
                exists = resolved.exists()
                record = {
                    "source": relative,
                    "source_category": category(relative),
                    "line": line_number,
                    "target": target,
                    "resolved": resolved.relative_to(ROOT).as_posix(),
                    "exists": exists,
                }
                references.append(record)
                if not exists:
                    broken_links.append(record)

inbound = defaultdict(list)
for reference in references:
    inbound[reference["resolved"]].append({
        "source": reference["source"],
        "source_category": reference["source_category"],
        "line": reference["line"],
    })

payload = {
    "base_commit": subprocess.check_output(["git", "rev-parse", "HEAD"], cwd=ROOT, text=True).strip(),
    "counts": {
        "text_files": len(files),
        "roulette_related_files": len(roulette_files),
        "stale_term_files": len(stale_files),
        "markdown_references": len(references),
        "broken_links": len(broken_links),
    },
    "roulette_files": roulette_files,
    "stale_files": stale_files,
    "broken_links": broken_links,
    "inbound_references": dict(sorted(inbound.items())),
    "file_inventory": files,
}
REPORT_JSON.write_text(json.dumps(payload, ensure_ascii=False, indent=2) + "\n", encoding="utf-8")

md: list[str] = [
    "# C1 승인 룰렛 계약 복구 — 기계 수집 감사 입력",
    "",
    f"- 기준 커밋: `{payload['base_commit']}`",
    f"- 텍스트 파일: {len(files)}",
    f"- 룰렛 관련 파일: {len(roulette_files)}",
    f"- 구형 상태·명칭 후보 파일: {len(stale_files)}",
    f"- 마크다운 내부 참조: {len(references)}",
    f"- 깨진 내부 링크: {len(broken_links)}",
    "",
    "이 파일은 기계 수집 입력이다. ACTIVE/HISTORICAL 판정과 삭제 여부는 후속 사람이 검증한다.",
    "",
    "## 룰렛 관련 파일",
]
for entry in roulette_files:
    md.extend([
        "",
        f"### `{entry['path']}` — {entry['category']} / {entry['lines']} lines",
        "",
        "Hits: " + ", ".join(f"`{term}` {lines}" for term, lines in entry["hits"].items()),
        "",
        "```text",
    ])
    for line in entry["context"]:
        md.append(f"{line['line']:>5}: {line['text']}")
    md.append("```")
    if entry["full_content"] is not None:
        md.extend(["", "<details><summary>Full content</summary>", "", "```text", entry["full_content"], "```", "", "</details>"])

md.extend(["", "## 구형 상태·명칭 후보"])
for entry in stale_files:
    md.extend([
        "",
        f"### `{entry['path']}` — {entry['category']}",
        "",
        "Hits: " + ", ".join(f"`{term}` {lines}" for term, lines in entry["hits"].items()),
        "",
        "```text",
    ])
    for line in entry["context"]:
        md.append(f"{line['line']:>5}: {line['text']}")
    md.append("```")

md.extend(["", "## 깨진 내부 링크"])
if broken_links:
    for entry in broken_links:
        md.append(f"- `{entry['source']}:{entry['line']}` → `{entry['target']}` (`{entry['resolved']}`)")
else:
    md.append("- 없음")

md.extend(["", "## 파일 인벤토리"])
for item in files:
    md.append(f"- `{item['path']}` — {item['category']} / {item['lines']} lines / {item['bytes']} bytes")
REPORT_MD.write_text("\n".join(md) + "\n", encoding="utf-8")

for relative in ("tools/_collect_c1_roulette_audit.py", ".github/workflows/collect-c1-roulette-audit-once.yml"):
    path = ROOT / relative
    if path.exists():
        path.unlink()

run("git", "config", "user.name", "github-actions[bot]")
run("git", "config", "user.email", "41898282+github-actions[bot]@users.noreply.github.com")
run("git", "add", "-A")
run("git", "commit", "-m", "collect C1 roulette and stale-reference audit input")
run("git", "push", "origin", "HEAD:agent/c1-approved-roulette-contract-recovery")

```

</details>

### `tools/validate_project_core_docs.py` — ACTIVE / 233 lines

Hits: `룰렛` [89, 175, 198]

```text
   86: 
   87: ROADMAP_PRESERVED_PHRASES = (
   88:     "별도 `EnemyUnitProfile` 없음",
   89:     "룰렛 최소 100,000시드 시뮬레이션",
   90:     "지상 120·비행 24·투사체 160·VFX 80 정상 목표",
   91:     "모든 Gate와 Phase 종료 시 다음을 갱신한다",
   92: )
  172:     roadmap = _read(root, "docs/OMENWARD_ROADMAP.md")
  173:     required_sequence = (
  174:         "정본·프로젝트 코어 확정·잠금 완료",
  175:         "승인 룰렛 계약 복구",
  176:         "전투 목적 루프 연결",
  177:         "승인 코어 UX 6종",
  178:         "코어 플레이테스트",
  195:         errors.append(f"DECISIONS_PENDING missing preserved alternative: {missing}")
  196:     if "Godot 4.7.1·Compatibility·960×540" not in decisions:
  197:         errors.append("DECISIONS_PENDING does not distinguish implemented technical baseline")
  198:     if "승인 룰렛 계약 복구" not in decisions:
  199:         errors.append("DECISIONS_PENDING does not point to the next decision gate")
  200:     if "프로젝트 코어 확정·잠금 — 완료" not in decisions:
  201:         errors.append("DECISIONS_PENDING does not record the resolved project-core lock")
```

## 구형 상태·명칭 후보

### `AGENTS.md` — ACTIVE

Hits: `구현 전` [37]

```text
   35: 
   36: - 기획/조정 AI: 플레이어 경험, 사양, 범위, 제외 범위, 책임 원본, 완료 기준과 검증 방법을 정리하고 확정 사항을 GitHub 문서에 반영한다.
   37: - Codex Plan Mode: 저장소와 참고 자료를 읽기 전용으로 조사하고 구현 전에 검토 가능한 제안서를 작성한다.
   38: - Codex 구현 모드: 사용자가 승인한 제안서와 Issue/Goal만 구현하고 제품 방향을 임의로 확장하지 않는다.
   39: - 아트·콘텐츠 작업자: 프로젝트 아트·연출 계약과 공용 데이터 구조를 유지하며 실제 화면 크기에서 검수한다.
```

### `docs/ACTIVE_CONTEXT.md` — ACTIVE

Hits: `구현 전` [241]

```text
  239: 
  240: - 새 Codex 채팅은 `docs/PROJECT_CORE.md`와 `docs/CURRENT_IMPLEMENTATION_STATUS.md`를 먼저 읽는다.
  241: - 현재 저장소를 `구현 전` 또는 `수직 슬라이스 완료` 중 하나로 단순화하지 않는다.
  242: - 다음 게임 기능 PR은 승인 룰렛 계약 복구만 포함한다.
  243: - 시각·병종·UI 작업은 새 병종 비주얼 책임 문서와 시각자료 인덱스를 반드시 읽는다.
```

### `docs/CORE_RECOVERY_AUDIT_2026-07-22.md` — REFERENCE_OR_OTHER

Hits: `플레이 가능한 수직 슬라이스 구현 완료` [21], `구현 전` [22, 39], `phase 0 plan mode` [23]

```text
   19: | 위치 | 기존 주장 | 실제 증거 | 조치 |
   20: |---|---|---|---|
   21: | `README.md` | 플레이 가능한 수직 슬라이스 구현 완료 | 기술·데이터 구성요소는 있으나 룰렛·전투 목적·코어 UX가 미완결 | `CORE_VERTICAL_SLICE_PARTIAL`로 교정 |
   22: | `OMENWARD_GAME_DESIGN.md` | Phase 0 대기·구현 전 | Godot 프로젝트·Scene·GDScript·Resource·테스트 존재 | 기술 기준선 구현과 코어 미완결을 분리 |
   23: | `OMENWARD_ROADMAP.md` | Phase 0 Plan Mode 대기 | P1 기반과 P2 일부가 이미 구현 | 현재 복구 단계 중심으로 재작성 |
   24: | `DECISIONS_PENDING.md` | 엔진·화면·상태 소유를 최초 승인 대기 | 실제 project·GameSession·Resource 구조 존재 | 구현 사실과 재검증 필요를 분리 |
   25: | 열린 Issue 다수 | 구현 금지·과거 브랜치 정본 | 현재 main과 상태가 다름 | 본 PR에서 문서 정본을 먼저 복구하고 후속 Issue 정리 대상으로 기록 |
   37: 이 네 상태를 동시에 사용한다. 어느 하나만 사용하면 다음 오판이 생긴다.
   38: 
   39: - `구현 전`만 사용: 이미 존재하는 기술 기반과 파일을 다시 설계한다.
   40: - `수직 슬라이스 완료`만 사용: 승인 룰렛·전투 목적·UX 미완성을 놓친다.
   41: - `테스트 통과`만 사용: placeholder 테스트를 제품 계약 검증으로 오인한다.
```

### `docs/CURRENT_IMPLEMENTATION_STATUS.md` — ACTIVE

Hits: `구현 전` [137]

```text
  135: - README·GDD·로드맵·상태·인수인계·미확정 목록이 같은 단계 용어를 사용한다.
  136: - 현재 구현과 미구현을 파일 증거로 분리한다.
  137: - 과거 `구현 전`과 과도한 `수직 슬라이스 완료` 주장을 현재 상태로 사용하지 않는다.
  138: - 프로젝트 코어는 2026-07-22 사용자 확인으로 `CORE_CONFIRMED`·`CORE_LOCKED`다.
  139: - 다음 변경은 게임 코드 전체가 아니라 승인 룰렛 계약 복구로 한정한다.
```

### `docs/DOCUMENTATION_MAP.md` — ACTIVE

Hits: `0001-phase-0-codex-plan-mode` [40, 59, 68], `구현 전` [43, 69], `roulettebound` [108], `율비` [108], `경계의 율` [108], `은종성채` [108], `무명야` [108]

```text
   38: |---|---|---|
   39: | 현재 main 감사·다음 개선 Plan Mode | `work_orders/0002-current-main-audit-and-next-iteration-plan-mode.md` | **활성 시작 문서** |
   40: | Phase 0 Bootstrap | `goals/0001-engine-selection-and-bootstrap.md`, `work_orders/0001-phase-0-codex-plan-mode.md` | 구현 이후의 과거 입력·변경 이력 |
   41: | 수직 슬라이스 | `goals/0002-core-vertical-slice.md`, 관련 Issue·validation | 실제 main과 테스트 재확인 대상 |
   42: 
   43: 새 Codex 채팅은 `work_orders/0002-current-main-audit-and-next-iteration-plan-mode.md`에서 시작한다. 과거 `0001` Work Order의 `구현 전` 문구를 현재 상태로 사용하지 않는다.
   44: 
   45: ## 항상 확인할 공식 문서
   57: | `images/VISUAL_REFERENCE_INDEX.md` | 이미지 상태·우선순위·누락 감사 |
   58: | `work_orders/0002-current-main-audit-and-next-iteration-plan-mode.md` | 현재 새 Codex 채팅 작업 요청·복사 프롬프트 |
   59: | `work_orders/0001-phase-0-codex-plan-mode.md` | Phase 0 이전에 사용한 과거 작업 요청 |
   60: | `design/proposals/0001-phase-0-godot-bootstrap.md` | Phase 0 사전 기술 추천안·변경 이력 |
   61: 
   66: | 프로젝트 코어·우선순위·기능 제거 판단 | `PROJECT_CORE.md`, `CURRENT_IMPLEMENTATION_STATUS.md`, 관련 APPROVED 문서 |
   67: | 새 Codex 채팅·현재 main 조사 | `PROJECT_CORE.md`, `CURRENT_IMPLEMENTATION_STATUS.md`, `work_orders/0002-current-main-audit-and-next-iteration-plan-mode.md`, `PROPOSAL_WORKFLOW.md`, 현재 Issue·PR·Goal |
   68: | 과거 Phase 0 결정 추적 | `work_orders/0001-phase-0-codex-plan-mode.md`, `design/proposals/0001-phase-0-godot-bootstrap.md`, Goal 0001 |
   69: | Codex가 작성한 구현 전 제안 검토 | Codex 제출 제안서, 관련 작업 요청서, 현재 Issue/Goal |
   70: | 문서 추가·교체·정리·인수인계 | `DOCUMENT_LIFECYCLE.md`, `HANDOFF_CONTEXT.md`, `archive/README.md` |
   71: | GitHub Issue·로컬 미러 동기화 | `issues/README.md`, `DOCUMENT_LIFECYCLE.md`, `tools/sync_repo.ps1` |
  106: ```
  107: 
  108: 레거시 명칭 `Roulettebound`, `율비`, `경계의 율`, `은종성채`, `무명야`는 과거 변경 이력 외에는 사용하지 않는다.
  109: 
  110: ## 핵심 책임 원본
```

### `docs/HANDOFF_CONTEXT.md` — ACTIVE

Hits: `구현 전` [19]

```text
   17: 1. 오멘워드는 건물로 룰렛의 토큰·확률과 증원 체계를 설계하고 세 전선을 지휘하는 판타지 전략 오토배틀 게임이다.
   18: 2. 저장소에는 Phase 0 기술 기준선과 수직 슬라이스 구성요소가 존재하지만 승인 룰렛·전투 목적·코어 UX는 미완결이다.
   19: 3. 과거 Phase 0 Work Order의 `구현 전`과 README의 과도한 `수직 슬라이스 완료`를 현재 상태로 재사용하지 않는다.
   20: 4. 새 Codex 채팅은 `PROJECT_CORE.md`, `CURRENT_IMPLEMENTATION_STATUS.md`, 실제 main, validation 문서와 Issue·PR을 대조한 뒤 다음 최소 변경을 제안한다.
   21: 5. 아군과 적군은 별도 병종 전투 데이터를 만들지 않고 공용 10병종에 서로 다른 FactionVisualProfile을 연결한다.
```

### `docs/OMENWARD_GAME_DESIGN.md` — ACTIVE

Hits: `구현 전` [718]

```text
  716: - 최종 이미지·팔레트·아이콘·오디오.
  717: 
  718: ### 구현 전 미확정
  719: 
  720: - 정확한 Godot stable 버전.
```

### `docs/OMENWARD_ROADMAP.md` — ACTIVE

Hits: `0001-phase-0-codex-plan-mode` [89, 110]

```text
   87: ### 완료 산출물
   88: 
   89: - `docs/work_orders/0001-phase-0-codex-plan-mode.md`.
   90: - 복사 가능한 시작 프롬프트.
   91: - 제품 컨텍스트와 불변 조건.
  108: - Issue #1.
  109: - Goal 0001.
  110: - `docs/work_orders/0001-phase-0-codex-plan-mode.md`.
  111: 
  112: ### Codex가 조사할 것
```

### `docs/PROJECT_CORE.md` — ACTIVE

Hits: `구현 전` [117]

```text
  115: 
  116: - 프로젝트 코어, GDD, 로드맵, 현재 상태와 Issue가 같은 구현 단계를 가리킨다.
  117: - `구현 전`, `완료`, `검증됨`을 실제 증거 없이 사용하지 않는다.
  118: 
  119: ### C1 — 룰렛 인과
```

### `docs/benchmarks/0001-core-game-benchmark-proposal.md` — REFERENCE_OR_OTHER

Hits: `roulettebound` [9, 36, 78, 88, 105, 136, 162, 177, 193, 220, 249, 274, 303, 326]

```text
    7: 
    8: - 해결할 문제: 슬롯·룰렛, 실시간 3라인 전투, 건설 경제, 병종 상성, 적 웨이브 전조를 결합할 때 각 시스템의 장점이 서로를 가리지 않고 하나의 명확한 플레이 경험으로 이어져야 한다.
    9: - 플레이어/개발 가치: 이미 반응이 검증된 게임의 강점과 불만 요소를 분리해, Roulettebound가 표면적인 장르 혼합에 머무르지 않고 `건물로 확률을 설계하고 예고된 전선 위협에 대응한다`는 독자적인 핵심을 빠르게 검증한다.
   10: 
   11: ## 2. 현재 상태와 근거
   34: - 개발사 공식 소개
   35: - 주요 비평 기사에서 반복적으로 언급된 장점과 문제
   36: - Roulettebound 기획과의 직접 연결성
   37: 
   38: 정확한 리뷰 비율은 조사 시점의 스냅샷이며 이후 변동할 수 있다.
   76: - 가장 가까운 표면 비교 대상이지만 시장 반응이 검증된 게임으로 보기는 어렵다.
   77: 
   78: **Roulettebound 적용 후보**
   79: 
   80: - 슬롯 결과가 즉시 전장에 영향을 주는 강한 연출
   86: - 흡수·진화, 랜덤 능력치, 각인, 아이템, 코어를 MVP에 동시에 넣는 구조
   87: - 고유 유닛명, 진화 계보, UI 배치와 슬롯 연출 복제
   88: - Roulettebound의 건물 기반 토큰 구성보다 유닛 흡수 성장을 우선하는 구조
   89: 
   90: ### 4.2 Luck be a Landlord
  103: - 슬롯 외형 때문에 실제 화폐 도박이나 소액결제가 없음에도 플랫폼 등급·정책 문제를 겪었다.
  104: 
  105: **Roulettebound 적용 후보**
  106: 
  107: 1. **룰렛 토큰 장부**
  134: - 90% 이상 평가를 받은 비교작보다 반응이 낮다는 점은 장르 결합 자체가 자동으로 높은 만족도를 만들지는 않으며, 명확성·완성도·밸런스가 중요하다는 위험 신호로 본다.
  135: 
  136: **Roulettebound 적용 후보**
  137: 
  138: - 병종 초상에 역할과 상성 아이콘을 고정 표시한다.
  160: - 최근 평가가 장기 평가보다 낮은 점은 지속적인 밸런스와 콘텐츠 운영이 반응을 흔들 수 있음을 보여준다.
  161: 
  162: **Roulettebound 적용 후보**
  163: 
  164: 1. **공개 정보 기반 대응**
  175: - PvP 중심의 수십 개 유닛과 다수 기술 분기
  176: - 시즌 메타와 랭크 운영
  177: - Roulettebound의 실시간 경제보다 라운드별 완전 정지 준비를 우선하는 흐름
  178: 
  179: ### 4.5 Thronefall
  191: - 최소한의 시스템으로 경제와 방어의 깊이를 만든 점, 짧은 플레이와 반복성이 높은 평가를 받았다.
  192: 
  193: **Roulettebound 적용 후보**
  194: 
  195: - 노드를 누르면 농장·시장·훈련소·포탑의 핵심 차이를 한 줄로 비교하는 즉시 건설 메뉴를 연다.
  218: - 적 행동을 투명하게 공개하면서도 높은 전략성을 유지하는 대표 사례다.
  219: 
  220: **Roulettebound 적용 후보**
  221: 
  222: - 30초 전조를 세 단계로 나눈다.
  247: - 무작위 건물 획득이 플레이어를 적응시키는 장점과 원하는 빌드를 막는 좌절을 동시에 만든다는 평가가 있었다.
  248: 
  249: **Roulettebound 적용 후보**
  250: 
  251: - 이미 확정한 `후방 6노드 + 라인별 3노드`를 단순 공간 제한이 아니라 게임의 핵심 타협 메커니즘으로 유지한다.
  272: - 후속 업데이트에서는 높은 난이도의 RNG 의존도를 낮추고 적 선택권을 늘리는 방향이 강조됐다.
  273: 
  274: **Roulettebound 적용 후보**
  275: 
  276: - MVP 이후 `지휘관 교리` 1개를 선택해 특정 병종 또는 건물 빌드에 방향성을 주는 실험
  301: **적용 결론**
  302: 
  303: - Roulettebound에서는 `건물 배치가 룰렛 덱빌딩`이라는 문장을 UI와 튜토리얼의 중심으로 둔다.
  304: - 확률 수치를 숨기지 않고 건설·파괴 전후 변화를 보여준다.
  305: 
  324: ## 6. 반복된 불만과 위험
  325: 
  326: | 위험 | 관찰된 문제 | Roulettebound 대응 |
  327: |---|---|---|
  328: | 장르 혼합 과부하 | 시스템이 많지만 각 선택의 효과가 희미함 | MVP 병종·건물 수 제한, 한 화면에서 인과 연결 |
```

### `docs/benchmarks/PLAYER_REACTION_THEMES.md` — REFERENCE_OR_OTHER

Hits: `roulettebound` [21]

```text
   19: - 확률과 슬롯 외형에서 발생하는 도박 이미지 및 등급 정책 위험
   20: 
   21: ## Roulettebound 검증 질문
   22: 
   23: - 플레이어가 건물과 룰렛 확률의 연결을 즉시 이해하는가?
```

### `docs/benchmarks/README.md` — REFERENCE_OR_OTHER

Hits: `roulettebound` [3], `구현 전` [9]

```text
    1: # 벤치마킹 문서
    2: 
    3: 벤치마킹은 참고 게임을 복제하기 위한 문서가 아니라, 플레이어 반응과 시스템 문제를 분석해 Roulettebound에 적용하거나 제외할 결론을 남기는 공간이다.
    4: 
    5: ## 현재 상태
    7: - `0001-core-game-benchmark-proposal.md`: 사용자 검토 완료
    8: - 승인된 결론의 책임 원본: `../design/APPROVED_BENCHMARK_DECISIONS.md`
    9: - 구현 전 후속 기획: `../design/DESIGN_FREEZE_CHECKLIST.md`
   10: 
   11: ## 문서 목록
```

### `docs/benchmarks/REVIEW_CHECKLIST.md` — REFERENCE_OR_OTHER

Hits: `구현 전` [9]

```text
    7: - [ ] 전방 생산시설 추가 보너스 여부 결정
    8: - [ ] 승인된 항목만 `GAME_DESIGN.md`에 반영
    9: - [ ] 구현 전 별도 Codex Plan Mode 제안서 작성
```

### `docs/design/APPROVED_BELLU_MASCOT_AND_GUIDE_CONTRACT.md` — ACTIVE

Hits: `율비` [8, 24, 210], `경계의 율` [210]

```text
    6: - 세계관 명칭: `docs/design/APPROVED_OMENWARD_WORLD_AND_NAMING.md`
    7: 
    8: 이 문서는 기존 `율비` 명칭을 사용하는 마스코트 문서보다 우선한다.
    9: 
   10: ## 1. 한 문장 정의
   22: 이름은 종을 연상시키는 부드러운 `벨`과 루메른 문화권의 어감을 가진 `루`가 합쳐진 인상이다. 게임 안에서 어원을 직접 설명하지는 않는다.
   23: 
   24: 기존 `율비` 명칭은 사용하지 않는다.
   25: 
   26: ## 3. 단일 안내자 역할
  208: 
  209: - 모든 필수 안내는 벨루와 HUD만으로 이해할 수 있다.
  210: - `율비`, `경계의 율` 명칭이 신규 UI·대사에 노출되지 않는다.
  211: - 상태 반응 뒤 기본형으로 돌아와도 지속 상태가 끝난 것으로 오해되지 않는다.
  212: - 벨루가 전장 조작이나 중요 정보를 가리지 않는다.
```

### `docs/design/APPROVED_BELLU_SINGLE_GUIDE_AND_FIRST_10_MINUTE_FLOW.md` — ACTIVE

Hits: `율비` [8, 260], `roulettebound` [260], `경계의 율` [260]

```text
    6: - 마스코트 계약: `docs/design/APPROVED_BELLU_MASCOT_AND_GUIDE_CONTRACT.md`
    7: 
    8: 이 문서는 기존 `율비` 이름을 사용하는 첫 10분 문서보다 우선한다.
    9: 
   10: ## 1. 단일 안내자
  258: - 두 번째 룰렛은 플레이어 조작으로 2줄 엘리트가 된다.
  259: - 첫 10분 안에 건설→룰렛→배치→방어 루프를 두 번 경험한다.
  260: - 신규 대사와 UI에 `율비`, `경계의 율`, `Roulettebound`를 사용하지 않는다.
```

### `docs/design/APPROVED_DOPAMINE_DRIVEN_DESIGN_AND_FIRST_10_MINUTES.md` — ACTIVE

Hits: `roulettebound` [164], `율비` [164], `경계의 율` [164]

```text
  162: - 벨루의 설명 없이도 HUD가 필수 숫자를 전달한다.
  163: - 설명을 건너뛰어도 플레이가 막히지 않는다.
  164: - 신규 문서와 대사에 `Roulettebound`, `율비`, `경계의 율`을 사용하지 않는다.
```

### `docs/design/APPROVED_OMENWARD_WORLD_AND_NAMING.md` — ACTIVE

Hits: `roulettebound` [7, 41, 174], `율비` [7, 177], `경계의 율` [7, 94, 175], `은종성채` [7, 62, 178], `무명야` [7, 180]

```text
    5: - 적용 범위: 게임명, 세계관, 튜토리얼, HUD, 웨이브 카드, 대사, 마케팅, 이후 모든 기획 문서
    6: 
    7: 이 문서는 기존 `Roulettebound`, `경계의 율`, `율비`, `은종성채`, `무명야` 등 임시·이전 명칭보다 우선한다.
    8: 
    9: ## 1. 게임 공식 명칭
   39: ```
   40: 
   41: 저장소명과 내부 코드명 `roulettebound-prototype`은 구현 마이그레이션 계획이 승인될 때까지 유지한다.
   42: 
   43: ## 2. 명명 원칙
   60: 
   61: - `실버벨 배스천`처럼 영어 일반명사를 모두 음차
   62: - `은종성채`처럼 번역투 고유명사만 사용
   63: - 같은 문화권에서 음차형과 한자풍 고유명사를 불규칙하게 혼합
   64: 
   92: ## 4. 베일과 베일의 법칙
   93: 
   94: 기존 `경계의 율` 공식 명칭을 다음으로 교체한다.
   95: 
   96: ```text
  172: | 이전 명칭 | 공식 명칭 |
  173: |---|---|
  174: | Roulettebound | 오멘워드 / OMENWARD |
  175: | 경계의 율 | 베일의 법칙 |
  176: | 율 | 법칙 / 베일 |
  177: | 율비 | 벨루 |
  178: | 은종성채·실버벨 배스천 | 실베른 성채 |
  179: | 삼문경계·쓰리게이트 프론트 | 트리븐 전선 |
  180: | 무명야·베일와일즈 | 베일런 황야 |
  181: | 외경종·비욘드본 | 베일종 |
  182: | 경계의 징조·바운더리 오멘 | 베일의 징조 |
```

### `docs/design/APPROVED_PERFORMANCE_DATA_TEST_READINESS_POC_V1.md` — ACTIVE

Hits: `phase 0 plan mode` [411]

```text
  409: - 패배 직전 60초 결정 로그.
  410: 
  411: ## 9. Phase 0 Plan Mode 진입 조건
  412: 
  413: 현재 충족된 구조:
```

### `docs/design/APPROVED_PREPRODUCTION_POC_BASELINE_V1.md` — ACTIVE

Hits: `phase 0 plan mode` [3, 193], `구현 전` [3, 179]

```text
    1: # 승인된 오멘워드 프리프로덕션 PoC 통합 기준 V1
    2: 
    3: - 상태: **프리프로덕션 구조 승인 완료 / 공용 10병종 데이터·진영 비주얼 분리 승인 / 전장·연출 초기값 승인 / Phase 0 Plan Mode 대기 / 구현 전**
    4: - 작성일: 2026-07-16
    5: - 최신 갱신일: 2026-07-16
  177: 이 값은 플레이테스트와 측정 근거로 같은 승인 구조 안에서 변경할 수 있다.
  178: 
  179: ## 9. 구현 전 남은 결정
  180: 
  181: - 정확한 Godot stable 버전.
  191: 
  192: ```text
  193: Issue #1 Phase 0 Plan Mode
  194: → 사용자 승인
  195: → Phase 0 구현
```

### `docs/design/APPROVED_ROULETTE_PROBABILITY_TARGETS_POC_V1.md` — ACTIVE

Hits: `구현 전` [124]

```text
  122: ## 9. 시뮬레이션 검증
  123: 
  124: 구현 전 또는 구현 초기에 최소 100,000시드를 검증한다.
  125: 
  126: - 건물 1/2/4/6동 조합.
```

### `docs/design/DESIGN_FREEZE_CHECKLIST.md` — REFERENCE_OR_OTHER

Hits: `구현 전` [7, 13], `phase 0 plan mode` [187]

```text
    5: - 최초 인수인계: `docs/HANDOFF_CONTEXT.md`
    6: 
    7: 이 체크리스트는 모든 수치를 완벽히 고정하기 위한 문서가 아니다. Codex가 제품 구조와 데이터 책임을 임의로 해석하지 않도록 **구현 전에 잠글 구조**, **PoC에서 조정할 수치**, **실제 플레이로 검증할 경험**을 구분한다.
    8: 
    9: ## 상태 정의
   11: - `[x] 승인`: 구조·책임·불변 조건 확정.
   12: - `[~] PoC 조정`: 첫 구현값이 있으나 측정 후 조정 가능.
   13: - `[ ] 구현 전 결정`: Issue #1 또는 #32 Plan Mode에서 선택.
   14: - `[>] 장기 확장`: 수직 슬라이스 이후 별도 승인.
   15: 
  185: 
  186: ```text
  187: Issue #1 Phase 0 Plan Mode
  188: → 사용자 승인
  189: → Phase 0 구현
```

### `docs/design/proposals/0001-match-structure-and-start-conditions.md` — HISTORICAL

Hits: `구현 전` [8]

```text
    6: ## 1. 목적
    7: 
    8: 플레이어가 게임 시작 직후 무엇을 보고 무엇을 선택해야 하는지, 전투가 언제 본격적으로 시작되는지, 한 판이 어떤 단계로 발전하고 어떤 조건에서 끝나는지를 구현 전에 확정한다.
    9: 
   10: 이 제안서는 비용·체력·공격력 같은 최종 밸런스 수치를 고정하지 않는다. 대신 Codex가 임의로 해석하면 재작업이 커지는 흐름, 상태, 입력과 종료 계약을 잠그는 것을 목표로 한다.
```

### `docs/design/proposals/0001-phase-0-godot-bootstrap.md` — HISTORICAL

Hits: `구현 전` [15, 70]

```text
   13: ### 해결할 문제
   14: 
   15: 오멘워드는 프리프로덕션의 제품 방향과 데이터 계약은 승인됐지만, 아직 `project.godot`, Scene, GDScript, Resource와 테스트가 없는 구현 전 저장소다. 기술 기반을 먼저 임의로 만들면 다음 위험이 발생한다.
   16: 
   17: - 정확한 Godot 버전과 화면 정책이 뒤늦게 바뀜.
   68: ### Base에서 참고한 원칙
   69: 
   70: - 구현 전 사양과 승인 게이트를 분리한다.
   71: - 상태와 책임 원본을 한 곳에 둔다.
   72: - 공용 데이터와 표현 레이어를 분리한다.
```

### `docs/design/proposals/0004-roulette-resolution-bench-and-lucky-chance.md` — HISTORICAL

Hits: `구현 전` [196]

```text
  194: 이 배율을 그대로 적용하면 시장 3개만 존재하는 구성에서 9칸 금화가 결정론적으로 발생할 수 있고, 매 회전 500% 환급으로 즉시 무한 경제가 가능하다.
  195: 
  196: 사용자 배율을 유지하면서 구현하려면 다음 중 하나를 구현 전에 확정해야 한다.
  197: 
  198: 1. 룰렛 회전에 금화와 별개의 시간 충전 자원을 요구한다.
```

### `docs/design/proposals/0009-world-naming-directions.md` — HISTORICAL

Hits: `은종성채` [3], `무명야` [3]

```text
    1: # 대체됨 — 초기 세계관 명칭 방향
    2: 
    3: 이 문서의 `은종성채`, `삼문경계`, `무명야` 등 초기 이름은 공식 명칭으로 교체됐다.
    4: 
    5: 공식 문서:
```

### `docs/images/VISUAL_REFERENCE_INDEX.md` — REFERENCE_OR_OTHER

Hits: `구현 전` [100]

```text
   98: ## 5. Codex·작업자 검수 질문
   99: 
  100: 시각 작업 또는 UI·전장·병종 구현 전 다음을 답할 수 있어야 한다.
  101: 
  102: - 현재 병종 월드 스프라이트 형식의 승인 기준 이미지는 무엇인가.
```

### `docs/issues/0002.md` — HISTORICAL

Hits: `roulettebound` [28, 42], `구현 전` [55]

```text
   26: ## 목표
   27: 
   28: Slotbound, Luck be a Landlord, Commander Quest, Mechabellum, Thronefall, Into the Breach, The King is Watching, 9 Kings의 시스템과 반응을 비교해 Roulettebound 수직 슬라이스에 적용할 요소와 제외할 요소를 확정한다.
   29: 
   30: ## 책임 문서
   40: - 높은 반응을 얻은 비교작은 단순한 기본 규칙, 깊은 조합, 명확한 인과관계, 제한된 건설 공간, 빠른 재시도를 공통적으로 가진다.
   41: - 반복된 위험은 장르 혼합 과부하, 통제 불가능한 RNG, 전투 결과 불명확, 경제 스노우볼, UI 과밀이다.
   42: - Roulettebound의 차별점은 `건물이 룰렛 토큰 풀을 구성하고, 예고된 적 웨이브에 3라인으로 대응한다`는 연결로 유지한다.
   43: 
   44: ## 검토할 핵심 결정
   53: 
   54: - 승인된 후보만 `GAME_DESIGN.md`, `ROADMAP.md`, `DECISIONS_PENDING.md`에 반영
   55: - 구현 전 별도 Codex Plan Mode 제안서 작성
   56: - 승인되지 않은 후보는 아이디어 백로그에만 유지
   57: 
```

### `docs/issues/0016.md` — HISTORICAL

Hits: `경계의 율` [36]

```text
   34: ## 베일의 법칙
   35: 
   36: 기존 `경계의 율` 명칭은 `베일의 법칙`으로 교체됐다.
   37: 
   38: - 괴물의 공세 시간·통로·규모에 영향을 줌
```

### `docs/issues/0017.md` — HISTORICAL

Hits: `율비` [32]

```text
   30: ## 공식 이름과 기원
   31: 
   32: 기존 `율비` 명칭은 `벨루`로 교체됐다.
   33: 
   34: 벨루는 실베른 성채의 오래된 경계종 울림에서 태어난 작은 감시 정령이다.
```

### `docs/issues/0019.md` — HISTORICAL

Hits: `경계의 율` [32], `율비` [33]

```text
   30: 괴물 영역: 베일런 황야
   31: 세계 현상: 베일
   32: 기존 경계의 율: 베일의 법칙
   33: 기존 율비: 벨루
   34: 적 공식 분류: 베일종
   35: ```
```

### `docs/issues/0020.md` — HISTORICAL

Hits: `roulettebound` [56]

```text
   54: ## 보류 범위
   55: 
   56: 저장소명과 내부 코드명 `roulettebound-prototype`은 구현 마이그레이션 계획이 별도로 승인될 때까지 유지한다.
   57: 
   58: 로고 키워드:
```

### `docs/issues/0030.md` — HISTORICAL

Hits: `phase 0 plan mode` [30, 110], `구현 전` [30, 120]

```text
   28: ## 현재 상태
   29: 
   30: **프리프로덕션 구조 승인 완료 / 공용 10병종 데이터·진영별 Visual Set 분리 승인 / 전장·애니메이션 초기값 승인 / Phase 0 Plan Mode 대기 / 구현 전**
   31: 
   32: ## 최초 인수인계
  108: 
  109: ```text
  110: Issue #1 Phase 0 Plan Mode
  111: → 사용자 승인
  112: → Phase 0 구현
  118: ```
  119: 
  120: 현재 Godot 코드·Scene·Resource·테스트는 구현 전이다.
```

### `docs/issues/0033.md` — HISTORICAL

Hits: `구현 전` [24]

```text
   22: ## 현재 상태
   23: 
   24: **공용 10병종의 이동·공격·피격·사망·승리 연출 승인 / 아군·적군은 같은 상태·타이밍 계약과 다른 이미지 세트 사용 / 구현 전**
   25: 
   26: ## 책임 문서
```

### `docs/proposals/0002-issue-body.md` — HISTORICAL

Hits: `roulettebound` [9], `구현 전` [29]

```text
    7: ## 목표
    8: 
    9: Slotbound, Luck be a Landlord, Commander Quest, Mechabellum, Thronefall, Into the Breach, The King is Watching, 9 Kings의 시스템과 반응을 비교해 Roulettebound 수직 슬라이스에 적용할 요소와 제외할 요소를 확정한다.
   10: 
   11: ## 책임 문서
   27: 
   28: - 승인된 후보만 `GAME_DESIGN.md`, `ROADMAP.md`, `DECISIONS_PENDING.md`에 반영
   29: - 구현 전 별도 Codex Plan Mode 제안서 작성
   30: - 승인되지 않은 후보는 아이디어 백로그에만 유지
```

### `docs/work_orders/0001-phase-0-codex-plan-mode.md` — HISTORICAL

Hits: `0001-phase-0-codex-plan-mode` [35]

```text
   33: 4. docs/DOCUMENTATION_MAP.md
   34: 5. docs/PROPOSAL_WORKFLOW.md
   35: 6. docs/work_orders/0001-phase-0-codex-plan-mode.md
   36: 7. docs/OMENWARD_GAME_DESIGN.md
   37: 8. docs/design/APPROVED_PREPRODUCTION_POC_BASELINE_V1.md
```

### `docs/work_orders/0002-current-main-audit-and-next-iteration-plan-mode.md` — HISTORICAL

Hits: `구현 전` [18, 147]

```text
   16: 먼저 시작 문서를 처음부터 끝까지 읽고, 그 안의 읽기 순서와 조사 지시에 따라 현재 main의 문서·코드·데이터·Scene·Resource·테스트·검증 문서를 확인하세요.
   17: 
   18: 과거 Work Order의 `구현 전` 상태를 현재 사실로 가정하지 마세요. project.godot과 실제 파일, 최신 커밋·Issue·PR·validation 문서를 근거로 현재 완료·미완료·충돌·기술 부채를 구분하세요.
   19: 
   20: 시각 작업에서는 반드시 다음을 읽으세요.
  145: ### A. 구현·문서 정합성
  146: 
  147: - 구현 전이라고 남은 레거시 문구 제거.
  148: - 실제 main과 validation 결과에 맞춘 Handoff·Active Context·Roadmap·Issue 동기화.
  149: - 오래된 Phase 0 Work Order를 기록용으로 강등하고 현재 Work Order를 활성 라우터로 지정.
```

### `tests/python/test_project_core_docs.py` — ACTIVE

Hits: `플레이 가능한 수직 슬라이스 구현 완료` [25], `existing_core_identified` [50], `core_lock_pending_user_confirmation` [51], `pending_user_confirmation` [51]

```text
   23:             readme.write_text(
   24:                 readme.read_text(encoding="utf-8")
   25:                 + "\n플레이 가능한 수직 슬라이스 구현 완료\n",
   26:                 encoding="utf-8",
   27:             )
   48:             core.write_text(
   49:                 core.read_text(encoding="utf-8")
   50:                 .replace("- 상태: `CORE_CONFIRMED`", "- 상태: `EXISTING_CORE_IDENTIFIED`")
   51:                 .replace("- 잠금 상태: `CORE_LOCKED`", "- 잠금 상태: `CORE_LOCK_PENDING_USER_CONFIRMATION`"),
   52:                 encoding="utf-8",
   53:             )
```

### `tools/_collect_c1_roulette_audit.py` — ACTIVE

Hits: `0001-phase-0-codex-plan-mode` [24], `phase 0 plan mode` [24], `구현 전` [24], `existing_core_identified` [25], `core_lock_pending_user_confirmation` [25], `pending_user_confirmation` [25, 26], `codex/phase-0-godot-bootstrap` [26], `roulettebound` [27], `율비` [27], `경계의 율` [27], `은종성채` [27], `무명야` [27], `플레이 가능한 수직 슬라이스 구현 완료` [28]

```text
   22: )
   23: STALE_TERMS = (
   24:     "0001-phase-0-codex-plan-mode", "phase 0 plan mode", "구현 전",
   25:     "existing_core_identified", "core_lock_pending_user_confirmation",
   26:     "pending_user_confirmation", "codex/phase-0-godot-bootstrap",
   27:     "roulettebound", "율비", "경계의 율", "은종성채", "무명야",
   28:     "플레이 가능한 수직 슬라이스 구현 완료",
   29: )
   30: ACTIVE_PREFIXES = (
```

### `tools/validate_project_core_docs.py` — ACTIVE

Hits: `플레이 가능한 수직 슬라이스 구현 완료` [38], `phase 0 plan mode` [39, 40, 43], `구현 전` [43, 46], `existing_core_identified` [140], `core_lock_pending_user_confirmation` [141], `pending_user_confirmation` [141, 142]

```text
   36: STALE_CURRENT_CLAIMS = {
   37:     "README.md": (
   38:         "플레이 가능한 수직 슬라이스 구현 완료",
   39:         "Issue #1 Phase 0 Plan Mode",
   40:         "정확한 경로와 파일은 Phase 0 Plan Mode 승인 후 확정합니다.",
   41:     ),
   42:     "docs/OMENWARD_GAME_DESIGN.md": (
   43:         "Phase 0 Plan Mode 대기 / 구현 전",
   44:     ),
   45:     "docs/OMENWARD_ROADMAP.md": (
   46:         "Codex Plan Mode 실행 대기 / 구현 전",
   47:         "현재는 Phase 0 구현이나 수직 슬라이스 구현을 시작하지 않는다.",
   48:     ),
  138: 
  139:     pending_core_terms = (
  140:         "EXISTING_CORE_IDENTIFIED",
  141:         "CORE_LOCK_PENDING_USER_CONFIRMATION",
  142:         "PENDING_USER_CONFIRMATION",
  143:     )
  144:     for relative in (
```

## 깨진 내부 링크
- 없음

## 파일 인벤토리
- `.github/ISSUE_TEMPLATE/config.yml` — ACTIVE / 1 lines / 27 bytes
- `.github/ISSUE_TEMPLATE/feature.md` — ACTIVE / 38 lines / 441 bytes
- `.github/PULL_REQUEST_TEMPLATE.md` — ACTIVE / 30 lines / 363 bytes
- `.github/workflows/collect-c1-roulette-audit-once.yml` — ACTIVE / 27 lines / 664 bytes
- `.github/workflows/decode-planning-image-staging.yml` — ACTIVE / 40 lines / 1369 bytes
- `.github/workflows/issue-to-repo.yml` — ACTIVE / 50 lines / 1855 bytes
- `.github/workflows/repo-to-issue.yml` — ACTIVE / 71 lines / 2762 bytes
- `.github/workflows/validate-project-core-docs.yml` — ACTIVE / 48 lines / 1633 bytes
- `.github/workflows/validate-skill-system.yml` — ACTIVE / 39 lines / 1305 bytes
- `AGENTS.md` — ACTIVE / 222 lines / 12369 bytes
- `README.md` — ACTIVE / 103 lines / 5223 bytes
- `data/bootstrap_catalog.tres` — ACTIVE / 265 lines / 11870 bytes
- `data/stages/regular_stage.tres` — ACTIVE / 178 lines / 5174 bytes
- `data/stages/tutorial_stage.tres` — ACTIVE / 59 lines / 1699 bytes
- `data/units/archer.tres` — ACTIVE / 10 lines / 435 bytes
- `data/units/assassin.tres` — ACTIVE / 10 lines / 445 bytes
- `data/units/cavalry.tres` — ACTIVE / 10 lines / 442 bytes
- `data/units/flier.tres` — ACTIVE / 10 lines / 431 bytes
- `data/units/giant.tres` — ACTIVE / 10 lines / 433 bytes
- `data/units/greatsword_warrior.tres` — ACTIVE / 10 lines / 489 bytes
- `data/units/mage.tres` — ACTIVE / 10 lines / 429 bytes
- `data/units/priest.tres` — ACTIVE / 10 lines / 438 bytes
- `data/units/shield_guard.tres` — ACTIVE / 10 lines / 465 bytes
- `data/units/spear_guard.tres` — ACTIVE / 10 lines / 461 bytes
- `docs/ACTIVE_CONTEXT.md` — ACTIVE / 245 lines / 9205 bytes
- `docs/BASE_RULES_VERSION.md` — REFERENCE_OR_OTHER / 65 lines / 2588 bytes
- `docs/CORE_RECOVERY_AUDIT_2026-07-22.md` — REFERENCE_OR_OTHER / 133 lines / 6082 bytes
- `docs/CURRENT_IMPLEMENTATION_STATUS.md` — ACTIVE / 139 lines / 6018 bytes
- `docs/DECISIONS_PENDING.md` — ACTIVE / 414 lines / 14315 bytes
- `docs/DOCUMENTATION_MAP.md` — ACTIVE / 156 lines / 10979 bytes
- `docs/DOCUMENT_LIFECYCLE.md` — REFERENCE_OR_OTHER / 93 lines / 4851 bytes
- `docs/GODOT_PROJECT_STRUCTURE.md` — REFERENCE_OR_OTHER / 456 lines / 13460 bytes
- `docs/HANDOFF_CONTEXT.md` — ACTIVE / 309 lines / 12097 bytes
- `docs/OMENWARD_GAME_DESIGN.md` — ACTIVE / 778 lines / 23543 bytes
- `docs/OMENWARD_ROADMAP.md` — ACTIVE / 368 lines / 11722 bytes
- `docs/PHASE_0_VALIDATION.md` — ACTIVE / 23 lines / 1320 bytes
- `docs/PROJECT_CORE.md` — ACTIVE / 170 lines / 9081 bytes
- `docs/PROPOSAL_WORKFLOW.md` — REFERENCE_OR_OTHER / 114 lines / 3376 bytes
- `docs/REFERENCE_REPOSITORIES.md` — REFERENCE_OR_OTHER / 208 lines / 7786 bytes
- `docs/VERTICAL_SLICE_VALIDATION.md` — ACTIVE / 21 lines / 1089 bytes
- `docs/archive/README.md` — HISTORICAL / 8 lines / 548 bytes
- `docs/base/ADVERSARIAL_REVIEW_2026-07-21.md` — REFERENCE_OR_OTHER / 83 lines / 3838 bytes
- `docs/base/BASE_SKILL_SYNC_AUDIT_2026-07-21.md` — REFERENCE_OR_OTHER / 48 lines / 1854 bytes
- `docs/base/SKILL_EXECUTION_REPORT.md` — REFERENCE_OR_OTHER / 36 lines / 1296 bytes
- `docs/base/SKILL_REGISTRY.json` — REFERENCE_OR_OTHER / 238 lines / 9448 bytes
- `docs/base/START_HERE_SKILLS.md` — REFERENCE_OR_OTHER / 15 lines / 570 bytes
- `docs/base/WORK_MODE_AND_SKILL_ROUTING.md` — REFERENCE_OR_OTHER / 35 lines / 952 bytes
- `docs/benchmarks/0001-core-game-benchmark-proposal.md` — REFERENCE_OR_OTHER / 428 lines / 21541 bytes
- `docs/benchmarks/APPLICATION_CANDIDATES.md` — REFERENCE_OR_OTHER / 31 lines / 1004 bytes
- `docs/benchmarks/DECISION_STATUS.md` — REFERENCE_OR_OTHER / 6 lines / 254 bytes
- `docs/benchmarks/PLAYER_REACTION_THEMES.md` — REFERENCE_OR_OTHER / 27 lines / 1263 bytes
- `docs/benchmarks/README.md` — REFERENCE_OR_OTHER / 25 lines / 1502 bytes
- `docs/benchmarks/REVIEW_CHECKLIST.md` — REFERENCE_OR_OTHER / 9 lines / 474 bytes
- `docs/benchmarks/SOURCE_SNAPSHOT_2026-07-14.md` — REFERENCE_OR_OTHER / 16 lines / 915 bytes
- `docs/design/APPROVED_15_WAVE_STAGE_CLOCK_AND_OVERTIME_V2.md` — ACTIVE / 254 lines / 7758 bytes
- `docs/design/APPROVED_ART_DIRECTION_AND_PRODUCTION_GUIDE_V1.md` — ACTIVE / 491 lines / 18291 bytes
- `docs/design/APPROVED_BARRACKS_AND_SPECIAL_CORPS_UNIT_TREE_V5.md` — ACTIVE / 342 lines / 10824 bytes
- `docs/design/APPROVED_BARRACKS_TIER2_TIER3_INTEGRATED_TREE_V2.md` — ACTIVE / 397 lines / 14098 bytes
- `docs/design/APPROVED_BARRACKS_TIER3_EVOLUTION_AND_GRADE_SKILLS.md` — ACTIVE / 444 lines / 13828 bytes
- `docs/design/APPROVED_BATTLEFIELD_TOPOLOGY_AND_SCALE_V1.md` — ACTIVE / 313 lines / 11414 bytes
- `docs/design/APPROVED_BELLU_MASCOT_AND_GUIDE_CONTRACT.md` — ACTIVE / 213 lines / 7054 bytes
- `docs/design/APPROVED_BELLU_SINGLE_GUIDE_AND_FIRST_10_MINUTE_FLOW.md` — ACTIVE / 260 lines / 6845 bytes
- `docs/design/APPROVED_BENCHMARK_DECISIONS.md` — ACTIVE / 84 lines / 4015 bytes
- `docs/design/APPROVED_BUILDINGS_TACTICAL_MERCENARY_POC_V1.md` — ACTIVE / 149 lines / 5413 bytes
- `docs/design/APPROVED_BUILDING_SPECIALIZATION_AND_TACTICAL_COMMANDS.md` — ACTIVE / 310 lines / 12743 bytes
- `docs/design/APPROVED_COMBAT_KEYWORDS_STATUS_EFFECTS_AND_FLIGHT.md` — ACTIVE / 460 lines / 15399 bytes
- `docs/design/APPROVED_COMMON_COMBAT_AND_RANK_BUDGET_POC_V1.md` — ACTIVE / 139 lines / 6307 bytes
- `docs/design/APPROVED_DOPAMINE_DRIVEN_DESIGN_AND_FIRST_10_MINUTES.md` — ACTIVE / 164 lines / 5900 bytes
- `docs/design/APPROVED_ENEMY_WARRIOR_LINEAGE_MILESTONES_V1.md` — ACTIVE / 619 lines / 17226 bytes
- `docs/design/APPROVED_OMENWARD_WORLD_AND_NAMING.md` — ACTIVE / 184 lines / 5641 bytes
- `docs/design/APPROVED_PERFORMANCE_DATA_TEST_READINESS_POC_V1.md` — ACTIVE / 469 lines / 13450 bytes
- `docs/design/APPROVED_PLAYER_TEN_UNIT_LINEAGES_POC_V1.md` — ACTIVE / 203 lines / 7908 bytes
- `docs/design/APPROVED_PREPRODUCTION_POC_BASELINE_V1.md` — ACTIVE / 217 lines / 8411 bytes
- `docs/design/APPROVED_PRIEST_HEAL_AND_COMMAND_SUPPORT_CLASS_V1.md` — ACTIVE / 346 lines / 10205 bytes
- `docs/design/APPROVED_ROULETTE_CORE_RULES.md` — ACTIVE / 270 lines / 9305 bytes
- `docs/design/APPROVED_ROULETTE_PROBABILITY_TARGETS_POC_V1.md` — ACTIVE / 138 lines / 4598 bytes
- `docs/design/APPROVED_SHARED_ARCHETYPE_WAVE_1_20_POC_V1.md` — ACTIVE / 204 lines / 7443 bytes
- `docs/design/APPROVED_SHARED_UNIT_ARCHETYPE_AND_FACTION_VISUAL_DATA_V1.md` — ACTIVE / 274 lines / 8995 bytes
- `docs/design/APPROVED_SPECIAL_CORPS_BUILDING_AND_GIANT_CLASS_V3.md` — ACTIVE / 256 lines / 7204 bytes
- `docs/design/APPROVED_STAGE_ECONOMY_AND_BUILDING_COST_BASELINE_V1.md` — ACTIVE / 533 lines / 17081 bytes
- `docs/design/APPROVED_TUTORIAL_CAMPAIGN_PROCEDURAL_POC_V1.md` — ACTIVE / 176 lines / 6459 bytes
- `docs/design/APPROVED_TUTORIAL_FIRST_FOUR_WAVES_BALANCE_V1.md` — ACTIVE / 599 lines / 17513 bytes
- `docs/design/APPROVED_UI_ART_AUDIO_POC_BIBLE_V1.md` — ACTIVE / 267 lines / 12398 bytes
- `docs/design/APPROVED_UNIT_ANIMATION_AND_BATTLE_PRESENTATION_GUIDE_V1.md` — ACTIVE / 539 lines / 21045 bytes
- `docs/design/APPROVED_UNIT_GRADE_AND_ABILITY_GROWTH.md` — ACTIVE / 240 lines / 8058 bytes
- `docs/design/APPROVED_UNIT_VISUAL_FORMAT_AND_REFERENCE_USE_V1.md` — ACTIVE / 172 lines / 7746 bytes
- `docs/design/APPROVED_WARRIOR_FAMILY_TIER2_ABILITIES.md` — ACTIVE / 318 lines / 10473 bytes
- `docs/design/DESIGN_FREEZE_CHECKLIST.md` — REFERENCE_OR_OTHER / 198 lines / 8925 bytes
- `docs/design/notes/0005a-construction-durability-and-doctrine-recommendation.md` — REFERENCE_OR_OTHER / 244 lines / 10136 bytes
- `docs/design/notes/0006a-tactical-command-economy-and-cooldown-recommendation.md` — REFERENCE_OR_OTHER / 218 lines / 7079 bytes
- `docs/design/notes/building-proposal-review-questions.md` — REFERENCE_OR_OTHER / 28 lines / 1412 bytes
- `docs/design/notes/roulette-proposal-review-questions.md` — REFERENCE_OR_OTHER / 34 lines / 1660 bytes
- `docs/design/proposals/0001-match-structure-and-start-conditions.md` — HISTORICAL / 348 lines / 13540 bytes
- `docs/design/proposals/0001-phase-0-godot-bootstrap.md` — HISTORICAL / 1138 lines / 40809 bytes
- `docs/design/proposals/0002-prototype-campaign-and-stage-generator.md` — HISTORICAL / 341 lines / 12140 bytes
- `docs/design/proposals/0003-constraint-system.md` — HISTORICAL / 250 lines / 8867 bytes
- `docs/design/proposals/0004-roulette-resolution-bench-and-lucky-chance.md` — HISTORICAL / 345 lines / 13506 bytes
- `docs/design/proposals/0005-building-tiers-lifecycle-and-economy.md` — HISTORICAL / 316 lines / 12121 bytes
- `docs/design/proposals/0006-tactical-commands.md` — HISTORICAL / 262 lines / 9736 bytes
- `docs/design/proposals/0007-boss-cleave-and-barricade-counter.md` — HISTORICAL / 199 lines / 8393 bytes
- `docs/design/proposals/0008-combat-keywords-status-effects-and-interrupts.md` — HISTORICAL / 51 lines / 2827 bytes
- `docs/design/proposals/0009-world-naming-directions.md` — HISTORICAL / 21 lines / 490 bytes
- `docs/design/proposals/0010-english-world-names-and-game-title.md` — HISTORICAL / 25 lines / 616 bytes
- `docs/design/proposals/0011-korean-natural-fantasy-names-law-and-mascot.md` — HISTORICAL / 24 lines / 553 bytes
- `docs/goals/0001-engine-selection-and-bootstrap.md` — HISTORICAL / 28 lines / 1285 bytes
- `docs/goals/0002-core-vertical-slice.md` — HISTORICAL / 275 lines / 9807 bytes
- `docs/images/README.md` — REFERENCE_OR_OTHER / 46 lines / 2102 bytes
- `docs/images/VISUAL_REFERENCE_INDEX.md` — REFERENCE_OR_OTHER / 108 lines / 8073 bytes
- `docs/issues/0001.md` — HISTORICAL / 47 lines / 1874 bytes
- `docs/issues/0002.md` — HISTORICAL / 60 lines / 2513 bytes
- `docs/issues/0003.md` — HISTORICAL / 34 lines / 1101 bytes
- `docs/issues/0004.md` — HISTORICAL / 39 lines / 1257 bytes
- `docs/issues/0005.md` — HISTORICAL / 59 lines / 2291 bytes
- `docs/issues/0006.md` — HISTORICAL / 34 lines / 1446 bytes
- `docs/issues/0007.md` — HISTORICAL / 42 lines / 1489 bytes
- `docs/issues/0008.md` — HISTORICAL / 34 lines / 1250 bytes
- `docs/issues/0009.md` — HISTORICAL / 43 lines / 1339 bytes
- `docs/issues/0010.md` — HISTORICAL / 44 lines / 1803 bytes
- `docs/issues/0011.md` — HISTORICAL / 34 lines / 1230 bytes
- `docs/issues/0012.md` — HISTORICAL / 40 lines / 1805 bytes
- `docs/issues/0013.md` — HISTORICAL / 34 lines / 1261 bytes
- `docs/issues/0014.md` — HISTORICAL / 38 lines / 1226 bytes
- `docs/issues/0015.md` — HISTORICAL / 85 lines / 2605 bytes
- `docs/issues/0016.md` — HISTORICAL / 71 lines / 2188 bytes
- `docs/issues/0017.md` — HISTORICAL / 90 lines / 2602 bytes
- `docs/issues/0018.md` — HISTORICAL / 38 lines / 1556 bytes
- `docs/issues/0019.md` — HISTORICAL / 63 lines / 1664 bytes
- `docs/issues/0020.md` — HISTORICAL / 66 lines / 1412 bytes
- `docs/issues/0021.md` — HISTORICAL / 109 lines / 4326 bytes
- `docs/issues/0022.md` — HISTORICAL / 72 lines / 2868 bytes
- `docs/issues/0023.md` — HISTORICAL / 105 lines / 3422 bytes
- `docs/issues/0024.md` — HISTORICAL / 44 lines / 1959 bytes
- `docs/issues/0025.md` — HISTORICAL / 52 lines / 2108 bytes
- `docs/issues/0026.md` — HISTORICAL / 85 lines / 2974 bytes
- `docs/issues/0027.md` — HISTORICAL / 124 lines / 3662 bytes
- `docs/issues/0028.md` — HISTORICAL / 136 lines / 4396 bytes
- `docs/issues/0029.md` — HISTORICAL / 105 lines / 3419 bytes
- `docs/issues/0030.md` — HISTORICAL / 120 lines / 4370 bytes
- `docs/issues/0031.md` — HISTORICAL / 86 lines / 2759 bytes
- `docs/issues/0032.md` — HISTORICAL / 137 lines / 4653 bytes
- `docs/issues/0033.md` — HISTORICAL / 120 lines / 3923 bytes
- `docs/issues/0034.md` — HISTORICAL / 117 lines / 3730 bytes
- `docs/issues/README.md` — HISTORICAL / 12 lines / 1049 bytes
- `docs/proposals/0002-benchmarking-review-gate.md` — HISTORICAL / 16 lines / 671 bytes
- `docs/proposals/0002-issue-body.md` — HISTORICAL / 30 lines / 1137 bytes
- `docs/proposals/README.md` — HISTORICAL / 5 lines / 228 bytes
- `docs/superpowers/plans/2026-07-16-vertical-slice-stage-progression.md` — REFERENCE_OR_OTHER / 297 lines / 16579 bytes
- `docs/superpowers/specs/2026-07-16-vertical-slice-stage-progression-design.md` — REFERENCE_OR_OTHER / 50 lines / 3461 bytes
- `docs/work_orders/0001-phase-0-codex-plan-mode.md` — HISTORICAL / 390 lines / 15413 bytes
- `docs/work_orders/0002-current-main-audit-and-next-iteration-plan-mode.md` — HISTORICAL / 202 lines / 8987 bytes
- `project.godot` — REFERENCE_OR_OTHER / 24 lines / 703 bytes
- `scenes/battle/battlefield.tscn` — ACTIVE / 6 lines / 195 bytes
- `scenes/main/main.tscn` — ACTIVE / 19 lines / 767 bytes
- `scenes/ui/stage_hud.tscn` — ACTIVE / 121 lines / 3271 bytes
- `scenes/ui/stage_select.tscn` — ACTIVE / 61 lines / 1549 bytes
- `scenes/units/unit.tscn` — ACTIVE / 6 lines / 180 bytes
- `schemas/skill-registry-v3.schema.json` — REFERENCE_OR_OTHER / 81 lines / 3128 bytes
- `scripts/battle/assassin_bypass_state.gd` — ACTIVE / 64 lines / 1715 bytes
- `scripts/battle/battle_simulator.gd` — ACTIVE / 139 lines / 4546 bytes
- `scripts/battle/battlefield_view.gd` — ACTIVE / 73 lines / 2815 bytes
- `scripts/battle/clash_zone_state.gd` — ACTIVE / 20 lines / 444 bytes
- `scripts/battle/gate_state.gd` — ACTIVE / 49 lines / 1277 bytes
- `scripts/battle/lane_state.gd` — ACTIVE / 54 lines / 1487 bytes
- `scripts/battle/outpost_state.gd` — ACTIVE / 171 lines / 5081 bytes
- `scripts/battle/unit_instance.gd` — ACTIVE / 139 lines / 3975 bytes
- `scripts/buildings/building_service.gd` — ACTIVE / 97 lines / 3347 bytes
- `scripts/buildings/building_state.gd` — ACTIVE / 14 lines / 457 bytes
- `scripts/core/bootstrap_validator.gd` — ACTIVE / 46 lines / 2223 bytes
- `scripts/core/combat_clock.gd` — ACTIVE / 12 lines / 256 bytes
- `scripts/core/data_registry.gd` — ACTIVE / 46 lines / 1514 bytes
- `scripts/core/determinism_service.gd` — ACTIVE / 24 lines / 676 bytes
- `scripts/core/game_session.gd` — ACTIVE / 53 lines / 1858 bytes
- `scripts/core/stage_economy.gd` — ACTIVE / 56 lines / 1712 bytes
- `scripts/core/stage_manifest.gd` — ACTIVE / 27 lines / 653 bytes
- `scripts/core/stage_progression.gd` — ACTIVE / 15 lines / 334 bytes
- `scripts/core/stage_run.gd` — ACTIVE / 120 lines / 4092 bytes
- `scripts/data/animation_contract.gd` — ACTIVE / 5 lines / 211 bytes
- `scripts/data/attack_profile.gd` — ACTIVE / 7 lines / 181 bytes
- `scripts/data/battlefield_profile.gd` — ACTIVE / 8 lines / 253 bytes
- `scripts/data/bootstrap_catalog.gd` — ACTIVE / 15 lines / 698 bytes
- `scripts/data/boss_behavior_package.gd` — ACTIVE / 5 lines / 126 bytes
- `scripts/data/boss_phase_profile.gd` — ACTIVE / 5 lines / 118 bytes
- `scripts/data/building_definition.gd` — ACTIVE / 7 lines / 189 bytes
- `scripts/data/faction_visual_profile.gd` — ACTIVE / 7 lines / 209 bytes
- `scripts/data/rank_profile.gd` — ACTIVE / 6 lines / 147 bytes
- `scripts/data/stage_definition.gd` — ACTIVE / 24 lines / 766 bytes
- `scripts/data/tier_profile.gd` — ACTIVE / 6 lines / 147 bytes
- `scripts/data/unit_archetype_profile.gd` — ACTIVE / 9 lines / 273 bytes
- `scripts/data/unit_spawn_definition.gd` — ACTIVE / 24 lines / 689 bytes
- `scripts/data/wave_definition.gd` — ACTIVE / 20 lines / 570 bytes
- `scripts/roulette/roulette_service.gd` — ACTIVE / 40 lines / 1434 bytes
- `scripts/ui/bootstrap_visual_probe.gd` — ACTIVE / 10 lines / 425 bytes
- `scripts/ui/stage_hud.gd` — ACTIVE / 71 lines / 2009 bytes
- `scripts/ui/stage_select.gd` — ACTIVE / 37 lines / 1018 bytes
- `scripts/units/deployment_service.gd` — ACTIVE / 28 lines / 800 bytes
- `scripts/units/unit_view.gd` — ACTIVE / 44 lines / 1383 bytes
- `scripts/waves/wave_director.gd` — ACTIVE / 44 lines / 1162 bytes
- `skills/README.md` — REFERENCE_OR_OTHER / 26 lines / 1082 bytes
- `skills/SHARED_EXECUTION_CONTRACT.md` — REFERENCE_OR_OTHER / 70 lines / 3077 bytes
- `skills/disciplines/01-narrative/SKILL.md` — REFERENCE_OR_OTHER / 34 lines / 724 bytes
- `skills/disciplines/02-game-design/SKILL.md` — REFERENCE_OR_OTHER / 35 lines / 802 bytes
- `skills/disciplines/03-ux-ui-accessibility/SKILL.md` — REFERENCE_OR_OTHER / 37 lines / 745 bytes
- `skills/disciplines/04-engineering/SKILL.md` — REFERENCE_OR_OTHER / 38 lines / 816 bytes
- `skills/disciplines/05-technical-art-pipeline/SKILL.md` — REFERENCE_OR_OTHER / 36 lines / 810 bytes
- `skills/disciplines/06-art/SKILL.md` — REFERENCE_OR_OTHER / 37 lines / 783 bytes
- `skills/disciplines/07-audio/SKILL.md` — REFERENCE_OR_OTHER / 36 lines / 720 bytes
- `skills/disciplines/08-qa/SKILL.md` — REFERENCE_OR_OTHER / 36 lines / 703 bytes
- `skills/disciplines/09-production-pm/SKILL.md` — REFERENCE_OR_OTHER / 35 lines / 766 bytes
- `skills/disciplines/10-analytics-user-research/SKILL.md` — REFERENCE_OR_OTHER / 36 lines / 710 bytes
- `skills/disciplines/11-integration-review/SKILL.md` — REFERENCE_OR_OTHER / 40 lines / 879 bytes
- `skills/foundation/evolving-project-discipline-skills/SKILL.md` — REFERENCE_OR_OTHER / 45 lines / 1101 bytes
- `skills/foundation/maintaining-project-context-and-handoff/SKILL.md` — REFERENCE_OR_OTHER / 43 lines / 1081 bytes
- `skills/foundation/managing-base-change-proposals/SKILL.md` — REFERENCE_OR_OTHER / 41 lines / 1108 bytes
- `skills/foundation/managing-design-documents/SKILL.md` — REFERENCE_OR_OTHER / 43 lines / 1049 bytes
- `skills/foundation/managing-game-project-operating-system/SKILL.md` — REFERENCE_OR_OTHER / 45 lines / 1114 bytes
- `skills/foundation/managing-project-intake-and-work-contract/SKILL.md` — REFERENCE_OR_OTHER / 47 lines / 1052 bytes
- `skills/foundation/reviewing-and-validating-project-changes/SKILL.md` — REFERENCE_OR_OTHER / 47 lines / 1210 bytes
- `skills/specialists/analyzing-and-refining-game-concepts/SKILL.md` — REFERENCE_OR_OTHER / 39 lines / 929 bytes
- `skills/specialists/auditing-and-refining-ui-art/SKILL.md` — REFERENCE_OR_OTHER / 39 lines / 1000 bytes
- `skills/specialists/auditing-canonical-reference-freshness/SKILL.md` — REFERENCE_OR_OTHER / 38 lines / 944 bytes
- `skills/specialists/designing-art-prompts-and-technique-cards/SKILL.md` — REFERENCE_OR_OTHER / 38 lines / 952 bytes
- `skills/specialists/designing-vertical-slices/SKILL.md` — REFERENCE_OR_OTHER / 39 lines / 930 bytes
- `skills/specialists/orchestrating-deepseek-worktrees/SKILL.md` — REFERENCE_OR_OTHER / 42 lines / 1103 bytes
- `src/README.md` — REFERENCE_OR_OTHER / 3 lines / 136 bytes
- `tests/README.md` — ACTIVE / 3 lines / 176 bytes
- `tests/headless/battle_simulation_test.gd` — ACTIVE / 163 lines / 9328 bytes
- `tests/headless/economy_roulette_test.gd` — ACTIVE / 128 lines / 7376 bytes
- `tests/headless/phase_0_contract_test.gd` — ACTIVE / 35 lines / 1836 bytes
- `tests/headless/scene_contract_test.gd` — ACTIVE / 53 lines / 2584 bytes
- `tests/headless/stage_data_contract_test.gd` — ACTIVE / 152 lines / 5962 bytes
- `tests/headless/stage_run_test.gd` — ACTIVE / 109 lines / 5625 bytes
- `tests/python/test_adversarial_review_contract.py` — ACTIVE / 33 lines / 1372 bytes
- `tests/python/test_issue_mirror.py` — ACTIVE / 110 lines / 4303 bytes
- `tests/python/test_project_core_docs.py` — ACTIVE / 93 lines / 4057 bytes
- `tests/python/test_skill_package_integrity.py` — ACTIVE / 54 lines / 2093 bytes
- `tests/python/test_skill_routing_contract.py` — ACTIVE / 63 lines / 2716 bytes
- `tools/_collect_c1_roulette_audit.py` — ACTIVE / 241 lines / 9282 bytes
- `tools/issue_mirror.py` — ACTIVE / 205 lines / 7748 bytes
- `tools/route_skills.py` — ACTIVE / 161 lines / 5574 bytes
- `tools/sync_repo.ps1` — ACTIVE / 33 lines / 971 bytes
- `tools/validate_project_core_docs.py` — ACTIVE / 233 lines / 8666 bytes
- `tools/validate_skill_system.py` — ACTIVE / 163 lines / 7148 bytes
