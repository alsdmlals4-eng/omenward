# Five Sequential Front Maps · Five-Pass Adversarial Review

```yaml
reviewed_at: 2026-09-02 KST
decision: OMW-PLAN-20260902-FIVE-SEQUENTIAL-FRONT-MAPS-01
scope: code_data_ui_current_router_asset_provenance_machine_evidence
result: MACHINE_SCOPE_PASS_5_OF_5__LIVE_REGULAR_TRANSITION_HUMAN_RIGHTS_DEVICE_RELEASE_NOT_RUN
```

This review attacks the actual retained single-front implementation, not an earlier three-front
blueprint. A pass below means that the listed machine-scope evidence did not find a violation; it is
not a general player-quality or release certification.

| Pass | Attack | Evidence and result | Remaining boundary |
|---:|---|---|---|
| 1 | Could five labels still be decorative cells, selectable branches, a three-front recovery, or a single long map? | `FrontMapDefinition`, regular stage data, bootstrap validation, and `five_sequential_front_maps_contract_test.gd` require exactly five ordered packages and exactly one current `front`. `selectable` is always false. **PASS.** | A person has not judged the labels/reading pace. |
| 2 | Could a non-final victory prematurely finish the run, skip its assigned Wave package, or lose global state? | Review initially found that early enemy-base destruction could skip W1–W4 and that an in-flight Lumern assassin could be discarded. The repaired contract now rejects early base destruction until the current four-wave package is exhausted, preserves a living Lumern bypass instance, and makes a living Veil bypass block clear. Map 5 alone reaches Stage `VICTORY`, with all five ribbon states cleared. **PASS after repair.** | A live regular save has not been manually completed. |
| 3 | Could UI permit jumping ahead, hide the map result, duplicate units in the ribbon, or add map buildings/towers? | `strategic_map_ui_contract_test.gd` asserts read-only state and actual Review CTA invocation. Run Command keeps global roster in PREPARE; BattleFocus uses one active front; MarchMinimap has one tower glyph. **PASS.** | Human legibility of the ribbon and CTA is `NOT_RUN`. |
| 4 | Could generated terrain silently replace a locked art asset or introduce blocked routes? | Five source candidates are hash-recorded as `GENERATED_CANDIDATE__AWAITING_USER_VISUAL_CONFIRMATION`; `BattleFocusView` resolves terrain keys but deliberately retains the user-locked modular foundation. Candidate briefs require clear centre corridors and edge-only props. **PASS.** | User has not selected/canonized any candidate; commercial rights and final art review remain `NOT_RUN`. |
| 5 | Could routing/evidence claim more than current verification or leave a protected-path approval gap? | Full Godot `35 / 35`, Python `571`, C2 mutation regression, current project-doc validation, and exact Base approved-change validator passed. The new additive approval receipt includes the new protected files. Runtime capture is explicitly limited to tutorial entry; the live regular transition remains pending. **PASS.** | Exact PR-head CI, live regular handoff, human/device/accessibility/release review still remain. |

## Retained risk register

1. The normal player path cannot launch regular maps until the tutorial grants its existing unlock.
   That protects progression, but it means the next live transition smoke needs a tutorial-complete
   session or a deliberately documented test fixture.
2. The five backgrounds are not runtime bytes. Binding one before user confirmation would violate the
   asset lifecycle even though the terrain key is implemented.
3. The PREPARE capture's image analyzer emitted `possible_clipping`; this is not dismissed as a
   general UI quality pass, because the large intentionally empty board surface can trigger that
   heuristic.

## Machine evidence

```text
Godot focused sequential contract: PASS
Godot focused UI contract: PASS
Godot full batch: 35 / 35 PASS
Python full batch: 571 PASSED
Project current-document validator: PASS
Exact Base approved project operating-contract validator: PASS
Live entry error diagnostics: 0 errors, 0 warnings
```
