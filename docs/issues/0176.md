---
issue_number: 176
title: "[PR175 Follow-up] Close role-output authority and FV fixture gaps via HiGodot"
state: open
github_url: "https://github.com/alsdmlals4-eng/omenward/issues/176"
github_updated_at: "2026-08-09T14:09:54Z"
last_synced_body_sha: ebae45b659ba852b8d24314ea0fd1b03e2b159e86dea31d97222c789c5d8825b
labels: []
assignees: []
canonical_documents: []
---

# Issue #176: [PR175 Follow-up] Close role-output authority and FV fixture gaps via HiGodot

## Issue body
## Authority

Parent implementation package Decision: `OMW-DEC-20260809-PLANNING-BARRACKS-ROLE-OUTPUT-RUNTIME-IMPLEMENTATION-PACKAGE-V1`
Parent executor: #174
Draft implementation PR: #175
Reviewed exact head: `621ae7ce80f24b1e5a9d13ba4fc1962bec42dd96`
Review record: `4891564142`

This is a follow-up execution packet under the already-approved package. **No new product Decision and no reapproval are required for the same approved scope.** Persistent Godot/GDScript mutation remains HiGodot-only.

## Blocking gaps to close

1. Priest encouragement must have the approved provisional behavior: 5 seconds, attack-speed +8%, with start/end events and measurable support uptime. Current head only records telemetry. Add minimal non-generic runtime effect and a GUT RED→GREEN test proving attack timing changes during the buff and returns after expiry.
2. Do not intercept every `role == support` unit and unconditionally skip existing movement/combat/objective behavior. Restrict the Priest role action to the intended archetype/action and preserve deterministic existing fallback when no support action occurs. Add regression coverage.
3. Remove the invented universal air-target eligibility filter. `flying` is a priority, not a permission boundary in the approved contract. Keep air units in the legal enemy set unless another existing rule excludes them; Archer `flying` priority must still select Flier. `AIR_TARGETABILITY_EXPOSURE` remains `BLOCKED_RUNTIME_OUTPUT`.
4. Fix `cluster` density tie-break to existing lane order/unit-id, not distance-sorted candidate order. Add a fixture where those choices differ.
5. Add required Giant collector outputs `FRONTLINE_SURVIVAL_TIME` and `STRUCTURE_DAMAGE` with deterministic instrumentation/tests. Do not encode an unavailable value as numeric zero.
6. Replace/augment the simplified `FV-*` smoke harness with the approved registered fixtures from the measurement Decision:
   - FV-PRIEST-01: ally shield_guard + greatsword_warrior; enemy pressure greatsword_warrior + archer; Pair A with Priest vs Pair B without Priest; same geometry/opponent.
   - FV-MAGE-01: Mage vs Archer under identical fixed cluster setup.
   - FV-FLIER-01: Flier vs Assassin with identical frontline/backline geometry.
   - FV-GIANT-01: Giant vs Greatsword with identical frontline and explicit fixed structure phase.
   - FV-COMMON-01: preserve same single-target contact inputs and collect its registered measurable outputs.
   Preserve raw events for every scenario and do not create a weighted scalar.
7. Correct `TARGETS_HIT_PER_CAST`: current accumulation is total targets across casts. Expose true per-cast semantics (or per-cast event values aggregated by the scenario collector) and add multi-cast coverage.

## Required execution sequence

1. Fresh-check branch/main and preserve existing work.
2. Confirm OMENWARD HiGodot session is the target.
3. Add/adjust GUT tests through HiGodot and establish intended RED with >0 tests.
4. Persistent changes only through HiGodot/Godot AI MCP.
5. Godot 4.7.1 parse/import PASS.
6. GUT GREEN with >0 discovered tests; run existing headless regressions.
7. Run each approved FV fixture twice; require complete raw output identity and preserve blocked sentinels.
8. Hera live QA only after Green; require tracked-source delta NONE.
9. Ensure no simulation CSV `.csv.import`/`.translation` sidecars and no final FV scalar/vector/product numeric selection.
10. Commit and push only `runtime/barracks-role-output-implementation-20260809`; do not merge locally.

## No-approval local execution

The existing PowerShell executor supports `-NonInteractive`, but it reads an Issue body as its packet. Run this follow-up directly with:

```powershell
cd C:\Users\user\Documents\GitHub\Ninza\omenward
git fetch origin
git switch runtime/barracks-role-output-implementation-20260809
git pull --ff-only
.\tools\invoke_barracks_role_output_executor.ps1 -IssueNumber 176 -NonInteractive
```

The executor uses workspace-write + approval-never. If a genuine authority conflict or unverifiable prerequisite occurs, stop as `BLOCKED_UNVERIFIED`; do not broaden scope or bypass HiGodot.
