# [증거] Generated operating artifact LF checkout incident · 2026-08-28

## Classification

```text
INCIDENT_ID = OMW-INC-20260828-GENERATED-ARTIFACT-EOL-01
CLASS = P1_WORKFLOW_CANONICALIZATION
STATUS = RESOLVED_PROJECT_GUARD__BASE_PROMOTION_PROPOSED
PROJECT_ISSUE = https://github.com/alsdmlals4-eng/omenward/issues/247
BASE_PROMOTION = PROPOSED
BASE_ISSUE = https://github.com/alsdmlals4-eng/Base/issues/768
```

## Incident

After PR #246 merged, the Base operating-contract `--check` reported four deterministic generated views as manually modified or stale in this Windows checkout. The current game canon, board topology, and GitHub main content were not changed by this finding.

## Verified root cause

This checkout has `core.autocrlf=true` and no project `.gitattributes` rule for Base-generated outputs. The operating contract compares raw generated bytes; CRLF checkout conversion can therefore differ from the generator's LF bytes even when the committed source is correct. Re-running the generator restored LF bytes and made both generated-artifact and operating-contract checks pass.

## Solution

`.gitattributes` pins only the five outputs emitted by the project operating-artifact generator to `text eol=lf`:

1. `.agents/skills/omenward-workflow-router/SKILL.md`
2. `docs/PROJECT_OPERATING_DASHBOARD.html`
3. `skills/BASE_V9_ADAPTER.json`
4. `skills/PROJECT_BASE_SKILL_ADAPTER.json`
5. `skills/PROJECT_SKILL_SNAPSHOT.json`

The regression test verifies the exact attribute for all five paths. No Godot code, Scene, Resource, asset, player-facing rule, or visual decision changes are part of this solution.

## Lesson and Base disposition

The raw-byte reproducibility requirement is shared infrastructure rather than an OMENWARD-specific rule. The project keeps only its narrow checkout guard and evidence record. A reusable Base-owned prevention pattern is proposed in Base Issue #768; the project must not duplicate a global operating rule into game canon.

## Validation record

```text
RED = test_generated_operating_artifacts_are_checked_out_with_lf failed for all five paths before .gitattributes
GREEN = same test passed after .gitattributes
GENERATED_ARTIFACT_CHECK = PASS
BASE_OPERATING_CONTRACT_CHECK = PASS
```
