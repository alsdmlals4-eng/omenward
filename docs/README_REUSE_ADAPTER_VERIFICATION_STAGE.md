# Reuse adapter verification stage

This branch starts with a deliberate RED contract. The adapter is already present on `main`; this PR adds durable workflow triggering and exact verification evidence without integrating it into Omenward product runtime.
