name: Sync Validation Evidence

on:
  pull_request:
    branches:
      - main
    paths:
      - "tools/_sync_evidence_runtime.py"
      - ".github/workflows/sync-validation-evidence.yml"

permissions:
  contents: write

jobs:
  sync:
    if: github.head_ref == 'agent/c1-approved-roulette-contract-recovery'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/c1-approved-roulette-contract-recovery
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Synchronize current C1 evidence
        shell: bash
        run: |
          set +e
          python tools/_sync_evidence_runtime.py > /tmp/evidence-runtime.log 2>&1
          status=$?
          if [ "$status" -ne 0 ]; then
            tail -n 380 /tmp/evidence-runtime.log > /tmp/evidence-runtime-failure.log
            git fetch origin agent/c1-approved-roulette-contract-recovery
            git reset --hard origin/agent/c1-approved-roulette-contract-recovery
            cp /tmp/evidence-runtime-failure.log docs/_EVIDENCE_RUNTIME_FAILURE.log
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add docs/_EVIDENCE_RUNTIME_FAILURE.log
            git commit -m "record C1 evidence runtime failure"
            git push origin HEAD:agent/c1-approved-roulette-contract-recovery
          fi
          exit $status
