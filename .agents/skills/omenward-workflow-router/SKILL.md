---
name: omenward-workflow-router
description: Resolve OMENWARD project-local routes through the current project adapter while fresh-reading current Base behavioral authority.
---

# Project Workflow Router

Before selecting a route, fresh-read **Base latest completed main**, current Base `AGENTS.md`, current `skills/SKILL_REGISTRY.json`, and only the Base owner required by the current trigger. The project adapter's exact Base **release pin** remains adoption/integrity evidence; it does not replace current Base behavioral authority.

Then validate this repository's project operating contract against `skills/PROJECT_BASE_ADAPTER.json` and the generated `skills/PROJECT_SKILL_SNAPSHOT.json`. On a nonzero validator result, stop route execution as `BLOCKED_UNVERIFIED`; do not infer around a broken adapter/snapshot.

After validation, resolve `effective_routes` exactly as generated. Project-local routes take precedence over same-name Base routes. Follow the selected recorded package at its path; this router contains no copied Base shared Skill body.

Google Sheets is a migration/compatibility source only. Human-facing project meaning is resolved from the exact Project Notion workspace, while structured/runtime truth remains in the repository and actual execution evidence.
