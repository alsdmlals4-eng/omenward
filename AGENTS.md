# Roulettebound 프로젝트 AI 작업 규칙

이 저장소는 ChatGPT가 기획과 Issue/Goal을 정리하고, Codex가 승인된 범위 안에서 Godot 프로젝트를 구현하는 공동 작업용 저장소다.

## 규칙 우선순위

1. 사용자의 최신 지시
2. 사용자가 승인한 Codex Plan Mode 제안서와 현재 GitHub Issue/Goal
3. 이 프로젝트의 `AGENTS.md`
4. `docs/DOCUMENTATION_MAP.md`, `docs/GAME_DESIGN.md`와 프로젝트 문서
5. `docs/ai/`의 로컬 공용 규칙 요약
6. 실제 파일과 실행 결과
7. 외부 스킬 및 벤치마킹 자료

충돌하거나 불명확한 내용은 추정해 구현하지 말고 `확인 필요`로 보고한다.

## 작업 전 읽기 순서

1. `AGENTS.md`
2. `docs/BASE_RULES_VERSION.md`
3. `docs/DOCUMENTATION_MAP.md`
4. `docs/GAME_DESIGN.md`
5. 현재 Issue 또는 Goal
6. `docs/PROPOSAL_WORKFLOW.md`
7. 승인된 제안서가 있으면 해당 제안서
8. 관련 코드·데이터·테스트 파일
9. `docs/ACTIVE_CONTEXT.md`

## 역할 분리

- 기획/조정 AI: 플레이어 경험, 사양, 범위, 제외 범위, 완료 기준, 검증 방법을 정리하고 확정 사항을 GitHub 문서에 반영한다.
- Codex Plan Mode: 저장소를 읽기 전용으로 조사하고 구현 전에 검토 가능한 제안서를 작성한다.
- Codex 구현 모드: 사용자가 승인한 제안서와 Issue/Goal만 구현하고 제품 방향을 임의로 확장하지 않는다.
- 사용자: 방향, 우선순위, 제안서와 중요한 미확정 사항을 승인한다.

## GitHub 반영 원칙

- 이 대화에서 사용자가 확정한 기획·규칙·우선순위는 별도 요청이 없어도 관련 프로젝트 문서와 Goal에 반영하고 `main`에 push한다.
- 실제 게임 코드 구현은 기본적으로 `codex/issue-<번호>-<설명>` 브랜치와 Pull Request를 사용한다.
- 문서 반영과 코드 구현을 한 작업으로 섞지 않는다. 코드가 필요한 결정은 문서와 Issue/Goal을 먼저 확정한다.
- push 또는 PR 생성에 실패한 작업은 GitHub에 반영됐다고 보고하지 않는다.

## Codex Plan Mode 제안서 게이트

- 코드·Scene·Resource·데이터 구조·Godot 설정을 변경하는 모든 작업은 먼저 Codex Plan Mode 제안서를 거친다.
- Plan Mode에서는 저장소와 참고 자료를 읽고 `docs/PROPOSAL_WORKFLOW.md`의 형식으로 목적, 근거, 구조, 단계, 파일, 위험, 검증과 사용자 결정 요청을 제시한다.
- 제안서 검토 중에는 `project.godot`, 게임 코드, Scene, Resource, 데이터 파일을 생성하거나 수정하지 않는다.
- 사용자가 `제안서 승인`, `이 계획으로 구현 진행`처럼 명확히 승인하기 전에는 구현 브랜치·커밋·PR을 만들지 않는다.
- 질문, 의견, 부분 동의는 승인으로 간주하지 않는다. 제안서 범위가 바뀌면 변경안을 다시 검토받는다.
- 구현 중 승인 범위를 벗어나는 필요가 발견되면 작업을 멈추고 제안서 수정안으로 되돌린다.

## 기술 불변 조건

- 엔진은 **Godot**이다.
- 기본 구현 언어는 **GDScript**다. C# 또는 GDExtension은 명시적인 성능·통합 근거와 사용자 승인 없이 추가하지 않는다.
- Godot 정확한 minor 버전, 목표 플랫폼, 기준 해상도는 `docs/DECISIONS_PENDING.md`에서 확정한다.
- `.godot/`과 로컬 캐시·에디터 생성물은 커밋하지 않는다.
- 장면은 `scenes/`, 스크립트는 `scripts/`, 밸런스·정적 데이터는 `data/` 또는 `resources/`, 검증 코드는 `tests/`에 둔다.
- 전역 AutoLoad는 여러 장면이 공유해야 하는 상태·서비스에만 사용한다. 같은 상태를 장면과 AutoLoad가 중복 소유하지 않는다.
- 반복 UI는 Godot `Control`, `Container`, `Theme`, 재사용 Scene과 명시적 Signal을 우선한다. 별도 범용 UI 프레임워크를 만들지 않는다.
- 재사용 UI 컴포넌트는 게임 상태를 직접 수정하지 않고 표시 데이터를 입력받아 사용자 의도를 Signal로 반환한다.

## 구현 시작 조건

Codex는 다음이 모두 준비되기 전 코드를 수정하지 않는다.

- 사용자가 승인한 Plan Mode 제안서
- 목표와 플레이어 가치
- 포함 범위와 제외 범위
- 완료 기준
- 데이터·UI·저장 영향
- 검증 방법
- 실행할 Godot 버전과 실행 명령

## 프로젝트 고유 원칙

- 핵심은 `건물 구성 → 룰렛 확률 변화 → 유닛 획득 → 3라인 배치 → 접전지·시설 공방`의 연결이다.
- 건물 티어는 생산량, 생산속도, 품질과 전투 성능을 강화하지만 룰렛 토큰 수는 **완공된 건물 개수**로 계산한다.
- 기본 포탑 사거리는 중앙 접전지에 닿지 않아야 한다. 여러 단계의 명시적 사거리 업그레이드 후에만 가능하다.
- 적은 플레이어와 같은 룰렛 AI를 사용하지 않고 건물 기반 정기 웨이브를 사용한다.
- 모든 핵심 수치와 상성은 가능한 한 데이터 기반으로 관리한다. 코드에 밸런스 값을 흩어놓지 않는다.
- 전방 시설 파괴, 식량 한도 감소, 토큰 제거처럼 시스템 간 연쇄 효과는 명시적으로 테스트한다.
- 화면과 조작은 세 라인의 위협, 점령 상태, 다음 적 웨이브, 룰렛 확률을 동시에 이해할 수 있어야 한다.

## 참고 저장소 사용 원칙

- `alsdmlals4-eng/Base`: 공용 spec-first 작업 흐름, Issue/Goal, 검증, Compound 회고의 기준 원본이다.
- `alsdmlals4-eng/urban-legend`: Godot 프로젝트 폴더 구조, AutoLoad 상태 소유, 데이터 로딩, 네이티브 UI, headless 검증과 인수인계 운영의 참고 사례다.
- 두 저장소의 코드를 무단 복사하거나 자동 동기화하지 않는다. 현재 작업에 필요한 구조를 먼저 분석하고 Roulettebound의 장르·성능·데이터 요구에 맞게 축소 적용한다.
- 참고 기준과 채택·제외 결론은 `docs/REFERENCE_REPOSITORIES.md`에 기록한다.

## 벤치마킹 및 저작권 경계

- 참고 게임과 저장소의 시스템 의도와 문제 해결 방식만 분석한다.
- 코드, 아트, 사운드, UI 배치, 고유 명칭, 캐릭터 디자인을 복제하지 않는다.
- 프로젝트 자산은 직접 제작하거나 사용 권한이 확인된 자산만 사용한다.
- 임시 자산도 출처와 교체 필요 여부를 문서화한다.

## 구현 원칙

- 실제 저장소 상태와 호출 흐름을 먼저 확인한다.
- 승인 없이 범위 밖 리팩터링 또는 기능 추가를 하지 않는다.
- Godot 기본 노드·Resource·Signal·Container와 이미 존재하는 구조를 우선한다.
- 가장 작은 변경으로 관찰 가능한 완료 기준을 만족한다.
- 정상 동작 중인 사용자 변경을 되돌리지 않는다.
- 실패 상태와 경계 조건을 먼저 정의하고 테스트 가능한 seam을 만든다.
- 변경에 맞춰 `git diff --check`, Godot headless 파싱/실행, 변경 Scene, 영향 플레이 경로 순으로 검증한다.

## 제안서 보고 형식

`docs/PROPOSAL_WORKFLOW.md`의 필수 형식을 사용한다. 제안서 마지막에는 현재 상태를 `제안서 검토 대기`로 표시하고, 승인 전 구현 금지를 명시한다.

## 구현 작업 시작 보고 형식

```md
## 승인 범위 확인
- 승인된 제안서:
- 목표:
- 플레이어 가치:
- 범위:
- 제외 범위:
- 예상 변경 파일:
- 데이터/스키마 영향:
- UI/UX 영향:
- 위험과 의존성:
- 완료 기준:
- 검증 계획:
```

## 완료 보고 형식

```md
## 결과
- 변경 파일:
- 구현 내용과 이유:
- 실행한 검증:
- 검증 결과:
- 미검증 항목:
- 남은 위험:
- 사용자 확인 항목:
- Compound 교훈:
- Base 승격 후보:
```

테스트하지 못한 항목을 완료했다고 보고하지 않는다.
