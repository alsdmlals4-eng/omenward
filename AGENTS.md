# Roulettebound 프로젝트 AI 작업 규칙

이 저장소는 ChatGPT가 기획과 Issue/Goal을 정리하고, Codex가 승인된 범위 안에서 구현하는 공동 작업용 저장소다.

## 규칙 우선순위

1. 사용자의 최신 지시
2. 현재 승인된 GitHub Issue 또는 `docs/goals/` Goal
3. 이 프로젝트의 `AGENTS.md`
4. `docs/GAME_DESIGN.md`와 프로젝트 문서
5. `docs/ai/`의 로컬 공용 규칙 요약
6. 실제 파일과 실행 결과
7. 외부 스킬 및 벤치마킹 자료

충돌하거나 불명확한 내용은 추정해 구현하지 말고 `확인 필요`로 보고한다.

## 작업 전 읽기 순서

1. `AGENTS.md`
2. `docs/BASE_RULES_VERSION.md`
3. `docs/GAME_DESIGN.md`
4. 현재 Issue 또는 Goal
5. 관련 코드·데이터·테스트 파일
6. `docs/ACTIVE_CONTEXT.md`

## 역할 분리

- 기획/조정 AI: 플레이어 경험, 사양, 범위, 제외 범위, 완료 기준, 검증 방법을 정리한다.
- Codex/구현 AI: 승인된 Issue 또는 Goal만 구현하고 제품 방향을 임의로 확장하지 않는다.
- 사용자: 방향, 우선순위, 엔진과 중요한 미확정 사항을 승인한다.

## 구현 시작 조건

Codex는 다음이 준비되기 전 코드를 수정하지 않는다.

- 목표와 플레이어 가치
- 포함 범위와 제외 범위
- 완료 기준
- 데이터·UI·저장 영향
- 검증 방법
- 엔진 및 실행 방법

엔진이 확정되기 전에는 Unity, Godot, Unreal 또는 웹 프로젝트를 임의로 생성하지 않는다.

## 프로젝트 고유 원칙

- 핵심은 `건물 구성 → 룰렛 확률 변화 → 유닛 획득 → 3라인 배치 → 접전지·시설 공방`의 연결이다.
- 건물 티어는 생산량, 생산속도, 품질과 전투 성능을 강화하지만 룰렛 토큰 수는 **완공된 건물 개수**로 계산한다.
- 기본 포탑 사거리는 중앙 접전지에 닿지 않아야 한다. 여러 단계의 명시적 사거리 업그레이드 후에만 가능하다.
- 적은 플레이어와 같은 룰렛 AI를 사용하지 않고 건물 기반 정기 웨이브를 사용한다.
- 모든 핵심 수치와 상성은 가능한 한 데이터 기반으로 관리한다. 코드에 밸런스 값을 흩어놓지 않는다.
- 전방 시설 파괴, 식량 한도 감소, 토큰 제거처럼 시스템 간 연쇄 효과는 명시적으로 테스트한다.
- 화면과 조작은 세 라인의 위협, 점령 상태, 다음 적 웨이브, 룰렛 확률을 동시에 이해할 수 있어야 한다.

## 벤치마킹 및 저작권 경계

- 참고 게임의 시스템 의도와 문제 해결 방식만 분석한다.
- 코드, 아트, 사운드, UI 배치, 고유 명칭, 캐릭터 디자인을 복제하지 않는다.
- 프로젝트 자산은 직접 제작하거나 사용 권한이 확인된 자산만 사용한다.
- 임시 자산도 출처와 교체 필요 여부를 문서화한다.

## 구현 원칙

- 실제 저장소 상태와 호출 흐름을 먼저 확인한다.
- 승인 없이 범위 밖 리팩터링 또는 기능 추가를 하지 않는다.
- 기존 엔진 기능, 표준 기능, 이미 존재하는 구조를 우선한다.
- 가장 작은 변경으로 관찰 가능한 완료 기준을 만족한다.
- 정상 동작 중인 사용자 변경을 되돌리지 않는다.
- 실패 상태와 경계 조건을 먼저 정의하고 테스트 가능한 seam을 만든다.

## 작업 시작 보고 형식

```md
## 작업 이해
- 목표:
- 플레이어 가치:
- 범위:
- 제외 범위:
- 예상 변경 파일:
- 데이터/스키마 영향:
- UI/UX 영향:
- 위험과 의존성:
- 완료 기준:
- 검증 계획:
```

## 완료 보고 형식

```md
## 결과
- 변경 파일:
- 구현 내용과 이유:
- 실행한 검증:
- 검증 결과:
- 미검증 항목:
- 남은 위험:
- 사용자 확인 항목:
- Compound 교훈:
- Base 승격 후보:
```

테스트하지 못한 항목을 완료했다고 보고하지 않는다.
