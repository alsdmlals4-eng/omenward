# OMENWARD 프로젝트 AI 작업 규칙

이 저장소는 ChatGPT가 기획·문서·Issue·Goal을 정리하고, Codex가 승인된 범위 안에서 Godot 프로젝트를 구현하는 공동 작업용 저장소다.

## 규칙 우선순위

1. 사용자의 최신 지시.
2. 사용자가 승인한 Codex Plan Mode 제안서와 현재 Issue/Goal.
3. 이 프로젝트의 `AGENTS.md`.
4. `docs/DOCUMENTATION_MAP.md`와 `docs/planning/`의 관련 5개 책임 본책.
5. 실제 파일과 실행 결과.
6. 프로젝트가 고정한 Base 기준과 로컬 공용 규칙.
7. Base의 최신 공용 지식, 외부 스킬과 벤치마킹 자료.

충돌하거나 불명확한 내용은 추정해 구현하지 말고 `확인 필요`로 보고한다.

## 작업 전 읽기 순서

1. `AGENTS.md`
2. `docs/BASE_RULES_VERSION.md`
3. `docs/DOCUMENTATION_MAP.md`
4. 현재 작업과 관련된 `docs/planning/01_GAME_DESIGN.md` ~ `05_QA_PM_PLAN.md`
5. `docs/HANDOFF_CONTEXT.md`
6. 관련 `docs/design/APPROVED_*.md` 부록
7. 현재 Issue 또는 Goal과 Work Order
8. `docs/PROPOSAL_WORKFLOW.md`
9. 승인된 제안서가 있으면 해당 제안서
10. 관련 코드·데이터·Scene·테스트 파일
11. `docs/ACTIVE_CONTEXT.md`

모든 문서를 무조건 읽지 않는다. `DOCUMENTATION_MAP.md`를 통해 작업별 책임 원본을 선택한다.

## 역할 분리

- 게임 기획팀: 게임의 약속, 규칙, 시나리오, UI/UX 행동, 밸런스 원칙과 `01_GAME_DESIGN.md`를 책임진다.
- 프로그래밍 팀: Godot 시스템·Scene·데이터·AI·전투·성능·MVP 로드맵과 `02_PROGRAMMING_MVP_ROADMAP.md`를 책임진다.
- 아트 팀: 캐릭터·전장·건물·UI의 시각 언어, 애니메이션·VFX·제작 규격과 `03_ART_DIRECTION.md`를 책임진다.
- 사운드 팀: BGM·SFX·벨루 음성·믹싱·접근성과 `04_SOUND_DIRECTION.md`를 책임진다.
- QA·PM: 자동·수동·시각·오디오·성능 QA, 버그·일정·위험·예산·릴리스 게이트와 `05_QA_PM_PLAN.md`를 책임진다.
- 기획/조정 AI: 플레이어 경험, 사양, 범위, 제외 범위, 책임 원본, 완료 기준과 검증 방법을 정리하고 확정 사항을 GitHub 문서에 반영한다.
- Codex Plan Mode: 저장소와 참고 자료를 읽기 전용으로 조사하고 구현 전에 검토 가능한 제안서를 작성한다.
- Codex 구현 모드: 사용자가 승인한 제안서와 Issue/Goal만 구현하고 제품 방향을 임의로 확장하지 않는다.
- 아트·콘텐츠 작업자: 프로젝트 아트·연출 계약과 공용 데이터 구조를 유지하며 실제 화면 크기에서 검수한다.
- 사용자: 방향, 우선순위, 제안서와 중요한 미확정 사항을 승인한다.

## GitHub 반영 원칙

- 사용자가 확정한 기획·규칙·우선순위는 관련 책임 문서, 로드맵, Handoff/Active Context와 현재 Issue에 동기화한다.
- 실제 게임 코드 구현은 기본적으로 `codex/issue-<번호>-<설명>` 브랜치와 Pull Request를 사용한다.
- 문서 반영과 코드 구현을 한 작업으로 섞지 않는다.
- push 또는 PR 생성에 실패한 작업은 GitHub에 반영됐다고 보고하지 않는다.
- 외부 저장소 또는 Base 변경을 자동 병합하지 않는다.

## 문서 생명주기와 인수인계

- 문서 운영은 `docs/DOCUMENT_LIFECYCLE.md`를 따른다.
- 한 주제에는 활성 책임 원본을 하나만 둔다.
- 새로운 승인 내용은 책임 원본을 같은 경로에서 갱신한다.
- `_v2`, `_최종`, `_new`, `_copy` 같은 임의 중복 파일을 만들지 않는다.
- 일반 이전 버전은 Git 커밋 이력을 백업으로 사용한다.
- 외부 납품본이나 대규모 방향 전환 비교본만 `docs/archive/YYYY-MM/`에 둔다.
- 작업 시작 시 영향 분야를 선택하고 관련 본책을 읽는다. 종료 시 구현·검증 상태와 최신 자료를 같은 본책에 갱신한다.
- 전체 방향이나 데이터 소유가 바뀌면 최소한 관련 5개 본책, Documentation Map, Handoff/Active Context와 현재 Issue/Goal을 확인한다.
- 최신 공식 이미지는 `docs/images/current/`의 버전 없는 안정 경로를 사용하고 이전 버전은 Git 이력으로 보존한다.
- 사용자에게 폐기된 안은 활성 문서에서 제거하며 아카이브를 구현 근거로 사용하지 않는다.
- 새 작업자가 다섯 본책만으로 10분 안에 핵심 경험, 구현 상태, 최신 시각 방향, 다음 단계, 금지 범위와 검증 방법을 설명할 수 있어야 한다.

## Codex Plan Mode 게이트

- 코드·Scene·Resource·데이터 구조·Godot 설정을 변경하는 모든 작업은 먼저 Plan Mode 제안서를 거친다.
- Plan Mode에서는 `docs/PROPOSAL_WORKFLOW.md` 형식으로 목적, 근거, 구조, 단계, 파일, 위험, 검증과 사용자 결정 요청을 제시한다.
- 제안서 검토 중에는 `project.godot`, 코드, Scene, Resource, 게임 데이터와 테스트를 생성하거나 수정하지 않는다.
- 사용자가 `제안서 승인`, `이 계획으로 구현 진행`처럼 명확히 승인하기 전에는 구현 브랜치·커밋·PR을 만들지 않는다.
- 질문, 의견, 부분 동의는 승인으로 간주하지 않는다.
- 구현 중 승인 범위를 벗어나는 필요가 발견되면 작업을 멈추고 제안서 수정안으로 돌아간다.

## 기술 불변 조건

- 엔진은 **Godot**, 기본 언어는 **GDScript**다.
- C#·GDExtension·외부 ECS는 측정된 필요와 사용자 승인 없이 추가하지 않는다.
- 정확한 Godot stable 버전, 내부 해상도, stretch와 실행 명령은 Issue #1 Plan Mode에서 확정한다.
- `.godot/`과 로컬 캐시·에디터 생성물은 커밋하지 않는다.
- Scene은 `scenes/`, Script는 `scripts/`, 정적·밸런스 데이터는 `data/` 또는 `resources/`, 검증은 `tests/`에 둔다.
- AutoLoad는 여러 Scene이 공유해야 하는 상태·서비스에만 사용한다.
- 같은 상태를 Scene과 AutoLoad가 동시에 책임 원본으로 관리하지 않는다.
- UI는 Godot `Control`, `Container`, `Theme`, 재사용 Scene과 명시적 Signal을 우선한다.
- UI 컴포넌트는 표시 데이터를 입력받고 사용자 의도를 반환하며 전역 게임 규칙을 직접 수정하지 않는다.

## 구현 시작 조건

Codex는 다음이 준비되기 전 코드를 수정하지 않는다.

- 사용자가 승인한 Plan Mode 제안서.
- 목표와 플레이어 가치.
- 포함·제외 범위.
- 예상 변경 파일과 데이터 책임.
- 완료 기준.
- 자동·수동 검증 방법.
- 실행할 Godot 버전과 명령.
- 보호 경로와 마이그레이션 위험.

## 프로젝트 핵심 경험

- 핵심은 `건물 구성 → 룰렛 확률 변화 → 병력 획득 → 3라인 배치 → 접전지·거점·성문 공방`의 연결이다.
- 다음 공세를 미리 읽고 제한된 자원으로 대응을 설계한 뒤 전선을 간발의 차이로 뒤집는 경험을 우선한다.
- 새로운 기능은 첫 10분과 핵심 판단을 강화하지 않으면 추가보다 삭제·통합을 먼저 검토한다.

## 전장 불변 조건

- 전장은 좌우 대칭 독립 3라인이다.
- 각 라인은 `본진 → 성문 → 중간거점 → 중앙 접전지 → 적 중간거점 → 적 성문 → 적 본진` 순서를 사용한다.
- 일반 유닛의 라인 횡단과 기본 라인 변경은 없다.
- 중간거점은 전방 2·후방 1 건설 노드를 가진다.
- 점령 시 건설권과 기본 생산권이 이전된다.
- 암살자는 적 후방에 직접 생성하지 않고 평상시 안개로 가린 같은 라인 우회로를 사용한다.
- 기본 전략 줌에서 전장 전체를 보며 미니맵은 사용하지 않는다.
- 건물과 최대 점유 영역은 도로를 막지 않는다.

## 공용 병종 데이터 불변 조건

오멘워드는 전투 규칙 기준 **공용 UnitArchetype 10개만** 사용한다.

```text
UnitArchetypeProfile
+ TierProfile
+ RankProfile
+ owner_team_id
+ FactionVisualProfile
```

- 아군·적군 별도 스탯·스킬·타기팅·애니메이션 타이밍 데이터를 만들지 않는다.
- 별도 `EnemyUnitProfile`을 만들지 않는다.
- 같은 아키타입·Tier·Rank는 진영과 무관하게 동일한 전투 결과를 낸다.
- 아군과 적군의 차이는 소유 팀, 출격 방식, 이미지·초상화·아이콘·팔레트·표시명이다.
- `visual_faction_id`는 능력치와 전투 규칙을 변경하지 않는다.
- 적 웨이브는 공용 `archetype_id`에 enemy 소유권과 베일종 Visual Set을 지정한다.
- 일반 적군 난이도는 수량, Tier, Rank, 라인과 출격 시점으로 만든다.
- W15·W20 보스는 공용 base archetype에 BossBehaviorPackage·BossPhaseProfile과 전용 Visual Set을 추가한다.
- 공용 데이터 분리는 사용자 승인 없이 도입하지 않는다.

책임 원본: `docs/design/APPROVED_SHARED_UNIT_ARCHETYPE_AND_FACTION_VISUAL_DATA_V1.md`.

## 애니메이션·연출 불변 조건

- 한 아키타입은 공용 AnimationContract 하나를 사용한다.
- 아군·적군 이미지 시트는 같은 상태 목록, 프레임 수, 배열, 피벗과 판정 이벤트를 사용한다.
- 적군 전용 모션 상태 머신과 별도 공격 타이밍을 만들지 않는다.
- 공격은 준비→판정→회복을 구분한다.
- 접촉·투사체 발사와 실제 판정 오차는 한 프레임 이내를 목표로 한다.
- 이동 위치는 코드가 소유하고 애니메이션 루트 모션으로 결정하지 않는다.
- 가벼운 연속 피격은 큰 경직 대신 짧은 플래시·미세 반동을 사용한다.
- 일반 공격마다 큰 화면 흔들림과 히트 스톱을 사용하지 않는다.

책임 원본: `docs/design/APPROVED_UNIT_ANIMATION_AND_BATTLE_PRESENTATION_GUIDE_V1.md`.

## 기타 프로젝트 원칙

- 건물 Tier는 생산량·속도·품질과 전투 성능을 강화하지만 룰렛 토큰 수는 **완공된 건물 개수**로 계산한다.
- 기본 포탑 한 기가 중간거점과 중앙 접전지 사이 전체를 단독으로 덮지 않는다.
- 적은 플레이어와 같은 룰렛 AI를 사용하지 않고 StageManifest·웨이브·생산시설로 출격한다.
- 핵심 수치와 상성은 데이터로 관리하고 코드에 흩어놓지 않는다.
- 전방 시설 파괴, 식량 한도, 토큰 제거, 거점 권한 이전처럼 연쇄 효과를 명시적으로 테스트한다.
- 화면은 세 라인의 위협, 성문·거점·접전지, 다음 웨이브와 룰렛 상태를 동시에 이해할 수 있어야 한다.

## Base 공용 지식과 참고 저장소

- `alsdmlals4-eng/Base`: spec-first 흐름, 문서 인수인계, 아트·연출 방법, 조사·벤치마킹, 실무 검수와 일반화된 사례의 공용 원본이다.
- `alsdmlals4-eng/urban-legend`: Godot 폴더 구조, 상태 단일 소유, 데이터 로딩, 네이티브 UI와 headless 검증의 참고 사례다.
- Base의 methods·skills·cases는 작업 방법과 검수 참고이며 오멘워드 최신 승인 문서를 덮어쓸 수 없다.
- 참고 문서를 통째로 복사하거나 자동 동기화하지 않는다.
- 채택·제외 결론과 기준 커밋은 `docs/REFERENCE_REPOSITORIES.md`, `docs/BASE_RULES_VERSION.md`에 기록한다.

## 벤치마킹과 저작권 경계

- 참고 게임·저장소의 문제 해결 원리와 사용자 경험을 분석한다.
- 코드, 아트, 사운드, UI 배치, 문구, 고유 명칭과 캐릭터 디자인을 복제하지 않는다.
- 프로젝트 자산은 직접 제작하거나 사용 권한이 확인된 것만 사용한다.
- 생성형 이미지는 탐색용이며 최종 자산은 픽셀·관절·무기·문자·광원 검수를 거친다.
- 임시 자산도 출처와 교체 필요 여부를 기록한다.

## 구현 원칙

- 실제 저장소 상태와 호출 흐름을 먼저 확인한다.
- 승인 없이 범위 밖 리팩터링·기능·데이터 분기를 추가하지 않는다.
- Godot 기본 노드·Resource·Signal·Container와 이미 존재하는 구조를 우선한다.
- 가장 작은 변경으로 관찰 가능한 완료 기준을 만족한다.
- 정상 동작 중인 사용자 변경을 되돌리지 않는다.
- 실패 상태와 경계 조건을 먼저 정의하고 테스트 가능한 seam을 만든다.
- 공용 아키타입과 진영 Visual Set의 참조·프레임·이벤트 호환성을 자동 검증한다.
- 변경 성격에 맞춰 `git diff --check`, 데이터 검사, Godot headless, 변경 Scene, 실제 플레이 경로 순으로 검증한다.

## 제안서 보고 형식

`docs/PROPOSAL_WORKFLOW.md`의 형식을 사용한다. 제안서 마지막에는 `제안서 검토 대기 / 승인 전 구현 금지`를 표시한다.

## 구현 작업 시작 보고 형식

```md
## 승인 범위 확인
- 승인된 제안서:
- 목표:
- 플레이어 가치:
- 범위:
- 제외 범위:
- 예상 변경 파일:
- 데이터·스키마 영향:
- UI·UX·아트 영향:
- 위험과 의존성:
- 완료 기준:
- 검증 계획:
```

## 완료 보고 형식

```md
## 결과
- 변경 파일:
- 구현 내용과 이유:
- 실행한 검증:
- 검증 결과:
- 미검증 항목:
- 남은 위험:
- 사용자 확인 항목:
- Compound 교훈:
- Base 사례·방법 승격 후보:
```

테스트하지 못한 항목을 완료했다고 보고하지 않는다.
